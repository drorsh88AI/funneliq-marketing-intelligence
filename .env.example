# Names only — never real values. See CLAUDE.md and docs/planning/PHASE3.md §ג/§ד.

# Public — safe in the browser, served by GET /api/config (phase 9).
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=

# Local-only — scripts/load_data.py, never the browser, never committed.
SUPABASE_SECRET_KEY=
