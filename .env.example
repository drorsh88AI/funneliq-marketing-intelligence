# Names only — never real values. See CLAUDE.md and docs/planning/PHASE3.md §ג/§ד,
# docs/planning/PHASE4.md §ג (D16).

# Public — safe in the browser, served by GET /api/config (phase 4).
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=

# Local-only — scripts/load_data.py and scripts/create_users.py, never the
# browser, never committed.
SUPABASE_SECRET_KEY=
