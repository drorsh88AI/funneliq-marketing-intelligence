# FunnelIQ — אפיון מלא (Project 1)

> **מצב:** אפיון-העל הושלם; פאזות 0–2 הושלמו ואושרו, ופאזות ההמשך מתוכננות
> ומאושרות בהדרגה לפי שער המעבר האחיד. המצב החי מתועד ב-`ROADMAP.html`.
> **גרסה:** לאחר סבבי ביקורת Codex והכרעות המשתמש עליהם.

## Context

הבריף (`FunnelIQ_Assignment.html`) מבקש מוצר end-to-end ולא נוטבוק. המשפט
המכריע: *"המייסד צריך מוצר חי: כלי פנימי שהצוות נכנס אליו, עם נתונים במסד נתונים
אמיתי, מודלים מאחורי API אמיתי, לוח מחוונים שהצוות יכול לפתוח בדפדפן, הכל רץ על
תשתית שנשארת פעילה גם אחרי סגירת המחשב הנייד."* התוצר יוגש למנחה הקורס.

> **מצב התיקייה בעת כתיבת האפיון (לפני פאזה 1) — תצלום זמן, אינו מתעדכן.
> המצב העדכני ב-`ROADMAP.html`.**

התיקייה מכילה כרגע רק `CLAUDE.md`, `environment.yml`, `funnel_marketing_data.csv`
ו-`FunnelIQ_Assignment.html`. **אינה git repo. אפס שורות קוד.**

**סביבה:** conda `pro1_FunnelIQ` עם כל התלויות. **חסר `pytest`.**
`environment.yml` מכיל `prefix:` בשורה 167 עם נתיב מקומי — להסיר, לא נייד.

**קיים:** GitHub וחשבון Supabase, ללא פרויקט ייעודי ל-FunnelIQ. **אין חשבון
Render** — הוא נפתח בפאזה 2.

---

## עובדות שנמדדו (3,500 × 19)

`ltv_months` חסר ×4 · `cumulative_profit` חסר ×29 → 33 שורות.
`upsell` 42% · `referred` Yes 39% · `purchased` 90%.
`ad_budget` 500–20,000 (חציון 3,000) · `ltv_months` 1–56 (ממוצע 22).

**תקין:** `leads_answered + leads_not_answered = num_leads` ב-100%. מונוטוניות
מלאה `leads_answered → followup_1 → … → followup_5`, אפס חריגות. הזהות הסוגרת:
`closed + not_closed = followup_5` — **לא** `leads_answered`. אין ערכים שליליים,
אין `num_leads = 0`, `referred` נקי. תקציבים בדידים: 500, 800, 1000, 1500, 2000,
2500, 3000, 4000, 5000, 6000, 7000, 8000, 10000, 12000, 15000, 20000 — הפער
1501–1999 בהגדרת הבריף **ריק בפועל**.

**ממצאים שכבר עונים על שאלות בבריף:**
- **נשירה:** fu1 21.7% · fu2 25.7% · fu3 18.6% · **fu4 10.4%** · **fu5 29.2%**.
  שלב 4 הנמוך בשרשרת.
- **אורך המסלול לסגירה** — ⚠ `calls_to_closed` מתואר בבריף כ**ממוצע ברמת
  רשומה** ומופיע ב-CSV בערכים שלמים. *ייתכן שעוגל או נוצר מראש כערך בדיד; אין
  תיעוד שמכריע כיצד חושב.* אין בו היסטוריה פר-עסקה.
  **הכרעת CP4-D:** תשובת B46b משתמשת בכל הרשומות שבהן `closed>0` ‏(3,318):
  חציון 3, שכיח 2, ממוצע 3.706; ב־1,595 רשומות (48.07%) הממוצע הוא 4+
  שיחות. חמשת `followup_1`–`followup_5` הם שלבי מעקב של לידים; הם אינם
  `calls_to_closed`, שמגיע עד 9. הנתונים הישנים שלהלן נשמרים כראיה היסטורית
  למימוש `purchased=1` שטרם תוקן, ולא כתשובה הפעילה לבריף:
  - **ברמת רשומה:** ב-**1,519 מתוך 3,163** רשומות שבהן `purchased = 1` (48%)
    הערך הממוצע הוא 4 ומעלה. *זה לא אומר ש-48% מהעסקאות הבודדות דרשו 4+ שיחות.*
  - **בתת-אוכלוסייה `closed = 1`** (488 רשומות), שם הממוצע מתייחס לעסקה בודדת:
    **439 מתוך 488 (89.96%) מציגות `calls_to_closed >= 4`.**
- **התת-אוכלוסייה נבדלת תיאורית — ותו לא:**

  **כל השורות בטבלה הזו הן ברמת רשומה** — כל רשומה נספרת פעם אחת,
  בלי קשר למספר העסקאות שבה:

  | | `closed = 1` | `closed ≥ 2` |
  |---|---|---|
  | `calls_to_closed` ממוצע | 5.65 | 3.35 |
  | תקציב (חציון, רמת רשומה) | 1,000 ₪ | **4,000 ₪** |
  | לידים (חציון, רמת רשומה) | 20.5 | **47** |
  | שיעור המרה | 5.01% | 7.32% |
  | `ltv_months` | 11.05 | 25.13 |

  מתאם `closed` ↔ `calls_to_closed` = −0.238.

  **ניתוח משוקלל לפי מספר עסקאות סגורות** — טבלה נפרדת. יחידת המשקל
  היא **עסקה**, לא רשומה (כל רשומה משוכפלת רעיונית לפי `closed`):

  | | `closed ≥ 2` |
  |---|---|
  | `ad_budget` weighted median | 5,000 ₪ |
  | `num_leads` weighted median | 52 |

  ⚠ **אינו בר-השוואה ישירה** למדדים שברמת רשומה. מוצג כדי לתעד את
  מקור הערך `5,000 ₪`, שהופיע קודם בטעות בטבלה הראשית כאילו הוא ברמת
  רשומה. הערך `52.5` שהופיע שם ללידים **הוסר** — הוא אינו משוחזר לא
  ברמת רשומה (47) ולא בשקלול (52).
  **אין לערבב שתי יחידות משקל באותה טבלה.**

  ⚠ **אין להסיק מכאן שיעור ברמת עסקה, ולא את כיוון ההטיה.** יחידת ההשוואה היא
  רשומה, ורשומת `closed ≥ 2` נספרת כאחת אף שהיא מכילה כמה עסקאות — לכן השוואת
  ממוצעים בין קבוצות רשומות **אינה חוסמת** את שיעור העסקאות הבודדות, לא למעלה
  ולא למטה. *(הניסוח "חסם עליון" הוסר; הוא לא נבע.)* ואין לכנות את הקבוצה
  "קשה יותר" כעובדה — נמדדו בה תקציב ולידים נמוכים יותר, המרה נמוכה יותר
  ו-`calls_to_closed` גבוה יותר, וזה כל מה שנמדד.
- **המרה לפי טייר:** Low 4.52% · **Mid 8.22%** · High 5.43%. לא מונוטוני.
- CAC ↔ `ad_budget`: r = 0.83.
- **`leads_not_answered = num_leads − leads_answered` בדיוק.** קולינאריות מושלמת.

⚠ **המספרים כאן הם תוצאות בדיקה מקדימה מול ה-CSV, לא תוצרי סקריפט.**
`scripts/analysis.py` **עדיין אינו קיים** — אין קוד בפרויקט. בשלב הביצוע כל
המדדים **יאומתו ויופקו מחדש** באופן שחזורי ע"י הסקריפט וייכתבו ל-`docs/`.
אין להטמיע מספרים ידנית ב-README או ב-REPORT.

---

## ניקוי נתונים

### סדר העבודה — מה לפני הפיצול ומה אחריו

⚠ "ניקוי לפני פיצול" הוא ניסוח מסוכן. הקו העובר הוא בין פעולות
**דטרמיניסטיות** לבין פעולות ש**לומדות פרמטרים מהנתונים**.

1. **לפני הפיצול** — טעינת המקור ללא שינוי · אימות schema וטיפוסים ·
   בדיקות Data Contract · סימון חסרים, כפילויות וערכי קצה · יצירת
   `source_row_id`. ⚠ **בלי** חישוב חציונים, בלי בחירת פיצ'רים לפי קשר
   ליעד, בלי הסרת outliers, בלי התאמת transformer.
2. **פיצול** — ה-Holdout ננעל.
3. **על Development בלבד** — EDA שמשפיע על מידול, החלטות פיצ'רים,
   השוואת מודלים, 5-fold CV.
4. **בתוך Pipeline/fold** — imputation, scaling, encoding, `budget_tier`,
   `answer_rate`, בחירת שתיים משלישיית הקולינאריות, וכל Winsorization
   אופציונלי.
5. **Calibration** — רק אחרי בחירת המודל, על סט ה-calibration.
6. **Holdout** — הערכה אחת של הצינור והמכייל הנעולים.

ב-REPORT יופרד `dataset description` (מותר על מלוא הנתונים) מ-
`model-development analysis` (Development בלבד).

**כפילויות — לא למחוק.** 10 שורות עודפות, **19 שורות ב-9 קבוצות זהות**,
בתקציבים 500 / 800 / 1500. כולן `purchased = 0`; **2 מהן `closed = 1`**. אין
`id` ואין חותמת זמן שמוכיחים שאלה שגיאות ולא קמפיינים קטנים זהים במקרה.
→ המקור נשמר כמות שהוא, `source_row_id` (אינדקס ה-CSV) נוסף, הקבוצות מסומנות,
ומורץ **ניתוח רגישות עם ובלי**. מחיקה רק אם תימצא ראיה.

**היקף הרגישות — מדוד:** כל 19 השורות הכפולות הן `purchased = 0` (נמדד
בפאזה 0). מכאן שהן **מחוץ לאוכלוסיית P2/P3/P4** לחלוטין, ובדיקת הרגישות
עם/בלי עשר השורות העודפות **אינה רלוונטית ל-P2/P3/P4**, אך **כן**
רלוונטית ל:

| היכן | מה עלול להשתנות |
|---|---|
| **P6** | האוכלוסייה היא כלל השורות ⇒ הכפילויות בתוכה |
| **חבילה 1** | המרה לפי טייר תקציב — הכפילויות בתקציבים 500/800/1500, כלומר **בטייר Low** · עקומת `ad_budget → num_leads` |
| **חבילה 5** | אגרגטי המשפך ושיעורי הנשירה fu1–fu5 |

**הבדיקה תבוצע בפועל בשלושת המקומות האלה**, ותדווח בכל אחד מהם.
⚠ **אם מדד אינו משתנה או שהשינוי זניח — יש לדווח זאת במפורש.** אין
להניח מראש שאין השפעה ואין לדלג על הדיווח.
⚠ בדיקה זו **נפרדת** מבדיקת ערכי הקצה ואין לערבב ביניהן.
⛔ **אין למחוק כפילויות מנתוני המקור.** `source_row_id` נשמר, הקבוצות
מסומנות, וההשפעה מתועדת.

### מדיניות ערכי קצה — סימון, לא מחיקה

**ברירת המחדל: ערך קצה חוקי נשמר.** זיהוי outlier **מסמן שורה לבדיקה
ואינו מוחק אותה**. מחיקה מותרת **רק** בעקבות ראיה מתועדת לשגיאת נתונים.
נתוני המקור נשמרים ללא שינוי.

⚠ **הפרדה מחייבת בין פיצ'רים ליעד:**

| מה | מדיניות |
|---|---|
| **פיצ'רים** | Winsorization היא **בדיקת רגישות אופציונלית בלבד**, נלמדת **בתוך כל fold** מנתוני האימון של אותו fold |
| **יעד `cumulative_profit` (P6)** | ⛔ **אין Winsorization, בשום מסלול.** קיצוץ הזנב הימני מוריד את התוחלת המותנית, והסימולטור **מסכם** תחזיות — התוצאה הטיה שיטתית כלפי מטה בסך הרווח |
| **יעד `ltv_months` (P2)** | אין צורך — ההתפלגות סימטרית וחסרת זנב |

⚠ **מודלי עצים עמידים יחסית לערכי קצה בפיצ'רים** — הפיצולים נעשים על
סטטיסטיקות סדר, ולכן ערך קיצוני בפיצ'ר משפיע השפעה חסומה. *(עמידות זו
אינה חלה על ערכי קצה ב**יעד** תחת squared loss.)* מכאן: **אין להפעיל
Winsorization כברירת מחדל** — היא נכנסת רק כבדיקת רגישות מתועדת.

כל סף, Winsorization או טרנספורמציה **נלמדים אך ורק מנתוני האימון
ובתוך כל fold**.

**חסרים ביעד** — מוסרים במשימה הרלוונטית בלבד: **0 ב-P2**, 29 ב-P6.
*(נמדד בפאזה 0: כל 4 השורות עם `ltv_months` חסר הן `purchased = 0`, ולכן
מסנן האוכלוסייה של P2 כבר מוציא אותן — אין הסרה נוספת, ו-N = 3,163 בדיוק.)* ⚠ **אין להסיר
אותם מספירות תיאוריות** — 5 רשומות סופר-לקוח חסרות `cumulative_profit` עומדות
בהגדרה לכל דבר (הפניה, אפסייל ו-LTV ידועים), והסרתן מוקדם מדי היא שגיאת ספירה.

**Imputation** — לא נקבע גורף; מוכרע בתוך כל pipeline, ויכולת ה-NaN של כל
ספרייה מתועדת בנפרד.

### גרעיניות מעורבת — השערת עבודה, לא עובדה

הבריף כולל **תיאור לכל 19 עמודות המקור**, אך אינו כולל מזהה לקוח, מזהה קמפיין,
תאריכים או היסטוריית snapshots שמכריעים מהי יחידת השורה. החלוקה להלן היא
**מסקנה סבירה** מתיאורי השדות ומהסתירות בנתונים, ותתועד ככזו בארבע שכבות
מופרדות:

| שכבה | תוכן |
|---|---|
| ידוע מהבריף | "one row per customer/campaign record" — לא מכריע |
| נצפה בנתונים | 155 שורות `closed > 0` עם `purchased = 0`; 333 שורות `purchased = 0` עם `ltv_months > 0`; ב־2,675 רשומות רוכשים `closed > 1` |
| **מוסק בלבד** | `ad_budget`, `num_leads`, שלבי המשפך, `closed`, `not_closed`, `calls_to_*`, CAC ← רמת **קמפיין**. `purchased`, `ltv_months`, `upsell`, `referred`, `cumulative_profit` ← רמת **לקוח** |
| השפעה | קובעת את ה-snapshot, את אוכלוסיות האימון ואת גבולות הפרשנות של כל מסקנה |

המונח **"לקוח חדש"** מתאר את שלב הקשר עם הלקוח, לא היעדר נתוני קמפיין:
P2 רשאי לענות על לקוח שזה עתה נרכש רק לאחר שמחזור הקמפיין שלו הסתיים וכל
13 אגרגטי המשפך ידועים. P4S אינו ציון לליד לפני רכישה; הוא ציון מוקדם ביחסי
הלקוח עבור רוכש שכבר זוהה, בתנאי השימוש המפורטים להלן.

חוזה היחידות, האוכלוסייה, מועד הידיעה ובסיס כל קביעה לכל 19 העמודות נמצא
ב־`docs/feature_matrix.md` § חוזה משמעות וזמן. אין לפרש שורה כלקוח יחיד או
כקמפיין יחיד מעבר למה שהבריף מאפשר.

---

## מטריצת זמינות פיצ'רים — מקור האמת נגד דליפה

רשימת פיצ'רים לבדה אינה מספיקה. **טבלה מפורשת לכל עמודה**, ב-`docs/feature_matrix.md`
וב-REPORT: משמעות עסקית · גרעיניות · **מתי הערך נהיה זמין** · Feature / Target /
נגזרת / מוחרגת בכל אחת מ-P2/P3/P4/P6 · **סיבת ההכללה או ההחרגה**. כל עמודה עוברת
בדיקת זמינות מול ה-snapshot של המשימה.

---

## נקודות חיזוי (snapshot)

| # | הצילום | מה זמין |
|---|---|---|
| P2, P3, P4 | **בתום מחזור הקמפיין שבו הלקוח נרכש** | כל אגרגטי המשפך של הקמפיין |
| P4S | **לאחר שהרכישה ידועה ומעקב 1 הושלם לקבוצת לידים חודשית סגורה** | `ad_budget`, `num_leads`, `leads_answered`, `followup_1` בלבד |
| P6 | **ברגע הקצאת התקציב החודשי** | `ad_budget` בלבד; יתר הקלט נגזר יחד מפרופיל רב־משתני תקף שנבחר דטרמיניסטית משורת train שנצפתה באותו תקציב, ללא היעד (`metrics.json.P6_profile_method`) |

⚠ **"ברגע הרכישה" אינו ה־snapshot של P2/P3/P4.** הפיצ'רים הם אגרגטים — `not_closed`,
`calls_to_not_closed`, CAC סופי ומספר העסקאות הסופי אינם ידועים כשלקוח בודד
רוכש, אלא רק כשהקמפיין נסגר.

**P4S — תנאי הפעלה, לא הוכחת זמן מן הקובץ.** ארבעת ערכי הקלט תקפים יחד רק
לקבוצת לידים קבועה: חלון גיוס הלידים וההוצאה החודשית נסגרו, מעקב 1 הושלם,
והרכישה הראשונית של הלקוח כבר ידועה. מפעילים את המודל רק לאחר שכל שלושת
התנאים מתקיימים. אין להחליף את `num_leads` החודשי הסופי בספירת "עד היום",
ואין להציג את הציון לליד או לפני רכישה. מאחר שאין בקובץ תאריכים או snapshots,
הסדר הזה הוא **הנחת שימוש של הפרויקט** ואינו תיקוף אמפירי נגד דליפת זמן.

P2/P3/P4 מופעלים לאחר רכישה ולאחר סגירת מחזור הקמפיין, ובהנחה שהיעד של המשימה
(`ltv_months`, `upsell` או `referred`) עדיין אינו ידוע ברגע ההפעלה. הקובץ
אינו כולל תאריכים שמוכיחים שהיעד התממש אחרי הפיצ'רים; לכן גם כאן סדר הזמן הוא
הנחת שימוש מפורשת. P2 מעריך **אורך חיים כולל**, לא יתרת חיים, משום שאין נתון ותק.

⚠ **הערה היסטורית — ניסוי P4 המוקדם בלבד (פאזה 6, S8, ‏05.09.2026):**
`LogisticRegression(max_iter=50000)` ננעל אז כדי להשוות את P4 עם ארבעה
פיצ'רים מול P4 המלא בלי לשנות משפחת מודל. זו אינה הוראה למודל P4S, שנוצר
רק בפאזה 8A. ההכרעה הפעילה מ־12.09.2026 היא CatBoost שאומן על יעד P4S;
Logistic נשאר השוואה; CP9 פרס את CatBoost הייעודי ל־P4S.

---

## אוכלוסיות אימון — סגור

**P2, P3, P4: `purchased = 1` בלבד (3,163 שורות).**
- P2: מי שלא קנה אינו לקוח.
- P3: אפסייל הוא אירוע שלאחר רכישה; בלי הסינון P3 לומד רכישה ראשונית.
- P4: הבריף כותב "given a new **customer**'s early funnel data" — customer, לא
  lead. *(קריאתי הקודמת כ"ליד" הייתה שגויה.)*

**מודל P4 על כלל האוכלוסייה — ניתוח רגישות ב-REPORT בלבד, לא production
נוסף.** דנומינטורים שונים ושיעורי בסיס שונים בין שתי האוכלוסיות אינם
מכשירים פריסה על סמך ביצועים בלבד — ביצועי מודל אינם מחליפים הגדרת
שימוש. מנגנון האימון וההערכה — כולל תיקון הסתירה שהייתה בניסוח "כלל
3,500" מול הפרדת ה-Holdout — מפורט ב-§פיצול הנתונים תחת "שתי אוכלוסיות
משנה".

תוצאות מחייבות:
- **`purchased` יורד מהפיצ'רים** — קבוע באוכלוסייה (`nunique = 1`).
- ⚠ **שני מספרים שונים, אסור לבלבל:**
  - **שיעור בסיס** (`upsell = 1`) = **46.35%** (1,466 / 3,163) — מה שמוצג
    למשתמש כהשוואה ("גבוה ב-X נקודות משיעור האפסייל הבסיסי").
  - **majority baseline** (דיוק ניחוש `upsell = 0` תמיד) = **53.65%**
    (1,697 / 3,163), **לא 58%** — מה שהמודל צריך לנצח במדדי ההערכה.
  - שיעור בסיס להפניה באותה אוכלוסייה = **42.71%**.

**P6:** ברמת קמפיין, ללא סינון לקוחות.

---

## החרגות דליפה

| # | Target | מוחרג | נימוק |
|---|---|---|---|
| P2 | `ltv_months` | `cumulative_profit`, `upsell`, `referred`, `purchased` | תוצאות מאוחרות מחיי הלקוח / קבוע |
| P3 | `upsell` | `cumulative_profit`, `referred`, `ltv_months`, `purchased` | LTV והפניה מתממשים אחרי ההחלטה |
| P4 | `referred` | `cumulative_profit`, `upsell`, `ltv_months`, `purchased` | תוצאות מאוחרות מחיי הלקוח |
| P6 | `cumulative_profit` | `ltv_months`, `upsell`, `referred` | הסימולטור מכיר רק תקציב |

**קולינאריות.** כל העמודות נשמרות במקור, אבל במודלים נכנסות **שתיים בלבד**
מתוך `num_leads` / `leads_answered` / `leads_not_answered` — או `answer_rate`
יחד עם `num_leads`. ההמרה **בתוך ה-Pipeline**.

**ה-Holdout אינו משתתף בשום שלב פיתוח:** לא ב-Winsorization, לא בבחירת
פיצ'רים, לא ב-hyperparameters, לא בכיול, לא ב-Conformal ולא ב-Bootstrap.

**כל הכרעות הרגישות ננעלות לפני פתיחת ה-Holdout:** Winsorization,
`log1p`, Tweedie מול squared loss, טיפול בכפילויות, בחירת פיצ'רים
ו-hyperparameters — כולן מוכרעות ב-CV על train בלבד.
⚠ ניתוח פלחים על ה-Holdout הוא **דיווח בלבד**. אם ממצא בו יגרום לשנות
טיפול או מודל — **ה-Holdout נשרף וההערכה בטלה**.

**הכלל העסקי הידני ב-P3 — שניים, לא אחד.** (א) הכלל שהבריף מבקש
(`LTV > X and CAC < Y`), מוצג במפורש כהשוואה **בדיעבד** שאינה זמינה בזמן החיזוי.
(ב) כלל תפעולי חלופי המשתמש **רק** בפיצ'רים שזמינים ב-snapshot של P3 — ההשוואה
ההוגנת למודל.

---

## חמש שאלות המקור → מטריצת קבלה

| # | השאלה | Endpoint | פאנל | חבילה | בדיקה | ראיה |
|---|---|---|---|---|---|---|
| 1 | **כמה זמן צפוי לקוח חדש להישאר?** | `POST /api/predict/ltv` | טופס משותף → חודשים. ⚠ מגבלת היקף גלויה: לקוח שנרכש בקמפיין **שהסתיים**, על בסיס נתוני סוף הקמפיין — ⛔ אינה תחזית early-funnel (`IA.md` §2) | P2 | API + 401 + סכמה | CV + Holdout |
| 2 | מי יקנה עוד? | `POST /api/predict/upsell` | אותו טופס → הסתברות | P3 | API + baseline | מדדים מול 53.65% |
| 3 | מי יהפוך ללקוח-על? | `POST /api/predict/super-customer` | רוכש ידוע אחרי מעקב 1 ובחלון חודשי סגור → ציון 0–100 | חבילה 4 (P4S) | API + כיול | Brier + עקומת כיול (P4S) |
| 4 | לאן ילך התקציב? | `GET /api/simulate/budget` | סימולטור ₪50k → רווח מצטבר צפוי | P6 | סכום = 50,000 | טווח אי-ודאות + n לכל תקציב |
| 5 | האם להפסיק אחרי המעקב השלישי? | `GET /api/insights/followup` | גרף נשירה, התפלגות שיחות והמלצה | P5 | חישוב מ-Supabase | צילום + בדיקת RLS |

⚠ **תיקון D20 (`docs/planning/PHASE9.md`, 09.09.2026):** שאלה 3 ממופה
ל-`P4S` (פאזה 8A) ולנתיב השביעי `POST /api/predict/super-customer`,
⛔ לא ל-`P4`/`referral`. ה-API מחזיר **הסתברות 0–1**; פאזה 11 ממירה
ומציגה **0–100**, כלשון הבריף. פרטי האימון והתוצאה של P4S אינם נכתבים
כאן — הפניה ל-`docs/planning/PHASE8A.md` (D1–D21, קריטריוני הקבלה,
ה-Holdout).

`POST /api/predict/referral` (P4, `target=referred`) נשאר **endpoint
משלים** לחיזוי הפניה בלבד — ⛔ אינו התשובה לשאלה 3. ⚠ **ציון P4 הוא
נטייה להפניה, לא הסתברות ללקוח-על.** ה-target הוא `referred` בלבד; אינו
ההסתברות לעמוד בו-זמנית בשלושת התנאים (הפניה + אפסייל + LTV ≥ 34) —
זו ההגדרה המדויקת ש-P4S מנבא. ההסתייגות מופיעה בטקסט הממשק, ב-REPORT
ובמטריצה הזו.

**טופס אחד.** P2, P3 ו-P4 חולקים snapshot, אוכלוסייה וסכמת קלט.
P4S חולק את אוכלוסיית `purchased=1` אך נושא snapshot מותנה משלו:
רכישה ידועה, חלון גיוס חודשי סגור ומעקב 1 שהושלם (`PHASE8A.md` D1).
זהו טופס עצמאי, לא חלק מהטופס המשותף.

---

## חוזה שימוש עסקי פעיל — CP3

חמש השורות הראשיות שלהלן הן חמש התשובות שהמוצר חייב להנגיש. P4 הוא יכולת
משלימה לחיזוי הפניה ואינו מחליף את ציון לקוח־העל P4S. "מי" פירושו אומדן
לרשומה שהמשתמש מזין; הבריף אינו מחייב דירוג אוטומטי של כל 3,163 הרשומות.

| יכולת | השאלה המלאה | אוכלוסייה ויעד | קלט, זמינות וקבלתו | פלט | פעולה אפשרית | מגבלה מרכזית | מקור ראיה | B |
|---|---|---|---|---|---|---|---|---|
| **P2 — אורך חיי לקוח** | כמה זמן צפוי לקוח חדש להישאר? | רוכשים בלבד (`purchased=1`); יעד `ltv_months` | 13 ערכי `FunnelInput` לאחר רכישה וסגירת מחזור הקמפיין; הזנה מלאה עצמאית או דוגמה היסטורית שנערכת | אומדן חודשים כולל + טווח split-conformal; ב־OOD אין תחזית | לתכנן שירות ושימור לפי אומדן משך הקשר | אורך חיים כולל, לא חודשים שנותרו; אין ותק או הוכחת זמן; אותו קלט מחזיר אותו אומדן | `app/predict.py::predict_ltv` · `models/P2.meta.json` · `metrics.json.P2_holdout` | B25–B29c |
| **P3 — רכישה נוספת** | אילו לקוחות נוטים לקנות שירות נוסף? | רוכשים בלבד; יעד `upsell` | אותם 13 ערכים ובאותו snapshot; הזנה עצמאית או דוגמה שנערכת; מניחים שתוצאת האפסייל טרם ידועה | הסתברות מכוילת, שיעור בסיס וקטגוריית נטייה; ב־OOD אין תחזית | לתעדף בדיקה או פנייה להצעה נוספת | הסתברות אינה תוצאה ידועה ואינה השפעה סיבתית; אין מידע אישי מעבר להקשר; אותו קלט מחזיר אותו פלט | `app/predict.py::predict_upsell` · `models/P3.meta.json` · `metrics.json.P3_holdout` | B30–B37d |
| **P4S — לקוח־על** | מי מבין הרוכשים עשוי לשלב הישארות ארוכה, רכישה נוספת והפניה? | רוכשים בלבד; יעד סינתטי `referred=Yes AND upsell=1 AND ltv_months>=34` | ארבעה ערכים בלבד, אחרי רכישה ידועה, סגירת חלון גיוס חודשי והשלמת מעקב 1; הזנה עצמאית או דוגמה | `event_probability` מומר בממשק לציון 0–100, עם שיעור בסיס ונטייה; ב־OOD אין ציון | להשתמש בציון כאות לבחינה ידנית של התרחיש/הרשומה שסופקו | 34 הוא סף תפעולי של הפרויקט, לא מספר מהבריף; אין פיצ'ר אישי או הוכחת זמן; שני קלטים אגרגטיביים זהים יקבלו אותו ציון | `app/features.py::super_customer_label` · `app/predict.py::predict_super_customer` · `models/P4S.meta.json` · `metrics.json.P4S_holdout` | B41–B43 |
| **P6 — הקצאת תקציב** | איזו מארבע אסטרטגיות ₪50,000 עדיפה לפי המודל? | 3,471 רשומות עם יעד לא־חסר; יעד `cumulative_profit` | אין טופס קלט: GET מחזיר lookup קבוע שחושב מפרופיל train רב־משתני נצפה בכל רמת תקציב | ארבע אסטרטגיות מדורגות, רווח מצטבר צפוי, טווח bootstrap, n ורמת ראיות | אין להכריז מנצחת כשהטווחים חופפים; `25x2000` מתאים רק לניסוי מוגבל בגלל כשל ה־backtest ב־500 | לא רווח חודשי, ROI או הבטחה; 29 רשומות חסרות יעד אינן באימון; הנחות אדיטיביות ואי־חפיפה; ארבע אסטרטגיות קבועות בלבד | `app/predict.py::simulate_budget` · `models/P6_simulation.json` · `metrics.json.P6_profile_method/P6_backtest` | B48–B53b |
| **P5 — מעקב שיחות** | האם להפסיק אחרי המעקב השלישי; היכן הנשירה חריגה וכמה שיחות נדרשו ברשומות שנסגרו? | נשירה: כל הרשומות המורשות ב־RLS; התפלגות שיחות: `closed>0` ‏(3,318) לפי נוסח השאלה ומילון הבריף | אין טופס קלט: GET קורא שני חלקים מ־Supabase עם JWT מתוך העתק ה־CSV הקפוא; כל חלק יכול להיכשל בנפרד | נתוני מוצר: חמש נשירות והתפלגות `calls_to_closed`; טקסט ההמלצה נוסף בשכבת הממשק ממקור מתועד | להמשיך מעקבים אחרי השלישי באופן מבוקר ולמדוד עלות ושיעור סגירה שולי | חמשת שלבי המעקב אינם מספר השיחות; `calls_to_closed` הוא ממוצע ברשומה, לא היסטוריה לעסקה; אין מסקנה סיבתית | `app/insights.py::_fetch_calls_to_closed` · `findings.json.calls_to_closed` | B44–B46c |
| **P4 — הפניה, משלים** | מהי הנטייה של רוכש להפנות אדם נוסף? | רוכשים בלבד; יעד `referred` | 13 ערכי סוף־קמפיין, בהנחה שההפניה טרם ידועה | הסתברות מכוילת, שיעור בסיס וקטגוריית נטייה | לתעדף בדיקת פוטנציאל להפניה | אינו ציון לקוח־על ואינו משלב LTV או אפסייל; אותו קלט מחזיר אותו פלט | `app/predict.py::predict_referral` · `models/P4.meta.json` · `metrics.json.P4_holdout` | B38–B40 |

### שלושת מצבי הקלט

1. **דוגמה היסטורית:** טעינת רשומה קיימת והפעלת המודל מדגימות את הזרימה בלבד.
   אם התוצאה האמיתית כבר ידועה, היא מוצגת בנפרד כ"תוצאה היסטורית". תחזית על
   שורת אימון אינה ראיית איכות; איכות נמדדת ב־CV וב־Holdout.
2. **תרחיש נערך:** שינוי ערך בדוגמה יוצר תרחיש what-if אסוציאטיבי. הוא מוכיח
   אינטראקציה וחישוב מחדש, לא השפעה סיבתית ולא לקוח אמיתי.
3. **תרחיש חדש:** אפשר לספק את כל הקלט התקין בלי לטעון דוגמה. `FunnelInput`
   עדיין נשלח עם 13 ערכים ו־`EarlyFunnelInput` עם ארבעה; הממשק רשאי לחשב
   ערך מוכח או להקל הזנה, אך אינו רשאי להשלים נתון חסר בניחוש.

### חוזה זרימת הקלט והקיפול — פאזה 10A, CP5

**הטופס המשותף.** מוצגים 12 שדות נערכים ו־`not_closed` לקריאה בלבד, המחושב
כ־`followup_5 - closed`; כל 13 הערכים נשלחים ללא שינוי לחוזי P2/P3/P4.
חוסר באחד מערכי המקור משאיר את הנגזרת חסרה. יתרה שלילית מוצגת כשגיאה
וחוסמת שליחה; אין חיתוך לאפס, שינוי שקט של `closed` או חישוב מחדש של CAC.

הזרימה היא: אישור הקשר לתרחיש עצמאי → עזר דוגמה היסטורית אופציונלי → סיכום
נתונים גלוי → אזור בדיקה ועריכה מתקפל → כפתור שליחה יחיד → שלושה פאנלים.
בתרחיש ריק אזור העריכה פתוח; אחרי טעינת דוגמה הוא סגור וניתן לפתיחה. דוגמה
שנערכה מסומנת `תרחיש שנערך`, דורשת אישור שהלקוח רכש והקמפיין הסתיים,
ומבטלת תוצאה קודמת עד שליחה. השינוי אינו נשמר ב־CSV או ב־Supabase.

טעינת הדוגמה היא עזר מקומי: loading, כשל או אפס שורות אינם נועלים קלט ידני,
אינם מוחקים אותו ואינם חוסמים שליחה תקינה. `source_row_id` נשמר פנימית בלבד;
המוצר מציג תווית סדרתית לא מזהה ואינו ממציא שם לקוח. דוגמה היסטורית אינה
ראיית איכות, ועריכתה היא תרחיש what-if אסוציאטיבי בלבד.

**P4S.** מוצגים ארבעת שדות `EarlyFunnelInput` בלבד ועזר דוגמה נפרד. לפני
שליחת תרחיש עצמאי או נערך נדרש אישור קצר שהרכישה ידועה, מעקב 1 הושלם וחלון
הגיוס החודשי נסגר. האישור אינו פיצ'ר חמישי, אינו נשלח ואינו נשמר. טעינת
דוגמה אינה מוכיחה לקוח חדש ואינה מחליפה את מסע הקבלה הידני של `B59`.

**תוצאות.** התשובה, המשמעות העסקית, הפעולה והמגבלה שמשנה החלטה נשארות
גלויות. רק פירוט מתודולוגי משני, גרסה ומדדים מתקפלים. ב־OOD אין מספר, ותנאי
ההפעלה נשארים גלויים גם בלי פלט. בכשל חלקי מוצג כל פאנל תקין לצד השגיאה
המקומית, בלי מסקנה משותפת שמסתמכת על פלט חסר.

**שפת מוצר וערכי דוגמה.** Overview מציג חמש יכולות עסקיות גנריות, ללא
“שאלות המייסדת”, קודי משימות או רשומות עבודה פנימיות. התווית התקציבית היא
`רמת הוצאת פרסום חודשית`. מספרי חיזוי במפרט ובמוקאפ הם placeholders או פלט
שמור מזוהה שמסומן כמקורו; מספר המחשה אינו ראיית חיזוי. בפרט אין להציג 18.4
וטווח צר שאינם פלט ה־split-conformal של אותה בקשה.

### חוזה מקור והמלצה — פאזה 10A, CP6–CP7

ה־CSV הוא snapshot סופי וקפוא של מטלת הקורס ולעולם לא יקבל נתונים חדשים.
המוצר אינטראקטיבי משום שהוא מחשב תחזית לתרחיש שהמשתמש מזין ומציג תוצאות
בתגובה לפעולתו; קריאות API ו־Supabase אינן זרם נתונים של חברה. כל מספר במסך
מגיע מתגובת הבקשה הנוכחית, מנגזרת של אותה תגובה או מנכס עובדות מצומצם
וגרסאי שנבנה מאותו snapshot.

המלצה אישית מוצגת רק כאשר הקלט בתחום, רמת הראיות אינה נמוכה, וב־P3/P4 גם
הציון מכויל ונמצא מעל שיעור הבסיס. תוצאה לא מכוילת אינה מוצגת כהסתברות
ואין לפעול לפיה. ב־P6 חפיפת שתי האסטרטגיות המובילות מונעת הכרזת מנצחת;
ב־Overview נשמר שוויון והמקסימום נבחר רק מרמות Low/Mid/High בעלות שיעור.
בכשל חלקי של Follow-up מוצג רק החלק התקין, בלי המלצה משולבת ובלי מספר שמור
כתחליף.

`app/static/business_facts.json` נוצר ב־CP9 ומכיל רק עובדות מאושרות,
גיבובי CSV ו־metrics, גרסאות ומפתחות מקור. עבור P2 הוא ישמור את המדורג ראשון
בכל אחד משלושת האלגוריתמים ויפיק `dominant_feature` רק אם שלושתם מסכימים.
הנכס אינו מקור לנתונים חדשים ואינו fallback לכשל API.

### CatBoost, P4 ו־P4S

CatBoost אכן **נבנה וכוונן** ל־P4: `budget_tier` הועבר אליו כקטגוריה נייטיבית,
והחיפוש כלל `learning_rate`, `depth` ו־`iterations`. ב־CV התקבל
ROC-AUC ממוצע 0.7897 לעומת 0.7847 ל־Logistic; שניהם היו זכאים לפי One-SE,
ו־Logistic נבחר לפי כלל הפשטות שקובע כי baseline זכאי גובר על Boosting,
לפני פתיחת ה־Holdout.
P4 מנבא `referred` בלבד מתוך 13 ערכי סוף־קמפיין.

ב־P4S שני המועמדים קיבלו אותה נגזרת `budget_tier`; Logistic היה המועמד היחיד
הזכאי לפי One-SE (ROC-AUC ממוצע 0.7925 לעומת 0.7835 ל־CatBoost) ולכן נבחר
במקור. CP9 החליף את הארטיפקט הנפרס ל־CatBoost לפי דרישת הבריף המפורשת.
P4S מנבא את היעד המשולב מארבעת ערכי ההקשר עבור רוכשים. אלה שני מודלים ושתי
שאלות שונות; אין להציג את P4 כאילו הוא ציון לקוח־על.

**הכרעת משתמש, 12.09.2026:** רצף ההוראות בחבילה 4 נקרא שמרנית: ציון ה־0–100
המשולב יופק באמצעות CatBoost שאומן על יעד P4S וארבעת הקלטים המוקדמים.
Logistic נשאר מודל השוואה ותוצאתו הטובה מעט יותר ב־CV מדווחת ביושר, אך הוא
לא יהיה המודל הראשי. CatBoost של P4 ממשיך לנבא `referred` בלבד ואינו מוחלף
ב־P4S. הארטיפקט וה־API הפעילים הם `P4S-catboost-20260912-1c70ca8`;
B41 נשארת `planned` רק עד חשיפת ציון 0–100 במסע הדשבורד.

### ראיות ותשובות CP4-A — P2/P3

**P2 — אורך חיי לקוח.** שלושת מודלי ה־boosting כמעט זהים ב־CV: CatBoost
RMSE=2.804 ו־R²=0.9451, XGBoost ‏2.809/0.9448 ו־LightGBM ‏2.820/0.9443.
CatBoost שנבחר השיג ב־Holdout ‏MAE=2.106 חודשים, RMSE=2.705 ו־R²=0.9481.
טווח ה־split-conformal מוסיף/מחסיר 5.139 חודשים (עם חיתוך תחתון באפס) וכיסה
92.89% מה־Holdout; זה פחות מכיסוי ה־95% המתוכנן ולכן מוצג כאומדן אי־ודאות,
לא כהבטחה. `calls_to_closed` הוא הפיצ'ר הראשון בכל שלושת המודלים וב־SHAP של
המנצח ערכו 9.556 לעומת 0.463 לפיצ'ר הבא. יחידות importance שונות בין ספריות,
ולכן משווים דירוג בתוך מודל ולא את המספרים הגולמיים ביניהן.

**תשובת P2 בשני משפטים:** מספר השיחות הממוצע עד סגירה הוא האות החזק ביותר
במודל לאורך חיי הלקוח, אך זו תלות חזויה מנתוני סוף הקמפיין ולא הוכחה שיותר
שיחות מאריכות את הקשר. Northbound צריכה להשתמש בו לפילוח ובדיקת מדיניות
המעקב בניסוי שמודד שימור בפועל, ולא להגדיל שיחות אוטומטית לפי התחזית.

**P3 — רכישה נוספת.** באוכלוסיית 3,163 הרוכשים יש 1,466 חיוביים (46.35%)
ו־1,697 שליליים (53.65%); יחס המשקל בכל fold הוא כ־1.16 בלבד. ב־XGBoost
שנפרס, weighting הוריד ROC-AUC ממוצע מ־0.79357 ל־0.79281. השיפורים הזעירים
ב־CatBoost (+0.00068) וב־LightGBM (+0.00029) אינם מצדיקים שינוי, ולכן ההחלטה
הסופית היא **לא להשתמש במשקלי מחלקה במודל P3 הנפרס**. זו ראיית הסגירה של B31.

| מדד P3 | XGBoost ב־Holdout | baseline רוב באותו Holdout | משמעות |
|---|---:|---:|---|
| Accuracy | 76.15% | 53.71% | תוספת 22.43 נקודות אחוז; Accuracy לבדו מטעה כי baseline אינו מזהה אף רכישה נוספת |
| Precision | 69.19% | 0% | מתוך המסומנים כחיוביים, כ־69% אכן חיוביים |
| Recall | 87.37% | 0% | המודל מזהה את רוב החיוביים, במחיר false positives |
| F1 | 77.22% | 0% | איזון בין Precision ל־Recall בסף 0.5 |
| ROC-AUC | 78.94% | 50% | יכולת דירוג בין חיובי לשלילי לאורך כל הספים |

שיעור הבסיס 46.35% הוא שיעור ה־upsell החיובי; הוא אינו baseline הדיוק.
המודל מכויל ב־sigmoid; Brier=0.1733 ו־log loss=0.5221 ב־Holdout. לכן פלט
המוצר הוא הסתברות מושווית לשיעור הבסיס, לא תוצאת רכישה מובטחת.

חשיבות P3 מרוכזת בעיקר ב־`calls_to_closed` (mean |SHAP|=0.926) וב־CAC
(0.411), עם תרומות קטנות יותר של `leads_answered`, `followup_2` ושאר הקלטים.
כלומר יש אות דומיננטי וצירוף משני, לא כלל של שדה יחיד. כלל הבריף הרטרוספקטיבי
(`ltv_months>23` ו־CAC<1000) פשוט וב־CV מדויק יחסית כשהוא מסמן חיובי
(Precision=72.33%), אך מזהה רק 27.08% מהחיוביים ו־LTV אינו זמין בזמן החיזוי.
הכלל התפעולי הזמין (`closed>3` ו־CAC<1000) חלש עוד יותר: Recall=12.47%
ו־ROC-AUC=0.537. המודל עדיף לזיהוי רחב; הכללים מועילים רק כהסבר שמרני או
מסנן ידני, ואינם מחליפים אותו.

### ראיות ותשובות CP4-B — P4/P4S

**P4 — נטייה להפניה, יכולת משלימה.** Logistic שנבחר לפי One-SE ופשטות השיג
ב־Holdout ‏ROC-AUC=0.7989, ‏PR-AUC=0.6993, ‏Precision=68.18%, ‏Recall=83.33%
ו־F1=75.00%. העשירון העליון כלל 64 רשומות, עם 73.44% הפניות לעומת שיעור בסיס
של 42.65% באותו Holdout (lift=1.72). הכיול הוא sigmoid; ‏Brier=0.1722
ו־log loss=0.5192. לפי טייר תקציב, שיעור בפועל/ממוצע חזוי היה
16.38%/22.32% ב־High, ‏12.20%/9.84% ב־Low ו־67.87%/68.57% ב־Mid;
לכן הכיול טוב במיוחד ב־Mid ופחות מדויק בקצוות. אלה ראיות ליכולת דירוג סבירה,
לא להבטחה לגבי אדם מסוים.

הסבר ה־SHAP של המודל הנפרס מדרג בראש את `followup_3` (1.484), אחריו
`calls_to_closed` (0.822), `followup_2` (0.587), `followup_4` (0.571)
ו־`leads_answered` (0.491). אין לערבב זאת עם חשיבות CatBoost, שבו
`calls_to_closed` ראשון אך המודל אינו המודל הנפרס. כל הקלטים הם נתוני
סוף־קמפיין, ולכן השימוש המותר הוא בדיקה ידנית אם כדאי לבקש הפניה; אין להסיק
שהגדלת מעקבים תגרום להפניה ואין להציג P4 כציון לקוח־על.

**P4S — ציון לקוח־על.** היעד הוא `referred=Yes AND upsell=1 AND
ltv_months>=34` בקרב 3,163 רוכשים. 529 מהם חיוביים (16.72%); היסטורית הם
יצרו 33.61% מהרווח המצטבר ועלות הרכישה הממוצעת שלהם הייתה 990.71 ש״ח,
לעומת 1,437.46 ש״ח בכלל הרוכשים — פער של 31.08%. זהו פרופיל תיאורי של
התוצאה שכבר קרתה, לא הוכחה שהמודל יודע לזהות אדם כזה מראש.

ב־CV על ארבעת הקלטים, Logistic השיג ROC-AUC=0.7925 ו־PR-AUC=0.3462,
לעומת 0.7835 ו־0.3171 ב־CatBoost. בשני המודלים, בסף 0.5, ‏Precision,
Recall ו־F1 היו אפס וה־Accuracy ‏83.30% הייתה זהה למעשה ל־majority baseline;
לכן Accuracy זה אינו הישג שימושי. הוא אינו מבטל אות דירוג רציף, אך הוא שולל
הצגה של הסף הקיים כמסווג שמאתר לקוחות־על.

ראיית Logistic ההיסטורית נשמרה ב־`metrics.json.P4S_pre_cp9_audit`:
‏ROC-AUC=0.8187, ‏PR-AUC=0.3779, ‏Brier=0.1126 ו־Recall=0 בסף 0.5.
CatBoost שנפרס וכויל ב־sigmoid השיג ב־Holdout ‏ROC-AUC=0.8014,
PR-AUC=0.3420, ‏Brier=0.1145 ו־Recall=0 בסף 0.5 — מעט חלש יותר.
כל החיוביים ב־Holdout
נמצאים בטייר Mid; ב־Low וב־High אין חיוביים ולכן AUC אינו מוגדר, ובתוך Mid
ה־ROC-AUC של CatBoost הוא 0.5285 בלבד. מכאן שה־AUC הכולל מושפע מאוד מהפרדת טיירים ואינו
ראיה מספקת לתעדוף אישי בין רוכשים דומים. ל־P4S גם אין ארטיפקט SHAP קיים,
ולכן אין לייחס לו “גורם מוביל” שלא נמדד.

CP9 השתמש בפרמטרים שכבר נבחרו (`depth=6`, ‏`iterations=400`,
`learning_rate=0.01`), כייל על קבוצת הכיול והעריך פעם אחת על אותו Holdout,
בלי חיפוש, כוונון סף או בחירה לפיו. מאחר שה־Holdout כבר נפתח עבור Logistic,
זו הערכה post-hoc לאחר הכרעת דרישה ולא פתיחה עיוורת ראשונה.

**תשובת P4S בשני משפטים:** הנתונים מאפשרים לתאר היטב מי היו לקוחות־העל
בדיעבד, אך ארבעת האותות הקיימים עדיין אינם מצדיקים זיהוי או דירוג אישי אמין.
CatBoost מאפשר להשתמש בציון הרציף רק כאות מסייע לבדיקה ידנית של רוכש ידוע,
אחרי מעקב 1 וסגירת החלון החודשי; אין תעדוף אוטומטי, וביצועיו החלשים מעט
מ־Logistic מוצגים בגלוי.

### ראיות ותשובות CP4-C — P6

**איכות המודל המלא אינה איכות הסימולטור.** LinearRegression נבחר לפי One-SE
ופשטות, והשיג ב־Holdout ‏MAE=3,678.89 ש״ח, RMSE=4,590.95 ו־R²=0.7961 כאשר
כל 14 הפיצ'רים קיימים. בעשירון הרווח העליון הוא נטה לחסר בממוצע 5,328.92
ש״ח. הסימולטור, לעומת זאת, יודע רק את התקציב ומחליף את יתר הפיצ'רים בפרופיל;
לכן מדדי המודל המלא אינם מאמתים את איכות ההקצאה.

| אסטרטגיה | נקודת המודל | טווח bootstrap | תמיכת אימון |
|---|---:|---:|---:|
| 100×500 | 789,594 ש״ח | 379,299–856,634 | n=92, בינונית |
| 25×2,000 | 530,953 ש״ח | 519,335–549,756 | n=322, גבוהה |
| 10×5,000 | 227,214 ש״ח | 177,031–261,079 | n=249, גבוהה |
| 2×20,000 + 1×10,000 | 7,642 ש״ח | 477–13,981 | n מינימלי=57, בינונית |

לפי נקודת המודל, `100x500` ראשונה ו־`25x2000` שנייה. הטווח של השנייה נמצא
כולו בתוך טווח הראשונה; זו חפיפה תיאורית בין שני טווחים נפרדים, לא מבחן
פורמלי להפרש ולא הוכחה לשוויון. הטווח הוא שונות של אומדן הסכום בעת דגימה
חוזרת ואימון מחדש על נתוני האימון; הוא אינו טווח חיזוי לרווח שיתקבל בפועל.

**Backtest לפי רמת תקציב מדויקת:** ברמת 500 ש״ח המודל חזה 7,895.94 ש״ח
לרשומה לעומת ממוצע Holdout ‏918.65 ש״ח (n=17, פי 8.59). ב־2,000 ש״ח הוא
חזה 21,238.12 לעומת 20,650.88 (n=85); ב־5,000 — 22,721.45 לעומת
20,643.35 (n=63); ב־10,000 — 6,047.50 לעומת 5,743.39 (n=33); וב־20,000 —
797.13 לעומת 4,470.14 (n=14). תחת אותה הנחת אדיטיביות, הכפלת ממוצעי
ה־Holdout נותנת benchmark של 91,865 ש״ח ל־`100x500`, ‏516,272 ל־`25x2000`,
‏206,433 ל־`10x5000` ו־14,684 לאסטרטגיה המרוכזת. זה אינו ניסוי הקצאה אמיתי,
אך הוא מראה שההובלה של `100x500` אינה יציבה וש־`25x2000` היא היחידה מבין
שתי המובילות שנקודת המודל שלה קרובה ל־benchmark הזה.

**ראיית התשואה הפוחתת היא תיאורית.** חציון הלידים עולה מ־13 בתקציב 500
ש״ח ל־122 בתקציב 20,000 ש״ח: התקציב גדל פי 40, אך הלידים רק פי 9.38.
במונחי לידים לכל 1,000 ש״ח, היחס יורד מ־26 ל־6.1 (ובאמצע: 15 ב־2,000
ו־10.6 ב־5,000). זה תומך בכיוון של פיזור, אך אינו מוכיח שהקטנת כל קמפיין
תגדיל רווח מצטבר.

**תיקון CP9:** בכל רמת תקציב נבחרת שורת train נצפית אחת שממזערת מרחק L1
מנורמל־IQR מחציוני הקבוצה; `source_row_id` שובר שוויון והיעד אינו משתתף.
כך נשמרות זהויות המשפך. שורות המקור הן 1074, 880, 2351, 1773 ו־532 לרמות
500, 2,000, 5,000, 10,000 ו־20,000. הסימולציה וה־bootstrap נבנו מחדש מ־train
בלבד; תחזיות ה־backtest עודכנו מול ערכי ה־actual הקפואים בלי לפתוח שוב את
Holdout של P6. ארבע האסטרטגיות וחוזה ה־API לא השתנו.

**תשובת P6 בשני משפטים:** עקומת הלידים וה־backtest תומכים בכיוון של פיזור
מתון, אך אינם מאמתים את `100x500` שהמודל מדרג ראשון והטווחים המובילים חופפים.
אין להעביר לפיה את מלוא 50,000 ש״ח; אם נדרש ניסוי מבין החלופות, להתחיל במדגם
מוגבל בדפוס `25x2000`, למדוד רווח מצטבר באותו אופק, ורק אז לשנות הקצאה.

---

## הצגת אי-ודאות — סגור

**העיקרון:** ההקפדה המתודולוגית על ניסוח מדויק נמחקת אם המסך מציג "87" בלי
הקשר. **טווח שמוצג חייב שיטת חישוב מתועדת** — אין פס ויזואלי שרירותי לפי גודל
מדגם.

| # | תצוגה ראשית | לצידה | שיטה |
|---|---|---|---|
| P2 | `תחזית: {X} חודשים` — מהתגובה, מעוגל ובולט | `טווח חיזוי: {Y}–{Z} חודשים` — מאותה תגובה, משני אך גלוי | **Split Conformal** על סט calibration נפרד ⇒ `interval_method = split_conformal`. ⚠ **אין** להציג RMSE/סטיית תקן של CV כטווח אישי לרשומה |
| P3 | `נטייה לאפסייל: {X}%` | הפרש בנקודות משיעור הבסיס 46.35% + קטגוריית נטייה | הסתברות מסווג |
| P4 | `נטייה להפניה: {X}%` | קטגוריה + שיעור בסיס 42.71% + **`התחזית מעריכה את הסיכוי שהלקוח יפנה לקוחות נוספים. ציון לקוח-על, שמשלב הישארות, רכישה נוספת והפניה, מוצג במסך נפרד.`** + סטטוס כיול + גרסת מודל | הסתברות מסווג |
| P4S | `ציון לקוח-על: {X}` (0–100, מומר מהסתברות ה-API) | שיעור בסיס 16.72% + `calibration_status="calibrated"` (חובה, אין נפילה ל-uncalibrated) + גרסת מודל | Brier + עקומת כיול (P4S) |
| P6 | רווח צפוי לאסטרטגיה, מדורג | טווח + `n` + רמת ראיות | **Bootstrap על רשומות** (ראה פרוטוקול) |

⚠ **הכותרת ב-P4 היא "נטייה להפניה", לא "ציון לקוח-על"** — הציון ללקוח-על
מוצג רק בפאנל P4S. סטטוס הכיול וגרסת המודל מוצגים בפרטי התוצאה, לא רק
ב-REPORT, בשני הפאנלים.

⚠ **P6 — כשהטווחים של האסטרטגיות המובילות חופפים מהותית, אין להכריז על מנצחת.**
הדירוג מנוסח `מועדפת לפי המודל`, לא כהבטחת רווח. חפיפה → הצגה שההבדל אינו
חד-משמעי + המלצה לניסוי מבוקר.

⚠ **מדדי המודל (RMSE, R², Precision/Recall/F1/ROC-AUC) שייכים ל"פרטי מודל"
ול-REPORT** — לא לצד כל חיזוי, כאילו הם אי-הוודאות של הרשומה היחידה.

### מתי לא מציגים מספר

| מצב | תגובה |
|---|---|
| קלט חסר או לא תקין · **הפרת `Data invariant`** (זהות משפך, מונוטוניות, ערך שלילי) | **validation error**, ללא חיזוי. ⚠ זו **אינה** בדיקת OOD |
| **מחוץ לתחום האימון (OOD)** — `ad_budget` < 500 או > 20,000 · פיצ'ר מספרי מחוץ ל-min/max | `אין מספיק נתונים לחיזוי אמין` + הסיבה · `in_training_domain = false` |
| **ערך פנימי שלא נצפה** — בתוך 500–20,000 אך לא אחד מ-16 הערכים (למשל 3,500) | **חיזוי מוחזר** · `evidence_level = low` + `warnings` שהערך לא נצפה + טווח תקף. אם אין טווח תקף — אין המלצה חד-משמעית. **אינו OOD קשיח** |
| מדגם קטן בתוך התחום | רמת ראיות נמוכה. אם אין טווח תקף — **אין המלצה חד-משמעית** |

⚠ **תיקון S10 (פאזה 7, 05.09.2026 — אושר בביקורת Codex ואושר על ידי
המשתמש):** הניסוח הקודם בשורה
האחרונה — *"רמת ראיות נמוכה + טווח רחב לפי השיטה"* — ניתן לקריאה כקשר
סיבתי ("הרחב את הטווח כשהראיות נמוכות"), ואינו מתקיים בפועל. תוקן:

> הטווח מחושב לפי השיטה שנקבעה מראש (`split_conformal` ל-P2 ·
> `bootstrap_percentile` ל-P6). `evidence_level` מתאר תמיכת קלט בלבד
> ואינו משנה את `lower_bound`/`upper_bound`. ב-P2 הקוונטיל קבוע לכל
> הרשומות והגבול התחתון נחתך באפס; הטווח אינו מותאם לרמת הראיות או
> לגודל מדגם מקומי.

הראיה: `conformal_interval` ב-`scripts/train.py:407-414` הוא
`max(0.0, point − q), point + q` עם `q = 5.139` יחיד לכל הרשומות.
⚠ גם הטענה ההפוכה — "רוחב הטווח ב-P2 קבוע לכל הרשומות" — שגויה: החיתוך
באפס מקצר את הטווח כאשר `point < q`. הקבוע הוא **הקוונטיל**, לא הרוחב.

**ארבעה מונחים נפרדים — אין לערבב:**

| מונח | הגדרה |
|---|---|
| `Data invariant` | חוק מבני שחייב להתקיים — זהויות המשפך, מונוטוניות, אי-שליליות |
| `Training-domain boundary` | תחום ה-min/max שנצפה באימון |
| `Unseen value inside training range` | ערך פנימי חוקי שלא נצפה בדיוק |
| `Snapshot expectation` | מאפיין של קובץ המקור הנוכחי; **אינו** חוק עסקי קבוע |

**בדיקת OOD — מוגדרת קונקרטית** (אחרת זו דרישה ריקה): `ad_budget` מחוץ
ל-500–20,000, או כל פיצ'ר מספרי מחוץ ל-min/max של האימון. הגבולות נשמרים
ב-metadata של הארטיפקט.

⚠ **הפרת זהות משפך אינה בדיקת OOD** — היא validation error על תקינות
הקלט. ערבוב של השתיים מטשטש את ההבחנה בין קלט פגום לבין קלט תקין שרחוק
מהתפלגות האימון.

⚠ **16 ערכי התקציב הם `Snapshot expectation`, לא `Data invariant`** — אין
לדחות לפיהם קלט חוקי. ערך פנימי שלא נצפה מקבל רמת ראיות נמוכה, לא
דחייה.

### פיצול הנתונים — שלושה חלקים, לא שניים

**20% Holdout לכל משימה, נפתח פעם אחת בלבד.** 5-fold CV מתבצע אך ורק
בתוך 80% נתוני הפיתוח. סט ה-calibration נלקח **מתוך** ה-80%, לא בנוסף.
**הפיצול נעשה בנפרד לכל משימה, ב-seed קבוע ומתועד; האינדקסים נשמרים.**

| משימה | N | train | calibration | holdout | stratify |
|---|---|---|---|---|---|
| **P2** `ltv_months` | 3,163 | 2,024 (64%) | 506 (16%) | 633 (20%) | לא — רגרסיה |
| **P3** `upsell` | 3,163 | 2,024 (64%) | 506 (16%) | 633 (20%) | כן — `upsell` |
| **P4** `referred` | 3,163 | 2,024 (64%) | 506 (16%) | 633 (20%) | כן — `referred` |
| **P6** `cumulative_profit` | 3,471 | 2,776 (80%) | **אין** | 695 (20%) | לא — רגרסיה |

⚠ **תיקון (פאזה 6, אישור המשתמש 04.09.2026):** הגדלים ל-P6 היו 2,777/694.
`train_test_split(test_size=0.2)` מחשב `n_test = ceil(0.2·n)`; עבור
n=3,471 זה 695, ולכן train=2,776. תוקן כאן ל-2,776/695 — הגדלים
האמיתיים בפועל, לא ניחוש.

**תפקיד סט ה-calibration שונה בין המשימות:** ב-P2 — **Split Conformal**
לטווח החיזוי, לא כיול הסתברות. ב-P3/P4 — **התאמת מכייל sigmoid**. הוא
אינו משמש להתאמת המודל ואינו ה-Holdout.

⚠ **P6 אינו זקוק לסט calibration**: רגרסיה, טווחו מ-Bootstrap, אין לו
מכייל. כפיית 64/16/20 עליו הייתה מקפיאה 555 שורות אימון לחינם.

⚠ **P3 ו-P4 אינם יכולים לחלוק פיצול** אף שהם על אותן 3,163 שורות —
ה-stratification לפי יעד שונה. שלושה פיצולים נפרדים מעל אותה אוכלוסייה,
כל אחד עם seed משלו.

**שתי אוכלוסיות משנה:**

⚠ **תיקון (פאזה 6, אישור המשתמש 04.09.2026):** הניסוח הקודם — "פיצול
משלו" וגם "אסור לגעת ב-Holdout של P4 הראשי" על "כלל 3,500" — אינו ניתן
לקיום: 3,500 מכיל את 633 שורות ה-Holdout של P4, ולכן כל פיצול עצמאי
מכניס חלק מהן לאימון. תוקן:

ניתוח הרגישות של P4 על כלל האוכלוסייה **מתאמן** על 3,500 פחות 633 שורות
ה-Holdout של P4 (= **2,867 שורות**), עם folds פנימיים משלו (seed 46),
**בהיפר-פרמטרים בירושה מהזוכה של P4 ובלי שלב כוונון חדש** — ולכן גם
ההיפר-פרמטרים נקיים מה-Holdout. **אין לו Holdout נפרד:** שני המודלים —
P4 הראשי ורגישות כלל-האוכלוסייה — מוערכים על **אותן 633 שורות**, בתוך
**הפתיחה היחידה** של ה-Holdout של P4, רשומים מראש כחלק ממנה (אותה
מכניקה שכבר מותרת ל-Backtest ארבע האסטרטגיות למטה). אף אחד מהם לא ראה
את 633 השורות באימון או בכוונון.

הגרסה המוקדמת (אחרי follow-up 1) **חולקת את פיצול** המשימה שאליה היא
מושווית.

### פרוטוקול Bootstrap ל-P6

⚠ *הניסוח הקודם "Bootstrap על folds האימון" הוסר — חמישה folds אינם יחידות
דגימה ואינם מייצרים התפלגות אי-ודאות תקפה.*

0. ⚠ **תיקון (פאזה 6, S7, אישור המשתמש 05.09.2026):** הניסוח הקודם —
   "למודל הזוכה בלבד" וגם "ה-Baselines... אינם מקבלים טווחי Bootstrap"
   — סותר את עצמו כאשר הזוכה בפועל הוא Baseline (`LinearRegression`
   או `LogisticRegression`), בדיוק המצב שקרה ב-P6 (checkpoint 10).
   תוקן: **ה-Bootstrap רץ על המודל הנפרס הזוכה, גם אם הוא Linear**;
   רק `Dummy` ומודלים מפסידים (Baseline או Boosting שלא נבחרו) אינם
   מקבלים טווח — אין להם תצוגה בממשק.
1. בכל חזרה דוגמים מחדש **רשומות מנתוני האימון, עם החזרה**.
2. בתוך כל דגימה מריצים מחדש את **כל** צינור P6: preprocessing, התאמת המודל,
   חישוב פרופילי המשפך לכל תקציב, וסימולציית ארבע האסטרטגיות.
3. שומרים את הרווח הכולל החזוי לכל אסטרטגיה בכל חזרה.
4. הטווח נגזר מאחוזונים **שנקבעו מראש** (2.5% / 97.5% לטווח 95%) ומדווח
   כ-`interval_method = bootstrap_percentile`.
5. מספר החזרות נקבע מראש לפי זמן ריצה ויציבות האחוזונים, מתועד ב-metadata
   וב-REPORT — **ולא נבחר אחרי הסתכלות על התוצאה**.

**ה-Holdout אינו נכנס לדגימות.** הטווח משקף שונות מדגמית ומודלית תחת הנחות
הסימולטור; הוא **אינו** מבטל את מגבלת הסיבתיות ואת הנחת האדיטיביות.

⚠ **תיקון מחייב מ־CP4-C:** “חישוב פרופיל” בשלב 2 אינו רשאי עוד להיות חציון
נפרד לכל עמודה. CP9 החליף אותו בבחירה רב־משתנית של שורת train תקפה שנצפתה
באותה רמת תקציב, ללא שימוש ביעד, ושחזר בעזרתה את אומדן הנקודה ואת 1,000
דגימות ה־bootstrap מנתוני train.

### פרוטוקול כיול ל-P3, P4 ו-P4S — מדידה אינה כיול

⚠ **Brier ועקומת כיול מודדים את איכות הכיול; הם אינם מכיילים.** אם הממשק מציג
`calibrated`, חייב להיות calibrator שהותאם בפועל.

**השיטה נעולה מראש: `sigmoid`.** ⚠ *הניסוח הקודם — "sigmoid מול isotonic
בפרוטוקול שנקבע מראש" — הוסר. בחירה בין שתי שיטות על אותו סט שמתאים אותן היא
הערכה אופטימית: הסט לא יכול גם להתאים וגם לשפוט.*

1. המודל וההיפר-פרמטרים נבחרים ב-CV **על train בלבד**.
2. אחרי הנעילה, calibrator מסוג **sigmoid** מותאם **פעם אחת** על סט
   ה-calibration. השיטה נקבעה **לפני** פתיחת הסט.
3. ה-Holdout משמש **פעם אחת** להערכת **המערכת המלאה** — preprocessing + model
   + calibrator.
4. Brier, **log loss**, PR-AUC, עקומת כיול ויתר המדדים מדווחים על
   ה-Holdout עבור המערכת הסופית.
5. **`isotonic` אינו חלק מה-production.** מוזכר כאפשרות עתידית עם יותר נתונים
   או פרוטוקול nested calibration — **לא כבחירה מול sigmoid על אותו סט**.
6. ה-metadata וה-API מחזירים `calibration_status`, `calibration_method`
   וגרסת המודל/המכייל.
7. **`calibration_status = calibrated` רק אם התאמת ה-sigmoid הצליחה.** אחרת
   `uncalibrated` + אזהרה.

⚠ **P4S (פאזה 8A, `docs/planning/PHASE8A.md` D10) מחמיר על כלל 7:** אין
נפילה ל-`uncalibrated` — `calibration_status` נעול ל-`"calibrated"`
בלבד בחוזה; כשל כיול הוא כשל רועש בזמן בניית הארטיפקט, לא ערך חלופי
שמוגש. פרוטוקול הכיול עצמו (sigmoid, פעם אחת, לפני פתיחת ה-Holdout)
זהה לחלוטין לשלושת המשימות.

### Design tokens חדשים

`evidence-high` · `evidence-medium` · `evidence-low` · `prediction-range`
(רכיב נקודה+טווח) · `model-disclaimer` · `out-of-distribution` ·
`calibrated` / `uncalibrated` · טיפוגרפיה משנית לראיות ולהסתייגות — **קטנה
יותר, לא קטנה עד כדי הסתרה**.

⚠ **אין להסתמך על צבע בלבד** — כל מצב מקבל גם טקסט וגם אייקון. **צבעי
אי-ודאות ניטרליים/ענבריים**, לא ירוק/אדום שמרמזים הצלחה או כישלון.

### חוזי תגובה — שדות מינימליים

`point_estimate` · `lower_bound` · `upper_bound` · **`interval_method`** ·
`event_probability` · `base_rate` · `propensity_band` · `evidence_level` ·
`sample_size` · `in_training_domain` · `warnings` · `model_version` ·
`calibration_status` · **`calibration_method`**.

**שני הערכים הקנוניים היחידים של `interval_method`:** `split_conformal`
(P2) · `bootstrap_percentile` (P6). **נעולים כאן**; פאזה 8 מקבעת אותם
בסכמה ואינה מכריעה בהם.

⚠ **אין להחזיר טווח או רמת ראיות שאין מאחוריהם שיטת חישוב מתועדת.**

---

**Prefill הוא קיצור דרך, לא תנאי שימוש.** בטופס המשותף אפשר לטעון רשומה
היסטורית מ־`public.funnel_records` (`purchased = 1`, עם JWT), לערוך אותה,
או להזין תרחיש תקין בלי לטעון דוגמה. כל שליחה עדיין נושאת את 13 ערכי
`FunnelInput`; CP5 מגדיר כיצד לצמצם את עומס ההזנה בלי להשמיט ערך מהחוזה,
להמציא ערך או לחסום תרחיש עצמאי.

החלופה הישנה "פרופיל לפי תקציב" אינה מקור קלט לטופס המשותף. ⛔ מנגנון P6
נפרד ממנו: CP4-C הוכיח שחציון נפרד לכל עמודה יוצר קלט בלתי־תקף. חוזה היעד
ל־CP9 הוא פרופיל רב־משתני נצפה, תקף ודטרמיניסטי מתוך train בלבד, ללא היעד.

⚠ **תוקן בשער `9→10`:** המספר היה 12 ואינו תואם את `FunnelInput`, שמונה **13**
שדות (`app/features.py`, `IA.md` §3.1).

⛔ **הכלל הזה אינו חל על מסך P4S.** ארבעת שדותיו המוקדמים —
`ad_budget`, `num_leads`, `leads_answered`, `followup_1` — הם דווקא המסלול
המעשי להזנה ידנית עבור **רוכש חדש** לאחר שחלון הגיוס נסגר ומעקב 1 הושלם.
הזנה ידנית היא המסלול הראשי ו-prefill הוא עזר בלבד (`IA.md` §3א.3,
`PHASE10.md` D7ב). אין להשתמש בו לליד לפני רכישה.

---

## שש החבילות

1. **ניקוי + EDA.** חסרים, כפילויות + רגישות, קורלציות מול `cumulative_profit`,
   עקומת `ad_budget → num_leads`, המרה לפי טיירים (Low ≤1500 / Mid 2000–5000 /
   High >5000) → `docs/FINDINGS.md` + `GET /api/insights/budget-tiers`.
2. **P2 — `ltv_months`.** XGBoost + LightGBM + CatBoost, 5-fold CV, RMSE + R²,
   **ממוצע ± סטיית תקן**. השוואת feature importance בשלושתם, עם הערת הקולינאריות.
   **Baselines:** `DummyRegressor` · `LinearRegression`.
   **מדד ראשי: MAE** — היעד בחודשים והשגיאה מתפרשת ישירות; ההתפלגות
   סימטרית (skew −0.03, kurtosis −1.49) ולכן MAE ו-RMSE מדרגים כמעט
   זהה. RMSE ו-R² משניים ומדווחים — דרישת הבריף.
3. **P3 — `upsell`.** אותם שלושה מסווגים, **stratified** 5-fold,
   Accuracy/Precision/Recall/F1/ROC-AUC מול baseline 53.65%. **כיול מפורש**
   כמו ב-P4 — הממשק מציג הסתברות. **להשוות תצורה
   רגילה מול משוקללת ולהכריע על ראיה.** + שני הכללים הידניים.
   **Baselines:** `DummyClassifier(strategy="most_frequent")` ·
   `LogisticRegression`.
   **מדד ראשי: ROC-AUC.** משניים: PR-AUC, Brier, log loss,
   Top-decile lift, ויתר מדדי הבריף.
4a. **P4 — `referred`.** CatBoost עם `budget_tier` קטגוריאלי נייטיבי. Tuning
   ב-**5-fold CV בתוך חלק האימון**. *(Nested CV הוסר: תפקידו לתת אומדן
   הכללה בלתי-מוטה כשיש tuning — וה-Holdout כבר עושה בדיוק את זה.
   ⚠ ה-tuning עצמו **נשאר** במלואו, בתוך ה-5-fold CV.)*
   **Baselines מחייבים:** `DummyClassifier` · **`LogisticRegression`** —
   כדי להוכיח שהמורכבות של CatBoost מוסיפה ערך.
   **מדד ראשי: ROC-AUC** — ⚠ **לא Brier**: sigmoid הוא טרנספורמציה
   מונוטונית, ROC-AUC חסין לה ו-Brier אינו, ולכן בחירה לפי Brier ב-CV
   מדרגת לפי הסקאלה הלא-מכוילת שהמכייל בא להחליף. Brier, log loss,
   PR-AUC ו-Top-decile lift מדווחים **אחרי** הכיול.
   ⚠ **תיקון D20 (`PHASE9.md`, 09.09.2026): ⛔ אין ציון 0–100 ל-P4.**
   הנתיב `POST /api/predict/referral` מתועד כ**endpoint משלים** לחיזוי
   הפניה בלבד (`target=referred`) — ⛔ אינו התשובה לשאלה 3 של המייסדת.
   ר' סעיף P4S מיד לאחר מכן.

4b. **P4S — `super_customer` (יעד סינתטי, פאזה 8A).** התווית המשולבת:
   `referred=Yes AND upsell=1 AND ltv_months>=34`, מחושבת ב-
   `app/features.py:super_customer_label` (מקור אמת יחיד). נתיב:
   `POST /api/predict/super-customer`. **ציון 0–100** — ה-API מחזיר
   הסתברות 0–1 (חוזה נעול, `SuperCustomerPrediction`); **פאזה 11
   ממירה ומציגה** 0–100, כלשון הבריף. `calibration_status` נעול
   ל-`"calibrated"` בלבד (⛔ אין נפילה ל-uncalibrated). ⛔ **פרטי
   האימון, המודל שנבחר, וקריטריוני הקבלה אינם נכתבים כאן מחדש** —
   הפניה מלאה ל-`docs/planning/PHASE8A.md` (הכרעות D1–D21, 24
   קריטריוני קבלה, פתיחת ה-Holdout). ⛔ אין להמציא כאן מספר שאינו
   מופיע שם.

   **מועד השימוש:** רק לרוכש ידוע, לאחר סגירת חלון גיוס הלידים החודשי ולאחר
   השלמת מעקב 1 לקבוצה הקבועה. זהו "מוקדם" ביחס למימוש LTV/אפסייל/הפניה,
   לא לפני רכישה. כל לקוחות אותה רשומה בעלי אותם ארבעה ערכי הקשר יקבלו אותו
   ציון; אין בקובץ מאפיין אישי נוסף. סדר הזמן אינו מוכח בתאריכים ולכן מוצג
   כהנחת שימוש ומגבלה.

   **פרופיל סופר-לקוח (ניתוח תיאורי נפרד, קדם ל-8A):**
   `purchased = 1 AND referred = Yes AND upsell = 1 AND ltv_months >= 34`
   (רבעון עליון, נגזר מהנתונים ומוצג כבחירה מוצהרת).
   נמדד: **529 רשומות** מתוך 3,163 (**16.72%**) — ⚠ הספירה היא ברמת רשומות
   המקור; אין מזהה לקוח שמוכיח ששורה = לקוח נפרד — **33.61% מסך הרווח**,
   **CAC ממוצע 990.71 ₪ מול 1,437.46 ₪ — 31.1% זול יותר.**
   *ממצא ה-CAC יציב בכל סף שנבדק (חציון/רבעון/עשירון) ולכן אינו תלוי בבחירה.*
   ⚠ **אותה הגדרה בדיוק הפכה בפאזה 8A ל-target של מודל מאומן (P4S)** —
   הניתוח התיאורי כאן קדם לכך ונשאר תקף כפרופיל, לא כחיזוי.
5. **P5 — follow-up.** נשירה לכל שלב + התפלגות `calls_to_closed` + המלצת מדיניות.
   **הניסוח המדויק** (ראה סעיף המסקנה למטה) — לא "מנהל המכירות טועה".
6. **P6 — סימולטור.** מודל `cumulative_profit`, ₪50,000:
   **2×20,000 + 10,000** · 10×5,000 · 25×2,000 · 100×500.
   ⚠ כל אסטרטגיה נבדקת שהיא מסתכמת **בדיוק** ב-50,000.
   **הערה היסטורית:** התכנון המקורי השתמש בחציון נפרד לכל עמודה מתוך train.
   CP4-C הוכיח שהצירופים מפרים זהויות מקור בארבע מחמש רמות, ולכן השיטה והפלט
   הישן נחסמו. CP9 החליף אותם בפרופיל רב־משתני תקף מתוך שורת train שנצפתה באותו
   תקציב, בדטרמיניזם וללא היעד, ובנה מחדש את הסימולציה וה־bootstrap. מדווח **n
   לכל תקציב**; אין extrapolation מחוץ ל־500–20,000, אין טענה סיבתית והנחת
   האדיטיביות מתועדת.
   **Baselines:** `DummyRegressor` · `LinearRegression`.
   **מדד ראשי: RMSE.** הנימוק אינו סגנוני: מזעור MAE מכוון לחציון
   המותנה, מזעור MSE לתוחלת. הסימולטור **מסכם** תחזיות, ותוחלת של סכום
   היא סכום התוחלות. `cumulative_profit` בעל זנב ימני כבד (skew +2.10,
   kurtosis 19.5, ממוצע 12,682 מול חציון 9,035), ולכן מנבא
   חציון-אופטימלי יחסיר כ-**28.8%** מהסך באסטרטגיית 25×2,000.
   **MAE מדווח כמשני** בדיוק בשל אותו זנב. R² ויציבות בין folds משניים.

   **יחידת הפלט:** לפי מילון הבריף, יעד המודל הוא **סך הרווח המצטבר שהופק
   מלקוח, בשקלים**. בסימולטור כל מקטע תקציב מייצג רשומת לקוח/קמפיין עצמאית
   עם הוצאה חודשית נתונה ופרופיל היסטורי רב־משתני נצפה לאותו תקציב; תחזיות
   הרשומות מסוכמות. הסכימה מניחה רשומות בלתי־חופפות, השפעה אדיטיבית ויחידות
   הקצאה בנות־השוואה. הפלט אינו רווח שיתממש בחודש הבא, אינו רווח חודשי ואינו
   ROI סיבתי; אין להכפיל אותו שוב ב־`closed`, ואין לחסר ממנו את התקציב.

   **מועמדים לטיפול בזנב — נבחנים ב-CV, לא נקבעים מראש:**

   | שיטה | חוזה | מעמד |
   |---|---|---|
   | squared loss | תוחלת מותנית | **מועמד ברירת מחדל** |
   | **Tweedie** (`1<p<2`) | תוחלת מותנית | **מועמד מומלץ** — compound Poisson-Gamma: מסה באפס + רצף חיובי מוטה. `reg:tweedie` / `objective='tweedie'`, אפס תלויות חדשות |
   | Gamma | תוחלת מותנית | ⛔ **נדחה** — דורש תמיכה חיובית ממש, וה-`cumulative_profit` כולל אפסים (min = 0.00, p1 = 0.00, נמדד) |
   | Huber | בין ממוצע לחציון | ⚠ רגישות אופציונלית בלבד — האופטימום אינו התוחלת |
   | Quantile | קוונטיל | ⛔ **אינו מועמד ל-point estimate** של תוחלת רווח |
   | `log1p(target)` | חציון אחרי `expm1` | ⚠ **מחקרי-אופציונלי בלבד**, **מחוץ לתקציב הבחירה** — ראה למטה |

   **מועמדי P6 — כולם `XGBRegressor`**, כדי שכל מקבילה תהיה בדיוק P6-1
   וכל השוואה תהיה שינוי יחיד אמיתי:

```text
P6-1 Reference:  reg:squarederror                                regular deployable + Reference_P6
P6-2 Tweedie:    reg:tweedie, tweedie_variance_power=1.5 LOCKED  comparator = P6-1
P6-3 Huber:      reg:pseudohubererror, huber_slope=1.0 LOCKED    comparator = P6-1
P6-5 Winsorized: reg:squarederror + feature clip p1/p99 in-fold  comparator = P6-1

selection budget = 4 candidates x 20 configs x 5 folds = 400 fits

P6-4 log1p:      OPTIONAL RESEARCH EXPERIMENT - OUTSIDE the budget
                 no search; single fixed configuration inherited from
                 the P6-1 winning hyperparameters; 5 fits (one per fold)
                 never a deployment candidate; never enters One-SE;
                 never vetoed by the Guardrail (it is not competing)
```

   ⚠ שלושת ה-objectives אומתו בבנייה מול **xgboost 3.4.1**.
   `reg:pseudohubererror` הוא **pseudo-Huber**, לא Huber קלאסי — מגבלת
   השוואה מתועדת ב-REPORT.

   **P6-4 — `log1p` עם תיקון Duan, ניסוי מחקרי מחוץ לתחרות:**

```text
Requires a CUSTOM sklearn-compatible estimator.
TransformedTargetRegressor is NOT sufficient: it applies a deterministic
inverse_func and does NOT learn or store a smearing factor S.

class LogSmearingRegressor(BaseEstimator, RegressorMixin)

  __init__(self, base_estimator=None):
      self.base_estimator = base_estimator     # stored unmodified
      # no validation, no mutation, no cloning inside __init__

  fit(self, X, y):
      self.estimator_ = clone(self.base_estimator)
      self.estimator_.fit(X, log1p(y))
      r = log1p(y) - self.estimator_.predict(X)   # TRAINING ROWS ONLY
      self.smearing_ = mean(exp(r))
      return self

  predict(self, X):
      return exp(self.estimator_.predict(X)) * self.smearing_ - 1

Nested parameter routing works because base_estimator is an __init__
parameter stored under the identical name:
  base_estimator__learning_rate
  base_estimator__max_depth
  base_estimator__n_estimators

smearing_ is recomputed inside every fold from that fold's training rows
only, and is persisted in the artifact metadata.

FORBIDDEN: smearing_ from validation rows, from the Holdout, or once
globally across folds.
```

   ⚠ **הטיה מתועדת:** `smearing_` מחושב משאריות **בתוך המדגם**. מודל
   גמיש מכווץ אותן, ולכן `S` יוצא **קטן מדי**, התיקון מחסר בחסר,
   והתחזית נוטה **כלפי מטה** — הכיוון שפוגע בסימולטור המסכם. תיקון
   out-of-fold היה דורש nesting שנדחה. **לכן `log1p` נשאר ניסויי-מחקרי
   בלבד ואינו מועמד לפריסה.**

   ⚠ **על תיקון ההטיה ב-`log1p`:** `E[exp(X)] ≠ exp(E[X])`, ולכן המרה
   חזרה ללא תיקון מחזירה חציון ומחסירה מהסך. Duan smearing הוא התיקון
   המקובל, אך **גם הוא עלול להיות מוטה כשהשונות מותנית בפיצ'רים** — הוא
   מניח התפלגות שגיאה אחידה על סקאלת ה-log. **אין להציגו כפתרון מובטח.**

   **כל המודלים מושווים על סקאלת היעד המקורית** — RMSE ראשי, MAE משני;
   לא על deviance פנימי של Tweedie.
   ⛔ **אין Winsorization על היעד** (ראה § מדיניות ערכי קצה).

### מסקנת P5 — גרסת פאזה 5 ההיסטורית

⚠ הפסקה הבאה נשמרת ללא שינוי כדי לתעד ולשחזר את `docs/FINDINGS.md` ואת מחולל
פאזה 5. היא מבוססת על `purchased=1` ולכן הוחלפה כתשובה הפעילה על ידי הכרעת
CP4-D שמיד אחריה. היא אינה הטקסט שיוצג במוצר לאחר תיקון CP9.

הניסוח שייכתב, ובאותה לשון בכל שלושת המקומות (עובדות, חבילה 5, המלצה עסקית):

> הנתונים **אינם תומכים** בעצירה אוטומטית אחרי המעקב השלישי. שיעור הנשירה בשלב
> הרביעי הוא הנמוך בשרשרת (10.4%). בנוסף, ב-1,519 מתוך 3,163 רשומות שבהן
> `purchased = 1` (48%) הערך **הממוצע** של `calls_to_closed` הוא 4 ומעלה,
> ובתת-האוכלוסייה `closed = 1` — 439 מתוך 488 רשומות (89.96%) מציגות
> `calls_to_closed >= 4`.
> **מה שהנתונים אינם מוכיחים:** `calls_to_closed` הוא ממוצע ברמת רשומה ולא
> היסטוריה פר-עסקה, ולכן **אין לטעון ש-48% מהעסקאות הבודדות דרשו 4+ שיחות**
> ואין לטעון שעצירה אחרי השלישית הייתה מוותרת על כל 1,519 הרשומות.
> תת-האוכלוסייה `closed = 1` נבדלת תיאורית בגודל, בתקציב, בהמרה ובמספר השיחות,
> ולכן אינה מייצגת בהכרח את כלל הרשומות או העסקאות. **לא ניתן להסיק ממנה את
> שיעור העסקאות הכולל ולא את כיוון ההטיה** — יחידת ההשוואה היא רשומה, ורשומת
> `closed ≥ 2` נספרת כאחת אף שהיא מכילה כמה עסקאות.
> הנתונים אגרגטיביים ואינם מאפשרים לאמוד סיבתיות או כדאיות כלכלית שולית.
> **ההמלצה:** להמשיך את המעקבים באופן מבוקר, ולמדוד בניסוי תפעולי את שיעור
> הסגירה השולי, זמן העבודה ועלות כל שלב.

### הכרעת CP4-D — תשובת P5 המחייבת

> **לא. אין לאמץ עצירה אוטומטית אחרי המעקב השלישי.** שיעור הנשירה לאחר המעקב
> הרביעי הוא הנמוך בשרשרת (10.4%). מבין 3,318 הרשומות שבהן נסגרה עסקה
> (`closed>0`), הקובץ אינו מאפשר למנות סבבי מעקב לעסקה בודדת. במדד הקרוב,
> התפלגות `calls_to_closed` ברמת הרשומה, החציון הוא 3, הערך
> השכיח הוא 2 והממוצע 3.706; ב־1,595 רשומות (48.07%) הממוצע הוא 4 שיחות ומעלה.
> חמשת שלבי המעקב מתארים כמה לידים נותרו אחרי כל סבב, ואילו
> `calls_to_closed` הוא ממוצע שיחות ברמת רשומה שיכול להגיע עד 9. לכן אי אפשר
> לומר ש־48.07% מהעסקאות הבודדות דרשו 4+ שיחות, או שהשיחות המאוחרות גרמו
> לסגירה. ההמלצה היא להמשיך מעקבים אחרי השלישי באופן מבוקר ולמדוד בכל שלב
> את שיעור הסגירה השולי, זמן העבודה והעלות לפני שינוי קבוע במדיניות.

**שלושת מצבי התשובה:**

| מצב | מה מציגים | פעולה |
|---|---|---|
| נתונים מלאים | שני הגרפים, הנתון הטיפוסי וההמלצה שלעיל | אין עצירה אוטומטית; ממשיכים ומודדים |
| תמיכה חלשה | אותם נתונים עם מגבלת הגרעיניות והיעדר הסיבתיות גלויה | אין שינוי קבוע על בסיס הנתונים לבדם |
| כשל/פלט חלקי | מציגים רק את החלק שהושלם ומסמנים את התשובה המשולבת כלא זמינה | אין המלצת מדיניות חלופית ממספרים שמורים או חלקיים |

**תיקון CP9 הושלם:** `docs/FINDINGS.md`, ‏`docs/findings.json` וה־API הפעיל
מבוססים כולם על `closed>0`; קריאת ה־API משתמשת בעימוד מלא ובספירה עצמאית
על אותו סינון. ה־Holdout אינו רלוונטי לניתוח התיאורי הזה ולא נפתח.

### Holdout, CV וארטיפקטים

- **P3, P4** — split **stratified** לפי מחלקת היעד.
  **P2, P6** — split רגרסיה רגיל עם **seed קבוע**. אם משתמשים ב-quantile bins,
  הם נגזרים **מנתוני האימון בלבד** והבחירה מנומקת.
- כל preprocessing, בחירת פיצ'רים, imputation, `budget_tier`, פרופילי תקציב
  וכיול — **בתוך `Pipeline` או בתוך ה-fold**. מבנית, לא בהבטחה.
- **Holdout נשמר סגור** עד לאחר בחירת המודל וההיפר-פרמטרים. **מדדיו מדווחים
  פעם אחת** ואינם משמשים לסבב כוונון נוסף.
- **מדיניות הארטיפקט נקבעת מראש, לפני פתיחת ה-Holdout, ואינה משתנה לפי
  תוצאתו.** ⚠ הניסוח הקודם ("אימון מחדש *אם* פער ה-Holdout יצביע על רעב
  לנתונים") היה שגוי — הוא משתמש ב-Holdout כדי לקבל החלטת פיתוח נוספת אחרי
  החשיפה אליו, וזו בדיוק הדליפה שה-Holdout קיים כדי למנוע. הוסר.
  **המדיניות שנבחרה: לפרוס את ארטיפקט ה-Evaluation** שאומן על train בלבד ושמדדי
  ה-Holdout שלו מתייחסים בדיוק אליו. *הנימוק: ב-3,163 שורות התוספת מ-refit
  שולית, והמדדים שמדווחים למנחה מתארים את המודל שבאמת רץ.* אימון מחדש על
  train+holdout הוא הרחבה עתידית שתדרוש מדיניות הערכה חדשה או נתונים חדשים —
  לא החלטה שתתקבל בתוך הפרויקט הזה.

### בחירת מודל זוכה — נקבעת לפני פתיחת ה-Holdout

| משימה | ראשי | משניים | שוברי שוויון |
|---|---|---|---|
| P2 | **MAE** | RMSE, R², סטיית תקן בין folds | יציבות · פשטות · זמן inference · RSS |
| P3 | **ROC-AUC** | PR-AUC, Brier, log loss, **Lift@10%**, Acc/P/R/F1 מול 53.65% | כיול · עלויות FP/FN · פשטות |
| P4 | **ROC-AUC** | PR-AUC, Brier, log loss, **Lift@10%**, עקומת כיול | כיול · פשטות |
| P6 | **RMSE** | MAE, R², יציבות בין folds, ביצועי עשירון עליון | RSS · זמן ריצת Bootstrap |
| P4S | **ROC-AUC** | PR-AUC, Brier, log loss, כיול חובה (⛔ אין נפילה ל-uncalibrated) | RSS · זמן inference — ר' `PHASE8A.md` D8 להכרעה המלאה |

**כלל הבחירה — One-standard-error, על המדד הראשי בלבד.**

לכל מודל מועמד i, מתוך 5 ציוני ה-CV שלו:

    mean_i = ממוצע הציונים בין ה-folds
    SE_i   = std(ציונים בין ה-folds, ddof=1) / √5

⚠ **שגיאת תקן, לא סטיית תקן.** החלוקה ב-√5 מחייבת.
⚠ **`std` היא סטיית תקן מדגמית — `ddof=1`.** `numpy.std` ברירת המחדל היא
`ddof=0` ו-`pandas.Series.std` היא `ddof=1`; בחמישה folds ההפרש הוא
`√(5/4) = 1.118`, כלומר **11.8%** בסף — די כדי להפוך הכרעה.

יהי b המודל בעל ממוצע ה-CV הטוב ביותר — **הנמוך** ביותר ב-MAE/RMSE,
**הגבוה** ביותר ב-ROC-AUC. סף הזכאות לפי כיוון המדד:

    MAE / RMSE (נמוך = טוב):   mean_i ≤ mean_b + SE_b
    ROC-AUC    (גבוה = טוב):   mean_i ≥ mean_b − SE_b

⚠ ה-SE הוא של **המודל הטוב ביותר** (`SE_b`), לא של המועמד.

מבין המועמדים **בני-הפריסה** הזכאים נבחר **הפשוט ביותר**, לפי דירוג
שנקבע מראש:

    1. `Linear` / `Logistic`
    2. מודל Boosting

*(אין הבחנה בין booster "יחיד" ל"מכוונן" — כל מועמדי ה-Boosting עוברים
tuning. ⚠ מודלי `Dummy` **אינם בדירוג** — אינם בני-פריסה, ראה למטה.)*

**שוויון בין Boosters נשבר לפי הסדר:** (א) יציבות בין folds — `std` נמוך
יותר · (ב) RSS וזמן חיזוי, כפי שיימדדו בפאזה 6.

⚠ כלל One-SE חל **אך ורק** על המדד הראשי. הוא **אינו** משמש כסף
לגארדריל העסקי ואינו חל על מדדי העשירון העליון.

### מעמד ה-Baselines — שתי קטגוריות נפרדות

| מודל | מעמד | One-SE | Guardrail | בר-פריסה |
|---|---|---|---|---|
| `DummyRegressor` / `DummyClassifier` | **Benchmark בלבד** | ⛔ אינו משתתף | ⛔ אינו כפוף | ⛔ **לא** |
| `LinearRegression` / `LogisticRegression` | **Baseline בר-פריסה** | ✅ משתתף | ⛔ אינו כפוף | ✅ כן |
| מודלי Boosting | מועמד | ✅ משתתף | ⚠ רק כווריאנט ניסויי של P6 | ✅ כן |

⚠ **תחולת ה-Guardrail — מצומצמת.** הוא חל **אך ורק על הווריאנטים
הניסויים של P6** (Tweedie, Huber, Winsorization) מול המקבילה שנקבעה
מראש. הוא **אינו** חל על מודלי Boosting רגילים ב-P2/P3/P4, ואינו חל על
`Reference_P6` עצמו — אין להם מקבילה להשוות אליה, והם נבחרים לפי המדד
הראשי בלבד.

⚠ **המודלים שהבריף מחייב** — שלושת ה-boosters ב-P2/P3, CatBoost ב-P4 —
**חייבים לעבור אימון, tuning והערכה מלאים ולהידווח**, גם אם
Linear/Logistic ייבחרו לבסוף לפריסה. **Baseline פשוט אינו מבטל את חובת
הצגת תוצאות מודלי הבריף.**

**כאשר Dummy שקול או טוב מכל המועמדים בני-הפריסה:** ⛔ **אין להכתיר
ואין לפרוס מודל Dummy.** מדווח במפורש שלא הוכח ערך חיזויי מספק מעבר
לניחוש בסיסי; מתועד כ**מגבלה מהותית** ב-REPORT ובמבחן הקבלה. ⛔ **אין
להסתיר את התוצאה באמצעות בחירת מדד אחר** — המדד הראשי נעול מראש.

⚠ **דיווח כל המודלים על ה-Holdout אינו בחירה** — הבחירה ננעלה לפניו.

⚠ **Baseline אינו מחליף את חובת מודלי הבריף.** שלושת ה-boosters ב-P2/P3
ו-CatBoost ב-P4 **חייבים אימון, tuning, הערכה ודיווח מלאים** — ללא יוצא
מן הכלל, ובלי תלות בתוצאה.

⚠ **אבל המודל הנפרס נבחר לפי כלל One-SE**, ויכול להיות
`LinearRegression` או `LogisticRegression` אם הוא זכאי ופשוט יותר.
חובת האימון והדיווח של מודלי הבריף **אינה** חובת פריסה שלהם. אם המודל
הפשוט נבחר, ה-REPORT **מדווח ביושר** שהמורכבות לא הוסיפה ערך מדיד.

⚠ ציון ה-CV של מודל מכוונן מוטה אופטימית מול Baseline לא-מכוונן, כי
הכוונון נעשה על אותם folds. ההשוואה הבלתי-מוטה היא על ה-Holdout, שם כולם
מוערכים פעם אחת באותו פרוטוקול. הדבר מצוין ב-REPORT.

**הגדרת "שגיאה בעשירון הרווח העליון" (P6):**
- העשירון מוגדר על `cumulative_profit` **בפועל**, בתוך חלק ה-validation
  של כל fold. ⚠ **לעולם לא על ה-Holdout.**
- שני גדלים מדווחים: **RMSE_top10** (דיוק) ו-**Bias_top10** =
  ממוצע(חזוי − בפועל) — **ההטיה השיטתית**, שהיא המזיקה לסימולטור המסכם.
- מדווחים תמיד **גם ביצועים כוללים וגם ביצועי העשירון העליון**.

**ההשוואה מזווגת.** כל המודלים נבחנים על **אותם** 5 validation folds,
ולכן לכל fold k מחושב הפרש מזווג מול המקבילה שנקבעה מראש:

    Δ_RMSE(k)     = RMSE_top10(מועמד, k) − RMSE_top10(מקבילה, k)
    Δ_absBias(k)  = |Bias_top10(מועמד, k)| − |Bias_top10(מקבילה, k)|

בשני הגדלים `Δ > 0` פירושו שהמועמד **גרוע יותר** באותו fold.

**כלל הווטו — שני תנאים שחייבים להתקיים יחד:**

    (1) כיוון עקבי:      Δ > 0 ב-4 folds או יותר מתוך 5
    (2) גודל מעל הרעש:   mean(Δ) > std(Δ, ddof=1) / √5

⚠ **החלוקה ב-√5 מחייבת** — זו שגיאת תקן של ההפרשים, לא סטיית תקן.
⚠ **`ddof=1`** — סטיית תקן מדגמית, באותה מוסכמה כמו בכלל One-SE.

הבדיקה חלה **בנפרד** על `Δ_RMSE` ועל `Δ_absBias`; **די בכשל באחד מהם**
כדי להפעיל את הווטו.

⚠ תנאי (2) הוא שגיאת התקן של **ההפרשים המזווגים**, ולא סטיית ה-`Bias`
עצמו. שני התנאים יחד מבטיחים גם כיוון עקבי וגם גודל שחורג מרעש המדידה —
כיוון בלבד היה עלול לפסול מודל על חמישה הפרשים זניחים.

⚠ **אין סף עסקי מוחלט.** לא נקבע ולא יומצא. אם יידרש — ייקבע ויתועד
**לפני** הרצת המודלים.

⚠ **זהו כלל וטו בלבד.** הגארדריל יכול רק לפסול מועמד, לא לבחור אותו.
מדדי העשירון **אינם** מחליפים את RMSE ואינם נכנסים לכלל One-SE.

⚠ **העשירון מוגדר לפי התוצאה בפועל — להערכה בלבד.** הוא **אינו** פיצ'ר
ואינו זמין בזמן חיזוי.

⚠ הכלל נרשם כאן **לפני** הרצת ה-CV ואין לשנותו אחרי צפייה בתוצאות.

### מקבילת ההשוואה ו-`Reference_P6`

⚠ ווטו הוא **השוואה**, ולכן חייב להיות מוגדר **מול מה**. לכל וריאנט
ניסויי מוגדרת מקבילה **לפני ההרצה** — **שינוי יחיד**: אותו אלגוריתם,
preprocessing, hyperparameters, folds, seeds ואוכלוסיית אימון.

| וריאנט | מקבילה |
|---|---|
| Tweedie | אותו אלגוריתם, objective רגיל לחיזוי ממוצע |
| Winsorization של פיצ'רים | אותו Pipeline, ללא Winsorization |
| Huber | אותו אלגוריתם, loss רגיל |

**בהיעדר מקבילה ישירה — Reference כללי, נעול כאן:**

> **`Reference_P6 = XGBRegressor(objective="reg:squarederror")`**

זהות האלגוריתם וה-objective **נקבעות כאן** ואינן נבחרות לפי התוצאות.
**הנימוק:** `squared loss` כבר מוגדר כמועמד ברירת המחדל ל-P6 — הוא
המודל שיינפרס אלמלא הווריאנט · הוא חוזה **תוחלת מותנית**, בדיוק כמו
הגארדריל שנועד להגן על סכימת התחזיות · הוא נגזר מהספריות שהאפיון כבר
מחייב ואינו מוסיף תלות.

⛔ **אסור לבחור את המתחרה אחרי צפייה בתוצאות.**
⛔ אם לא ניתן לקיים השוואה הוגנת — הווריאנט נשאר **מחקרי** ואינו מועמד
לפריסה.

### סדר ההכרעה — CV → Guardrail → One-SE → בחירה → Benchmark

    א׳  CV לכל המודלים, כולל Dummy כ-benchmark, על אותם splits/folds/seeds
    ב׳  Guardrail רק על וריאנטים ניסויים, מול המקבילה שנקבעה מראש
    ג׳  הסרת כל וריאנט שנפסל לפי שני תנאי הווטו
    ד׳  One-SE לפי המדד הראשי, למועמדים בני-פריסה שלא נפסלו; Dummy אינו משתתף
    ה׳  בחירת Linear/Logistic או Boosting לפי One-SE ודירוג הפשטות
    ו׳  השוואת הזוכה ל-Dummy ודיווח אם לא הוכח ערך חיזויי מספק

⚠ **הפרדת סמכויות:** Guardrail **פוסל בלבד** · One-SE **אינו יכול
להחזיר** וריאנט שנפסל · Baselines אינם עוברים בשלבים ב׳–ג׳.
⚠ **ה-Holdout נפתח פעם אחת בלבד, אחרי נעילת הבחירה, ואינו משנה אותה.**

**מצבי קצה, נקבעים מראש:** אם כל הווריאנטים נפסלו — הבחירה בין המודלים
הרגילים וה-Baselines; זו תוצאה תקפה · וריאנט ללא מקבילה ובלי Reference
מוקדם נשאר מחקרי · ⛔ אין לשנות folds, seeds, `Reference_P6`, מדדי
עשירון או כללי ווטו אחרי צפייה בתוצאות.
**תיעוד חובה ב-REPORT:** זוגות ההשוואה · תוצאת הווטו לכל זוג · **הסיבה
לכל פסילה** (איזה תנאי נכשל ובאיזה מדד).

### מרחב ותקציב ה-Tuning — נעולים

צירי החיפוש, לכל מועמד ולכל Reference: `learning_rate` · עומק · מספר
עצים — שלושת הצירים שהבריף נוקב בהם ל-P4.

```text
grid_learning_rate_1 = 0.01
grid_learning_rate_2 = 0.05
grid_learning_rate_3 = 0.1
grid_learning_rate_4 = 0.2

grid_max_depth_1 = 3
grid_max_depth_2 = 4
grid_max_depth_3 = 5
grid_max_depth_4 = 6

grid_number_of_trees_1 = 200
grid_number_of_trees_2 = 400
grid_number_of_trees_3 = 800

xgboost_param_depth = max_depth
xgboost_param_trees = n_estimators
lightgbm_param_depth = max_depth
lightgbm_param_trees = n_estimators
lightgbm_num_leaves_for_depth_3 = 8
lightgbm_num_leaves_for_depth_4 = 16
lightgbm_num_leaves_for_depth_5 = 32
lightgbm_num_leaves_for_depth_6 = 64
catboost_param_depth = depth
catboost_param_trees = iterations

search = RandomizedSearchCV
n_iter = 20
cv = 5
random_state = 42
n_jobs = 1

scoring_P2 = neg_mean_absolute_error
scoring_P3 = roc_auc
scoring_P4 = roc_auc
scoring_P6 = neg_root_mean_squared_error

stability_seed_1 = 42
stability_seed_2 = 7
stability_seed_3 = 2024

tweedie_variance_power = 1.5
huber_slope = 1.0
winsorization_lower_percentile = 1
winsorization_upper_percentile = 99
```

⚠ `lightgbm_num_leaves_for_depth_N = 2 ** N` — **מזווג לעומק, לא ציר
חיפוש עצמאי**. LightGBM מתפצל leaf-wise וברירת המחדל `num_leaves=31`
הייתה חוסמת במקום העומק מ-`max_depth=5` ומעלה, כך שאותו ערך היה מייצג
קיבולת שונה בין הספריות.

⚠ **`n_iter` זהה** לכל וריאנט ולכל Reference — זה מה שהופך את התקציב
לשקול, לא גודל הרשת.

⚠ **פרמטרים נוספים של objective נעולים ואינם במרחב החיפוש** —
`tweedie_variance_power = 1.5` ו-`huber_slope = 1.0` (אומת מול
xgboost 3.4.1: `pseudo_huber_param.huber_slope = "1"`). אחרת `n_iter`
זהה על מרחב בעל ממד נוסף היה מקפח את הווריאנט. זו בחירה **שמרנית נגד**
Tweedie ו-Huber, ומתועדת ככזו.

⚠ **ארבעת שמות ה-`scoring` אומתו** מול `get_scorer_names()` של
scikit-learn 1.9.0.

⚠ **`n_jobs = 1`** — מגבלת 512MB היא של Render **בזמן שירות**;
ה-tuning רץ offline. הסיכון האמיתי הוא oversubscription מול ה-threading
הפנימי של ה-booster, לא זיכרון.

**Objective שאינו נתמך בגרסה המותקנת:** הווריאנט **מוסר** ומתועד
כלא-זמין; ⛔ **אינו מוחלף** בספרייה או objective אחר. נבדק בפאזה 1
ומאושרר בפאזה 6 לפני החיפוש.

**יציבות:** learning curves · יציבות מדדים ופיצ'רים בין folds ·
**רשימת seeds קבועה ומתועדת מראש**, מדווח הפיזור בין seeds. ⚠ **אין
לבחור seed לפי התוצאה.** ⛔ אין Nested CV ואין Ensemble ללא צורך מוכח.

### הסבר מודל

Feature importance גלובלי לשלושת המודלים (דרישת הבריף) + **SHAP למודל הזוכה
בלבד, offline**: גרף summary ב-REPORT ו-1–2 הסברים מקומיים.
*אפס תלויות חדשות* — שלושת ה-boosters מחשבים SHAP נייטיבית
(`get_feature_importance(type='ShapValues')` / `pred_contribs=True` /
`pred_contrib=True`), הגרפים ב-matplotlib. לא מחושב בזמן בקשת API.
**מחושב על נתוני הפיתוח בלבד ואחרי שהמודל הזוכה ננעל** — ⚠ אין להשתמש
ב-SHAP לבחירת מודל, ובוודאי לא אחרי צפייה ב-Holdout.

## איכות עסקית — מעבר למדד ממוצע

מדד ממוצע אינו מוכיח שהמודל משפר **החלטה**. שלוש בדיקות נוספות:

**1. Top-decile lift ו-Lift@10% (P3/P4)** — נמדדים בפאזה 6 על תחזיות.

⚠ **מעמד: מדד עסקי משני, לדיווח בלבד.** ⛔ **אינו Guardrail** — לא הוגדר
לו כלל וטו ואין לו כוח לפסול מודל. אינו המדד הראשי ואינו מחליף אותו.
המדד הראשי לבחירת מודל ב-P3 וב-P4 נשאר **ROC-AUC**, ללא יוצא מן הכלל.
- **`K` נעול מראש על 10%.** `Lift@10%` ו-Top-decile lift הם אותו גודל.
  `K ∈ {5%, 20%}` **רשאים** להתלוות כדיווח בלבד.
- **`Lift@10% = precision@10% / base_rate`**, כאשר **`base_rate` הוא
  שיעור החיוביים בתוך אותו סט הערכה שעליו חושב `precision@10%`** —
  אותו validation fold, או ה-Holdout. **המכנה מחושב מחדש לכל סט.**
  - **P3** — שיעור `upsell = 1` באותו סט.
  - **P4** — שיעור `referred = Yes` באותו סט.
  ⚠ **46.35% ו-42.71% הם ערכי ייחוס לכלל האוכלוסייה** ומשמשים להשוואה
  בדיווח בלבד. **הם אינם המכנה בחישוב.** הפיצול ב-P3/P4 stratified ולכן
  שיעור ה-fold קרוב אליהם, אך **אינו זהה** — ומדווח בפועל לצד ה-Lift.
- **ה-Baseline להשוואה** הוא בחירה אקראית באותו K, ששיעור ההצלחה שלה
  שווה לשיעור הבסיס ⇒ **`Lift = 1.0` פירושו שהמודל אינו מוסיף דירוג.**
- נמדד לצד הכיול — **דירוג וכיול הן שתי תכונות נפרדות**.

⛔ **`Lift` אינו משמש לכוונון אחרי פתיחת ה-Holdout.** מדווח בלבד; כל
כוונון נעשה ב-CV על train, לפני הפתיחה.

**2. ביצועים לפי פלחים.** ⚠ **רק עמודות שקיימות ב-CSV.** אין עמודת
"מקור ליד" ולכן פילוח כזה בלתי אפשרי. הפלחים המותרים: `budget_tier` ·
עשירוני רווח · גודל `num_leads` · `referred` — כל אחד בכפוף למטריצת
זמינות הפיצ'רים של המשימה, כדי שהפילוח עצמו לא ייצור דליפה.

⚠ **הגבלת `referred`:** הפילוח לפיו הוא **לדיווח בלבד**, ורק במשימות
שבהן הוא תקין מתודולוגית. **אסור** להשתמש בו כפיצ'ר כאשר הוא היעד של
המשימה או כשאינו זמין בנקודת החיזוי. **מטריצת זמינות הפיצ'רים היא מקור
ההכרעה.**

⛔ **ב-P4, שבו `referred` הוא היעד** — אין להשתמש בו כפיצ'ר ואין לבסס
עליו פילוח ביצועים המוצג כאילו היה קלט חיזוי. פילוח לפי היעד הוא מעגלי.

**3. Backtest של ארבע אסטרטגיות התקציב.**
- **נרשם מראש כחלק מהפתיחה היחידה של ה-Holdout**, לא הצצה נוספת.
- ⚠ **הערכה תצפיתית בלבד.** אי אפשר "להריץ" אסטרטגיית הקצאה על נתונים
  תצפיתיים — רק להשוות רווח חזוי מול בפועל בכל רמת תקציב. **אין להציגו
  כהוכחה סיבתית.**
- ⚠ **אין לשנות מודל או טיפול בעקבותיו.** אם תוצאת ה-Holdout תגרום
  לשינוי כלשהו — **ההערכה נפסלת ונדרש Holdout חדש.**

## מה מחושב offline ומה בזמן בקשה

⚠ **תחת 512MB ב-Render, חישוב אי-ודאות בזמן בקשה הוא כשל ודאי.**
כל שיטות אי-הוודאות וההסבר מחושבות **בזמן בניית הארטיפקט**.

| שיטה | נשמר בארטיפקט | בזמן בקשה |
|---|---|---|
| Split Conformal (P2) | קוונטיל השארית מסט ה-calibration, ב-metadata | חיבור/חיסור מהתחזית |
| כיול sigmoid (P3/P4/P4S) | **ה-`Pipeline` המלא — preprocessing + מודל + מכייל sigmoid** | `predict_proba` של המודל **המכויל** |
| Bootstrap (P6) | **אחוזוני 2.5/97.5 לכל אסטרטגיה, מחושבים מראש** | **Lookup בלבד** |
| SHAP | SVG סטטי ב-`docs/` | לא נטען כלל |

**האפליקציה טוענת ארטיפקטים מוכנים ומבצעת חיזוי או lookup בלבד. היא
אינה מאמנת, אינה דוגמת, אינה מכיילת ואינה מחשבת SHAP בזמן בקשה.**

### תקציב משאבים — מה רץ offline ומה נטען ל-Render

**Offline, בזמן בניית הארטיפקטים — ולא על כל שילוב אפשרי:**

| מה | היקף |
|---|---|
| Learning curves | **רק על רשימת מועמדים מצומצמת או על המודל הזוכה** |
| בדיקות מרובות-seed | **רשימת seeds קבועה ומתועדת מראש**, על אותה רשימה |
| Bootstrap (P6) | **למודל הזוכה בלבד** |
| SHAP | **למודל הזוכה בלבד**, תוצר מצומצם לדוח |

⛔ אין להריץ learning curves או ריבוי seeds על כל צירוף של מודל
ו-hyperparameters. זו הנדסת יתר ב-3,500 שורות.

**מה נטען ל-Render בזמן ריצה — ורק זה:**

| נטען | לא נטען |
|---|---|
| ה-`Pipeline` **הזוכה** בלבד, לכל משימה (**חמש**: P2/P3/P4/P4S/P6 — P4S נוסף ב-8A) | מודלים מפסידים |
| metadata (סדר עמודות, טיפוסים, גבולות OOD, קוונטיל conformal) | דגימות Bootstrap מלאות |
| **P6:** אחוזוני Bootstrap / טבלת lookup בלבד — ⛔ ה-`Pipeline` עצמו אינו נטען (פאזה 9 D11) | ערכי SHAP גולמיים |
| — | learning curves ותוצרי seeds |

⚠ **מגבלת 512MB נבדקת בפועל, לא בהנחה:** מדידת RSS בפאזה 6 (בחירת
הארטיפקט הנפרס) ובפאזה 12 (מבחן קבלה). ההחלטה מתקבלת על ראיה מדודה.

**תקציב ההתאמות המתוכנן:** P2 300 · P3 300 · P4 100 · P6 400 =
**1,100 התאמות עם חיפוש**. ⚠ זמן ו-RSS הם **אומדן** (~18–35 דקות, שיא
300–600MB) **ונמדדים בפאזה 6**.

⚠ **המספר הוא תקציב פאזה 6 בלבד.** ה-CV של P4S (פאזה 8A — שני מועמדים,
`seed=48`) **אינו נכלל בו** — הוא נספח נפרד, נמדד ומתועד ב-`PHASE8A.md`
ולא ב-1,100 הנ"ל.

---

## Architecture

```
Project 1/                       ← git init כאן
├─ .github/workflows/ci.yml
├─ app/
│  ├─ main.py                    # FastAPI: routes + StaticFiles; same-origin, ללא CORSMiddleware
│  ├─ auth.py                    # Depends(current_user)
│  ├─ features.py                # רשימות פיצ'רים per-task
│  ├─ predict.py                 # טעינה עצלה + ולידציה מול הסכמה השמורה
│  ├─ insights.py                # follow-up + tiers מנתוני Supabase
│  └─ static/
│     ├─ index.html, style.css   ← עברית, RTL — מסך יחיד (פאזה 11, D1)
│     ├─ app.js                  # entry point
│     └─ js/                     # מודולי ES: router, session, api, ... (פאזה 11, D2)
├─ scripts/{load_data.py, analysis.py, train.py}
├─ models/                       # Pipeline + metadata לכל משימה
├─ docs/                         # FINDINGS.md, feature_matrix.md, metrics.json, גרפים
├─ tests/                        # pytest — CI, ⛔ ללא Playwright (ר' e2e/)
├─ e2e/                          # Playwright — מקומי בלבד, ⛔ לא נאסף ב-CI (פאזה 11, D7)
├─ supabase/{config.toml, migrations/}   # migration לסכמה+RLS+grants · migration ל-2 views
├─ requirements.txt
├─ requirements-e2e.txt          # Playwright, -r requirements.txt + גרסה נעולה — test-only (פאזה 11, D7)
├─ pytest.ini                    # testpaths = tests — מבודד את e2e/ מאיסוף CI (פאזה 11)
├─ render.yaml · .env.example · .gitignore
├─ README.md (EN) · REPORT.md (HE)
└─ FunnelIQ_Assignment.html   (funnel_marketing_data.csv — מקומי, ignored, לא ב-repo)
```

⚠ **שלושה נתיבים חדשים בפאזה 11, נפרדים בעץ הריפו ⛔ אינם קינון זה של זה:**
`app/static/js/` (מודולי frontend, נטענים בדפדפן) · `requirements-e2e.txt`
בשורש (תלות בדיקה, ⛔ לא ב-`requirements.txt` של Render) · `e2e/` בשורש
(⛔ לא `tests/e2e/` — `pytest.ini` עם `testpaths = tests` מבטיח ש-
`python -m pytest -q` הקיים ב-CI ממשיך לאסוף רק את `tests/`). פירוט מלא:
`docs/planning/PHASE11.md` §ו.

**ארטיפקט = Pipeline מלא, לא מודל חשוף.** כל ארטיפקט שומר preprocessing + מודל,
ולצידו metadata: **סדר עמודות, טיפוסים, target, snapshot, אוכלוסייה,
`evaluation`/`production`, מקורות ה-fit, גרסה, מדדי CV ו-Holdout, תאריך אימון,
checksum**. ה-API מוודא כל קלט מול הסכמה השמורה ונכשל ברעש. גודל הארטיפקטים
נבדק מול מגבלת GitHub לפני commit.

---

## Supabase, Auth ו-RLS

**טבלה** `funnel_records`: `id bigserial pk`, `source_row_id int`, + 19 העמודות
(`referred` כ-`text`).

**טעינה** — `scripts/load_data.py`, secret key (`sb_secret_…`) מ-`.env`, batches, ניתן להרצה
חוזרת עם `source_row_id` כמפתח יציב. **הכפילויות נשמרות.** *נדחתה הצעת staging /
upsert בטרנזקציה: טעינה חד-פעמית של CSV סטטי בן 3,500 שורות, בלי ingestion
מתמשך.*

### SQL — חלוקת אחריות מפורשת

**SQL לאחסון, שליפה, הרשאות ואגרגציות יציבות. Python לניקוי אנליטי, סטטיסטיקה
ו-ML.** אין להעביר ל-SQL אימון, CV, SHAP, conformal, bootstrap או כיול.

**`supabase/migrations/<ts>_schema.sql`** — יצירת `funnel_records`, טיפוסי
עמודות, primary key, `source_row_id` כמפתח יציב · **policies של RLS** לפי
`auth.jwt() -> 'app_metadata' ->> 'organization' = 'northbound'`.

**שאילתות Runtime מתועדות** לפחות לפיצ'ר אחד שהאפליקציה צורכת בפועל מ-Supabase
(דרישת בריף) — `insights/followup` ו-`insights/budget-tiers`.

**בדיקות SQL / Data Contract** — ספירת שורות, חסרים, כפילויות וזהויות המשפך.
⚠ **מדווחות חריגות, לא מוחקות נתונים.**
**בדיקות RLS** — `anon` חסום · `authenticated` בלי `organization` חסום ·
משתמש Northbound מורשה לקריאה.

**מקור אמת יחיד לטיירי התקציב.** אם ההגדרה מופיעה גם ב-view (Runtime) וגם
ב-Python (אימון) — **בדיקת parity** שמוודאת גבולות זהים.

**מה נשאר בחוץ, ולמה:**
- **אינדקסים — לא.** 3,500 שורות; Postgres יסרוק ויחזיר מיידית. אינדקס כאן הוא
  קישוט. *ponytail: להוסיף אם וכאשר `EXPLAIN` על שאילתה אמיתית יראה צורך.*
- **`CHECK` constraints — מינימליים.** האפיון **שומר** במכוון כפילויות ורשומות
  חריגות (155 `closed > 0` עם `purchased = 0`, 333 עם `ltv > 0`). constraint
  שידחה אותן יסתור את מדיניות הניקוי. רק `NOT NULL` היכן שנמדד שאין חסרים.
- **Views — נבחרו בפועל.** שניים, ב-`supabase/migrations/<ts>_views.sql`,
  עם `security_invoker = true`. ההגדרות והנימוק ב-`PHASE3.md` §ה.
- **בלי stored procedures ובלי שכבת ETL.**

**Auth — לסגור, לא לסנן.** "רק צוות Northbound יכול להיכנס" הוא טיעון *נגד*
הרשמה עצמית. ברירת המחדל של Supabase מאפשרת לכל אדם להירשם, ועם policy של
`authenticated` כל זר קורא הכל.

1. **הרשמה ציבורית מכובה**; משתמשים נוצרים ידנית ע"י מנהל. טוגל אחד, אפס קוד.
2. **הרשאה ב-`app_metadata`** (`organization=northbound`, `role=team_member`) —
   ב-JWT, ואינו ניתן לעריכה ע"י המשתמש. **לעולם לא `user_metadata`.**
   ```sql
   using (auth.jwt() -> 'app_metadata' ->> 'organization' = 'northbound')
   ```
3. **משתמש דמו למנחה** — נוצר ע"י מנהל, מסומן מאומת מראש, בלי שליחת מייל.
   *מבטל את בעיית הדומיין הבדיוני במקום לעקוף אותה.* מקיים את סעיף 06:
   *"a stranger can open your live URL, sign in"*. **איפוס סיסמה מתבצע ע"י מנהל
   Supabase** — מייל שחזור לדומיין בדיוני לא יגיע, וזה נכתב במפורש.
4. **Session** — שחזור אחרי רענון דף, sign-out שמנקה.
5. **בדיקות שליליות:** אנונימי · מאומת בלי `organization=northbound` ·
   token פג תוקף.

### שער החלטה לפני פאזה 4 — מסירת גישת הדמו

✅ **ההכרעה נסגרה — 02.09.2026, אישור המשתמש.** שני משתמשי דמו:
`demo-northbound` עם `organization=northbound` (הזרימה הרגילה) ו-`demo-noorg`
בלעדיו (מדגים דחיית 403). **המשתמש מוסר את שני הפרטים בעצמו** למרצה. **ערוץ
המסירה הספציפי אינו נקבע כאן ולא נקבע בשום מסמך אחר בפרויקט** — זו החלטה
מכוונת, לא פער: הבחירה בערוץ נשארת בידי המשתמש בזמן המסירה בפועל, ואין לנחש
או לקבע אותה מראש. פרטי ההכרעה המלאים ב-`docs/planning/PHASE4.md` (D1) וביומן
ההכרעות ב-`codex-review.md`.

הרשמה ציבורית נשארת כבויה בכל החלופות, ושני משתמשי הדמו נשארים משתמשים
מאומתים מראש; רק `demo-northbound` נושא `organization=northbound`.

### מטריצת הרשאות ל-endpoints

| Endpoint | הרשאה | תוכן |
|---|---|---|
| `GET /health` | **ציבורי** | health-check קל |
| `GET /api/config` | **ציבורי** | `SUPABASE_URL` + **publishable key בלבד** (`sb_publishable_…`). לעולם לא secret key או סוד אחר |
| כל השאר (`/api/predict/*`, `/api/simulate/*`, `/api/insights/*`) | **JWT תקף + `organization=northbound`** | 401 בלי token, 403 בלי ההרשאה |

⚠ *הניסוח הקודם "כל `/api/*` דורש Bearer" סתר את זרימת ההתחברות — הדפדפן חייב
לקבל את הגדרות Supabase לפני שיש לו token. תוקן.*

*נדחה: rate limiting עצמי. ה-API לא נוגע בסיסמאות; supabase-js מדבר ישירות מול
Supabase שכבר מגביל את endpoints האימות שלו. המערכת פועלת ב-same-origin:
הסטטי וה-API יוצאים מאותו origin ב-Render, ו-`supabase-js` פונה ישירות
ל-Supabase שמנהלת CORS משלה. אין קריאה cross-origin בשליטתנו, ולכן **אין
`CORSMiddleware`**. יש לבחון זאת מחדש רק אם יתווסף origin נפרד.*

⚠ **תיקון S12 (תוכנית היישור לבריף, 09.09.2026 — אושר בביקורת Codex,
ממתין לאישור מפורש של המשתמש):** הניסוח הקודם כאן — *"CORS מוגבל — כן."* —
סתר במפורש את עץ הארכיטקטורה למעלה (`main.py # FastAPI: routes +
StaticFiles + CORS מוגבל`), בעוד בפועל אין `CORSMiddleware` ב-`app/main.py`
(הכרעת פאזה 4, D11). שני המקומות תוקנו לניסוח זהה בהחלטה ובמשמעות:
same-origin הוא ההגבלה, ואין middleware. ר' יומן ההכרעות ב-`codex-review.md`.

קריאות הדשבורד ל-Supabase נושאות את ה-JWT של המשתמש, **לא** secret key.

**שני מפתחות.** `sb_publishable_…` — ציבורי, לדפדפן, ממופה לתפקיד `anon`
ב-DB. `sb_secret_…` — **עוקף RLS**, רק ב-loader המקומי מ-`.env`; לעולם לא
בדפדפן, בגיט או בפלט. אינם מבוססי JWT signing key ומנוהלים בנפרד. קריאות
הנתונים נושאות את ה-JWT של המשתמש כדי ש-RLS תיאכף.

---

## מדיניות ה-CSV ב-repo — סגור

**`funnel_marketing_data.csv` אינו נכנס ל-repo.** הוא נשאר מקומי בלבד, מוחרג
ב-`.gitignore`, ואינו קיים באף commit. ⚠ **היפוך מהחלטה קודמת** (שהקובץ
ייכנס ל-repo הציבורי) — ראו יומן ההכרעות ב-`codex-review.md` לנימוק ולתאריך.

ה-README יתעד שקובץ הנתונים **אינו מופץ** עם ה-repo, ושכדי להריץ את
`scripts/verify_phase0.py`, `scripts/load_data.py` ו-`scripts/analysis.py`
יש להניח אותו בשורש הפרויקט. ה-SHA-256 המצופה מתועד ב-`PHASE0.md`.
`.gitignore` ממשיך להחריג dumps, ייצואים, `.env` וסביבות.

---

## עיצוב — Stitch MCP

**✅ החיבור נבדק ועובד** (קריאות קריאה בלבד, ללא שינוי):
- `list_projects` → 3 פרויקטים בבעלות המשתמש (`userRole: OWNER`), שניים מהם
  דשבורדים פיננסיים **בעברית RTL**.
- `list_design_systems` → 3 מערכות עיצוב בפרויקט העמלות
  (`projects/17924735486994635996`).

⚠ **שלושת הפרויקטים הקיימים אינם קשורים ל-FunnelIQ ולא ייגעו.** הם שימשו
לאימות החיבור בלבד. **FunnelIQ מקבל פרויקט Stitch חדש ומערכת עיצוב משלו.**

*(נשקלה ונדחתה הצעה למחזר את מערכת העיצוב `Insurance Commission Precision`
מפרויקט העמלות. מה שהיה עובר בפועל הוא ארבע מוסכמות שנכונות לכל אפליקציה
בעברית; מה שייחודי בה — צפיפות נתונים לטבלאות פיננסיות, ניווט מנהל/סוכן,
badges של `storno`/`cancelled` — שייך לדומיין הביטוח וזר ל-FunnelIQ. גרירת
דומיין שלם כדי לחסוך ארבע שורות היא חוב, לא חיסכון.)*

### מוסכמות עברית-RTL — נכתבות ישירות, לא נורשות

`dir="rtl"` ויישור לימין כברירת מחדל · **Rubik** (קריאוּת עברית מצוינת) ·
פורמט מטבע ₪ ב-`he-IL` עם מפרידי אלפים · מצבי loading/empty עם הודעה בעברית.

### מה שנכתב מאפס עבור FunnelIQ

סקאלת טיפוגרפיה · רשת ומרווחים · elevation · roundness · **פלטה סמנטית
שנגזרת מהמשפך ולא מדומיין אחר**:

| מושג ב-FunnelIQ | תפקיד הטוקן |
|---|---|
| מותג | primary |
| סגירה / אפסייל / סופר-לקוח — **כתוצאה שנצפתה בנתונים** | חיובי |
| נשירה / לא נסגר — **כתוצאה שנצפתה** | שלילי |
| **אי-ודאות** — טווחי הסימולטור, ציון לקוח-על 0–100 (**P4S**, ⛔ לא P4), הסתייגויות מודל | ⚠ אין לו מקבילה בשום מערכת קיימת. **מרכזי כאן** ונדרש לו טיפול ויזואלי ייעודי |
| טיירי תקציב Low / **Mid** / High | סדרה מסודרת בת שלושה ערכים |

⚠ **תוקן בשער `9→10` (D10 ב-`PHASE10.md`) — אי-עקביות שהתגלתה בסבב החמישי.**
השורות למעלה שייכו "סופר-לקוח" ל**חיובי** ואת **ציון** P4S ל**אי-ודאות**,
בעוד ההסתברויות המכוילות של P3 ו-P4 לא שויכו לאף אחד מהם. שלושתן אותו סוג
של דבר. **הכלל המחייב, לכל המסכים:**

- `positive` / `negative` שמורים ל**תוצאה שנצפתה בנתונים**.
- ⛔ **תחזית או הסתברות — P2, P3, P4, P4S, ורווח צפוי ב-P6 — לעולם אינה
  נצבעת `positive`/`negative`.** תחזית בירוק משדרת "זה כבר קרה".
- `uncertainty` ו-`evidence-*` מתארים **ודאות ותמיכת נתונים**, לא רצייה.
- ⛔ צבע לעולם אינו הקריטריון היחיד לקטגוריית נטייה — `propensity_band`
  מקבל טקסט ואייקון.

### מפת שדות הקלט — תווית עסקית, לא שם עמודה

⚠ **נוסף בשער `9→10` (D11 ב-`PHASE10.md`).** משתמש עסקי אינו מכיר את
ה-CSV. לכל שדה קלט נדרשת מפה בת חמישה רכיבים:

**תווית עסקית בעברית** · שם טכני משני באנגלית · **יחידה** ·
**מועד המדידה** · הסבר קצר.

⚠ **מועד המדידה הוא רכיב חובה** — הוא מבחין בין ארבעת שדות P4S הזמינים אחרי
מעקב 1 בקבוצה חודשית סגורה לבין 13 השדות שדורשים snapshot של תום מחזור
הקמפיין. רכישה ידועה היא תנאי הפעלה נוסף ל־P4S, לא אחד מארבעת הפיצ'רים.

הטבלה המלאה נכתבת ב-`docs/DESIGN.md`, וארבעת שדות P4S מפורטים ב-`IA.md`
§3א.1. ⛔ אין לשכפל אותה כאן — מקור אחד לכל תוכן.

⛔ `source_row_id` נשמר **פנימית בלבד** לטעינה יציבה. במוצר מוצגת תווית
סדרתית לא מזהה (`דוגמה 1`, `דוגמה 2`); אין לחשוף מזהה מקור או להמציא זהות
לקוח, שם או פרטי קשר.

### מסכי יעד — שישה

Login · Overview · טופס החיזוי המשותף (P2+P3+P4 עם prefill כעזר, לא כתנאי) ·
**ציון לקוח-על מוקדם לרוכש ידוע (P4S — ארבעה שדות, הזנה ידנית כמסלול ראשי,
prefill כעזר בלבד)** · Budget Simulator · Follow-up ·
מצבי loading / empty / error / **OOD**.

P2 הוא המענה לשאלת אורך החיים של **לקוח שנרכש לאחרונה**, לאחר שמחזור הקמפיין
שלו הסתיים, ומעריך אורך חיים כולל. P4S עונה על שאלה אחרת — פוטנציאל לקוח־על
של רוכש ידוע אחרי מעקב 1 ובחלון גיוס סגור. אין להחליף בין השניים ואין להציג
את P4S כחיזוי לליד לפני רכישה. כרטיס שאלה 1 ב־Overview נושא את מגבלת ההיקף
הגלויה (`IA.md` §2).

⚠ **תוקן בשער `9→10`.** הרשימה נכתבה לפני פאזה 8A ומנתה חמישה מסכים בלי
P4S. שאר SPEC נושא את P4S נכון (§ חמש שאלות המייסדת, § הצגת אי-ודאות,
תיקון D20 בפאזה 9); זו הייתה השארית האחרונה. ר' `docs/planning/PHASE10.md`
§ב, ממצא F8.

**Overview הוא אינדקס חמש יכולות עסקיות** (`PHASE10.md` D8): חמישה כרטיסים
גנריים המובילים ל**ארבעה** מסכי יעד עסקיים — דרישות 1 ו-2 (LTV, upsell)
מובילות לטופס המשותף, שאלה 3 למסך P4S, שאלה 4 לסימולטור, שאלה 5
ל-Follow-up. הנימוק הוא דרישת §02 בבריף שכל תשובה תהיה נגישה מתוך
האפליקציה המחוברת. כותרות המוצר אינן חושפות את המייסדת או את משימות הבריף.
⚠ זו **הכרעת עיצוב חדשה** של שער `9→10`, לא סנכרון; הניסוח הגנרי ננעל ב־CP5.
⛔ **P4 אינו התשובה לשאלה 3.**

### Dataviz — ארבע תצוגות חיות ושתי משפחות offline, מערכת אחת

⚠ **המלאי נסגר בשער `9→10`.** הסעיף כתב "ארבע תצוגות" ומנה שתי תצוגות
חיות בלבד, בעוד `IA.md` §2 כולל גרף המרה ב-Overview ו-§7 כולל את התפלגות
`calls_to_closed`. ⇒ **ארבע חיות, לא שתיים.**

**חיות — בדפדפן, `SVG` ידני ונגיש, ⛔ בלי ספריית תרשימים:**

| # | תצוגה | מסך | מקור |
|---|---|---|---|
| 1 | שיעור המרה לפי טייר תקציב | Overview | `GET /api/insights/budget-tiers` |
| 2 | דירוג ארבע אסטרטגיות התקציב | Budget Simulator | `GET /api/simulate/budget` |
| 3 | נשירה `followup_1`→`followup_5` | Follow-up | `GET /api/insights/followup` |
| 4 | **התפלגות `calls_to_closed`** | Follow-up | אותו endpoint, מצטבר בצד השרת |

⚠ **הוכרע: `calls_to_closed` הוא גרף עמודות, לא טבלה.** זו התפלגות תדירות
על טווח שלמים קטן, והצורה הטבעית לה היא עמודות. הכלל הקיים כבר מחייב
**fallback טבלאי נגיש לכל גרף חי**, ולכן הטבלה קיימת ממילא — ⛔ ואין כאן
דפוס חדש ואין חריג.

**offline — `matplotlib` → `SVG` (⛔ לא PNG), ב-`REPORT.md` בלבד:**

| # | משפחה | היקף |
|---|---|---|
| 5 | עקומות כיול | P3 · P4 · P4S |
| 6 | SHAP — summary ו-local | P2 · P3 · P4 · P6 |

⛔ **עקומות הכיול ו-SHAP אינן בממשק** (`IA.md` §3.5) — ה-`<details>`
מפנה אליהן בטקסט בלבד.

**כללים משותפים:** עד **5 צבעים סמנטיים** מאותם tokens · **מספרים, צירים
וסימני מטבע ב-LTR בתוך container של RTL**; **כותרות, צירים ומקרא באנגלית**,
וההסבר, ה-caption וההסתייגויות **סביב** הגרף בעברית RTL (ר' § שפת הגרפים
ותיקון S11 למטה) · עמודות מתחילות באפס · grid עדין, בלי מסגרת כבדה · אותה משפחת גופן, עובי קווים,
radius, מרווחים ופורמט מספרים בארבעתן · טווחי אי-ודאות ב-whisker עקבי עם מקרא
טקסטואלי · **fallback טבלאי נגיש לכל גרף חי, ותיאור טקסטואלי לסטטיים**.

**לא לצייר:** 3D · גרדיאנטים דקורטיביים · ציר כפול · עוגה · אנימציות מיותרות ·
צבע שונה לכל נקודה בלי משמעות סמנטית. SHAP רשאי לשמור צבע דו-קוטבי לערך פיצ'ר
רק אם המקרא ברור, והצבעים לא ישמשו במקביל למשמעות הצלחה/כישלון במקום אחר.

### שפת הגרפים — טכני באנגלית, הסבר בעברית

⚠ *הנימוק אינו matplotlib ואינו שפת ה-README — ה-REPORT נשאר עברית. הנימוק הוא
**עקביות בין הגרף לבין ה-CSV, הקוד, feature importance ו-SHAP**, ומניעת בלבול
מתרגום שמות משתנים. הכלל חל על ארבע התצוגות, גם החיות.*

- **באנגלית:** כותרות גרפים, שמות צירים, מקרא, ושמות feature.
- **בעברית:** ההסבר, המסקנות, ההסתייגויות וה-caption סביב הגרף — ב-REPORT
  ובממשק.
- במופע הראשון של שדה מרכזי מוצג מיפוי, למשל
  `cumulative_profit — רווח מצטבר`, **בלי לשנות את שם המשתנה בתוך הגרף**.
- בדשבורד: התווית העסקית בעברית, ושם השדה המקורי באנגלית בסוגריים או ב-tooltip.
- **אין להוסיף תלות bidi ל-matplotlib** — התוויות הטכניות ממילא באנגלית.

⚠ **תיקון S11 (פאזה 7, 05.09.2026 — אושר בביקורת Codex ואושר על ידי
המשתמש) — סתירה פנימית ב-SPEC.**
§ Dataviz "כללים משותפים" קבע *"כותרות, הסברים ומקרא בעברית RTL"*, בעוד
הסעיף הזה קובע *"באנגלית: כותרות גרפים, שמות צירים, מקרא, ושמות feature"*.
שתי השורות סותרות זו את זו במפורש. **הסעיף הזה גובר** — הוא ההכרעה
המאוחרת (סבב ביקורת 8, `codex-review.md` § שפת הגרפים), הוא נושא נימוק
מפורש, והוא קובע במפורש שהכלל *"חל על ארבע התצוגות, גם החיות"*. הניסוח
ב-§ Dataviz היה שארית מלפני אותו סבב ותוקן במקומו.

**קריטריוני קבלה:** RTL תקין · עקביות רכיבים · נגישות מקלדת · ניגודיות ·
פריסת Desktop עקבית · התאמה בין העיצוב למימוש.

תוצרי העיצוב או תיעודם נשמרים ב-repo. **שום קריאת כתיבה ל-Stitch לא בוצעה
ולא תבוצע ללא אישור.**

---

## בדיקות וראיות

**pytest:** Data Contract (הזהויות שנמדדו) · סכמת קלט לכל endpoint מול metadata
הארטיפקט · טעינת ארטיפקטים · `/health` ציבורי · `/api/config` ציבורי ולא מכיל
secret key · endpoint עסקי ללא token = 401 · endpoint עסקי עם token בלי הרשאה
= 403 · **סכום כל אסטרטגיית תקציב = 50,000** · דירוג הסימולטור · חישוב הנשירה.

*הגנה על "אין fit על Holdout" היא **מבנית** — הכל בתוך `Pipeline` — ולא בדיקה
שמנסה לזהות הפרה בדיעבד.*

**צ'קליסט ידני ב-REPORT:** פריסה · restart · RLS בשלושה מצבי משתמש · מבחן קבלה
ממחשב אחר · זיכרון וזמן תגובה בפועל.

**ראיות להגשה:** פלט CI · `metrics.json` עם CV + Holdout · עקומת כיול · גרף
SHAP · צילומי מסך · URL חי · תרשים ארכיטקטורה · המלצות עם מגבלות מוצהרות.

---

## פאזות

| # | Branch | תוצר |
|---|---|---|
| 0 | — | **צ'קליסט אימות**: החלטות סגורות + Data Contract. אין החלטה חוסמת פתוחה |
| 1 | `chore/skeleton` | `git init`, `.gitignore`, `requirements.txt` נעול, `pytest`, הסרת `prefix`, FastAPI עם `/health`, CI ירוק |
| 2 | `feat/deploy` | חשבון Render, `render.yaml`, URL ציבורי, **שלד עם health-check עובד בענן**, redeploy אוטומטי מ-GitHub + אימות שהשירות חוזר ועובר `/health` לאחר restart יזום |
| 3 | `feat/supabase-data` | `supabase/migrations/` — סכמה, טיפוסים, grants מפורשים, RLS לפי `app_metadata` + שני views, טעינה, שאילתות Runtime מתועדות, בדיקות Data-Contract ובדיקות RLS בשלושה מצבי משתמש |
| 4 | `feat/auth` | *(הכרעת מסירת גישת הדמו היא הכרעה ייעודית **בשער 3→4**, לא תוצר של הפאזה)*; login עברי, הרשמה מכובה, משתמש דמו, session, sign-out, בדיקות שליליות |
| 5 | `feat/analysis` | ניקוי, Data Contract, מטריצת זמינות, חבילות 1+5 |
| 6 | `feat/models` | P2/P3/P4/P6 עם CV, calibration set, Holdout, כיול, conformal, bootstrap, SHAP, גרסה מוקדמת כניסוי. ⚠ P4S **אינו** כאן — נוסף מאוחר יותר, ר' פאזה 8A |
| 7 | `design/ia` | **עיצוב מבני קודם ל-API**: מסכי יעד, היררכיית מידע, מצבי loading/empty/error/**OOD**, הצגת נקודה+טווח, שיעורי בסיס, רמת ראיות והסתייגויות |
| 8 | `feat/api-contract` | **נעילת סכמות OpenAPI לפי צרכי המסכים** — שישה נתיבים |
| 8A | `feat/super-customer` | **הרחבה אדיטיבית** (`docs/planning/PHASE8A.md`): מודל P4S, נתיב שביעי `POST /api/predict/super-customer`, חוזה מורחב לשבעה נתיבים. תוקן ב-`SPEC.md` (D20, `docs/planning/PHASE9.md`) |
| 9 | `feat/api` | מימוש ה-API לפי החוזה הנעול — **שבעה** נתיבים |
| 10 | `feat/design` | פרויקט Stitch **חדש** ל-FunnelIQ → design tokens והשלמת העיצוב החזותי |
| 11 | `feat/dashboard` | דשבורד עברי RTL עם prefill — **דרך PR** |
| 12 | `test/acceptance` | קבלה, restart, ביצועים ואבטחה + מסע מלא ממחשב אחר: פתיחת ה-URL, התחברות ללא התערבות המפתח, תחזית ללקוח חדש, תובנות follow-up ותקציב וקריאת ההמלצות |
| 13 | `docs/report` | `README.md` (EN) עם סקירה, ארכיטקטורה, התקנה מקומית ו-URL חי + `REPORT.md` (HE) + ראיות |
| 14 | — | מצגת סיכום למנחה |

### חוזה ההמשך לאחר 10A — CP8, ‏12.09.2026

זהו **תכנון־על בלבד**. תוכנית הביצוע של כל פאזה תיכתב בשער שלה, לאחר בדיקת
המצב בפועל, מיפוי חוזר מול `REQUIREMENTS.md` והבריף, ועדכון ROADMAP. אזכור
דרישה כאן אינו משנה את הסטטוס שלה ואינו מחליף ראיית ביצוע.

| שלב | תנאי כניסה | תוצר נדרש | תנאי קבלה | מסלול הדרישות הפתוחות |
|---|---|---|---|---|
| **השלמת 10A** | CP8 מאושר; מפרטי CP3–CP7 הם המקור הפעיל | CP9 מתקן את CatBoost/P4S, אוכלוסיית Follow-up ופרופילי P6; CP10 מבצע שתי ביקורות; CP11 מכריע סגירה וביקורת עצמאית | אין ארטיפקט חסום, פלט שגוי או חוסר התאמה בין התכן למימוש | B49/B51; הכנת הראיה המתוקנת ל־B41/B50/B52/B53a/B53b; B46b נשארת done אך מקור runtime מיושר לראייתה |
| **חזרה לפאזה 10** | 10A נסגרה ואושרה; רשימת תיקוני המסכים קונקרטית | checkpoint 8 בודק כל מסך מול SPEC/IA/DESIGN; 9 מייצא נכסים ומטריצת כיסוי; 10 מבצע ביקורת חזותית; 11 מעדכן ROADMAP; 12 סוגר את הפאזה | אין שימוש בייצוא ישן למסך שהשתנה; כל פרומפט Stitch קצר, מוסבר מראש ומוזן ידנית בידי המשתמש | תרומה חזותית ל־B21/B24/B28/B29c/B33/B36/B41/B42–B45/B50/B52/B55b/B56/B59; אפס סגירה על מוקאפ בלבד |
| **פאזה 11 — דשבורד** | פאזה 10 נסגרה ושער 10→11 אישר תוכנית מפורטת | מימוש RTL של ששת המסכים; קלט עצמאי ו־prefill אופציונלי; חיווט שבעת הנתיבים ונכס העובדות; הרשאות, שגיאה, OOD, כשל חלקי, קיפול וביטול תשובה ישנה | משתמש מחובר מפעיל כל יכולת מהדפדפן; אין נתון מומצא, fallback שקט או תלות בדוגמה היסטורית | מימוש B10/B28/B36/B41/B45/B50/B52/B56; תרומה ל־B55b/B59 |
| **פאזה 12 — קבלה** | פאזה 11 נסגרה ונפרסה; שער 11→12 אישר תרחישי קבלה | מסע בדפדפן נקי עם משתמש דמו: URL→login→קלט חדש/ערוך→P2/P3/P4/P4S→Follow-up/P6→המלצות; מקרי קצה של חסר, OOD, 401/403, sign-out, כשל נתיב וכשל prefill | המסע עובר ללא טרמינל, שינוי קוד או עזרת מפתח; המשתמש מבין תנאי שימוש, יחידות ומגבלות; כל ראיה קשורה לגרסה שנבדקה | אימות וסגירה אפשרית של B10/B28/B36/B41/B45/B50/B52/B55b/B56; ראיה חלקית ל־B59 עד השלמת README |
| **פאזה 13 — תיעוד ודוח** | קבלת המוצר הושלמה ושער 12→13 אישר תוכנית מפורטת | `README.md` באנגלית; `REPORT.md` בעברית; `schema.sql`; ארכיטקטורה, שחזור, URL, מקורות, כל שאלות §05, השוואות, ראיות ומגבלות | כל מספר נגזר מנכס מזוהה; כל שאלת בריף מקבלת תשובה ישירה; אדם זר יכול לשחזר ולהבין את המוצר | B1/B6/B18/B22/B23/B24/B29b/B29c/B32/B33/B37a/B37b/B37c/B37d/B43/B53a/B53b/B57/B60/B65; השלמת B59 עם ראיית פאזה 12. B54 נמדדת דרך ילדיה ואינה מקבלת `done` עצמאית; B58 נשארת N/A |
| **פאזה 14 — מצגת** | פאזות 12–13 אושרו | מצגת למנחה המבוססת על המוצר והדוח המאומתים | אין טענה חזקה יותר מהראיות ואין מספר חדש שהוקלד ידנית | אינה סוגרת דרישת בריף נפרדת; מציגה את התוצרים שכבר אושרו |

**נקודת עצירה בין השלבים:** כשל בתיקוני CP9 חוסם את סגירת 10A; כשל חזותי
חוסם את פאזה 10; כשל במסע הדפדפן חוסם קבלה; וחסר ב־README/REPORT משאיר את
B59 ודרישות התיעוד פתוחות. אין להעביר חוב כזה לשלב הבא באמצעות הסתייגות בלבד.

### שער תכנון ואישור בין כל שתי פאזות — מדיניות אחידה

הטבלה שלמעלה היא **תכנון-על**. רשימת תתי-המשימות של פאזה עתידית אינה תוכנית
ביצוע סופית: תוצאות הפאזה הקודמת עשויות לשנות אותה. לכן חל **שער מעבר אחיד על
כל המעברים** — `0→1`, `1→2`, `2→3`, `3→4` וכן הלאה עד `13→14` — ולא רק כחריג
נקודתי לפני פאזה 4.

לאחר השלמת פאזה `N` ולפני תחילת ביצוע פאזה `N+1`:

1. המשתמש מאשר במפורש את תוצרי פאזה `N`.
2. נבדק המצב בפועל: קבצים ותוצרים שנוצרו, תוצאות בדיקות, סטיות מהתכנון,
   החלטות חדשות, סיכונים או חסמים שהתגלו.
3. מתבצע תכנון מפורט מחדש של פאזה `N+1` על בסיס המצב בפועל.
4. תתי-המשימות, התלויות, ההרשאות, הסיכונים, הראיות וקריטריוני הקבלה של
   `N+1` מתעדכנים ב-`ROADMAP.html` ובמסמכי התכנון.
5. Codex מנסח פרומפט ביקורת לקלוד; קלוד מחזיר **ביקורת בלבד**.
6. ההמלצות נדונות עם המשתמש — אין לקבלן אוטומטית; סבבים חוזרים לפי הצורך.
7. רק לאחר אישור מפורש וסופי נכתבת הוראת ביצוע, ורק אז מותר לשנות את סטטוס
   הביצוע של `N+1` מ-`not_started`.

**שני סטטוסים נפרדים לכל פאזה** ב-`ROADMAP.html`, ואין למזג אותם לאחוז אחד:
`planning_status` (`outline_only` · `planning_in_progress` · `under_review` ·
`approved_for_execution`) ו-`execution_status` (הסטטוסים הקיימים). פאזה אינה
יכולה לעבור ל-`execution_status: in_progress` כל עוד
`planning_status ≠ approved_for_execution`; סתירה כזו מוצגת כאזהרת עקביות.

**השער אינו חלק מהפאזה.** הוא תהליך מעבר — לא תת-משימת ביצוע של הפאזה הקודמת
ולא תוצר מקצועי שלה: אינו נספר באחוז השלמת הביצוע שלה, ואינו הופך אותה
ללא-מושלמת אם תכנון הפאזה הבאה טרם הסתיים.

**הכרעות ייעודיות** נתלות בשער הרלוונטי ולא מנוחשות מראש. הידועה כיום:
מסירת גישת משתמש הדמו — בשער `3→4`, חוסמת את אישור הביצוע של פאזה 4 אך
**אינה** חוסמת את ביצוע פאזה 3 (§ שער החלטה לפני פאזה 4).

**מצב נוכחי — תצלום מתוארך 01.09.2026. המקור החי הוא `ROADMAP.html`:**
פאזות 0–2 הושלמו ואושרו · פאזה 3 ב-`planning_status: under_review` ו-
`execution_status: not_started`, כל 11 ה-checkpoints `not_started` · שער
`2→3` **פתוח**, ונסגר רק ב-`approved_for_execution` · פאזות 4–14 בתכנון-על
בלבד. ⚠ **אין להסיק מכאן שפאזה 3 אושרה לביצוע.**

⚠ **העיצוב המבני (7) מקדים את ה-API (8–9)** — אם התצוגה הנכונה היא טווח ולא
מספר, ה-API חייב להחזיר טווח. זה שינוי בסכמת התגובה, לא בעיצוב. אין צורך לסיים
כל פרט חזותי לפני ה-API; צריך לסגור **מה המסך נדרש להציג**.

---

## סיכונים

| סיכון | מענה |
|---|---|
| 512MB RAM ב-Render חינמי | טעינה עצלה + **מדידת RSS בפועל**, החלטה על ראיה |
| Cold start ~50 שניות | מתועד ב-README; Render Starter מסיר |
| ארטיפקטים גדולים ל-GitHub | נמדד לפני commit; חלופה: Supabase Storage |

---

## החלטות תקפות

Render (לא Railway) · סקריפטים בלבד · README אנגלית + REPORT עברית · מצגת סיכום ·
בלי סרטון דמו · ממשק עברית RTL · `git init` בתיקייה הנוכחית · `models/` בגיט
(בכפוף לבדיקת גודל) · אימות JWT ב-`get_user(token)` · הרשמה ציבורית כבויה +
משתמשים ידניים + `app_metadata` · SHAP offline למודל הזוכה · P2+P3+P4 על
`purchased = 1` · snapshot של P2/P3/P4 = תום מחזור הקמפיין · P4S = רוכש ידוע
לאחר מעקב 1 בקבוצת לידים חודשית סגורה · סף סופר-לקוח = 34 חודשים ·
ה-CSV מחוץ ל-repo — מקומי ו-ignored · Stitch כשלב עיצוב לפני הדשבורד.

## שאלות פתוחות

אין שאלת פרשנות פתוחה לגבי CatBoost: המשתמש הכריע ב־12.09.2026 ש־CatBoost
יהיה המודל הראשי לציון P4S ‏0–100, ו־CP9 פרס אותו. B41 נשארת פתוחה רק משום
שהציון טרם הוצג במסע המשתמש בדשבורד.

המשך Stitch מושהה במהלך פאזה 10A. לאחר החזרה ל־checkpoint 8 בפאזה 10,
הפרומפטים יימסרו למשתמש להזנה ידנית ורק לאחר הסבר קצר וקבלת דעתו.

## מה מכוון להישאר בחוץ

בלי notebook · בלי MLflow/Docker · בלי טבלת users משלנו · בלי staging/ETL ·
בלי rate limiting עצמי · בלי סרטון דמו · בלי SHAP בזמן בקשת API ·
בלי אימון מחדש על מלוא הנתונים כברירת מחדל.
