# פאזה 10 — עיצוב חזותי (`feat/design`)

> **עדכון פעיל — 11.09.2026:** [פאזה 10A](PHASE10A.md), תוכנית תיקון רוחבית,
> אושרה לביצוע (`approved_for_execution` / `in_progress`); CP0–CP8 נסגרו.
> CP5 נעל קלט, דוגמאות וקיפול; CP6 מיפה כל פלט לתגובת הבקשה הנוכחית או
> לעובדה קבועה גרסאית, ביטל את טענת "API בלבד" ונעל מסלולי A/B/C. ביקורת
> חוזרת הבהירה שה־CSV קפוא לצמיתות ושהאינטראקטיביות היא מול קלט המשתמש.
> CP7 סנכרן את מקורות התכן והסטטוסים; CP8 נעל את חוזה ההמשך עד ההגשה.
> העבודה מתקדמת לכל היותר checkpoint אחד בכל סבב.
>
> **עדכון פעיל — 13.09.2026:** פאזה 10A נסגרה במלואה (CP0–CP11, ביקורת Claude
> עצמאית כלולה — ר' `ROADMAP.html`). checkpoint 8 של פאזה 10 חזר לתור,
> תוקן ברובו — ר' §טו לפירוט המלא (חמישה מסכי Stitch מול `DESIGN.md`/`IA.md`
> המעודכנים, כולל שלוש הראיות האחרונות: הצהרת מועד המדידה, סדר Tab, וביקורת Login).
> checkpoint 9 (ייצוא צילומי מסכים + מטריצת כיסוי) התקדם בהתאם, בהכרעת
> סקופ מפורשת נגד יצירת וריאנטי Stitch חדשים למצבי runtime/prefill (נדחים
> לפאזה 11, בהכרעת סקופ — לא מגבלה טכנית) — ר' `DESIGN.md` §9. ⚠ **ביקורת
> נוספת (13.09.2026) פתחה מחדש את checkpoints 8–9** ומצאה שמסך P4S נשא
> בפועל `<h1>` גלוי `(P4S)` ושני מופעי `SUPER-CLIENT`; שורת `empty` ב-§9.2
> תוקנה; הצילומים יוצאו מחדש ברזולוציה מקורית במקום thumbnail 512px.
> **תוקן ונסגר סופית 13.09.2026**: פרומפט Stitch ממוקד הסיר את שני הפערים;
> סבב תיקון ראשון הוסיף רגרסיה קטנה (מחיקת `{model_version}` בטעות), נתפסה
> ב-`diff` ותוקנה בפרומפט נפרד. אימות סופי: `SUPER-CLIENT`=0, `(P4S)`
> ב-`<h1>`=0, `{model_version}`=1, `diff` מלא מול הבסיס מראה רק שני
> השינויים המבוקשים. `p4s.jpg` הוחלף. **checkpoint 10 (בדיקה חזותית ידנית)
> הותחל 13.09.2026** — שמונה ממצאים (שלושה מהם התגלו רק בביקורת עצמאית
> שנייה) ו-side-effect אחד תוקנו ואומתו לאורך שלושה סבבי Stitch. ⚠ **הצהרת
> "done" שניתנה בעקבותיהם הייתה מוקדמת**: ביקורת עצמאית שלישית מצאה שלושה
> ממצאים נוספים שלא תוקנו בפועל — toggle ריק ב-Overview/P4S, מיתוג לא אחיד
> (אייקון קיים בשלושה מסכים, נעדר בשניים), ו-"evidence: high/medium"
> באנגלית בתוויות התרשים בסימולטור (נפרדות מהתגים שכן תורגמו). **תוקן
> ונסגר סופית 13.09.2026**: ארבעה פרומפטים אטומיים תיקנו את שלושת
> הממצאים; כל תיקון אומת ב-HTML בפועל (לא צילום בלבד). ששת נכסי
> `docs/design/*.jpg` הוחלפו בפעם הרביעית. **checkpoint 10: `done`.**
> **checkpoint 11 (החלפת המוקאפ ב-ROADMAP) בוצע ונסגר 13.09.2026** —
> המוקאפ הידני שקדם לפאזה 8A (ללא Overview/P4S) הוסר לגמרי מ-`ROADMAP.html`
> והוחלף בששת צילומי `docs/design/*.jpg` הקנוניים; אפס markup חזותי כפול.
> **checkpoint 12 הותחל 13.09.2026** — שלב 1/6 (ביקורת קוד + סריקת
> סודות + בדיקות רלוונטיות): `diff` מול `a3d5674` נסקר במלואו, 0 סודות
> שמישים (דפוסי החיפוש עצמם מופיעים ביומן ואינם סוד), `pytest` ממוקד
> 23/23, אין מודלים/Holdout. לא נמצא ממצא הדורש תיקון.
> **שלב 2/6 (commit) בוצע באישור נפרד** — commit על `feat/design`,
> 9 קבצים (3 שונו + 6 `docs/design/*.jpg` חדשים נוספו), 331 insertions(+)/
> 286 deletions(-), אחרי שני סבבי `amend` שתיקנו טענת "עץ עבודה נקי"
> מוקדמת מדי וספירת insertions שגויה. **שלב 3/6 (push) בוצע באישור
> נפרד** — `git push origin feat/design`, fast-forward `a3d5674..3cf7d0a`,
> ללא force. SHA הסופי `3cf7d0a` יציב (push אינו כותב commit חדש) ולכן
> מתועד כאן במפורש. `origin/feat/design` = `3cf7d0a`, זהה ל-`HEAD` המקומי.
> **שלב 4/6 (PR) בוצע באישור נפרד** — PR #26 (`feat/design`→`main`)
> נפתח 13.09.2026 ותוקן פעמיים ב-14.09.2026: קישור/אזכור Claude Session
> הוסר, הכותרת/Summary תוקנו להבהיר שה-PR מצטבר וכולל גם שינויי Phase
> 10A ב-`app/`/`scripts/` (לא רק Phase 10 design-only), ותיאור ה-CI
> תוקן לניסוח עובדתי. **שלב 5/6 (אימות CI) בוצע פעמיים באישור נפרד** —
> פעם ראשונה על `headSha`=`70bfec3` (שתי ריצות `success`); לפני merge
> נדרש סבב מנהלי נוסף לתיעוד שלבים 4-5 עצמם (commit `3cf03fd`, push
> נפרד) שהפעיל CI חדש — אומת פעם שנייה על `headSha`=`3cf03fd` (שתי
> ריצות `push`/`pull_request` בנפרד, שתיהן `success`; ריצה ראשונה
> נתפסה `IN_PROGRESS`/`UNSTABLE` ולא דווחה כהצלחה עד השלמה בפועל).
> **שלב 6/6 (merge) בוצע באישור מפורש ונפרד 14.09.2026** — `gh pr merge
> 26 --merge`, PR #26 `MERGED`, merge commit `ef2eb83`; `main`/
> `origin/main`/`HEAD` מאומתים זהים, עץ עבודה נקי.
> **checkpoint 12: `done`. פאזה 10 נסגרה 14.09.2026** בהוראה מפורשת של
> המשתמש לעדכון תיעוד זה — `execution_status: in_progress` → `done`;
> `planning_status` ללא שינוי (`approved_for_execution`). כל 12 ה-
> checkpoints (ושער הפתיחה 0) בוצעו ואומתו.
> פרומפטים ל־Stitch נמסרים
> למשתמש להזנה ידנית, לאחר הסבר קצר על המסך והשינוי וקבלת דעתו.
> ראיות היסטוריות בהמשך שמשתמשות ב־`purchased=1` ל־B46b, בחצייני P6 או
> ב־`רענון נתונים` נשמרות כיומן בלבד; החוזה הפעיל נמצא ב־10A/SPEC/IA/DESIGN.
>
> הפתיח הבא הוא **תצלום היסטורי של שער 9→10**, ואינו מצב הביצוע הנוכחי.

> ✅ **התכנון אושר. checkpoint 0 הושלם.** ענף `feat/design` נפתח מ-`main`
> נקי (`ef289f5`, אחרי מיזוג PR #25). `execution_status: in_progress`.
> ⛔ **טרם בוצעו Stitch או קוד מוצר, וטרם נדחף `feat/design`.** כתיבה
> ל-Stitch דורשת אישור מפורש ונפרד ב-checkpoint 7; push ל-`feat/design`
> ופתיחת PR דורשים אישור נפרד משלהם, בהתאם לפרוטוקול האישור הכפול.
>
> נכתב בשער `9→10`, אחרי סגירת פאזה 9 (10.09.2026), כתוצר שלב 3–4 של השער
> (`SPEC.md` § שער תכנון ואישור בין כל שתי פאזות). עבר תשעה סבבי ביקורת
> Codex ואישור משתמש סופי — ר' §יד.

---

## א. מצב הפתיחה — נבדק בפועל

⚠ **תצלום היסטורי מלפני checkpoint 0.** הטבלה מתעדת את המצב שממנו יצא
התכנון, לפני שהוא אושר ולפני שהענף נפתח. ⛔ אין למחוק את העובדות ההיסטוריות;
המצב העדכני — לאחר checkpoint 0 — מתועד ב-§יד ובכרטיס פאזה 10 ב-`ROADMAP.html`.

| מה | מצב מאומת (לפני checkpoint 0) |
|---|---|
| פאזה 9 | סגורה. `execution_status: done`, PR #24 מוזג (`b176eab`), auto-deploy אומת, ראיה חיה מול Render בוצעה |
| פאזה 10 | `planning_status: outline_only` · `execution_status: not_started` · רשומת פאזה 10 ב-`ROADMAP.html` — שתי שורות תכנון-על בלבד |
| `PHASE10.md` | לא היה קיים לפני מסמך זה |
| תכנון קודם | ⛔ אין. נסרקה תיקיית `~/.claude/plans` — ההתאמות שנמצאו הן פאזה 3 תת-משימה 10, ו-Stitch בהקשר פרויקט אחר |
| ענף `feat/design` | לא קיים |
| `app/static/` | `index.html` · `app.js` · `style.css` — מסך התחברות מפאזה 4 בלבד |
| חוזה ה-API | שבעה נתיבים עסקיים; `docs/api/openapi.json` נעול, drift = 0 (פאזה 9 checkpoint 10) |

---

## ב. ממצאים שהתגלו בבדיקת המצב

עשרה ממצאים. **F1, F2, F6 ו-F10 חוסמים** — אי-אפשר לעצב מסך שהמפרט הלוגי שלו
סותר את עצמו או חסר.

### F1 ⛔ — `IA.md` מייחס תצוגת 0–100 ל-P4, בסתירה ל-SPEC

`docs/IA.md` §3.4, פאנל P4, **בגרסה שלפני תיקון שער `9→10`**:
**`נטייה להפניה: 71` (0–100) — התצוגה היא `event_probability × 100`**.
⚠ ההפניה היא לסעיף ולגרסה, ⛔ לא למספר שורה — התיקון עצמו הזיז את השורות.
`docs/planning/SPEC.md` § הצגת אי-ודאות: **`נטייה להפניה: 71%`**, ומיד אחריו —
*"הכותרת ב-P4 היא 'נטייה להפניה', לא 'ציון לקוח-על' — הציון ללקוח-על מוצג רק
בפאנל P4S"*.

**SPEC גובר.** הוא ההכרעה המאוחרת (תיקון D20 בפאזה 9), והוא נושא נימוק מפורש.
`IA.md` נכתב בפאזה 7, לפני שקיים P4S, ולכן ייחס ל-P4 את תצוגת ה-0–100 שהבריף
ביקש לסופר-לקוח. זו בדיוק הסתירה שפאזה 8A נוצרה כדי לפתור, ושארית ממנה נשארה
ב-IA. **תיקון: P4 מוצג באחוזים; ה-0–100 שייך ל-P4S בלבד.**

### F2 ⛔ — מטריצת "14 שדות החוזה" ב-`IA.md` §8 חסרה חמישה שדות

הושוותה מול `app/schemas.py`. חסרים במטריצה:

| שדה | מי מחזיק | מקור |
|---|---|---|
| `interval_details` (`nominal_coverage`, `measured_coverage`) | P2 בלבד | `schemas.py:333` |
| `model_algorithm` | P2 · P3 · P4 · P4S | `schemas.py:352, 388, 437` |
| `metrics` (`cv` + `holdout`) | P2 · P3 · P4 · P4S · P6 | `RegressionMetrics` / `ClassificationMetrics` |
| `target_definition` | P4S בלבד | `schemas.py:441` |
| `population_definition` | P4S בלבד | `schemas.py:442` |

המטריצה נכתבה בפאזה 7 מול חוזה שטרם ננעל (פאזה 8) וטרם הורחב (8A). **תיקון:
המטריצה נבנית מחדש מול הסכמות החיות**, ולכל שדה שהדפדפן צורך יש מיקום תצוגה
או נימוק מפורש לאי-הצגה.

### F3 ✅ — `evidence_level` של P4S זהה ל-P2/P3/P4 (אומת, לא הונח)

`SuperCustomerPrediction.evidence_level: Literal["low"] | None` (`schemas.py:435`)
— בדיוק כמו `LtvPrediction` ו-`PropensityPrediction`. הכלל של `IA.md` §5
("שתי רמות בלבד; `null` אינו טענה לראיות גבוהות") חל עליו כמות שהוא.
⚠ נבדק במפורש ולא הונח, לפי דרישת הביקורת.

### F4 — `calibration_status` של P4S צר יותר

P3/P4: `Literal["calibrated", "uncalibrated"]`. P4S: `Literal["calibrated"]`
בלבד (`schemas.py:438`, PHASE8A D10 — אין נפילה ל-`uncalibrated`). המשמעות
לעיצוב: **טוקן `uncalibrated` לעולם אינו מופיע במסך P4S**, ואין לתכנן לו מצב.

### F5 — P4S חסר `sample_size` ו-`interval_*`

אין ל-P4S טווח חיזוי ואין `n` לרשומה. **אין להציג לו טווח, whisker או רמת
ראיות מספרית.** `prediction-range` אינו טוקן של מסך P4S.

### F6 ⛔ — prefill ל-P4S אינו נעול בשום מקור

`IA.md` §3.3 מגדיר prefill לטופס 13 השדות בלבד. חיפוש ב-SPEC, ב-PHASE8A
וב-IA לא מצא הכרעה ל-P4S. ⇒ **הכרעה מפורשת נדרשת — ר' D7ב.**

### F7 — שאריות "חמישה מסכים / שלושה פאנלים" ב-`ROADMAP.html`

ב-`ROADMAP.html`, `accept.product` של פאזה 7 והתקציר שלה מתארים חמישה מסכים,
שלושה פאנלי חיזוי ומטריצת 14 שדות. נכון לזמן כתיבתם; מיושן אחרי 8A.

### F8 — `SPEC.md` § מסכי יעד חסר את P4S

`SPEC.md` § מסכי יעד מנה `Login · Overview · טופס החיזוי המשותף · Follow-up ·
Budget Simulator`. שאר SPEC נושא את P4S נכון (§ חמש שאלות המייסדת, § הצגת אי-ודאות, D20). **תיקון
ממוקד, לא שכתוב.**

### F9 — כותרת מיושנת בלשונית „תמונת מצב לתכנון” ב-`ROADMAP.html`

`<h2>פאזה 9 — מימוש API, בביצוע</h2>` — הפאזה נסגרה.

### F10 ⛔ — מונה הדור אינו מכסה את P4S

`IA.md` §9.4 נועל מונה דור אחד לשלוש בקשות הטופס המשותף, ומדגיש **"לעולם לא
פר-פאנל"**. P4S הוא טופס נפרד עם קלט נפרד; החלת אותו מונה עליו תגרום לבקשת
P4S לפסול תשובות של הטופס המשותף ולהפך. **הכרעה נדרשת — ר' D7ג.**

---

## ג. גבולות הפאזה

### בתחום

ערכי design tokens · פלטה סמנטית · טיפוגרפיה, spacing, radius, elevation ורשת ·
מפרט חזותי לרכיבים ולמצבים · פריסות Desktop · Dataviz חזותי · reference
חזותי מ-Stitch · צילומי מסכים · ניסוח ההסברים וההמלצות שיוצגו בממשק · תיקון
הסנכרון של `IA.md` ו-`SPEC.md` שבלעדיו אי-אפשר לעצב.

### מחוץ להיקף

HTML/CSS/JS בפועל · חיבור לנתונים · קריאות API/Supabase · state management ·
טיפול בתשובות מאוחרות · session/401/403/sign-out בפועל · נגישות אינטראקטיבית ·
כלל ההמרה בפועל לציון P4S (`PHASE8A.md` D15ב, פאזה 11) · שינוי
`openapi.json` או `app/schemas.py` · `README.md` · `schema.sql` ·
`REPORT.md` · סרטון דמו (`B58`, `N/A`).

### `app/static` — שני שלבים, שני כללים שונים

⚠ **תוקן אחרי ביקורת Codex.** הנוסח הראשון קבע "אפס שינוי ב-`app/static`"
ובאותה נשימה תכנן שם את `tokens.css`. סתירה. הכלל המדויק:

| שלב | מותר ב-`app/static` |
|---|---|
| **תכנון (עכשיו)** | ⛔ **אפס שינוי.** אף קובץ אינו נוצר ואינו נערך |
| **ביצוע פאזה 10** | **הוספת `app/static/tokens.css` בלבד** — קובץ חדש |
| **פאזה 11** | `index.html`, `app.js`, `style.css` — ⛔ אינם משתנים לפני כן |

⚠ **`tokens.css` אינו מחובר לממשק בפאזה 10.** אין `<link>` אליו, אין
`@import` ואין שימוש בערכיו בשום מסך. הוא נוצר כמקור אמת שהבדיקה האוטומטית
קוראת, והחיווט לממשק הוא פאזה 11.

---

## ד. הכרעות

### D1 — מעמד הפאזה מול הבריף: אפס בעלויות, עשרים ואחת תרומות

⚠ **תוקן בסבב הביקורת החמישי — הנוסח הקודם היה שגוי.** הוא קבע שפאזה 10
"אינה מופיעה במרשם כלל" ושאין `contributor=10`. זה סותר את `CLAUDE.md`
ואת `AGENTS.md`, שמגדירים במפורש את עמודת `contributors` כ**"דרישות
שהפאזה נוגעת בהן חלקית"**. פאזה 10 קובעת כיצד תוכן של דרישות בריף מגיע
למסך — זו נגיעה חלקית בהגדרה.

**מה נשאר נכון:** פאזה 10 **אינה סוגרת אף דרישה** ואינה בעלים של אף אחת.
⛔ אין `owner=10` · ⛔ אין שינוי `status` · ⛔ אין שינוי `evidence` ·
⛔ אין מרשם עקיבות מקביל.

**הכלל שהופעל:** דרישה מקבלת `contributor=10` **אם ורק אם** תוצר הפאזה —
המפרט החזותי, ה-tokens, וניסוח ההסברים וההמלצות המוצגים — קובע כיצד תוכן
הדרישה מגיע למשתמש **על המסך**.

**עשרים ואחת דרישות קיבלו `contributor=10`:**

| קבוצה | דרישות | מה פאזה 10 תורמת |
|---|---|---|
| נגישות התשובות | `B36` · `B55b` · `B59` | אינדקס חמש השאלות (D8) ומסע שבעת השלבים (§יג) |
| חשיפת חיזוי במסך | `B28` · `B41` · `B52` | מפרט פאנלי P2, מסך P4S והסימולטור |
| הצגה בדשבורד | `B21` · `B44` · `B45` · `B50` | טבלת הטיירים, גרף הנשירה, ורכיב D9 שנושא את ההמלצות |
| מעטפת מחוברת | `B10` · `B56` | מצבי Login/shell/sign-out ו-401/403 כמצבים חזותיים |
| תוכן שנוסח לממשק | `B24` · `B29c` · `B42` · `B43` · `B46a` · `B46b` · `B46c` · `B53a` · `B53b` | תשובת הטייר הממיר ב-Overview · תמצית המנוף ב-P2 · כרטיס פרופיל לקוחות-העל ב-P4S · שלב הנשירה המפתיע והתפלגות `calls_to_closed` בשני גרפי Follow-up · תשובת המדיניות · תשובת האסטרטגיה בסימולטור |

⚠ **תוקן בסבב השישי: `B24`, `B46a` ו-`B46b` נוספו (18 ⇒ 21).** ההחרגה
הקודמת שלהן נומקה ב"אין להן רכיב UI נפרד" — **נימוק שגוי.** `contributor`
מציין **נגיעה חלקית**, ⛔ לא בעלות על רכיב עצמאי. שלושתן מוצגות ישירות
במסכי הפאזה: `B24` (איזה טייר ממיר טוב יותר) ב-Overview וב-D9 · `B46a`
(באיזה שלב הנשירה מפתיעה) בגרף הנשירה ובהסברו · `B46b` (כמה מעקבים נדרשו)
בגרף `calls_to_closed`.

**החרגות מנומקות — ⛔ לא בשתיקה:**

| דרישה | למה **לא** `contributor=10` |
|---|---|
| `B9` | מסך ההתחברות כבר קיים ו-`done`; החובה (email+password) מתקיימת ללא תלות במראה, ופאזה 10 אינה נוגעת במבנה או בהתנהגות |
| **`B32`** | ⚠ **הכרעה עובדתית, לא שיקול דעת.** B32 דורש *Accuracy, Precision, Recall, F1 ו-ROC-AUC*, אך `_CvClassification` ו-`_HoldoutClassification` (`app/schemas.py`) נושאים **רק** `roc_auc`, `pr_auc`, `brier`, `log_loss` — **ארבעה מחמשת המדדים אינם קיימים בחוזה כלל** ולעולם אינם מגיעים לדפדפן. ⇒ `REPORT.md` בלבד; ⛔ **אין לכתוב שהם מוצגים ב"פרטי המודל"** |
| `B29b` · `B37a–d` | ניתוח כתוב; ⛔ פאזה 10 אינה מעלה אותו לתשובה הראשית והוא נשאר ב-`REPORT.md` |
| `B33` | baseline הרוב (53.65%) **מוחרג במכוון מהתצוגה** (`IA.md` §3.4) כדי שלא יתחלף בשיעור הבסיס — פאזה 10 אינה מציגה אותו |
| `B17` · `B22`–`B23` | מסמכי ממצאים; ⛔ אין להם מסך. ⚠ `B24` **הוסר מהקבוצה הזו** — הוא כן מקבל ביטוי ב-Overview וב-D9, ולכן הוא ברשימת התרומות |
| `B1` · `B6` · `B57` · `B60` · `B65` | תוצרי repo ותיעוד; ⛔ אין להם מסך |
| `B8` · `B11` · `B12` · `B62` | תשתית ואבטחה; §יג **אוסר** להציג JWT, policy או מפתחות בממשק |
| `B2`–`B5` · `B13`–`B16` · `B55a` | repo ותשתית פריסה; ⛔ אין רכיב חזותי |

**הבעלויות נשארות ללא שינוי:** `B41` (8A) · `B10`, `B45`, `B50`, `B56` (11) ·
`B55b`, `B59` (12) · `B29c`, `B43`, `B53a`, `B53b` (13) · `B21`, `B44`,
`B46c` (5) · `B42` (6) · `B28`, `B36`, `B52` (9).

**מעמד הפאזה:** שער חזותי שמאפשר את מימוש פאזה 11. היא אינה סוגרת דרישה,
אך ⛔ **אינה גם "מחוץ למרשם"** — היא תורמת חזותית ל-21 מהן.

### D2 — Stitch: reference חזותי, לא מקור אמת

Stitch נשאר חלק מפאזה 10, לפי `SPEC.md` § עיצוב — Stitch MCP.

⚠ **דיוק לרשומה:** פרויקט Stitch חדש ל-FunnelIQ מופיע ב-`SPEC.md` ובתקציר
המצב ב-`codex-review.md`, אך **אינו רשום כשורה עצמאית ביומן ההכרעות**. השורה
היחידה שם בנושא היא *"מיחזור מערכת עיצוב | נדחה | דומיין זר"*. אין לייחס
ליומן הכרעה שאינה בו.

**סדר העבודה המחייב בביצוע העתידי — שישה שלבים:**

1. גזירת טיוטת tokens, רכיבים, מצבים ופריסות מהבריף, מ-`IA.md` ומ-`SPEC.md`.
2. יצירת reference חזותי בפרויקט Stitch **חדש** של FunnelIQ.
3. בחינת התוצאה מול RTL, נגישות, ההבחנה P4/P4S וחמש שאלות המייסדת.
4. כל שינוי שמתקבל נכתב **במפורש** בחזרה למפרט ולמקור ה-tokens הקנוני ב-repo.
5. הרצה מחדש של בדיקות ה-tokens והסגירוּת.
6. רינדור חוזר של ה-reference מהמקור המעודכן.

**Stitch אינו מקור האמת.** ⛔ אין לייבא ממנו HTML/CSS/markup · ⛔ אין לתת
לפלט שלו לשנות tokens בשקט · ⛔ אין למחזר את מערכת העיצוב הביטוחית · ⛔ אין
לגעת בשלושת פרויקטי Stitch הקיימים שאינם קשורים ל-FunnelIQ.

התאמות RTL שנדרשות בעקבות תוצאת Stitch **מתועדות במפרט וממומשות בפאזה 11**.

**כתיבה ל-Stitch היא checkpoint ביצוע עתידי הדורש אישור מפורש ונפרד.** אין
לייחס לה "פרוטוקול אישור כפול" שלא הוגדר עבורה.

### D3 — שלוש שכבות אימות

**א. אוטומטי בפאזה 10** — **תשע בדיקות** ב-`tests/test_design_tokens.py`,
הקורא את מקור ה-tokens ואת מפת הרכיבים:

| # | בדיקה |
|---|---|
| 1 | WCAG AA לכל זוג `foreground/background` **מוצהר** במקור ה-tokens — 4.5:1 לטקסט רגיל |
| 2 | 3:1 לטקסט גדול ולרכיב גרפי/ממשקי חיוני, לפי התפקיד המוצהר |
| 3 | צבע גרף חיוני מול הרקע או מול הצבע הסמוך בסדרה — 3:1 |
| 4 | לכל מצב תצוגה ב-`IA.md` §9 יש רכיב וטיפול חזותי |
| 5 | לכל שדה חוזה שהדפדפן צורך יש מיקום תצוגה או נימוק מפורש לאי-הצגה |
| 6 | אין token יתום |
| 7 | אין רכיב שמפנה ל-token שאינו קיים |
| 8 | אין ערך צבע/גודל/spacing לא תקין; פורמט קנוני אחד |
| 9 | סגירוּת D9 על **מטריצת שש העמודות** ב-`DESIGN.md`: לכל פאנל ולכל גרף שהוגדרו יש שורה · ארבע השכבות **קיימות** (או הצהרה מפורשת שאין המלצה חד-משמעית) · ועמודת **מקור הראיה אינה ריקה** באף שורה. ⚠ הבדיקה מאמתת **קיום וסגירוּת מבנית בלבד** |

⛔ **אין לבדוק כל שילוב תאורטי של שני צבעים** — רק זוגות שימוש מוצהרים.
הבדיקה קוראת את קובץ ה-tokens ואת מפת הרכיבים; היא אינה מרנדרת דפדפן.

**ב. ידני חזותי בפאזה 10** (על התוצר הסטטי):
היררכיית מידע ברורה · RTL ו-bidi תקינים · מספרים, מטבעות, צירים ושמות שדות
באנגלית ב-LTR בתוך RTL · שום משמעות אינה מועברת בצבע בלבד · הסתייגויות
ואי-ודאות אינן מוסתרות · הבחנה ברורה בין P4 ל-P4S · פריסת Desktop עקבית ·
טקסטים אינם נחתכים · ההסבר וההמלצה אינם נדחקים מתחת למדדים טכניים.

⚠ **התאמת ההמלצה לראיות נבדקת כאן, ידנית.** בדיקה 9 מאמתת שארבע השכבות
**קיימות**; היא ⛔ אינה יכולה לקבוע אם הפעולה המומלצת נגזרת מהממצא, אם
המגבלה היא המגבלה הנכונה, או אם נאמר "אין המלצה חד-משמעית" במקום שבו
הראיות באמת חלשות. שיפוט תוכן הוא ידני.

#### שלמות מבנה Markdown — פקודת אימות מקומית, ⛔ לא בדיקת pytest

בשער `9→10` נמצאו שתי טבלאות שבורות ב-`IA.md`: פסקאות הסבר שנוספו **באמצע**
טבלה הוציאו את השורה שאחריהן ממנה (§3.4 שורת `<details>` של P4, §7 שורת
`המלצה`). ב-Markdown זה מרונדר כטקסט גולמי עם קווים אנכיים — כשל שקט
שקריאת diff אינה תופסת.

**הכלל:** ⛔ פסקה לעולם אינה נכנסת לאמצע טבלה; היא באה **אחרי** השורה
האחרונה שלה.

**האכיפה היא פקודת אימות מקומית בשלב הביקורת בלבד**, על חמישה קבצים:
`docs/IA.md` · `docs/planning/SPEC.md` · `docs/planning/PHASE10.md` ·
`docs/planning/codex-review.md` · `docs/DESIGN.md` (לאחר שייווצר).

⛔ **אין להפוך אותה לבדיקת `pytest` קבועה**, ומכאן גם שאינה בדיקה
מס' 10. שלוש סיבות: סריקת `docs/` כולה חורגת מהיקף פאזה 10 · היא נכשלת
על טבלה ישנה ב-`docs/planning/PHASE4.md`, ופאזה סגורה אינה נפתחת מחדש
בגלל בדיקת תיעוד חדשה · רשימת החרגות שתידרש כדי לעקוף זאת היא חוב
שנרקב. בדיקת עיצוב Markdown קבועה היא שבירה ואינה מוצדקת עבור שינוי
תיעוד הפיך.

⚠ הממצא ב-`PHASE4.md:516` (תא בטבלה עם טקסט רב-שורתי) **מתועד כאן
כידוע ולא מטופל** — ⛔ אין לגעת בו במסגרת פאזה 10.

**ג. נדחה לפאזות 11–12** (אין UI חי בפאזה 10):
מקלדת וסדר Tab · `focus` נראה · ARIA וקורא מסך · session/401/403/sign-out
בפועל · JWT/RLS וקריאות Supabase אמיתיות · פריסת Desktop בדפדפן חי · התאמה בין
העיצוב למימוש · מסע מלא מה-URL הציבורי.

⛔ **פאזה 10 אינה רשאית לטעון שנגישות האפליקציה "עברה".** היא בודקת מפרט
ותוצר סטטי בלבד.

### D4 — Login: המראה נכלל, המבנה לא

`app/static/style.css` מצהיר בעצמו, בשורה הראשונה, שהפלטה שבו פונקציונלית
וזמנית ושהעיצוב החזותי יגיע בפאזה 10.

הביטוי **"לא נוגעים"** ב-`IA.md` §1 לגבי Login מתייחס ל: מבנה זרימת ההתחברות ·
התוכן הפונקציונלי · Supabase Auth · ניהול ה-session · ה-JavaScript. **הוא אינו
מקפיא את המראה החזותי.**

פאזה 10 מגדירה כיצד מערכת ה-tokens חלה גם על Login. פאזה 11 מחליפה בפועל את
הפלטה הזמנית ומיישמת את אותה מערכת בכל המסכים.

⛔ **פאזה 10 אינה עורכת אף קובץ קיים ב-`app/static/`** — לא `index.html`,
לא `app.js` ולא `style.css`. התוספת היחידה המותרת בביצוע היא הקובץ החדש
`app/static/tokens.css`, שאינו מחובר לממשק (ר' § גבולות הפאזה).

### D5 — גבול האחריות 10 ↔ 11

| פאזה 10 | פאזה 11 |
|---|---|
| ערכי design tokens | HTML/CSS/JS בפועל |
| פלטה סמנטית | חיבור לפלטי API ו־Supabase מן ה־CSV הקפוא |
| טיפוגרפיה, spacing, radius, elevation, רשת | קריאות API ו-Supabase |
| מפרט חזותי לרכיבים ולמצבים | state management |
| פריסת Desktop | טיפול בתשובות מאוחרות |
| reference חזותי מ-Stitch | session, 401/403, sign-out בפועל |
| צילומי מסכים וייצוא תיעודי | נגישות אינטראקטיבית |
| ניסוח ההסברים וההמלצות | יישום ה-tokens והרכיבים |
| — | כלל ההמרה בפועל לציון P4S (`PHASE8A.md` D15ב) |

`IA.md` נשאר מקור האמת ל**תוכן ולהתנהגות**. פאזה 10 מוסיפה מפרט **חזותי**,
⛔ ואינה משכפלת לוגיקה עסקית ואינה משנה את חוזה ה-API.

### D6 — מקור עיצוב קנוני יחיד

**הפרדת מקורות אמת:**

| מקור | סמכות |
|---|---|
| `app/static/tokens.css` | **הערכים** — צבע, גודל, spacing, radius, elevation |
| `docs/DESIGN.md` | **הכללים** — רכיבים, מצבים, כללי שימוש, מיפוי מסכים |
| `docs/design/*.jpg` (ייצוא Stitch) | **ראיית עיצוב בלבד** — ⛔ לא מקור לערכים ולא להתנהגות |

`tokens.css` נבחר כפורמט המכני משום שהוא נצרך ישירות בפאזה 11 בלי שלב המרה,
והוא קריא לבדיקה האוטומטית ב-`custom property` פשוט. ⛔ אין קובץ tokens שני
בשום פורמט אחר.

**לשונית המוקאפ ב-`ROADMAP.html`:** ⚠ תיקון לרשומה — המאמת שהוצג בסבב קודם,
`grep -c "P4S" ROADMAP.html = 0`, **היה שגוי**; הערך בפועל הוא 15. הטענה
הנכונה: **לשונית המוקאפ עצמה** קדמה לפאזה 8A, מציגה שלושה פאנלים ואין בה מסך
P4S; 15 האזכורים נמצאים בהערות כרטיסי הפאזות.

- **עכשיו (תכנון):** המוקאפ מסומן גלוי כמוקאפ היסטורי שקדם ל-P4S, ואינו ראיית
  קבלה לפאזה 10.
- **בביצוע:** המוקאפ הידני הישן מוסר ומוחלף בקישורים או בתמונות מנכסי העיצוב
  הקנוניים. ⛔ אין לתחזק markup חזותי עצמאי נוסף בתוך ROADMAP.

### D7 — סנכרון העובדות הנעולות של P4S

P4S משתמש ב-`EarlyFunnelInput` עצמאי בן **ארבעה** שדות: `ad_budget` ·
`num_leads` · `leads_answered` · `followup_1` (`app/schemas.py:150`).

יש לו ולידציה משלו · OOD משלו (`SuperCustomerOODWarning`, `schemas.py:217`,
מוגבל לאותם ארבעה) · בקשת API משלו (`POST /api/predict/super-customer`) ·
תגובת `SuperCustomerPrediction` · snapshot מוקדם השונה מזה של הטופס המשותף.

⛔ **P4S אינו פאנל רביעי של טופס 13 השדות.**

#### D7ב — P4S: הזנה ידנית היא המסלול הראשי; prefill הוא עזר

**הכרעה חדשה** (F6 — לא היה נעול בשום מקור).

⚠ **תוקן אחרי ביקורת Codex.** הנוסח הראשון שלי גזר את הנימוק *"אף אחד לא
ימלא שדות ידנית"* מהטופס המשותף אל P4S. **זו הייתה שגיאה.** הנימוק ההוא תקף
ל-13 שדות שדורשים snapshot של תום מחזור הקמפיין; **הוא אינו תקף לארבעה
שדות משפך מוקדמים**, שאותם משתמש עסקי יודע ויכול להזין. גרוע מכך: אילו
המסלול היחיד היה בחירת רשומה היסטורית, המסך לא היה מספק **חיזוי ללקוח
חדש** כלל — הוא היה מציג חיזוי ללקוח קיים, ורכיב *"a prediction for a new
customer"* ב-`B59` היה נשאר בלתי ממומש. ארבעת השדות הם **בדיוק** המסלול
המעשי לחיזוי עצמאי.

| היבט | הכרעה |
|---|---|
| **הזנה ידנית** | **המסלול הראשי והשלם.** ארבעת השדות ניתנים למילוי ישיר, והטופס ניתן לשליחה בלי לגעת ב-prefill |
| **prefill** | **אפשרות עזר נפרדת** לטעינת דוגמה היסטורית — ⛔ **אינו תנאי לשליחה** |
| מקור ה-prefill | `public.funnel_records` עם JWT המשתמש, `where purchased = 1` — זהה ל-`population_definition` של P4S (`schemas.py:442`) |
| היקף | ארבעת שדות P4S בלבד; טעינת 13 שדות למסך בן 4 מטעה על מה שהמודל רואה |
| בורר | **מופע נפרד** מזה של הטופס המשותף |
| תקרת 1,000 שורות | מבחר מוגבל ומסודר לפי `source_row_id` — הבורר מציע דוגמאות ואינו מחשב אגרגט |

⚠ **בחירת prefill אינה הופכת את הרשומה ל"לקוח חדש".** רשומה שנטענה מהבורר
היא דוגמה היסטורית; גם ערכים שהוזנו ידנית הם תרחיש עצמאי ואינם הוכחה ללקוח
אמיתי. ההבחנה הזו חייבת להיות **גלויה במסך**, לא משתמעת.

⚠ **מבחן `B59` בפאזה 12 משתמש בערכים שהוזנו ידנית**, ⛔ לא בבחירת רשומה
היסטורית. אחרת המבחן אינו מוכיח את מה שהבריף דורש.

⛔ אין חישוב מחדש · אין כוונון · אין בחירת שורה לפי יופי התוצאה · אין להצהיר
שהמודל לא ראה את הרשומה (`seed 48` נפרד, אך הפיצולים אינם מבטיחים זאת).

⛔ **אין שינוי בסכמה ואין שינוי ב-endpoint** — `EarlyFunnelInput` כבר מקבל
ארבעה שדות מפורשים; ההכרעה כאן היא על **המסך** בלבד.

#### D7ג — מונה דור עצמאי ל-P4S

**הכרעה חדשה** (F10). מסך P4S נושא **מונה דור משלו**, נפרד לחלוטין מהמונה של
הטופס המשותף.

נימוק: הכלל ב-`IA.md` §9.4 ("המונה עולה ברמת הטופס בלבד, לעולם לא פר-פאנל")
נועד למנוע מבקשת פאנל אחד לפסול את תשובות שני הפאנלים האחרים **באותו טופס**.
P4S אינו פאנל באותו טופס אלא טופס אחר עם קלט אחר; מונה משותף היה גורם בדיוק
לנזק שהכלל בא למנוע — בקשת P4S הייתה פוסלת תשובות P2/P3/P4 שאין להן קשר אליה.

המונה של P4S עולה ב: שינוי אחד מארבעת שדותיו · שינוי אישור ההקשר · בקשת
דוגמה אחרת בבורר שלו · יציאה מהחשבון. ⛔ תגובה מאוחרת אינה רשאית לדרוס קלט חדש.

### D8 — הכרעת IA חדשה: שישה מסכים, ו-Overview כאינדקס חמש יכולות

⚠ **זו הכרעת עיצוב חדשה, לא "תיקון סנכרון".** היא מוצהרת ככזו במפורש כדי שלא
תיבלע בתוך תיקון תיעוד.

P4S מקבל **מסך עצמאי שישי** בשם **"ציון לקוח-על מוקדם"**.

| # | מסך |
|---|---|
| 1 | Login |
| 2 | Overview |
| 3 | טופס חיזוי משותף — P2 / P3 / P4 |
| 4 | **ציון לקוח-על מוקדם — P4S** |
| 5 | Budget Simulator |
| 6 | Follow-up |

**Overview הופך לאינדקס ברור של חמש יכולות עסקיות** — חמישה כרטיסים נפרדים
המובילים לארבעה מסכי יעד עסקיים:

| כותרת מוצר גנרית | מוביל אל |
|---|---|
| תחזית אורך חיי לקוח — ⚠ ללקוח שנרכש בקמפיין שהסתיים; ⛔ לא תחזית מתחילת המשפך | מסך 3 (טופס משותף) |
| תחזית רכישה נוספת | מסך 3 (אותו מסך) |
| ציון פוטנציאל לקוח־על | **מסך 4 (P4S)** |
| הקצאת תקציב פרסום | מסך 5 |
| יעילות מעקב שיחות | מסך 6 |

**נימוק:** הבריף §02 דורש *"Each answer must be reachable through your deployed,
signed-in application"*. אינדקס של חמש יכולות הופך את הדרישה לנראית במסך במקום
להשאירה מסקנה של הקורא, ומשרת ישירות את `B55b` ו-`B59`. חמש דרישות המקור ממופות
לארבעה יעדים ולא לחמישה, משום ש-LTV ו-upsell נענות מאותו טופס.

⚠ **עדכון 10A/CP5:** במוצר אין כותרות כגון "שאלות המייסדת", קודי משימות
או פרטי עבודה פנימיים. המיפוי לבריף נשמר במסמכי התכנון והעקיבות בלבד.

⛔ **P4 נשאר חיזוי הפניה משלים בתוך הטופס המשותף, ואסור להציגו כתשובה לשאלת
הסופר-לקוח** (`SPEC.md` § הצגת אי-ודאות).

Overview ממשיך להציג את טבלת הטיירים והגרף (חבילה 1) — האינדקס נוסף להם,
ואינו מחליף אותם.

### D9 — "סיכום והמלצה": תשובה לאדם, לא רק מספר

כל מסך תוצאה וכל גרף חייבים לתת למשתמש שאינו בקיא בסטטיסטיקה תשובה ברורה.

**רכיב אחיד בן ארבע שכבות:**

| שכבה | תוכן |
|---|---|
| **התשובה** | משפט קצר שעונה ישירות על הצורך העסקי |
| **מה זה אומר** | הסבר פשוט של הנתון או התחזית |
| **מה כדאי לעשות** | פעולה עסקית אחת, מעשית וזהירה |
| **חשוב לדעת** | אי-ודאות, מגבלה, או הסיבה שאין המלצה חד-משמעית |

**מטריצת D9 ב-`docs/DESIGN.md` — שורה לכל פאנל ולכל גרף**, עם שש עמודות:
שאלת המקור · תשובה פשוטה · משמעות עסקית · פעולה מומלצת **או** "אין המלצה
חד-משמעית" · מגבלה · **מקור ראיה מדויק**.

מכוסים: Overview · P2 · P3 · P4 · P4S · Budget Simulator · שני גרפי
Follow-up (נשירה, `calls_to_closed`). ⛔ Login אינו נכלל — אין בו תוצאה
ואין בו גרף.

**כללים:** התשובה, המשמעות העסקית, הפעולה והמגבלה שמשנה החלטה **גלויות מיד** ·
מדדים טכניים, שמות אלגוריתמים ופרטי כיול נשארים ב"פרטי המודל" המתקפל · אין מונח
סטטיסטי בלי הסבר פשוט · ההמלצות הן טקסט קבוע או תבנית דטרמיניסטית על שדות
התגובה הנוכחית ועל עובדות היסטוריות מנכס תצוגה מצומצם, גרסאי וממופה למקור ·
עובדה קבועה אינה מוצגת כמדידה חדשה; שינוי קלט מבטל מיד המלצה אישית ישנה ·
⛔ אין AI/LLM בזמן ריצה · ⛔ אין להמציא המלצה שאינה
נתמכת · ⛔ אין להסיק סיבתיות מנתונים תיאוריים · ⛔ אין להבטיח רווח, רכישה,
הפניה או משך חיים · כשאין ראיה להמלצה חד-משמעית — אומרים זאת במפורש ומציעים
בדיקה ידנית, איסוף נתונים או ניסוי מבוקר · הסתייגות מהותית נשארת לצד ההמלצה,
⛔ לא בכתב קטן.

### D10 — סמנטיקת צבע: תחזית אינה הצלחה שהתרחשה

**הכרעה חדשה** (סבב חמישי). הפלטה הסמנטית נשאה סתירה: "לקוח-על" שויך
ל-`--color-positive`, בעוד **ציון** P4S שויך ל-`--color-uncertain` — ושתי
ההסתברויות המכוילות האחרות (P3, P4) לא שויכו לאף אחד מהם. שלושתן אותו סוג
של דבר.

**הכלל, לכל המסכים:**

| מה | טוקן |
|---|---|
| **תוצאה שנצפתה בנתונים** — סגירה, אפסייל שקרה, הפניה שניתנה, לקוח-על בפרופיל ההיסטורי | `--color-positive` |
| **תוצאה שלילית שנצפתה** — נשירה, לא נסגר | `--color-negative` |
| **תחזית או הסתברות** — P2, P3, P4, P4S, רווח צפוי ב-P6 | ⛔ **לעולם לא** `positive`/`negative`. ניטרלי, עם `--color-uncertain` לטווח ולהסתייגות |
| **ודאות ותמיכת נתונים** | `evidence-*` · `out-of-distribution` |

⛔ **תחזית אינה מוצגת כהצלחה שכבר התרחשה.** `ציון לקוח-על: {X}` בירוק
משדר "זה קרה"; זו בדיוק ההטעיה ש-D9 אוסר.
⛔ **צבע לעולם אינו הקריטריון היחיד לקטגוריית נטייה** — `propensity_band`
מקבל טקסט ואייקון, כמו כל מצב אחר.
⚠ הכלל חל **באחידות על P2/P3/P4 ולא רק על P4S**; זו הייתה אי-העקביות.

### D11 — מפת שדות: קלט מובן בלי היכרות עם ה-CSV

`docs/DESIGN.md` נושא מפה לכל שדה קלט, בחמש עמודות: **תווית עסקית בעברית** ·
שם טכני משני באנגלית · יחידה · **מועד המדידה** · הסבר קצר.

ארבעת שדות P4S חייבים להיות מובנים לאדם זר:

| שדה | תווית עסקית |
|---|---|
| `ad_budget` | רמת הוצאת פרסום חודשית, בש״ח |
| `num_leads` | מספר הלידים שנוצרו |
| `leads_answered` | לידים שענו לטלפון |
| `followup_1` | לידים שנותרו אחרי המעקב הראשון |

⚠ **מועד המדידה הוא עמודה חובה** — הוא מה שמבחין בין ארבעת השדות
המוקדמים לבין 13 השדות שדורשים snapshot של סוף הקמפיין.

`source_row_id` נשמר פנימית בלבד. במוצר מופיעה תווית סדרתית כגון
`דוגמה 1`; ⛔ אין לחשוף מזהה מקור או להמציא זהות לקוח, שם או פרטי קשר.

---

## ה. ששת המסכים — מפרט חזותי ומצבים

### מסך 1 — Login

מבנה, תוכן ולוגיקה **ללא שינוי** (D4). מעוצב מחדש בפאזה 11 לפי ה-tokens.
מצבים: `loading` בבדיקת session · שגיאת `/api/config` · שגיאת התחברות.
⛔ אין הרשמה ציבורית · ⛔ אין תוכן עסקי למשתמש לא מחובר.

### מסך 2 — Overview

היררכיה: כותרת ← **אינדקס חמש היכולות** (D8) ← טבלת רמות הוצאת פרסום
חודשית ← גרף עמודות.

מצבים: `loading` · שגיאה + ניסיון חוזר · ⛔ **אין `empty` לגיטימי** — `200`
עם אפס שורות הוא שגיאת זמינות נתונים (`IA.md` §2.2). שורת טייר `NULL` מוצגת
כ-`gap (1501–1999)` עם ערכיה כפי שחזרו; טייר שלא חזר **אינו מוצג**.

**D9:** איזו רמת הוצאה המירה טוב יותר · למה זה מעניין · ⚠ קשר תיאורי, לא הוכחה
שהגדלת תקציב גורמת להמרה.

### מסך 3 — טופס חיזוי משותף (P2 · P3 · P4)

12 שדות להזנה + `not_closed` מחושב · חמישה כללי ולידציה חוסמים · אישור
הקשר לתרחיש עצמאי/נערך · דוגמה היסטורית אופציונלית · סיכום נתונים גלוי ·
אזור בדיקה ועריכה מתקפל · כפתור אחד · שלושה פאנלי תוצאה עצמאיים. טעינת
דוגמה שנכשלה אינה נועלת מילוי ידני; עריכה מסומנת ומבטלת תוצאה קודמת.

| פאנל | ראשי | משני | פרטי המודל |
|---|---|---|---|
| **P2** | `תחזית: {X} חודשים` | `טווח חיזוי משוער: {Y}–{Z} חודשים` — מאותה תגובה, גלוי, בלי אחוז | `interval_method` · `interval_details` (95% נומינלי / 92.9% נמדד) · `model_version` · `model_algorithm` · `metrics` |
| **P3** | `נטייה לאפסייל: {X}%` | קטגוריה + `שיעור האפסייל הבסיסי: 46.35%` + הפרש בנקודות | `calibration_status` · `calibration_method` · `model_version` · `model_algorithm` · `metrics` + מגבלת המונוטוניות |
| **P4** | `נטייה להפניה: {X}%` ⚠ **אחוזים, לא 0–100** (F1) | קטגוריה + `שיעור ההפניה הבסיסי: 42.71%` | כנ"ל + הפניה טקסטואלית לעקומת הכיול ב-`REPORT.md` |

הסתייגות גלויה ב-P4, **בשפה פשוטה לפי D9**:
**`התחזית מעריכה את הסיכוי שהלקוח יפנה לקוחות נוספים. ציון לקוח-על,
שמשלב הישארות, רכישה נוספת והפניה, מוצג במסך נפרד.`**

⚠ החליף את `המודל מנבא הפניה בלבד — endpoint משלים, לא ציון לקוח-על`.
המילה `endpoint` היא מונח טכני שאין למשתמש העסקי דרך לפרש, ⛔ ולכן אינה
יכולה להופיע בטקסט מוצג — D9 חל גם על הסתייגויות, לא רק על המלצות.
הנוסח הישן נשאר תקף כתיאור **ארכיטקטוני** של הנתיב בלבד.

**תמצית המנוף ב-P2 — שכבת "מה כדאי לעשות" של D9.** הפאנל נושא משפט קצר על
הפיצ'ר הדומיננטי במודל ה-LTV ועל הפעולה הנגזרת, בתמצית מ-`B29c`.

⚠ **הסתייגות חובה לצדו:** `feature importance` מתאר **מה המודל נשען עליו**,
⛔ ואינו מוכיח סיבתיות. שינוי הפיצ'ר אינו מבטיח שינוי בתוצאה.

⛔ **אין להעתיק את הניתוח המלא לדשבורד.** ההשוואה בין שלושת המודלים
(`B29b`) ותשובות `B37a–d` נשארות ב-`REPORT.md`. במסך — משפט אחד ופעולה
אחת; "פרטי המודל" נושאים **רק את המדדים שהחוזה אכן מכיל**.
⚠ **`B32` אינו נענה בממשק כלל** — ⛔ לא ב"פרטי המודל" ולא במקום אחר;
`REPORT.md` בלבד (הנימוק בטבלת ההחרגות ב-D1).
⚠ `B29c` נשאר `gap` בבעלות 13; פאזה 10 תורמת לו תמצית מוצגת בלבד.

**OOD פר-פאנל** (`IA.md` §9.2) — שבעה מ-13 הפיצ'רים נבדלים בין המשימות, ולכן
אותה רשומה יכולה להיות בתחום לפאנל אחד ומחוצה לו לאחר. באנר משותף רק כששלושת
הפאנלים מסכימים. בפאנל OOD: הודעה + **הסיבה הקונקרטית** + ⛔ **בלי מספר**.

### מסך 4 — ציון לקוח-על מוקדם (P4S)

**ארבעה** שדות קלט: `ad_budget` · `num_leads` · `leads_answered` · `followup_1`.
דוגמה היסטורית נפרדת (D7ב) · מונה דור נפרד (D7ג). לפני תרחיש עצמאי או
נערך נדרש אישור קצר: רכישה ידועה, מעקב 1 הושלם וחלון הגיוס החודשי נסגר.
האישור אינו פיצ'ר חמישי ואינו נשמר.

| שכבה | תוכן |
|---|---|
| ראשי | `ציון לקוח-על: {X}` — **שלם, 0–100**. כלל ההמרה נעול ב-`PHASE8A.md` D15ב ומיושם בפאזה 11 |
| משני | קטגוריית `propensity_band` + `שיעור בסיס: 16.72%` |
| הגדרות | `target_definition` ו-`population_definition` בשפה פשוטה — מי נחשב לקוח-על ועל מי המודל אומן |
| הסתייגות גלויה | הציון הוא **אומדן הסתברותי, לא הבטחה**; ⛔ אינו זהה ל-P4 |
| פרטי המודל | `model_version` · `model_algorithm` · `calibration_status` (תמיד `calibrated`, F4) · `calibration_method` · `metrics` |

⛔ **אין טווח, אין whisker, אין `sample_size`** (F5). ⛔ אין טוקן
`uncalibrated` במסך הזה (F4).

#### כרטיס ההקשר העסקי — סוגר את הפער מול `B43`

שכבת **"מה כדאי לעשות"** של D9 במסך הזה נושאת את פרופיל לקוחות-העל.
הנתונים מאומתים מ-`models/metrics.json` → `super_customer_profile`:

| מדד | ערך |
|---|---|
| `n_super` / `n_purchased` | 529 מתוך 3,163 |
| `pct_of_purchased` | **16.72%** |
| `pct_of_total_profit` | **33.61%** |
| `cac_super_mean` | ₪990.71 |
| `cac_population_mean` | ₪1,437.46 |
| `cac_savings_pct` | **31.08%** |

**הנוסח המחייב:**

> באוכלוסיית הרוכשים שנבדקה, לקוחות שענו להגדרת לקוח-על היו **16.7%**
> מהאוכלוסייה ויצרו **33.6%** מהרווח המצטבר. עלות הרכישה הממוצעת שלהם
> הייתה **נמוכה ב-31.1%**. הציון במסך מסייע לזהות דפוס דומה כבר לאחר
> המעקב הראשון, אך אינו מבטיח שהלקוח יהפוך ללקוח-על.

⚠ **דיוק חובה בניסוח:** כל שלושת האחוזים מתייחסים ל**אוכלוסיית הרוכשים**
(`purchased=1`, 3,163), ⛔ לא לכלל 3,500 הרשומות ולא לכלל לקוחות החברה.
נוסח שמשמיט את התיחום הזה הופך מספר נכון לטענה שגויה.

⛔ **אין לכתוב "מזהה אותם"** — הציון **מסייע לזהות דפוס דומה**. ⛔ אין
טענה סיבתית: עלות רכישה נמוכה אינה **גורמת** ללקוח להפוך ללקוח-על.

⚠ **זהו טקסט קבוע, גרסאי ומאומת — ⛔ לא קריאת runtime חדשה.** אין שדה
חוזה חדש, אין נתיב חדש, ואין שינוי בפאזה 9. הדפוס זהה להמלצת המדיניות
ב-Follow-up, שגם היא טקסט קנוני מ-`SPEC.md`. מקור הראיה נרשם ב-`DESIGN.md`.

⚠ **`B43` נשאר `gap` בבעלות 13.** פאזה 10 תורמת לו חזותית, ⛔ ואינה
סוגרת אותו: התשובה הכתובה המלאה שייכת ל-`REPORT.md`.

**ההבחנה מ-P4 היא דרישת עיצוב, לא הערת שוליים:** שני המסכים עשויים להציג
ערכים מספריים דומים. נדרשת הבחנה חזותית מפורשת — כותרת, יחידה (`{X}%` מול `{Y}`),
ומיקום במסך נפרד.

### מסך 5 — Budget Simulator

היררכיה: כותרת ← **הודעת החפיפה** ← טבלת ארבע האסטרטגיות ← גרף ← פרטים.
ארבע אסטרטגיות שסכומן 50,000 ₪ · רווח צפוי · טווח `bootstrap_percentile` ·
`sample_size` · `evidence_level` (שלוש רמות — היחיד).

⛔ **אין OOD** — מבנית בלתי אפשרי (`IA.md` §6.1). ⛔ אין הכרזה על מנצחת
כשהטווחים חופפים · ⛔ אין הבטחת רווח. **D9:** המלצה לניסוי מבוקר.

### מסך 6 — Follow-up

גרף נשירה `followup_1→followup_5` · התפלגות `calls_to_closed` · שני חלקים
עצמאיים (אחד נפל → השני מוצג עם ציון מה חסר).

**D9:** תשובת כן/לא פשוטה לצורך העסקי · הממצא שתומך בה · המלצת המדיניות
הנעולה ב-SPEC. ⛔ אין ניסוח סיבתי · ⛔ אין להציג `calls_to_closed` כהיסטוריית
שיחות ברמת עסקה.

### מצבי רוחב

`loading` ב-`role="status"` · שגיאה ב-`role="alert"` · `empty` מסביר מה חסר
ומה לעשות, ⛔ לעולם לא מסך ריק · `401` → Login + הודעה, הטופס אינו נשמר ·
`403` → חוסר הרשאה, ⛔ בלי נתונים, בלי ניסיון חוזר ובלי לולאת Login,
sign-out זמין.

---

## ו. Design tokens ופלטה סמנטית

⚠ **מלאי ה-tokens והכללים נקבעים כאן; הערכים בפועל נוצרים בביצוע** (checkpoint 3)
ומאומתים בבדיקה האוטומטית. הפרדה זו זהה לכל שאר הפאזות בפרויקט.

### פלטה סמנטית — נגזרת מהמשפך

| מושג ב-FunnelIQ | טוקן |
|---|---|
| מותג | `--color-primary` |
| סגירה · אפסייל · לקוח-על | `--color-positive` |
| נשירה · לא נסגר | `--color-negative` |
| **אי-ודאות** — טווחים, ציון P4S, הסתייגויות | `--color-uncertain` ⚠ **ניטרלי/ענברי, ⛔ לא ירוק/אדום** |
| טיירי תקציב Low / Mid / High | `--color-tier-1..3` — סדרה מסודרת בת שלושה |

### טוקני מצב

`evidence-high` · `evidence-medium` · `evidence-low` · `prediction-range` ·
`model-disclaimer` · `out-of-distribution` · `calibrated` · `uncalibrated` ·
טיפוגרפיה משנית לראיות ולהסתייגות — **קטנה יותר, ⛔ לא קטנה עד כדי הסתרה**.

⚠ **אין להסתמך על צבע בלבד** — כל מצב מקבל גם טקסט וגם אייקון.

### מערכת ויזואלית

סקאלת טיפוגרפיה · סקאלת spacing · `radius` · `elevation` · רשת — כולן
נכתבות מאפס ל-FunnelIQ. **Rubik** · `dir="rtl"` · מטבע `he-IL` עם מפרידי
אלפים · מספרים, מטבעות, צירים ושמות שדות באנגלית ב-**LTR בתוך container RTL**.

### Dataviz — מלאי סגור: ארבע חיות, שתי משפחות offline

⚠ **נסגר בשער `9→10`, סבב ביקורת שלישי.** הנוסח הקודם מנה "שתי תצוגות
חיות" בעקבות `SPEC.md`, בעוד `IA.md` §2 ו-§7 כוללים שתיים נוספות.

| # | תצוגה חיה | מסך |
|---|---|---|
| 1 | שיעור המרה לפי טייר תקציב | Overview |
| 2 | דירוג ארבע אסטרטגיות התקציב | Budget Simulator |
| 3 | נשירה `followup_1`→`followup_5` | Follow-up |
| 4 | התפלגות `calls_to_closed` | Follow-up |

⚠ **הוכרע: `calls_to_closed` הוא גרף עמודות, לא טבלה.** התפלגות תדירות על
טווח שלמים קטן; ה-fallback הטבלאי חל עליו כמו על כל גרף חי, ולכן אין דפוס
חדש. סונכרן ב-`SPEC.md` § Dataviz וב-`IA.md` §7.

**offline, ב-`REPORT.md` בלבד:** עקומות כיול (P3/P4/P4S) · SHAP summary
ו-local. ⛔ אינן בממשק.

כל ארבע החיות ב-**SVG ידני ונגיש, בלי ספריית תרשימים**. עד **חמישה** צבעים
סמנטיים · עמודות מתחילות באפס · grid עדין · אותה משפחת גופן, עובי קו,
radius ופורמט מספרים · טווחי אי-ודאות ב-whisker עם מקרא טקסטואלי ·
**fallback טבלאי נגיש לכל גרף חי**.

**שפת הגרפים:** כותרות, צירים, מקרא ושמות feature **באנגלית**; ההסבר,
ה-caption, המסקנות וההסתייגויות סביב הגרף **בעברית** (`SPEC.md` § שפת הגרפים,
תיקון S11 — הוא הגובר).

⛔ **לא לצייר:** 3D · עוגה · ציר כפול · גרדיאנטים דקורטיביים · אנימציות
מיותרות · צבע שונה לכל נקודה בלי משמעות סמנטית.

### פריסת Desktop

רכיבים קריטיים מסודרים לפי מפרט `DESIGN.md` §3.4; טבלאות מקבלות
`overflow-x` משלהן והגוף אינו נגלל לרוחב. היקף הפאזה מוגבל ל-Desktop.

---

## ז. תוצרים

| תוצר | נתיב | סמכות |
|---|---|---|
| תכנון הפאזה | `docs/planning/PHASE10.md` | המסמך הזה |
| ערכי ה-tokens | `app/static/tokens.css` | **מקור אמת לערכים** |
| מפרט מערכת העיצוב | `docs/DESIGN.md` | **מקור אמת לכללים ולרכיבים** |
| ייצוא חזותי מ-Stitch | `docs/design/*.jpg` | ראיית עיצוב בלבד |
| בדיקות | `tests/test_design_tokens.py` | אכיפה |
| תיקון סנכרון | `docs/IA.md` · `docs/planning/SPEC.md` | תוכן והתנהגות |
| מפת התקדמות | `ROADMAP.html` | סטטוס וראיות |

---

## ח. Checkpoints

**שנים-עשר checkpoints לביצוע (1–12), ולפניהם checkpoint 0 שהוא שער פתיחה
ולא תת-משימת ביצוע.** ⚠ הניסוח מפורש כדי למנוע עמימות בספירה.

| # | תוכן | precondition | ראיה |
|---|---|---|---|
| **0** ⚠ *שער פתיחה, לא תת-משימת ביצוע* — ✅ **`done`** | תכנון מאושר; ענף `feat/design` מ-`main` נקי | אישור מפורש של המשתמש + הוראת ביצוע | PR #25 (מסמכי התכנון) מוזג ל-`main` — `merge commit ef289f5`, CI ירוק; `git merge-base feat/design main` = `ef289f5` (בסיס זהה, מאומת); הראיה מתועדת בקומיט checkpoint 0 שבראש `feat/design` |
| **1** ✅ **`done`** | `docs/DESIGN.md` — מלאי רכיבים, מצבים ומיפוי מסכים | 0 | הקובץ נוצר (259 שורות, 29 רכיבים בעלי שם — נספר מכנית). כל מצב ב-`IA.md` §9 מופיע; 0 שורות טבלה יתומות; `git diff --check` נקי. תוקן אחרי ביקורת Codex: `calibration-badge` כולל P4S (נעול `calibrated`) · ולידציית P4S = 4 כללים · נוספה קבוצת Login/מעטפת-מחוברת/ניווט/sign-out (D4, `B10`/`B56`) · `panel-error` הורחב לשגיאה ברמת מסך או פאנל · הפניות `business-context-card`/`propensity-band-badge` תוקנו ל-`IA.md` §3א.6/§4 |
| **2** ✅ **`done`** | מיפוי מלא של שדות החוזה החיים לרכיבים (F2) | 1 | טבלה מול `app/schemas.py` — נבנתה מחדש מהקוד עצמו, לא הועתקה מ-`IA.md` §8; אימות עצמאי בסקריפט שהשווה כל שם שדה בארבע הסכמות (`LtvPrediction`, `PropensityPrediction`, `SuperCustomerPrediction`, `BudgetSimulation`+`StrategyResult`+`BudgetAllocation`) מול `docs/DESIGN.md`. חשף פער אמיתי — ל-`base_rate` לא היה רכיב כלל; נוסף `base-rate-line` (29→30 רכיבים) |
| **3** ✅ **`done`** | `app/static/tokens.css` — הערכים | 2 | הקובץ נוצר: 51 טוקנים ייחודיים, פורמט קנוני יחיד (`--name: value;` תחת `:root`). כל **10** זוגות `fg`/`bg` המוצהרים (נספר מכנית) עוברים WCAG AA (4.5:1), כל חמשת מילויי הגרפיקה עוברים 3:1 מול רקע העמוד — חושב מנוסחת הניגודיות בפועל על ערכי הקובץ עצמו, לא הונח. `--color-tier-1/2` הוגבלו לרקע שורת טבלה בלבד (נכשלים כמילוי גרף בפני עצמם); גרף ההמרה ב-Overview ישתמש ב-`--color-primary`. ⚠ **תוקן אחרי ביקורת עצמאית:** `evidence-high`/`calibrated` שיתפו את הירוק המדויק של `--color-positive`, ו-`evidence-low` נשא אדום מאותה משפחה כמו `--color-negative` — רמת ראיות וכיול הן טענות על תמיכת נתונים, ⛔ לא על תוצאה אמיתית, וקיבלו משפחת כחול/צפחה נפרדת לגמרי; `out-of-distribution` נשאר בענבר, ובכוונה — הוא באמת קשור לאי-ודאות תחזית כמו `--color-uncertain`, לא התנגשות מקרית. ⛔ אינו מחובר לממשק |
| **4** ✅ **`done`** | `tests/test_design_tokens.py` — תשע הבדיקות של D3א | 3 | הרצה ירוקה: 8 עברו, 1 דולג כראוי (test 9 — מטריצת D9 טרם קיימת, ר' checkpoint 6; מזוהה לפי header row אמיתי בטבלה, לא מבחין שביר בין "הוזכר" ל"נכתב"). כשל מכוון אומת נגד נתונים שבורים ב-זיכרון לכל תשעת מנגנוני הבדיקה (כולל test 9's דרך גוף אמיתי, לא placeholder) — כל אחד תופס קלט פגום בפועל. `pytest` המלא: 779 עברו, 1 דולג, אפס רגרסיה. ⚠ **תוקן אחרי ביקורת עצמאית שנייה (הרצת pytest בפועל מצאה בדיקות ירוקות אך חלשות מדי):** test 4 עבר מהתאמת מילות-מפתח להשוואה מבנית של טבלת מסך×מצב מול `IA.md` §9.1; test 5 צומצם ל-§5 בלבד (לא כל המסמך) והורחב לפריסת שדות מקוננים (`interval_details.*`, `metrics.cv.*`/`.holdout.*`) שנדרשו לשם כך גם ב-`DESIGN.md` §5 עצמו (הוסרו כל סימוני ה-wildcard/`*`/קיצור-סלאש, כל שם שדה Python נכתב במפורש); tests 6–7 עברו מהתאמת substring (שגויה בפוזיטיב — "color" תואם כל מקום שבו מופיע "color-primary") להתאמת backtick מדויקת, וחשפו פער תיעוד אמיתי (`--color-bg`/`--error-fg`/`--error-bg` מעולם לא תועדו) שתוקן ב-`DESIGN.md` §3.1/§1.1/§1.2; test 8 עבר מ"כל `var()` חוקי אוטומטית" לפתרון בפועל של השרשרת (קיום, ללא מעגלים, סוג התוצאה הנכון לפי שם הטוקן המפנה), דחיית ערכים שליליים ב-font-size/space/radius, `grid-columns>=1`, וטווחי shadow חוקיים (ערוצי RGB, alpha); test 9 מומש בגוף אמיתי (לא `pytest.fail()` קבוע) שיפעל אוטומטית כשה-checkpoint 6 יכתוב את המטריצה. ⚠ **תוקן שוב אחרי ביקורת עצמאית שלישית** שאימתה את `bd534a8` בפועל ומצאה ארבעה כשלים נוספים: (1) `_schema_field_defs` (test 5) נעצר ב-`\n\n` הראשון בגוף המחלקה — דוקסטרינג ואחריו שורה ריקה לפני השדות עצמם החזירו `[]` עבור `IntervalDetails`/`RegressionMetrics`/`ClassificationMetrics`, כך ש-`interval_details.*` ו-`metrics.cv/holdout.*` מעולם לא נבדקו בפועל (רק לא נכשלו); הוחלף ב-`ast.parse` שאוסף `AnnAssign` ישירות מגוף ה-`ClassDef`, חסין לדוקסטרינג/הערות. (2) test 4 בדקה רק שכל אחד משישה ה-markers הקבועים נמצא, ⛔ לא שכל שורה בפועל ב-IA/DESIGN משויכת לאחד מהם — שורת מסך נוספת הייתה חומקת בשקט; כעת כל שורה בשתי הטבלאות ממופה לבדיוק marker אחד, וקבוצת המסכים בשתי הטבלאות נדרשת להיות זהה לקבוצה הקבועה. (3) test 8: `--grid-gap` וכל שם המסתיים ב-`-font-size` (כמו `--model-disclaimer-font-size`) לא היו מכוסים בדיספאץ' לפי שם ונפלו ל-checker הגמיש שמקבל גם hex — נוספו כללים ייעודיים לשניהם. (4) test 9 אימתה רק סגירות שש-עמודות, ⛔ לא שקיימת בדיוק שורה אחת לכל אחד משמונת יעדי D9 הנעולים (`Overview · P2 · P3 · P4 · P4S · Budget Simulator · שני גרפי Follow-up`) — נוסף אימות מיפוי-שורה-ליעד עם disambiguation בין `P4`/`P4S`; `DESIGN.md` §6 עודכן לתעד את דרישת המזהה החד-משמעי בכל שורה עתידית. אומת: כשל מכוון על נתונים סינתטיים לכל ארבעת התיקונים (כולל בדיקות שליליות שמסירות שדה מקונן אמיתי ומוכיחות כשל, כנדרש), חזרה על כשלי הכשל המכוון מהסבב הקודם (ללא רגרסיה), `pytest` מלא: 779 עברו/1 דולג, `git diff --check` נקי. ⚠ **תוקן שוב אחרי ביקורת עצמאית רביעית** שאימתה את `6e7f1af` בפועל ומצאה שלושה פערים נוספים: (1) test 5 השתמשה במאגר `documented_paths` **גלובלי לכל §5** — מחיקת `event_probability` מסעיף §5.3 (P4S) בלבד עדיין עברה כי אותו שם בר-שדה מתועד גם ב-§5.2 (P3/P4); בנוסף, סוגי `warnings` לא נאכפו במדויק (רק "יש שורת bracket כלשהי"). הוחלף בבידוד מלא: כל סכמה נבדקת רק מול תת-הסעיף שלה (`_SUBSECTION_FOR_CLASS`, נאכף גם היעדר כפילויות בתוך תת-סעיף), ו-`_warning_union_members` קורא ב-`ast` את חברי ה-`Union` בפועל מ-`app/schemas.py` כך ש-`SuperCustomerPrediction.warnings` דורש בדיוק `warnings[SuperCustomerOODWarning]`/`warnings[UnobservedBudgetWarning]`, לא `warnings[OODWarning]` השגוי. (2) תיעוד `strategy-row` נשא הפניה משפחתית `` `evidence-*` `` ⛔ שאינה נבדקת לא בבדיקה 6 ולא ב-7 — שגיאת כתיב כמו `evidnce-*` הייתה עוברת בשתיהן בשקט; הוחלפה בשלושת שמות הטוקנים המפורשים (`evidence-high`/`medium`/`low`), ונוספה אכיפה מפורשת ב-test 7 שדוחה כל רישום `-*` עתידי מסוג זה. (3) test 9 חיפשה את מזהה היעד **בשורה כולה**, כך ששורת Overview שמפנה כדין ל-P2 (או פאנל P4S שמסביר את ההבדל מ-P4) נכשלה כבעלת שני יעדים; הצומצם לתא הראשון (שאלה) בלבד — יתר חמשת התאים חופשיים להזכיר יעדים אחרים; `DESIGN.md` §6 עודכן בהתאם. אומת: כשל מכוון כולל שלושת ה-probes המחייבים שהתבקשו (מחיקת `event_probability` מ-§5.3 בלבד; typo משפחתי `evidnce-*`; שורת Overview שמזכירה P2 בתשובה — עוברת), חזרה על 32 בדיקות הכשל מהסבבים הקודמים ללא רגרסיה, `pytest` מלא: 779 עברו/1 דולג, `git diff --check` נקי |
| **5** ✅ **`done`** | פריסת Desktop ו-Dataviz במפרט | 3 | `docs/DESIGN.md` §3.4 מגדיר את פריסת ששת המסכים ב-Desktop בלבד; §4.1 מגדיר SVG יחסי וטבלת fallback נגישה לארבע התצוגות החיות. זהו היקף התצוגה היחיד בפאזה. ראיית checkpoint 6 המאוחרת נשארת תקפה: `pytest tests/test_design_tokens.py` — 9 עברו; `pytest` מלא — 780 עברו; `git diff --check` נקי |
| **6** ✅ **`done`** | ניסוח D9 לכל **מסך תוצאה/תובנה** (Overview · טופס משותף · P4S · Simulator · Follow-up) ולכל **גרף עסקי** — ⛔ לא ל-Login, שאין בו תוצאה ואין בו גרף + **מסע המשתמש בן שבעת השלבים** (§יג) | 5 | `docs/DESIGN.md` §6–§8 נכתבו: **§6.1** — מטריצת שש העמודות (שאלה · תשובה · משמעות · פעולה · מגבלה · מקור ראיה) לשמונת היעדים הנעולים (Overview · P2 · P3 · P4 · P4S · Budget Simulator · שני גרפי Follow-up); ⚠ **P4/`calls_to_closed` אינם מחמש שאלות המייסדת** ובכל זאת מקבלים שורה, כמפורש בפתיח הסעיף. **בדיקה 9 עברה מ-`skipped` ל-`passed` בפועל** — לא הונחה, אומתה: 8 שורות · 6 תאים בכל שורה · אפס תא ריק · מזהה יעד יחיד לכל שורה, מאומת בפרסר האמיתי של הבדיקה (`_extract_markdown_table`), לא ברגקס בלבד. **§6.2** — מסקנת Follow-up (`SPEC.md` § מסקנת P5) מצוטטת מילה במילה, פעם אחת, כבלוק ברמת המסך — אומת מכנית מול המקור, זהות מלאה. **§6.3** — הערות מימוש (מונחים כמו `split_conformal`/`sigmoid`/`target_definition`/`bootstrap_percentile`) הופרדו מארבע העמודות המוצגות; נסרק מכנית — אפס מונחי מימוש בעמודות תשובה/משמעות/פעולה/מגבלה. **§7** — מפת שדות קלט (D11): 17 מופעים, 13 שמות טכניים ייחודיים, §7.1 (טופס משותף) ו-§7.2 (P4S) בשורות נפרדות ⛔ ללא מיזוג — כולל שתי הסתייגויות חובה (`closed`/`not_closed` סופרים יחידות שונות; `calls_to_*` הם ממוצע ברמת רשומה). **§8** — מסע שבעת השלבים הועבר מ-`PHASE10.md` §יג ⛔ ללא ניסוח מחדש, עם מיפוי לקריטריונים 23–27. אומת: `pytest tests/test_design_tokens.py` — **9 עברו** (מ-8/1 לפני כן); `pytest` מלא — **780 עברו, 0 דולג** (מ-779/1); `git diff --check` נקי; שלושה probes שליליים מחייבים (הסרת שורה, מזהה כפול בתא הראשון, תא ריק) — כולם נתפסים. ⚠ **תוקן אחרי ביקורת עצמאית — שלוש סטיות מהבריף:** (1) שורת `calls_to_closed` ענתה על B46b עם נתון 48%, בעוד ראיית B46b בפועל (`REQUIREMENTS.md`, `docs/findings.json`) היא שני הממוצעים `mean_calls_to_closed_closed_eq_1=5.65`/`.closed_ge_2=3.35` — התשובה נוסחה מחדש סביב שני הממוצעים הללו, נתון ה-48%/1,519 נשאר במשמעות (מותר, אינו מחליף את התשובה), ומקור הראיה קיבל את מפתחות `findings.json` המדויקים. (2) שתי שורות `ad_budget` ב-D11: הבריף מגדיר *"Monthly advertising spend"* — הוצאה בפועל, ⛔ לא תקציב מתוכנן; התווית תוקנה ל"הוצאה חודשית על פרסום", היחידה ל"ש״ח לחודש". (3) `customer_acquisition_cost`: הבריף מגדיר *"Cost per acquired customer"* — עלות ממוצעת ללקוח, ⛔ לא עלות כוללת לקמפיין; התווית תוקנה ל"עלות ממוצעת לרכישת לקוח", היחידה ל"ש״ח ללקוח". אומת מחדש: `pytest tests/test_design_tokens.py` — 9 עברו, `git diff --check` נקי (שינויי DESIGN.md בלבד; `pytest` מלא לא הורץ מחדש — אין שינוי קוד) |
| **7** ✅ **`done`** | כתיבה ל-Stitch | 6 + אישור | האישור ניתן; בפרויקט FunnelIQ נוצרו ששת מסכי היעד, והמשתמש הסיר את הכפילויות. הפרויקטים האחרים לא שונו |
| **8** ✅ **`done`** | בחינת פלט Stitch; כל שינוי מוחזר למקור הקנוני; בדיקות מורצות מחדש | 7 | ששת המסכים נבדקו ותוקנו מול `DESIGN.md`/`IA.md` המעודכנים; שלוש ראיות סגירה (מועד מדידה, סדר Tab, ביקורת Login) — ר' §טו לפירוט המלא. **סגירת P4S (13.09.2026, סבב שני):** תיקון ממוקד הוסיף רגרסיה קטנה (מחיקת "גרסת מודל: {model_version}" יחד עם התג השני של `SUPER-CLIENT`) — נתפסה ב-`diff` ותוקנה בפרומפט תיקון נפרד. אימות סופי מול ה-HTML החי: `SUPER-CLIENT`=0, `(P4S)` ב-`<h1>`=0 (`<title>` הבלתי-נוגע עדיין מציגו, כמבוקש), `{model_version}`=1 (שוחזר), `פרמטרים`=0, `שדות`=4; `diff` מלא מול הבסיס לפני שני הסבבים מראה שני שינויים בלבד: הסרת `(P4S)` מה-`<h1>` והסרת שני תגי `SUPER-CLIENT` — שום דבר אחר לא זז |
| **9** ✅ **`done`** | ייצוא צילומי מסכים ל-`docs/design/` + **מטריצת כיסוי** ב-`docs/DESIGN.md` | 8 | שישה נכסים (`login.jpg`/`overview.jpg`/`shared-form.jpg`/`p4s.jpg`/`budget-simulator.jpg`/`followup.jpg`) ב-`docs/design/`, ברזולוציה מקורית (1379–1775px, לא thumbnail 512px); `p4s.jpg` הוחלף לגרסה הסופית אחרי סגירת checkpoint 8. מטריצת כיסוי ב-`DESIGN.md` §9 (קריטריון 30 עודכן בהתאם): שבעת מצבי ה-runtime ומצב ה-`prefill` של P4S **נדחים לפאזה 11 ואינם מכוסים חזותית בפאזה 10**, בהכרעת סקופ מפורשת של המשתמש להסתפק בששת המסכים הקיימים ולא ליצור וריאנטים נוספים (לא מגבלה טכנית) — ההפניה לטבלאות §2/`IA.md` היא מקור להתנהגות, לא נכס חזותי חלופי. ממצא נלווה עדיין פתוח: מסך Login מציג `IQ Funnel` הפוך במקום `FunnelIQ` — מועמד ל-checkpoint 10, לא חוסם את checkpoint 9 |
| **10** ✅ **`done`** | בדיקה חזותית ידנית (D3ב) | 9 | **צ'קליסט D3ב + קריטריונים 16–18/31–37 על ששת נכסי `docs/design/*.jpg` ברזולוציה מקורית.** אחד-עשר ממצאים בסך הכול לאורך ארבעה סבבי ביקורת עצמאית (שמונה מקוריים + שלושה שנתגלו בביקורת שלישית) + side-effect אחד — **כולם תוקנו ואומתו ב-HTML בפועל בסבב הרביעי, לא רק בצילום**: (1–8) טופס משותף (9 שדות), Follow-up (מספרים+שתי כותרות אנגליות), Overview (`%`), צבע+תרגום תגי הטבלה בסימולטור, breadcrumb, לוגו (איחוד ראשוני), toggle (סבב ראשון חלקי). (9) `toggle` ריק ב-Overview (`<button>` ריק תחת "Filter Controls", 0 מופעים אחרי התיקון) וב-P4S (`<!-- User profile preview -->` ריק, הוסר) — אומת: שני הקבצים נבדקו ישירות, אין רכיב ריק נותר. (10) מיתוג אוחד — `filter_alt`/`w-9 h-9 rounded-lg bg-primary` (אייקון הלוגו) הוסר מ-Overview/טופס משותף/P4S; אומת ב-`grep` ל-0 מופעים בשלושתם; Budget Simulator/Follow-up/Login לא נגעו (כבר תואמים/בפריסה נפרדת). (11) "evidence: high/medium" תורגם ל"רמת ראיות: גבוהה"/"רמת ראיות: בינונית" בתוויות התרשים השמאלי בסימולטור; אומת ב-`grep` — 0 מופעי אנגלית, תגי הטבלה הימנית לא נגעו. **נבדק ואינו ממצא:** `8.2%` הירוק (D10), כפילות גרף/טבלת-fallback (D4.1). **ממצא משני לא-חוסם, לא נדרש:** קו מפריד ריק ב-Login; שבב כפול ב-P4S. **ששת נכסי `docs/design/*.jpg` הוחלפו לגרסתם הסופית בפעם הרביעית** (4 מהם עודכנו בסבב הזה: overview/p4s/shared-form/budget-simulator; login/followup ללא שינוי מהסבב הקודם, אומת ב-`file id` זהה ב-`list_screens`). ⚠ **לקח מתועד:** שתי הצהרות "done" קודמות התבררו כמוקדמות מדי — הראשונה כי הסתמכה חלקית על צילום בלי אימות HTML ממצה, השנייה כי חיפוש ה-`grep` היה צר מדי (חיפש שמות class ספציפיים במקום תוכן ריק/חזותי). הסבב הזה אומת כל ממצא ישירות מול תוכן ה-HTML החי לאחר כל תיקון. |
| **11** ✅ **`done`** | החלפת המוקאפ ב-ROADMAP בנכסים הקנוניים (D6) | 9 | לשונית "דוגמת ממשק — איך זה ייראה" ב-`ROADMAP.html` — המוקאפ הידני שקדם לפאזה 8A (`login-card` מזויף עם שדות מנוטרלים, שלושה `predict-card` עם נתונים בדויים ל-P2/P3/P4 בלבד, `SVG` יד-מצויר לגרף Follow-up, ומסך OOD נפרד; ⛔ ללא Overview וללא P4S — הוקדם ליצירתם) **הוסר לגמרי**, לא רק סומן כהיסטורי — הוחלף בשישה תגי `<img>` לצילומי `docs/design/*.jpg` הקנוניים (login/overview/shared-form/p4s/budget-simulator/followup), ברזולוציה מקורית, עם כיתוב עובדתי לכל מסך. **אפס markup חזותי כפול**: הוסרו לגמרי `login-card`, `predict-grid`/`predict-card`, `strategy-row`, `funnel-wrap`/SVG, `table.fallback` ומסך ה-OOD המזויפים — אינם נשארים לצד הצילומים. אומת: כל שישה קבצי `docs/design/*.jpg` קיימים בנתיב היחסי שנקבע (`docs/design/<name>.jpg`, שורש הריפו); `git diff --check` נקי; `node --check` על ה-`<script>` תקין (תפס ותיקן בעצמו שגיאת syntax אחת — מרכאות כפולות לא-בורחות בתוך מחרוזת JS); בדיקת איזון תגיות HTML מלאה (Python `HTMLParser`) — אפס תגית פתוחה או יתומה; `pytest tests/test_roadmap_render.py tests/test_requirements_traceability.py` — 14/14. **ניקוי CSS השלים את הסגירה**: הוסרו כל כללי ה-CSS ששייכים בלעדית למוקאפ שהוסר (`.tag`/`.tag.demo`/`.tag.spec`, `.login-card` על חמשת ילדיו, `.login-fine`, `.predict-grid`/`.predict-card`/`.predict-main`/`.predict-sub`, `.badge-row`/`.badge` על ארבעת הוריאנטים, `.predict-disclaimer`, `.funnel-wrap`/`.funnel-caption`, `table.fallback` על שלושת חוקיו, `.strategy-row`/`.strategy-label`/`.strategy-bar-wrap`/`.strategy-bar`/`.strategy-whisker`/`.strategy-meta`, `.alert-box` על שלושת ילדיו) — אומת ב-`grep` לכל שם selector: אפס מופעי `class="..."` בכל ה-HTML לפני המחיקה. `.mock-note`/`.mock-screen`/`.mock-screen img` נשארו (בשימוש פעיל בששת כרטיסי הצילום), ו-`.phase-note` נשאר (בשימוש פעיל ב-JS, שורת `noteHtml`, לא שייך למוקאפ) — אומת שהוא כן מופיע ב-`class="..."` לפני המחיקה, ולכן לא הוסר. אימות סופי אחרי המחיקה: `git diff --check` נקי, `node --check` תקין, `pytest` — 14/14. |
| **12** ✅ **`done`** | ביקורת וסריקה → commit → push → PR → CI → merge, כל שלב באישור נפרד | 11 | **שלב 1/6 בלבד (ביקורת קוד + סריקת סודות + בדיקות) בוצע באישור נפרד.** ביקורת קוד: `git diff --stat` מול `a3d5674` — שלושה קבצים בלבד (`ROADMAP.html`/`docs/DESIGN.md`/`docs/planning/PHASE10.md`, 297+/284-) בתוספת ספריה חדשה לא-עקובה `docs/design/*.jpg` (שישה קבצים, 16–208KB); `git status` אימת אפס שינוי ב-`app/`, `tests/`, `REQUIREMENTS.md`, `IA.md` — תואם ל-D1/קריטריון 19–20 (פאזת עיצוב בלבד). נסרקו שלושת קבצי ה-diff במלואם: עקביות סטטוסים בין הקבצים (checkpoints 8–11 `done` בכל מקום, אין "not_started" שיורי), איזון תגיות HTML מלא (`HTMLParser`), איזון `**` ב-Markdown (זוגי בשני הקבצים), אפס `TODO`/`FIXME`, נתיבי `docs/design/*.jpg` תואמים לקבצים בפועל. **סריקת סודות**: `grep` על דפוסי `api[_-]key`/`secret`/`password`/`token`/JWT (`eyJ...`)/`-----BEGIN`/`.env`/`SERVICE_ROLE`/`ANON_KEY` על ה-diff המלא — **0 סודות שמישים** (דפוסי החיפוש עצמם, כמילות מפתח, מופיעים ביומן הביקורת הזה גופו ואינם סוד — קריטריון 29); `login.jpg` נבדק חזותית — שדות אימייל/סיסמה ריקים; `git check-ignore` אימת ש-`docs/design/*.jpg` אינו מוחרג בטעות מ-`.gitignore`. **בדיקות רלוונטיות** (⛔ לא `pytest` מלא — אין שינוי קוד): `tests/test_design_tokens.py` + `tests/test_roadmap_render.py` + `tests/test_requirements_traceability.py` — 23/23. **⛔ אין מודלים/Holdout הורצו.** לא נמצא ממצא הדורש תיקון מקומי בסבב הזה. **שלב 2/6 (commit) בוצע באישור נפרד:** `git commit` על `feat/design`, 9 קבצים שונו (`ROADMAP.html`/`docs/DESIGN.md`/`docs/planning/PHASE10.md` + 6 קבצי `docs/design/*.jpg` חדשים כ-`create mode`), 331 insertions(+)/286 deletions(-). ⚠ ה-SHA המקורי אינו מתועד כאן — התיעוד עצמו (כולל השורה הזו) נערך אחרי ה-commit ולכן מצריך `amend`, שמשנה את ה-SHA; ה-SHA הנוכחי נבדק תמיד ב-`git log` בפועל, לא מוקלד כאן. `Co-Authored-By` בלבד בהודעת ה-commit, ⛔ ללא `Claude-Session` (ריפו ציבורי). **שלב 3/6 (push) בוצע באישור נפרד:** `git push origin feat/design` — fast-forward `a3d5674..3cf7d0a`, ⛔ ללא force (הצורך המקורי בשני סבבי `amend` קדם ל-push, לא בא אחריו). `origin/feat/design` אומת מול `3cf7d0a` — זהה ל-`HEAD` המקומי; `git status --short --branch` — אין `ahead`/`behind`. **שלב 4/6 (PR) בוצע באישור נפרד:** PR #26 (`feat/design`→`main`) נפתח; תוקן פעמיים באישור נפרד — הוסר קישור/אזכור Claude Session, הכותרת/Summary תוקנו להבהיר שה-PR מצטבר וכולל גם Phase 10A (`app/insights.py`/`app/supabase_client.py`/`app/features.py`/`app/static/business_facts.json`/`scripts/train.py`/`scripts/business_facts.py`/`tests/test_business_facts.py`) ולא רק Phase 10 design-only — רק commit הסגירה האחרון (`3cf7d0a`) הוא design/docs-only בפועל; תיאור ה-CI תוקן לניסוח עובדתי. **שלב 5/6 (אימות CI) בוצע באישור נפרד:** `gh pr view 26` — `headRefOid`=`70bfec398cdddbfe8c1775a47f4fea21fd10672a`, זהה ל-`HEAD` המקומי; שתי ריצות CI נבדקו בנפרד דרך `gh run view` — run `34782288802` (אירוע `push`) ו-run `34782430525` (אירוע `pull_request`), שתיהן עם `headSha`=`70bfec3` ו-`conclusion=success`; `mergeable=MERGEABLE`, `mergeStateStatus=CLEAN`, `state=OPEN`. **לפני merge נדרש סבב מנהלי נוסף** לסנכרון תיעוד שלבים 4-5 עצמם (לא צורף בשקט לאישור המיזוג) — commit `3cf03fd` (2 קבצים, 37 insertions(+)/5 deletions(-)) + push נפרד, שהפעילו CI חדש; שלב 5/6 אומת פעם שנייה על `headSha`=`3cf03fd` (ריצה ראשונה נתפסה `IN_PROGRESS`/`mergeStateStatus=UNSTABLE`, לא דווחה כהצלחה עד השלמה בפועל — פולינג עם השהיות קצרות; שתי הריצות הסופיות `push`/`pull_request` שתיהן `success`). **שלב 6/6 (merge) בוצע באישור מפורש ונפרד:** מצב טרי אומת שוב מיד לפני הביצוע (ללא שינוי מאז האישור); `gh pr merge 26 --merge` (merge commit, עקבי עם היסטוריית ה-PRs הקודמים בריפו) — PR #26 `state=MERGED`, `mergeCommit=ef2eb83`, `mergedAt=2026-09-13T21:14:54Z`; `main` המקומי עודכן ב-fast-forward, אומת `origin/main`==`HEAD`==`ef2eb83`, עץ עבודה נקי. **checkpoint 12: `done`.** |

---

## ט. קריטריוני קבלה

| # | קריטריון | מאמת |
|---|---|---|
| 1 | ⛔ **אין `owner=10`** באף שורה במרשם | פקודה |
| 2 | ב-`REQUIREMENTS.md` השתנתה **עמודת `contributors` בלבד**, ב-21 שורות; ⛔ אפס שינוי ב-`owner`, ב-`status` וב-`evidence` | `git diff` — כל hunk נוגע בעמודה 6 בלבד |
| 3 | קובץ tokens יחיד; ⛔ אין מקור ערכים שני | סקירת קבצים |
| 4 | כל זוג `foreground/background` מוצהר עומד ב-4.5:1 | בדיקה 1 |
| 5 | טקסט גדול ורכיב חיוני עומדים ב-3:1 | בדיקה 2 |
| 6 | צבעי גרף חיוניים עומדים ב-3:1 מול רקע או צבע סמוך | בדיקה 3 |
| 7 | לכל מצב ב-`IA.md` §9 יש רכיב | בדיקה 4 |
| 8 | לכל שדה חוזה שהדפדפן צורך יש מיקום או נימוק | בדיקה 5 |
| 9 | אין token יתום ואין הפניה ל-token חסר | בדיקות 6–7 |
| 10 | לכל מסך תוצאה/תובנה ולכל גרף יש הסבר, פעולה, מגבלה **ומקור ראיה מדויק** | בדיקה 9 |
| 11 | חמש שאלות המייסדת נגישות מ-Overview, ממופות לארבעה יעדים | סקירה ידנית |
| 12 | P4 מוצג באחוזים ב**מסמכי המקור החיים** (`SPEC.md`, `IA.md`, `PHASE10.md`, החלקים החיים של `codex-review.md`) ובכל תוצר עיצוב. ⚠ ארכיון מסומן (`codex-history.md`, וסעיפים המתועדים מפורשות כהיסטוריים) **מותר** שיכיל את הניסוח הישן | `grep` על המסמכים החיים + סקירה |
| 13 | P4S במסך נפרד עם ארבעה שדות; ⛔ אינו בטופס 13 השדות | סקירה |
| 14 | הבחנה חזותית מפורשת בין P4 ל-P4S | בדיקה ידנית |
| 15 | ⛔ אין `uncalibrated` ואין טווח במסך P4S | סקירה |
| 16 | RTL, bidi ו-LTR-בתוך-RTL תקינים | בדיקה ידנית |
| 17 | שום משמעות אינה מועברת בצבע בלבד | בדיקה ידנית |
| 18 | פריסת Desktop עקבית; ⛔ הגוף אינו נגלל לרוחב | בדיקה ידנית |
| 19 | ⛔ אפס שינוי ב-`app/static/*` בשלב התכנון | `git diff` |
| 20 | ⛔ אפס שינוי ב-`openapi.json` ו-`schemas.py` | `git diff` |
| 21 | ⛔ אין markup חזותי כפול ב-ROADMAP אחרי checkpoint 11 | סקירה |
| 22 | שיעורי הבסיס נכונים: 46.35 · 53.65 · 42.71 · 16.72 | `grep` |

### קריטריונים חזותיים למסע המשתמש (B59 — ר' §יג)

| # | קריטריון | מאמת |
|---|---|---|
| 23 | אדם שאינו מכיר את המערכת מבין **מהיכן מתחילים ולאן ממשיכים** | בדיקה ידנית על התוצר הסטטי |
| 24 | כל אחת מחמש שאלות המייסדת נגישה מ-Overview | סקירה מול §ה, מסך 2 |
| 25 | מסך P4S מזוהה בבירור כציון ל**רוכש ידוע**, אחרי מעקב 1 ובחלון גיוס חודשי סגור | סקירה ידנית |
| 26 | Follow-up, תקציב והמלצות נגישים בניווט ברור | סקירה ידנית |
| 27 | ההסבר והפעולה המומלצת **גלויים לצד** התוצאה או הגרף | בדיקה 9 (סגירוּת D9) |
| 28 | ⛔ אין צילום Stitch המוצג כהוכחה ל-B59 | סקירת §יא ו-§יג |
| 29 | ⛔ אין **סוד שמיש** בשום תוצר עיצוב: סיסמה · token · API key · זוג credentials שלם. ⚠ **מזהים לא-סודיים מותרים** (`demo-northbound`, `demo-noorg`, כתובות דמו) — הם כבר מופיעים ב-`scripts/create_users.py`, ב-`tests/`, ב-`REQUIREMENTS.md` וב-`SPEC.md` | `grep` לדפוסי סוד + סקירת התוצרים |
| **30** | **מטריצת כיסוי Stitch — מסלול תקין.** כל אחד מ**ששת מסכי ה-Desktop** מכוסה בנכס תמונה מזוהה בשמו ב-`docs/design/`, ברזולוציה מקורית (⛔ לא thumbnail מוקטן) שנבדקה בפועל מול תוכנה, ⛔ "הקובץ קיים" אינו ראיה. אותם נכסים משמשים גם ל-**D9 לצד כל תוצאה וכל גרף**. ⚠ **בהכרעת סקופ מפורשת של המשתמש, שבעת מצבי ה-runtime (`loading`/`error`/`empty`/`401`/`403`/`OOD`/כשל חלקי) ומצב ה-`prefill` של P4S אינם מכוסים חזותית בפאזה 10** — ניתן היה לעצב אותם כמסכים סטטיים נוספים ב-Stitch (זו אינה מגבלה טכנית של הכלי), אך המשתמש בחר במפורש שלא ליצור וריאנטים נוספים ולהסתפק בששת המסכים הקיימים כדי לצמצם היקף עבודה; הם **נדחים לפאזה 11** (שם האפליקציה עולה בפועל וניתן לצלם מצב חי אמיתי במקום עוד מוקאפ), ותוכנם ההתנהגותי מוגדר במלואו ב-`IA.md`/`DESIGN.md` §2 | המטריצה ב-`docs/DESIGN.md` §9 מול תוכן `docs/design/`; §9.2/§9.3 שם מתעדים במפורש את הדחייה לפאזה 11 |

### קריטריונים להיררכיית התוצאה ולשימושיות (D9 · D11)

| # | קריטריון | מאמת |
|---|---|---|
| 31 | **התשובה הישירה היא הרכיב הראשון והבולט** באזור התוצאה | בדיקה ידנית |
| 32 | ההסבר והפעולה מופיעים **לפני** "פרטי המודל" | בדיקה ידנית |
| 33 | הסתייגות מהותית **צמודה לתשובה אך אינה גדולה או בולטת ממנה** | בדיקה ידנית |
| 34 | ⛔ אין צורך לפתוח `<details>` כדי להבין את התשובה ואת הפעולה | בדיקה ידנית |
| 35 | קריטריונים 31–34 נבדקים גם על **ייצוא Stitch ב-Desktop** | מול `docs/design/` |
| 36 | **אדם זר משלים ידנית את ארבעת שדות P4S ומבין את התוצאה** בלי היכרות עם ה-CSV | בדיקה ידנית מול מפת השדות (D11) |
| 37 | תחזית או הסתברות ⛔ אינה צבועה `positive`/`negative` באף מסך | סקירה מול D10 |

⚠ **"נראה מקצועי" אינו קריטריון קבלה.** הרף הוא עקביות, כיסוי, נגישות,
פריסת Desktop, היררכיה, שימוש בטוקנים והיעדר סתירות — כולם ברשימה למעלה.

---

## י. סיכונים ובלמים

| סיכון | בלם |
|---|---|
| **בלבול P4 / P4S** — שני מספרים סביב 70 באותו מוצר | כותרות נפרדות, יחידות נפרדות (`71%` מול `71`), מסכים נפרדים, הסתייגות גלויה בשניהם, קריטריון קבלה 14 |
| **Drift** בין Stitch, tokens, מפרט, ROADMAP והמימוש | מקור ערכים יחיד (D6) · סדר העבודה בן שישה השלבים (D2) מחייב כתיבה חזרה למקור · בדיקות מורצות מחדש אחרי כל שינוי · המוקאפ הכפול מוסר (checkpoint 11) |
| **המלצה עסקית שאינה נתמכת בראיות** | D9 — תבנית דטרמיניסטית על תגובת הבקשה הנוכחית ועל עובדות היסטוריות ממופות בנכס גרסאי · ⛔ אין LLM בזמן ריצה · חובה לומר "אין המלצה חד-משמעית" כשאין ראיה · בדיקת סגירוּת 9 וביקורת תוכן אנושית |
| **הסתמכות על צבע בלבד** | כל מצב מקבל טקסט + אייקון · בדיקות ניגודיות · בדיקה ידנית ייעודית |
| **RTL/bidi** — מספרים ומטבעות שנשברים בתוך RTL | כלל LTR-בתוך-RTL במפרט · בדיקה ידנית · הבחינה בשלב 3 של סדר העבודה |
| **מוקאפ סטטי שיוצג בטעות כהוכחת מערכת עובדת** | סעיף יא · המוקאפ מסומן היסטורי עכשיו ומוסר בביצוע · ⛔ פאזה 10 אינה רשאית לטעון שנגישות "עברה" |
| **פאזה 10 תגלוש למימוש** | D5 · קריטריונים 19–20 · בביצוע מותר **להוסיף רק** `app/static/tokens.css`, ⛔ ואין לערוך קובץ `app/` קיים (§ גבולות הפאזה) |
| **Stitch ייגע בפרויקטים קיימים** | D2 · checkpoint 7 באישור מפורש ונפרד · שלושת הפרויקטים מזוהים ומוחרגים |

---

## יא. גבול הראיות של פאזה 10

צילומי Stitch מוכיחים **עיצוב בלבד**. ⛔ הם אינם מוכיחים: שימוש אמיתי
ב-Supabase · RLS או JWT · session או sign-out · קריאת runtime מהמסד · GitHub
Actions · פריסה ציבורית · auto-deploy · restart · מסע משתמש עובד.

ראיות אלה שייכות לפאזות 11–12 ולמערכת החיה. `README.md`, `schema.sql`
ו-`REPORT.md` אינם תוצרי פאזה 10. סרטון הדמו (`B58`) אופציונלי בבריף, נשאר
`N/A`, ⛔ ואין להוסיפו להיקף.

⛔ **בפרט: אין להציג צילום Stitch כהוכחה ל-`B59`.** צילום מראה שהמסע
**עוצב**, לא שהוא **עובד**.

⚠ **דיוק:** מסע חי ממחשב נקי בפאזה 12 הוא ההוכחה היחידה ל**תנאי המסע**
של B59 — ⛔ לא ל-B59 כולו. התנאי השני, *"Your repo tells them how it was
built and how to run it themselves"*, נסמך על `README.md` / `B1` בפאזה 13.
**B59 עובר ל-`done` רק בשער שאחרי פאזה 13, בצירוף שתי הראיות** (§יג).

---

## יב. מיפוי מול `REQUIREMENTS.md`

- **דרישות שנסגרות: אפס.** ⛔ אין `owner=10`.
- **שינוי במרשם:** עמודת `contributors` בלבד, ב-**21** שורות (D1).
  ⛔ אפס שינוי ב-`owner`, ב-`status` וב-`evidence`.
- **21 התרומות:** `B10` · `B21` · `B24` · `B28` · `B29c` · `B36` · `B41` ·
  `B42` · `B43` · `B44` · `B45` · `B46a` · `B46b` · `B46c` · `B50` · `B52` ·
  `B53a` · `B53b` · `B55b` · `B56` · `B59`. הכלל וההחרגות המנומקות — D1.
- **נשארות בבעלות אחרים:** `B41` (8A) · `B10`, `B45`, `B50`, `B56` (11) ·
  `B55b`, `B59` (12) · `B1`, `B6`, `B29c`, `B43`, `B53a`, `B53b`, `B57` (13).
- ⚠ **ספירות הסטטוס אינן זזות** — ולכן
  `test_status_counts_match_expected_snapshot` ו-
  `test_roadmap_b_registry_summary_matches_the_real_counts` אינם מושפעים.
  אף בדיקה במרשם אינה אוכפת את תוכן עמודת `contributors`.
- ⛔ אין מרשם עקיבות מקביל.

**`B59` — רף הסיום מקצה לקצה** מקבל טיפול מפורט ב-**§יג**: פאזה 10 תורמת
לו חזותית (מסע המשתמש בשבעה שלבים) ומסומנת `contributor=10`, ⛔ אך אינה
בעליו ואינה משנה את `status` שלו.

⚠ **B59 אינו נסגר בפאזה 12.** הוא מכיל שני תנאים: מסע חי (פאזה 12) ו-repo
שמסביר כיצד הוא נבנה ומורץ (`README.md` / `B1`, פאזה 13 — הקובץ אינו קיים).
המעבר ל-`done` מתרחש **בשער שאחרי פאזה 13**, בצירוף שתי הראיות.
`owner=12` ו-`verifier=12` נשארים; ⛔ אין שינוי במרשם כעת.

**סטיות מאושרות, מצוינות מחדש בשער הזה ו⛔אינן נפתחות לדיון:**
**Render במקום Railway** (מכוח ההיתר המפורש בבריף §07 —
*"Render or Fly.io are drop-in substitutes for this pillar"*) · **בלי notebook**.

---

## יג. B59 — רף הסיום: פאזה 10 מאפשרת חזותית, ⛔ אינה סוגרת

### הדרישה

> *"A stranger can open your live URL, sign in, get a prediction for a new
> customer, see the follow-up and budget insights, and read your
> recommendations — without you touching anything. Your repo tells them how it
> was built and how to run it themselves."* — הבריף §06

הציטוט רשום מילה במילה כ-**B59** ב-`docs/planning/REQUIREMENTS.md:135`.

### עקיבות — עמודת `contributors` בלבד

| שדה | ערך במרשם | פאזה 10 |
|---|---|---|
| בעלים | **12** | ⛔ ללא שינוי |
| תורמים | **9 · 10 · 11 · 13** | ✅ **פאזה 10 נוספה כתורמת** — היא קובעת את המפרט החזותי ואת מסע המשתמש |
| מאמת | 12 | ⛔ ללא שינוי |
| סטטוס | `planned` | ⛔ ללא שינוי |
| ראיה | *מתח "לקוח חדש" מרוכך ב-8A* | ⛔ ללא שינוי |

⚠ **תוקן בסבב השישי.** הנוסח הקודם כאן קבע ש"פאזה 10 אינה מתווספת
כתורמת" — בסתירה למרשם שכבר נושא `contributor=10` ל-`B59`, ולכלל שנקבע
ב-D1. הסתירה נוצרה כשהמרשם עודכן והסעיף הזה לא.

**פאזה 10 תורמת ל-B59 את המפרט החזותי ואת מסע המשתמש, ⛔ ואינה סוגרת
אותו.**

### ⚠ B59 אינו נסגר בפאזה 12 — הוא דורש שתי ראיות משתי פאזות

תוקן אחרי ביקורת Codex. הנוסח הראשון קבע ש-B59 "נסגר בפאזה 12". **זה לא
נכון.** הדרישה מכילה **שני תנאים נפרדים**:

| תנאי | מי מספק | מצב |
|---|---|---|
| א. *"A stranger can open your live URL... without you touching anything"* — מסע חי ללא התערבות המפתח | **פאזה 12** | טרם בוצע |
| ב. *"Your repo tells them how it was built and how to run it themselves"* | **פאזה 13** — `README.md` (`B1`) | ⛔ **הקובץ אינו קיים** |

`README.md` מתוכנן לפאזה 13 ו-`B1` נמצא ב-`status: planned` עם הראיה
*"הקובץ אינו קיים (אומת)"*. ⛔ **אי-אפשר לסגור דרישה שתנאי מפורש שלה טרם
נכתב.**

**לכן:**

- פאזה **12** מבצעת ומתעדת את המסע החי (תנאי א).
- פאזה **13** משלימה את `README.md` / `B1` (תנאי ב).
- `B59` עובר ל-`done` **רק בשער שאחרי פאזה 13**, בצירוף שתי הראיות.
- `verifier=12` נשאר כבעל פרוטוקול הקבלה.

⛔ **פאזה 10 אינה משנה את `owner` או את `status` של `B59`** — היא
מוסיפה את עצמה ל-`contributors` בלבד (D1), וההבהרה על סדר הסגירה היא
תיעוד תכנוני.

הראיה לתנאי א היא **מסע חי** — ⛔ לא צילום מסך, לא ייצוא Stitch ולא מוקאפ.

### מסע המשתמש שהעיצוב הקנוני חייב להציג

שבעה שלבים, ברצף אחד ובניווט ברור:

| # | שלב | מסך |
|---|---|---|
| 1 | התחברות | Login |
| 2 | **חמש שאלות המייסדת** והפנייה לארבעת יעדי המוצר | Overview (§ה, מסך 2) |
| 3 | **ציון לקוח־על לרוכש ידוע** — הזנת ארבעת שדות `EarlyFunnelInput` אחרי מעקב 1 ובחלון חודשי סגור | **P4S** (§ה, מסך 4) |
| 4 | תחזיות P2/P3/P4 | טופס משותף (§ה, מסך 3) |
| 5 | תובנות Follow-up | מסך 6 |
| 6 | סימולטור תקציב | מסך 5 |
| 7 | המלצות עסקיות ויציאה מהמערכת | רכיב D9 בכל מסך + sign-out |

⚠ **מענה ללקוח חדש בלי להמציא זמן:** B59 דורש *"a prediction for a new
customer"*. P2 עונה ישירות על שאלת אורך החיים של לקוח שנרכש לאחרונה, אך
רק אחרי סגירת מחזור הקמפיין ובאמצעות 13 ערכי הסוף. P4S עונה על שאלת
לקוח־העל עבור רוכש ידוע, אחרי מעקב 1 וסגירת חלון הגיוס החודשי. ארבעת
הפיצ'רים שלו מוקדמים ביחס לתוצאות LTV/אפסייל/הפניה, אך אינם מוכיחים שימוש
לפני רכישה. המסע חייב להציג את שני המענים ואת תנאי הזמינות שלהם, בלי לכנות
את P4S תחזית לליד חדש (`SPEC.md` § חוזה שימוש עסקי פעיל).

### מה מוצג בכל מסך תוצאה ובסמוך לכל גרף עסקי

ארבע השכבות של **D9**, בשפה פשוטה: מה התשובה או הממצא · מה משמעותו העסקית ·
מה הפעולה המומלצת · מה המגבלה החשובה.

ההמלצות **דטרמיניסטיות** ומבוססות על תגובת הבקשה הנוכחית ועל עובדות
היסטוריות מצומצמות עם מקור וגרסה. עובדה קבועה אינה מוצגת כמדידה חדשה,
ושינוי קלט מבטל המלצה אישית קודמת. ⛔ אין AI בזמן ריצה · ⛔ אין טענה
סיבתית · ⛔ אין הבטחת תוצאה · כשהראיות חלשות — אומרים במפורש שאין המלצה
חד-משמעית. מדדים טכניים ופרטי מודל יורדים לפרטים משניים, ⛔ ואינם מסתירים
את התשובה העסקית.

### חלוקת האחריות בין הפאזות

| פאזה | חלקה ב-B59 |
|---|---|
| **10** | **תורמת** (`contributor=10`) — המפרט החזותי ומסע המשתמש; ⛔ אינה בעלים ואינה סוגרת |
| **11** | מימוש הדשבורד והניווט |
| **12** | **בעלים ומאמת** — מבצעת ומתעדת את המסע החי (תנאי א): מחשב/דפדפן נקי, פרטי הדמו שנמסרו, ⛔ בלי קוד מקומי ובלי התערבות המפתח. ⚠ **אינה סוגרת את B59 לבדה** |
| **13** | `README.md` באנגלית — ארכיטקטורה, התקנה, הרצה ו-URL חי (**תנאי ב**, `B1`) · `REPORT.md` בעברית — ממצאים והמלצות |
| **שער אחרי 13** | ⚠ **כאן בלבד** `B59` עובר ל-`done`, בצירוף מסע פאזה 12 + `B1` מפאזה 13 |

⚠ **מבחן המסע בפאזה 12 מזין את ארבעת שדות P4S ידנית** (D7ב), ⛔ ולא בוחר
רשומה היסטורית מהבורר — אחרת הוא אינו מוכיח *"a prediction for a new
customer"*.

### גישת הדמו — ההכרעה הקיימת נשמרת

הרשמה ציבורית **כבויה**. פרטי הדמו נמסרים **בנפרד על ידי המשתמש**, ⛔ ואינם
מופיעים ב-repo, ב-Stitch או במסכי היישום. מסך Login מציג שדות ריקים בלבד;
⛔ אין placeholder שהוא אישור אמיתי, ואין רמז לסיסמה בשום תוצר עיצוב
(קריטריון קבלה 29).

---

## יד. הצהרת סטטוס

`planning_status`: `outline_only` → `planning_in_progress` →
**`approved_for_execution`**.
`execution_status`: `not_started` → `in_progress` → **`done`**
(14.09.2026, לאחר מיזוג `ef2eb83` — ר' פסקת הסגירה בסוף הסעיף הזה).

✅ **המשתמש אישר את התכנון סופית ב-10.09.2026**, אחרי ש-Codex אישר ללא
ממצאים נוספים.

✅ **checkpoint 0 הושלם.** PR #25 (מסמכי התכנון) מוזג ל-`main`
(`merge commit ef289f5`, CI ירוק), ענף `feat/design` נפתח מאותו `main`
נקי (`merge-base feat/design main` = `ef289f5`, מאומת), והראיה מתועדת
בקומיט checkpoint 0 שבראש הענף. ✅ `feat/design` נדחף ל-`origin` (ראש
נוכחי `a3d5674` בזמנו, באישור כפול). ⚠ **תוקן — פסקה זו הייתה נכונה
בזמן כתיבתה (לפני שנפתח PR); המצב הסופי מתועד בפסקת הסגירה בהמשך
הסעיף הזה ובסוף §טו.** האישור הנפרד לכתיבה ל-Stitch ניתן ב-11.09.2026.

**מצב הביקורת:** התכנון עבר **תשעה סבבי ביקורת Codex** (10.09.2026).

⚠ **הסבב החמישי פתח מחדש את האישור שניתן בסבב הרביעי.** קריאה חוזרת של
התכנון מול הבריף — ולא מול `SPEC.md` — חשפה פער בריף אמיתי (`B43` בסטטוס
`gap` בזמן שמעצבים את המסך שנועד לענות עליו), ובעקבותיה ביקורת Codex
מצאה טעות חוסמת ב-D1: הטענה שאין `contributor=10` סתרה את `CLAUDE.md`.
⛔ **אישור סבב 4 אינו בתוקף עוד**; המסמך שונה מהותית אחריו.

השינויים בסבב החמישי: מיפוי `contributor=10` · כרטיס פרופיל לקוחות-העל
ב-P4S (`B42`/`B43`) · תמצית המנוף ב-P2 (`B29c`) · **D10** סמנטיקת צבע ·
**D11** מפת שדות · קריטריונים 31–37.

**הסבב השישי** תיקן סתירה שנוצרה בסבב החמישי עצמו: המרשם עודכן אך
מסמכי התכנון לא. שלוש תרומות נוספו (`B24`, `B46a`, `B46b` — 18 ⇒ **21**),
כל שאריות "`B59` אינו תורם" הוסרו, `B32` הוכרע כ-`REPORT.md` בלבד על בסיס
החוזה, ומקור הראיה של D9 נאכף בבדיקה 9, בקריטריון 10 וב-checkpoint 6.

✅ **הביקורת החוזרת התקיימה ואישרה ללא ממצאים נוספים**, ובעקבותיה ניתן
אישור המשתמש הסופי — `planning_status` עודכן בהתאם.

✅ המשתמש נתן הוראת ביצוע, ובהמשך אישר בנפרד את הכתיבה ל-Stitch. ב-11.09.2026
העביר את המשך הביצוע ל-Codex עד חזרת Claude, עם דרישה לתיעוד מדויק לצורך
ביקורת חוזרת.

**עדכון — 13.09.2026, אחרי סגירת 10A:** checkpoint 8 חזר לתור ועבר סבב
בדיקה מלא נגד `DESIGN.md`/`IA.md` המעודכנים על **כל ששת המסכים**: טופס
משותף, P4S, Budget Simulator, Follow-up, Overview ו-Login. גישה ד׳ נבדקה
במלואה — `select-on-focus` בוצע ואומת בשני הטפסים, סדר Tab תקין (בדיקת
מוקאפ סטטי בלבד, לא אישור נגישות מקלדת), והצהרת מועד המדידה הקיימת
("כל הנתונים נמדדים בתום מחזור הקמפיין") מכסה במלואה את `DESIGN.md` §7.1
עבור כל 13 הערכים בטופס המשותף — לא נדרש `tooltip` נוסף. `commit`/`push`
של `a3d5674` בוצעו באישור המשתמש ותקינים. פירוט מלא ושלוש הראיות האחרונות
בטבלת §טו.
**checkpoint 8: `done`.**

**עדכון — 13.09.2026, checkpoint 9:** בהוראת המשתמש, ובהכרעת סקופ מפורשת
נגד הרחבה (אין יצירת וריאנטי Stitch חדשים למצבים שתלויים ב-API חי) —
שישה נכסי תמונה יוצאו מ-Stitch ל-`docs/design/`, ומטריצת כיסוי נכתבה
ב-`DESIGN.md` §9 לפי קריטריון 30: לכל אחד ממצבי `loading`/`error`/`empty`/
`401`/`403`/`OOD`/כשל חלקי, ולמסלול ה-`prefill` של P4S, אין נכס תמונה —
במקומו הפניה מפורשת לטבלת §2/`IA.md` המתאימה שכבר מפרטת אותו, עם נימוק
מדוע לא ניתן לצלם מצב שלא קיים בכלי עיצוב סטטי. `pytest
tests/test_design_tokens.py` — 9/9 עברו.
**checkpoint 9: `done`** *(תוקן למטה — ראה עדכון הביקורת הבא)*.

**עדכון — 13.09.2026, ביקורת נוספת על checkpoint 9:** שלושה ממצאים חייבו
פתיחה מחדש של checkpoints 8–9, **ללא נגיעה ב-Stitch עצמו** (לפי הוראה
מפורשת שלא לבצע שם שינוי בסבב הזה):
1. **שורת `empty` ב-§9.2 הייתה הפוכה** — נכתב שהיא רלוונטית רק ל-`Login`,
   בעוד `DESIGN.md` §2.1 קובע ההפך: תוכן `empty` ממשי קיים בטופס המשותף
   וב-P4S בלבד, ⛔ לא ב-Login. תוקן.
2. **קריטריון 30 וניסוח checkpoint 9 כינו הפניה לטבלת טקסט "נכס חזותי"** —
   אינה כזו. קריטריון 30 (`§ט`) ותיאור checkpoint 9 (`§ח`) נוסחו מחדש:
   ששת צילומי מסלול התקין מכוסים חזותית; שבעת מצבי ה-runtime ומצב
   ה-`prefill` של P4S **נדחים לפאזה 11 ואינם מכוסים חזותית בפאזה 10**.
3. **הצילומים היו thumbnails של 512px.** אותרה תוספת `=s0`/`=w<N>` על
   כתובת ה-`screenshot.downloadUrl` של Stitch (`lh3.googleusercontent.com`)
   שמחזירה רזולוציה מקורית (1379–1454px רוחב, פי ~3 מהקודם); ששת הנכסים
   יוצאו מחדש ברזולוציה הזו, ללא upscaling מלאכותי.
4. **בדיקת `p4s.jpg` מול Stitch החי חשפה פער אמיתי, לא ייצוא מיושן —
   אך מצומצם משחשבתי בסבב הקודם.** בדיקה טרייה של ה-HTML החי (לא
   הסבב הישן `p4s_check`/`p4s_check2`) מאתרת בדיוק: כותרת `<h1>` גלויה
   `ציון לקוח-על לרוכש ידוע (P4S)`, ושני מופעי תג `SUPER-CLIENT`.
   ⛔ **אין לכלול את `<title>` (כותרת המטא בכרטיסייה) כפער נפרד** — היא
   אינה טקסט עסקי גלוי באותו אופן. ⛔ **`4 פרמטרים` אינו פער** — נבדק
   ישירות: 0 מופעים ב-HTML החי, 4 מופעי `שדות` — התיקון הקודם ל"שדות"
   (§טו, סבב P4S) תקף ולא נסוג. הטענה "P4S/Super-Client הוסרו מטקסט
   עסקי גלוי" ב-checkpoint 8 (§ח, §טו) **הייתה שגויה** לגבי ה-`<h1>`
   ותגי `SUPER-CLIENT` בלבד — לא לגבי `פרמטרים`, ששם התיקון תקין.
   **checkpoint 8 נפתח מחדש** לשני הפריטים האלה בלבד במסך P4S —
   חמשת המסכים האחרים אינם מושפעים.
5. **ממצא נלווה, לא תוקן כאן:** מסך Login מציג `IQ Funnel` (סדר מילים
   הפוך ל-`FunnelIQ` הקנוני, `app/static/index.html` שורה 21) — נרשם
   כמועמד לבדיקה ב-checkpoint 10.

`pytest tests/test_design_tokens.py` — 9/9 עברו אחרי תיקון §9.2.
**checkpoint 8: `in_progress`** (שורת P4S בלבד). **checkpoint 9:
`in_progress`** (מטריצה + נכסים תוקנו; ממתין לתיקון בפועל של P4S
ב-Stitch לפני סגירה חוזרת). ⛔ אין שינוי ב-Stitch בסבב הזה, אין
push/PR/merge. הצעד הבא: תיקון P4S ב-Stitch (ידני, בהוראת המשתמש),
ואז סגירה חוזרת של checkpoints 8–9. checkpoint 10 טרם התחיל — ⛔ לא
יתחיל בלי הוראת משתמש נפרדת.

**עדכון — 13.09.2026, תיקון P4S ידני וסגירה סופית:** פרומפט Stitch ממוקד
ביקש שני שינויים בלבד במסך P4S: הסרת `(P4S)` מה-`<h1>`, ומחיקת שני תגי
`SUPER-CLIENT` כולל מעטפותיהם. סבב ראשון ביצע את שניהם, אך הוסיף רגרסיה
לא מבוקשת — מחיקת `<span>גרסת מודל: {model_version}</span>` יחד עם התג
השני של `SUPER-CLIENT` (חזרה מדויקת על תקלה שכבר תועדה בסבב P4S קודם
באותו checkpoint). נתפס ב-`diff` לפני כל עדכון סטטוס; לא הוחלף נכס ולא
עודכן סטטוס עד תיקון. פרומפט תיקון שני החזיר את שורת `{model_version}`
בלבד. אימות סופי מול `diff` נקודתי ומול הבסיס המקורי: `SUPER-CLIENT`=0,
`(P4S)` ב-`<h1>`=0 (`<title>` הבלתי-נוגע כמבוקש עדיין מציגו), `{model_version}`=1,
`פרמטרים`=0, `שדות`=4 — ה-`diff` המצטבר מול הבסיס לפני שני הסבבים מראה
בדיוק שני שינויים: הסרת `(P4S)` מה-`<h1>` והסרת שני תגי `SUPER-CLIENT`.
`p4s.jpg` הוחלף (רזולוציה מקורית, 1451×1775).
**checkpoint 8: `done`. checkpoint 9: `done`.** ⛔ אין push/PR/merge.
הצעד הבא: checkpoint 10 (בדיקה חזותית ידנית) — טרם התחיל, ⛔ לא יתחיל
בלי הוראת משתמש נפרדת.

**עדכון — 13.09.2026, checkpoint 10 (בדיקה חזותית ידנית):** בוצע באישור
מפורש. צ'קליסט D3ב + קריטריונים 16–18/31–37 על ששת נכסי `docs/design/*.jpg`
ברזולוציה מקורית מול `FunnelIQ_Assignment.html`/`REQUIREMENTS.md`/`IA.md`/
`DESIGN.md`. שמונה ממצאים אותרו (בדיקת ביקורת עצמאית נוספת חשפה שלושה
מהם שהסבב הראשון פספס — שדות חסרים בטופס המשותף, ספרות קטועות ב-Follow-up,
שבירת `%` ב-Overview) ותוקנו כולם לאורך שלושה סבבי תיקון Stitch, כולל
side-effect לא מבוקש אחד שנתפס ותוקן בנפרד (לוגו כפול בסימולטור התקציב).
כל תיקון אומת מול HTML/`grep`/דגימת פיקסל בפועל, לא צילום בלבד. ששת
נכסי `docs/design/*.jpg` הוחלפו לגרסתם הסופית. `pytest
tests/test_roadmap_render.py tests/test_requirements_traceability.py` —
14/14. **checkpoint 10: `done`.** ⛔ אין push/PR/merge. הצעד הבא:
checkpoint 11 (החלפת המוקאפ ב-ROADMAP בנכסים הקנוניים) — טרם התחיל,
⛔ לא יתחיל בלי הוראת משתמש נפרדת.

**תיקון — 13.09.2026, ההצהרה למעלה הייתה מוקדמת מדי:** ביקורת עצמאית
נוספת, ישירות מול ה-HTML (לא רק מול הצילום), מצאה ששלושה ממצאים לא
תוקנו בפועל: (1) toggle ריק עדיין גלוי ב-Overview (`<button>` ריק תחת
"Filter Controls") וב-P4S (רכיב מקביל, טרם אותר במדויק); (2) מיתוג לא
אחיד — Overview/טופס משותף/P4S נושאים אייקון `filter_alt` ליד הלוגו,
Budget Simulator/Follow-up לא; (3) "evidence: high (n=249)"/"evidence:
medium (n=57)" עדיין באנגלית מתחת לעמודות התרשים בסימולטור — תוויות
נפרדות מתגי הטבלה שכן תורגמו. **checkpoint 10 חוזר ל-`in_progress`.**
⛔ אין Stitch עד הוראה נפרדת, אין push/PR/merge.

**עדכון — 13.09.2026, checkpoint 10 נסגר:** ארבעה פרומפטים אטומיים
תיקנו את שלושת הממצאים שלמעלה — Overview/P4S: הרכיבים הריקים נעלמו
לגמרי; Overview/טופס משותף/P4S: אייקון הלוגו הוסר (`filter_alt`, 0
מופעים בכל שלושתם); סימולטור: "evidence: high/medium" הוחלף ב"רמת
ראיות: גבוהה"/"בינונית". כל תיקון אומת ב-HTML בפועל אחרי הפעולה, לא
רק בצילום. ⚠ שתי הצהרות "done" קודמות התבררו כמוקדמות — האימות הזה
נבנה במכוון רחב מהקודמים (תוכן מלא, לא רק חיפוש שם class). ששת נכסי
`docs/design/*.jpg` הוחלפו בפעם הרביעית. `pytest
tests/test_roadmap_render.py tests/test_requirements_traceability.py`
— 14/14. **checkpoint 10: `done`.** ⛔ אין
push/PR/merge. הצעד הבא: checkpoint 11 — טרם התחיל, ⛔ לא יתחיל בלי
הוראת משתמש נפרדת.

**עדכון — 13.09.2026, checkpoint 11 בוצע באישור מפורש:** לשונית "דוגמת
ממשק — איך זה ייראה" ב-`ROADMAP.html` — המוקאפ הידני שקדם לפאזה 8A
(`login-card` מזויף, שלושה `predict-card` ל-P2/P3/P4 בלבד עם נתונים
בדויים, `SVG` יד-מצויר ל-Follow-up, מסך OOD נפרד; ⛔ ללא Overview
וללא P4S) **הוסר לגמרי** — הוחלף בשישה תגי `<img>` לצילומי
`docs/design/*.jpg` הקנוניים, ברזולוציה מקורית. אפס markup חזותי כפול
(D6): שום רכיב מזויף לא נשאר לצד הצילומים. אומת: כל שישה קבצים קיימים
בנתיב היחסי; בדיקת איזון תגיות HTML מלאה — אפס תגית יתומה; `node
--check` על ה-`<script>` — תפס ותיקן שגיאת syntax אחת שגרמתי לה
(מרכאות כפולות לא-בורחות במחרוזת JS); `git diff --check` נקי; `pytest
tests/test_roadmap_render.py tests/test_requirements_traceability.py`
— 14/14. **checkpoint 11: `done`.** ⛔ אין push/PR/merge. הצעד הבא:
checkpoint 12 — טרם התחיל, ⛔ לא יתחיל בלי הוראת משתמש נפרדת.

**עדכון — 13.09.2026, checkpoint 12 הותחל באישור מפורש:** שלב 1/6
בלבד (ביקורת קוד + סריקת סודות + בדיקות רלוונטיות) — ר' §טו לפירוט
המלא. `git diff --stat` מול `a3d5674`: 3 קבצים בלבד, אפס שינוי ב-`app/`,
`tests/`, `REQUIREMENTS.md`, `IA.md`. סריקת סודות (דפוסי מפתח/טוקן/
סיסמה/JWT) — **0 סודות שמישים** (דפוסי החיפוש עצמם מופיעים ביומן הזה
ואינם סוד); `login.jpg` נבדק — שדות ריקים. `pytest
tests/test_design_tokens.py tests/test_roadmap_render.py
tests/test_requirements_traceability.py` — 23/23. ⛔ אין מודלים/Holdout.
לא נמצא ממצא הדורש תיקון.

**עדכון — 13.09.2026, שלב 2/6 (commit) בוצע באישור נפרד:** commit
על `feat/design`, 9 קבצים (3 שונו + 6 `docs/design/*.jpg` חדשים).
⚠ **תוקן:** הדיווח המקורי טען "עץ עבודה נקי" מיד אחרי ה-commit, אך
עדכון התיעוד לשלב 2/6 עצמו בוצע **אחרי** ה-commit ונשאר לא-מקומט —
נתפס באימות עצמאי. תוקן ב-`git commit --amend` (מכניס גם את תיעוד שלב 2/6 לאותו commit;
ה-SHA משתנה ב-`amend`, לכן **אינו מוקלד באף מקום בתיעוד** — כל הקלדה
כזו הייתה נפסלת מיד ע"י ה-`amend` הבא; נבדק תמיד ב-`git log` בפועל).
⛔ אין push/PR/CI/merge. **checkpoint 12: `in_progress`, שלב 2/6.**
⛔ אין commit נוסף ואין push/PR/CI/merge; השלב הבא הוא push באישור נפרד.

**עדכון — 13.09.2026, שלב 3/6 (push) בוצע באישור נפרד** ("מאושר",
בהקשר ישיר לאמירתי הקודמת שהשלב הבא הוא push באישור נפרד): `git push
origin feat/design` — fast-forward `a3d5674..3cf7d0a`, ⛔ ללא force.
`origin/feat/design` אומת מול `3cf7d0a`, זהה ל-`HEAD` המקומי; `git
status --short --branch` — אין `ahead`/`behind`. ה-SHA `3cf7d0a` יציב
מעתה (push אינו יוצר commit חדש) ומתועד כאן במפורש, שלא כמו ה-SHA
הזמני שקדם ל-amend. **checkpoint 12: `in_progress`, שלב 3/6.** ⛔ אין
PR, אין CI, אין merge — כל אחד משלושת השלבים הנותרים דורש אישור נפרד
ומפורש.

**עדכון — שלב 4/6 (PR) בוצע באישור נפרד ב-13.09.2026, תוקן ב-14.09.2026,
ושלב 5/6 (אימות CI) בוצע ב-14.09.2026 — כל שלב באישור נפרד:** PR #26
(`feat/design`→`main`) נפתח ב-13.09.2026, ותוקן פעמיים ב-14.09.2026
באישור נפרד — הוסר קישור/אזכור Claude Session; הכותרת/Summary תוקנו להבהיר
שה-PR מצטבר וכולל גם Phase 10A (commits `1c70ca8`/`4191805`/`17f1873`,
נוגעים ב-`app/insights.py`/`app/supabase_client.py`/`app/features.py`/
`app/static/business_facts.json`/`scripts/train.py`/
`scripts/business_facts.py`/`tests/test_business_facts.py`) ולא רק
Phase 10 design-only — רק commit הסגירה האחרון (`3cf7d0a`) הוא
design/docs-only בפועל; תיאור ה-CI תוקן לניסוח עובדתי (רץ אוטומטית
עם ה-push/PR, לא "טרם הופעל"). לאחר מכן, שלב 5/6: `gh pr view 26`
אומת `headRefOid`=`70bfec398cdddbfe8c1775a47f4fea21fd10672a` — זהה
ל-`HEAD` המקומי; שתי ריצות CI נבדקו **בנפרד**, לא רק דרך הרולאפ
המצטבר — `gh run view 34782288802` (אירוע `push`) ו-`gh run view
34782430525` (אירוע `pull_request`), שתיהן `headSha`=`70bfec3` ו-
`conclusion=success`; `mergeable=MERGEABLE`, `mergeStateStatus=CLEAN`,
`state=OPEN`. `git status --short --branch` — עץ עבודה נקי, אין
`ahead`/`behind`. **checkpoint 12: `in_progress`, שלב 5/6.** ⛔ אין
merge — השלב האחרון דורש אישור נפרד ומפורש.

**עדכון סופי — 14.09.2026, שלב 6/6 (merge) בוצע, checkpoint 12 ופאזה
10 נסגרים:** לפני המיזוג נדרש סבב מנהלי נוסף לסנכרון תיעוד שלבים 4-5
עצמם (לא צורף בשקט לאישור המיזוג, כהוראת המשתמש) — commit `3cf03fd`
(2 קבצים, 37 insertions(+)/5 deletions(-)) ו-push נפרד, שהפעילו CI
חדש. שלב 5/6 אומת פעם שנייה: `headRefOid`=`3cf03fd`; ריצה ראשונה
נתפסה `IN_PROGRESS`/`mergeStateStatus=UNSTABLE` ולא דווחה כהצלחה עד
השלמה בפועל (פולינג עם השהיות קצרות); שתי הריצות הסופיות (`push`/
`pull_request`) אומתו כל אחת בנפרד מול `headSha`=`3cf03fd` — שתיהן
`success`; `mergeable=MERGEABLE`, `mergeStateStatus=CLEAN`. מצב טרי
אומת שוב מיד לפני המיזוג עצמו (ללא שינוי מאז האישור). **שלב 6/6 בוצע
באישור מפורש ונפרד:** `gh pr merge 26 --merge` — PR #26 `state=MERGED`,
`mergeCommit=ef2eb83`, `mergedAt=2026-09-13T21:14:54Z`; `main` המקומי
עודכן ב-fast-forward, אומת `origin/main`==`HEAD`==`ef2eb83`, עץ עבודה
נקי. **checkpoint 12: `done`. פאזה 10 נסגרה 14.09.2026** בהוראה
מפורשת של המשתמש לעדכון תיעוד זה — `execution_status: in_progress` →
`done`; `planning_status` ללא שינוי (`approved_for_execution`). פאזה
10 עצמה לא שינתה לוגיקת שירות/API או חיווט runtime; היא כללה מסמכי
ונכסי עיצוב, `app/static/tokens.css` שלא חווט לממשק, ו-
`tests/test_design_tokens.py`. PR #26 המצטבר כלל בנוסף שינויי Phase
10A ב-`app/`, `scripts/`, `tests/` ו-`models/`, ולכן המיזוג עשוי
להפעיל auto-deploy. ה-auto-deploy לא נבדק ואינו ראיית סגירה לפאזה 10.
פאזה 11 לא נפתחה.

---

## טו. יומן ביצוע Codex והעברה ל-Claude

היומן מתעד רק פעולות שבוצעו בפועל. כל רשומה כוללת שינוי, ראיה ומצב Git;
ממצא שטרם תוקן מסומן במפורש ואינו מוצג כתוצר סגור.

| תאריך | checkpoint | פעולה וממצא | אימות וראיה | מצב Git |
|---|---:|---|---|---|
| 11.09.2026 | 7 | בפרויקט FunnelIQ ב-Stitch קיימים ששת מסכי היעד. המשתמש הסיר ידנית את הכפילויות | ספירה חזותית חוזרת בפרויקט: Login, Overview, טופס P2/P3/P4, P4S, Follow-up, Budget Simulator | תוצר חיצוני; אין ייבוא markup ל-repo |
| 11.09.2026 | 5–6 | היקף התצוגה ננעל ל-Desktop בלבד בחמשת מקורות התכנון הפעילים | `tests/test_design_tokens.py`: 9 passed; תחביר `ROADMAP.html` תקין; `git diff --check` נקי | commit מקומי `7081c87`, לא נדחף |
| 11.09.2026 | 7–8 | `ROADMAP.html` סונכרן: 7=`done`, 8=`in_progress` | ספירת ההתקדמות: 7 הושלמו, 1 בביצוע, 4 טרם התחילו; 58% | שינוי מקומי שייכלל עם סגירת checkpoint 8 |
| 11.09.2026 | 8 | ביקורת ראשונית קבעה שהכיוון החזותי מתאים, אך נדרשים תיקוני חוזה ותוכן לפני ייצוא: חמש שאלות המייסדת, שדות הקלט, משמעות התוצאות, נתוני הדוגמה, fallback טבלאי וטוקנים קנוניים | נבדק מול `FunnelIQ_Assignment.html`, `REQUIREMENTS.md`, `SPEC.md`, `IA.md`, `DESIGN.md`, `app/schemas.py` והנכסים השמורים | `in_progress`; טרם אושר ל-checkpoint 9 |
| 11.09.2026 | 8 | ניסיון להמשיך לעריכת Stitch נעצר משום שחיבור הדפדפן חשף את לשונית ROADMAP בלבד | מלאי הלשוניות המחוברות נבדק; לשונית פרויקט Stitch לא הייתה זמינה לשליטה | לא בוצע שינוי עיוור או חלקי ב-Stitch |
| 11.09.2026 | כלי עבודה | התוסף `Local REST API with MCP` גרסה 5.1.0 כבר היה מותקן ב-Vault. משתנה הסוד המקומי קיבל את השם התקני `OBSIDIAN_API_KEY`; Codex הוגדר ל-`http://127.0.0.1:27123/mcp/` עם Bearer token ממשתנה הסביבה | אומת בלי להציג את הסוד: הערך שב-`.env` זהה למפתח התוסף; REST root=`200`; קריאת Vault מאומתת=`200`; MCP initialize=`200` ומכיל `serverInfo` | ההתקנה והחיבור תקינים; אין מפתח ב-commit או ב-`config.toml`; `.env` מוחרג מ-Git. נדרש סשן Codex חדש לטעינת כלי השרת |
| 11.09.2026 | 8 | לשונית פרויקט FunnelIQ ב-Stitch נעשתה זמינה. נשלח סבב תיקון ראשון in-place לששת המסכים: שדות החוזה של הטופס המשותף ושל P4S, חמש שאלות המייסדת, נתוני Overview ו-Follow-up, fallback טבלאי וסמנטיקת צבע. Stitch דיווח שששת המסכים עודכנו ללא שכפול | בדיקת עץ הנגישות לאחר הרינדור אימתה את 13 שדות `FunnelInput`, ארבעת שדות `EarlyFunnelInput`, שאלות ה-Overview, שיעורי 4.5%/8.2%/5.4%, שיעורי הנשירה 21.7%/25.7%/18.6%/10.4%/29.2% ושני fallback-ים ב-Follow-up | תוצר חיצוני; checkpoint נשאר `in_progress` משום שביקורת חוזרת מצאה סטיות נוספות |
| 11.09.2026 | 8 | ביקורת חוזרת מצאה ערכי prefill שסותרים `closed + not_closed = followup_5`, יחידת CAC שגויה, המלצות P2/P3/P4 שאינן נגזרות מ-D9, תגי ראיות שאינם אפשריים בחוזה, ונתוני P6 מומצאים במקום `P6_simulation.json`. נוסח תיקון שני הוכן; לאחר שהממשק שלח אותו בעת ניקוי השדה הוא בוטל מיד, בהתאם להנחיית המשתמש שמעתה הוא מזין פרומפטים ל-Stitch ידנית | ההשוואה בוצעה מול `docs/DESIGN.md` §6–§7, `app/schemas.py`, `models/P6_simulation.json`, `models/metrics.json` ו-`docs/findings.json`; מצב המסכים לאחר הביטול נבדק ונשאר בתוצר הסבב הראשון | אין שינוי חיצוני מהסבב השני; ממתין להזנה ידנית ולאימות חוזר |
| 11.09.2026 | 8 | המשתמש הזין ידנית את סבב התיקון השני. אומתו תיקון `closed=8`/`not_closed=18`, יחידת CAC, שכבות D9 של P2/P3/P4/P4S, הסרת תגי ראיות בלתי אפשריים, וארבע אסטרטגיות P6 עם הערכים והטווחים מהארטיפקט | עץ הנגישות הושווה מחדש ל-`schemas.py`, ל-`P6_simulation.json` ולמטריצת D9. נותרו פערים: Overview עדיין כולל שדות וערכים שאינם ב-`TierRow`; Follow-up עדיין נושא `N=2,620`, התפלגות מומצאת והמלצה ישנה; פלטת Stitch עדיין מציגה `#346EF6`/Public Sans במקום ה-tokens הקנוניים; חסרים fallback-ים מפורשים בשני גרפי Follow-up | `in_progress`; נדרש פרומפט ידני שלישי ואימות חוזר לפני checkpoint 9 |
| 11.09.2026 | 8 | המשתמש הזין ידנית פרומפט ממוקד לתיקון תרשימי Follow-up. אומתו ערכי הנשירה, התפלגות `calls_to_closed`, הסרת `N=2,620` והוספת שתי טבלאות נגישות | בדיקת עץ הנגישות והשוואה ל-`docs/findings.json`: כל 14 הערכים תואמים. נמצאו תוספות לא נתמכות: "טווח אופטימלי: 4-5 שיחות", שמות מומצאים לשלבי המעקב, וטענות ישנות בפסקאות ההסבר וההמלצה | `in_progress`; נדרש סבב ממוקד לתוכן ולהמלצה במסך Follow-up |
| 11.09.2026 | 8 | אומת סבב כיתוב ממוקד במסך Follow-up: שמות השלבים פושטו ל`מעקב 1`–`מעקב 5`; הוסרו `נתוני זמן אמת`, טווח 30 ימים, baseline של 100% וטווח אופטימלי מומצא; נוספה הבחנה מפורשת בין חמשת המעקבים לבין `calls_to_closed` בטווח 1–9 | עץ הנגישות אימת שכל 14 ערכי התרשימים והטבלאות נשמרו. טקסט ההסבר וההמלצה הישנים עדיין כוללים שמות שלבים וטענות לא נתמכות, בהתאם להוראה לא לשנותם בסבב זה | `in_progress`; הסבב הבא יוגבל לשתי תובנות D9 ולהמלצה המשותפת |
| 11.09.2026 | 8 | אומת סבב D9 במסך Follow-up: כותרות הקבוצות הופרדו בין חמש נקודות המעקב לבין ממוצע שיחות ברמת רשומה; שתי התובנות הוחלפו בארבע שכבות; ההמלצה המשותפת תוקנה; הטענות על 73%, שיחות 3–6, צוואר בקבוק, CAC, ניסוי 50%/48 שעות ו-v2.4 הוסרו | עץ הנגישות אימת את תוכן D9 ואת שמירת כל ערכי התרשימים והטבלאות. נותר ליטוש מעטפת: פתיח עם תהליך מכירה שלא הוגדר, כפתור פעולה שאינו בחוזה, טענת SSL, וניסוח יחיד/רבים בטבלה | `in_progress`; נדרש להכריע אם לבצע ליטוש Follow-up קצר לפני מעבר ל-Overview |
| 11.09.2026 | 8 | הושלם ליטוש מסך Follow-up: הפתיח הוחלף בניסוח המתאר רק את הנתונים; כפתור פעולה שאינו בחוזה הוסר; טענת SSL לא מבוססת הוסרה; לשון היחיד תוקנה ל`1 שיחה` ו`רשומה אחת` | עץ הנגישות אימת את ארבעת השינויים ואת שמירת התרשימים, 14 הערכים, שתי טבלאות ה-fallback, שתי תובנות D9 וההמלצה | מסך Follow-up תקין תוכניתית בתוך checkpoint 8; ה-checkpoint כולו נשאר `in_progress` עד תיקון יתר המסכים והפלטה |
| 11.09.2026 | 8 | סבב טבלת Overview הצליח בטבלה: ארבע עמודות קנוניות, התווית `רמת הוצאת פרסום חודשית`, והערכים 780/1,717/1,003 עם 4.5%/8.2%/5.4%; עמודות CPA/מגמה/ROI הוסרו | בבדיקת עץ הנגישות נמצאה רגרסיה מחוץ להיקף הפרומפט: חמש שאלות המייסדת הוחלפו בתוכן מומצא, והגרף חזר לערכים 3.2%/7.8%/14.5% ולתוויות Tier ישנות. גם תוויות זמן חיות נשארו בכותרת | כשל חלקי; הטבלה מתקבלת, Overview נשאר `in_progress` ודורש שחזור ממוקד של הכרטיסים והגרף |
| 11.09.2026 | 8 | אזור הכרטיסים ב-Overview שוחזר ונוסח כממשק מוצר גנרי: `ניתוחים ותחזיות`, חמישה כרטיסי יכולת ללא שאלות מייסדת, קודי Q, נתונים מומצאים או חותמות זמן; תווית החשבון הוחלפה ל`משתמשת מחוברת` | עץ הנגישות אימת את חמש הכותרות, התיאורים והקישורים. טבלת רמות ההוצאה נשמרה עם הערכים הקנוניים. הגרף נשאר ללא שינוי בהתאם לפרומפט ולכן עדיין מציג 3.2%/7.8%/14.5% ותוויות Tier ישנות | `in_progress`; הסבב הבא יוגבל לגרף Overview ולתובנת D9 הצמודה אליו |
| 11.09.2026 | 8 | גרף ההמרה ב-Overview תוקן: כותרת גנרית לפי רמת הוצאת פרסום חודשית, ערכים 4.5%/8.2%/5.4%, ציר מתחיל ב-0%, fallback טבלאי נגיש ותובנת D9 בת ארבע שכבות; תוויות Tier, הטווחים הישנים והערכים 3.2%/7.8%/14.5% הוסרו | עץ הנגישות אימת את שלושת הערכים ואת תוכן התובנה; חמשת כרטיסי היכולת והטבלה הראשית נשמרו ללא רגרסיה | `in_progress`; נותר ליטוש מעטפת Overview והחלת כלל הניסוח הגנרי במפרט וביתר המסכים |
| 11.09.2026 | 8 | הושלם ליטוש מעטפת Overview: הוסרו תג `סביבה חיה`, בקר תאריכים וטענת SSL; כפתור הרענון נשמר כ`רענון נתונים`; ה-CTA הוחלף ב`הפקת תחזיות` עם קישור יחיד לטופס סוף הקמפיין | עץ הנגישות אימת שהכרטיסים, הטבלה הקנונית, הגרף 4.5%/8.2%/5.4%, fallback ותובנת D9 נשמרו ללא רגרסיה | תוכן Overview הושלם; checkpoint 8 נשאר `in_progress` לבדיקת יתר המסכים |
| 12.09.2026 | 8/10A-CP7 | הבהרת המקור הקבוע ביטלה את ההכרעה להשאיר `רענון נתונים`: ה־CSV לעולם אינו מתעדכן ולכן הכפתור מטעה וחסר פעולה עסקית | `SPEC.md`, ‏`IA.md` ו־`DESIGN.md` נועלים מוצר אינטראקטיבי על snapshot קפוא; פעולת התאוששות מכשל תיקרא `ניסיון חוזר` בלבד | שורת 11.09 נשמרת כהיסטוריה; בהמשך checkpoint 8 יש להסיר את הכפתור מ־Overview בפרומפט Stitch קצר לאחר סגירת 10A |
| 13.09.2026 | 8 | לאחר סגירת 10A, checkpoint 8 חזר לתור. מסך "טופס תחזיות משותף": כותרת שדות תוקנה (12 שדות + ערך מחושב, לא 13), תוויות/יחידות שגויות תוקנו ב-4 שדות (`ad_budget`, `calls_to_closed`, `calls_to_not_closed`, `customer_acquisition_cost`), `input-summary` קיבל בחזרה את הערך המחושב (`not_closed`), נוסף `input-details` מתקפל סביב כל 13 ערכי החוזה, "איפוס שדות" הוחלף ב-"נקה טופס" (`clear-form-action` הוכלל לשני הטפסים), footer ריק (ללא תוכן) הוסר, הסתייגות P4 הוחלפה בנוסח המלא מהספק, `onfocus="this.select()"` הורחב מ-P4S ל-12 שדות הטופס | כל שינוי אומת בנפרד מול ה-HTML המיוצא בפועל (הורדה + `diff`), לא מול עץ נגישות בלבד; שתי תופעות לוואי זעירות (`class=""` על `<p>` ריק) תועדו ולא תוקנו, ללא השפעה חזותית | תוצר Stitch חיצוני; שינויי `DESIGN.md`/`IA.md` הנלווים (`clear-form-action`, `field-input`, מונה הדור) נדחפו ב-`a3d5674` ל-`origin/feat/design` |
| 13.09.2026 | 8 | מסך "סימולטור תקציב": footer ריק הוסר; נוסף וטו מפורש על `100x500` (בדיקת עבר: פי 8.59 מהתוצאה בפועל), בנפרד מכלל החפיפה; שלושה placeholders של `{model_version}` מוזגו ל-`<details>` "פרטי מודל" יחיד (שיטת אינטרוול, `B=1,000`, אלגוריתם, גרסה, מדדי Holdout — כל הנתונים שמקורם בתגובת ה-API); הנחות אדיטיביות/אי-סיבתיות/טווח 500–20,000 נשארו גלויות מחוץ ל-`<details>`; קנה המידה של גרף הדירוג תוקן לפרופורציונלי אמיתי (בלי "רצפת נראות" מלאכותית לעמודה הקטנה), עם whiskers (טווח חזותי) על כל ארבע העמודות; "מייסדת" הוסרה מכותרת | אותה שיטת אימות; אחוזי רוחב/מיקום ה-whisker חושבו מראש מהערכים המדווחים בטבלה (לא הושארו לחישוב חופשי) | תוצר Stitch חיצוני |
| 13.09.2026 | 8 | מסך "Follow-up": כל התפלגות `calls_to_closed` (גרף + טבלת fallback + טקסט) הייתה בנויה על האוכלוסייה הישנה `purchased=1` (סכום 3,163, ‏1,519/48%) שתיקון CP9 החליף — עודכנה לאוכלוסייה הנכונה `closed>0` (סכום 3,318, ‏1,595 רשומות/48.07%, חציון 3, שכיח 2, ממוצע 3.706); בלוק ה"המלצה האופרטיבית" הוחלף מתמצית לציטוט `SPEC.md` §הכרעת CP4-D המלא, מילה במילה; footer ריק הוסר | `grep` שלילי אימת היעדר מוחלט של `3,163`/`1,519` בקובץ המיוצא; ציטוט CP4-D הושווה תו-אחר-תו לנוסח הנעול (שגיאת מיקום מילה אחת — "בשרשרת" — תוקנה בסבב נפרד) | תוצר Stitch חיצוני; איחוד משפט הפעולה של שתי שורות §6.1 (`DESIGN.md`) נדחף ב-`a3d5674` |
| 13.09.2026 | 8 | מסך P4S (המשך על גבי סגירה קודמת): אותרו והוסרו דליפות ניסוח פנימי לטקסט עסקי גלוי — קוד המסך "P4S" מכותרת/כפתור-שליחה/כותרת-תוצאה/שני תגים, ו-"פרמטרים" הוחלף ב-"שדות" בשתי כותרות ובהפניה לטופס המשותף; "Super-Client" הוסר משתי כותרות (תגי "SUPER-CLIENT" נפרדים נשארו, לא הוכרעו); `leads_answered`: `min="1"/max="15"` (ללא מקור בחוזה) תוקן ל-`min="0"` בלי `max` | כל שינוי אומת בנפרד; תקלה אחת בדרך (מחיקה בטעות של "גרסת מודל: {model_version}" בסבב ניקוי הטקסט) זוהתה ותוקנה חזרה באותו סבב | תוצר Stitch חיצוני; `{model_version}`/שמות טכניים אומתו כלא-נפגעים |
| 13.09.2026 | 8 | מסך P4S נסגר בסבב הזה. פערים שנותרו פתוחים ב-checkpoint 8: Overview ו-Login טרם נסקרו מחדש מול הספק המעודכן; גישה ד׳ בטופס המשותף בלבד — `tooltip` מועד מדידה, אם יימצא חסר (`select-on-focus` כבר בוצע ואומת בשני הטפסים) | — | checkpoint 8 נשאר `in_progress`, ממוקד כעת ב-Overview/Login; אין push/PR/merge נוסף ואין מעבר ל-checkpoint 9 |
| 13.09.2026 | 8 | Overview נבדק מחדש מול `DESIGN.md`/`IA.md` הנוכחיים: footer עם קישורים אמיתיים (תנאי שימוש/פרטיות/אבטחה/צור קשר), אין דליפות ניסוח פנימי ("מייסדת"/"פרמטרים"/"P4S"/`{model_version}`), טבלת רמות ההוצאה וכרטיסי היכולת תואמים לספק. פריט תלוי יחיד אותר: כפתור "רענון נתונים" שהוראת CP7 מ-12.09.2026 קבעה שיש להסירו לאחר סגירת 10A (ה-CSV קפוא לצמיתות, "רענון" מטעה). הוסר בפרומפט ידני יחיד | הורדת ה-HTML המיוצא לפני/אחרי ו-`diff`: שתי שורות בלבד השתנו (אייקון `refresh` וטקסט הכפתור, שניהם נמחקו יחד, בלי תחליף); שני שינויי קידוד זניחים ונטולי-השפעה (`'FILL' 1` → `&quot;FILL&quot; 1`, ערך CSS זהה) תועדו ולא תוקנו | תוצר Stitch חיצוני; מסך Overview נסגר בתוך checkpoint 8 |
| 13.09.2026 | 8 | מסך "התחברות" נבדק מול `app/static/index.html` (המקור המחייב לפי D4: "מבנה, תוכן ולוגיקה ללא שינוי" — לא מול מוקאפ). אותרו 9 פערים: תווית "כתובת אימייל" (קנוני: "אימייל"), כפתור "התחבר למערכת" (קנוני: "התחברות"), `autocomplete` חסר בשני השדות, `placeholder` מרמז בשניהם ("name@company.com"/"••••••••", כשהקנוני ריק לגמרי), קישור "שכחת סיסמה?", checkbox "זכור אותי", קישור "צור קשר עם מנהל המערכת", ו-footer עם זכויות יוצרים וטענת "חיבור מאובטח בתקן SSL" (אף אחד מאלה אינו קיים במקור) | כל 9 הפערים תוקנו בשלושה סבבים ואומתו ב-`diff` מול ה-HTML המיוצא לפני/אחרי כל סבב; סבב הביניים השאיר אייקון מנעול יתום אחרי הסרת טענת ה-SSL, זוהה ותוקן באותו סבב. ⛔ **לא נבדק ואינו מבוקש שירות: Auth/session אינם פועלים במוקאפ הסטטי** — הבדיקה כולה חזותית/מבנית מול השלד הקנוני בלבד | תוצר Stitch חיצוני; מסך "התחברות" נסגר בתוך checkpoint 8 |
| 13.09.2026 | 8 | סגירת checkpoint 8 — שלוש ראיות אחרונות: (1) הצהרת מועד המדידה "כל הנתונים נמדדים בתום מחזור הקמפיין" מופיעה פעם אחת, מודגשת, מיד מתחת לכותרת אזור הקלט של כל 13 הערכים בטופס המשותף; `DESIGN.md` §7.1 קובע שמועד המדידה זהה ומילולית "תום מחזור הקמפיין" לכל 13 השורות ללא יוצא מן הכלל — הצהרה אחת ל-13 ערכים בעלי מועד זהה ממצה את D11, ואינה gap. (2) סדר ה-Tab בטופס המשותף: `tabindex="-1"` מופיע פעם אחת בלבד, על `not_closed` בלבד; שאר 12 השדות בסדר DOM טבעי התואם לרשת החזותית — **בדיקת מוקאפ סטטי בלבד, אינה אישור נגישות מקלדת** (פוקוס/מלכודות/קוראי מסך נדחים לפאזות 11–12). (3) ביקורת Login תועדה בשורה שלמעלה | שלוש הראיות הוצגו ואומתו לפני עדכון הסטטוס | checkpoint 8 עובר ל-`done`; checkpoint 9 (טרם התחיל) הבא בתור |
| 13.09.2026 | 9 | checkpoint 9 בוצע לראשונה: שישה נכסי JPEG יוצאו מ-Stitch ל-`docs/design/` (thumbnails, 512px), ומטריצת כיסוי נכתבה ב-`DESIGN.md` §9 — מצבי runtime ו-`prefill` של P4S מופו לטבלאות §2/`IA.md` | `pytest tests/test_design_tokens.py` — 9/9 | checkpoint 9 עובר ל-`done` |
| 13.09.2026 | 8–9 | **ביקורת נוספת פתחה מחדש checkpoints 8–9, ⛔ ללא נגיעה ב-Stitch.** ארבעה ממצאים: (1) שורת `empty` ב-`DESIGN.md` §9.2 הייתה הפוכה מול §2.1 — תוכן `empty` אמיתי קיים בטופס המשותף וב-P4S, ⛔ לא ב-Login; תוקן. (2) קריטריון 30 ותיאור checkpoint 9 כינו הפניה לטבלת טקסט "נכס חזותי" — קריטריון 30 (§ט) ותיאור checkpoint 9 (§ח) נוסחו מחדש: ששת צילומי המסלול התקין מכוסים חזותית, שבעת מצבי ה-runtime ומצב ה-`prefill` של P4S נדחים לפאזה 11 ואינם מכוסים חזותית בפאזה 10. (3) הצילומים היו thumbnails 512px; אותרה תוספת `=s0` על כתובת ה-`screenshot.downloadUrl` (`lh3.googleusercontent.com`) שמחזירה רזולוציה מקורית (1379–1454px רוחב) — ששת הנכסים יוצאו מחדש. (4) **בדיקת `list_screens` על הפרויקט החי (read-only) מול `p4s.jpg` גילתה שהמסך עדיין נושא בפועל את הכותרת `FunnelIQ - ציון לקוח-על לרוכש ידוע (P4S)` ושני מופעי `SUPER-CLIENT`** — הטענה בשורת checkpoint 8 מ-13.09.2026 ("P4S/Super-Client/... הוסרו") שגויה מלכתחילה, לא תוצאה של ייצוא מיושן: ה-HTML שנשמר באותו סבב עצמו (`p4s_check`/`p4s_check2`, זהים ביט-לביט) כבר נשא את אותן מחרוזות | ההשוואה בוצעה מול `p4s_check2.html`/`_text.txt` (השמורים מסבב הבדיקה המקורי של checkpoint 8, לא הורדו מחדש) ומול תוכן חי טרי של `list_screens`+`=s0` screenshot; `pytest tests/test_design_tokens.py` — 9/9 אחרי תיקון §9.2. ממצא נלווה שלא תוקן: מסך Login מציג `IQ Funnel` (סדר הפוך ל-`FunnelIQ` הקנוני) — נרשם כמועמד ל-checkpoint 10 | checkpoint 8 חוזר ל-`in_progress` (שורת P4S בלבד; חמשת המסכים האחרים אינם מושפעים); checkpoint 9 חוזר ל-`in_progress` (מטריצה+נכסים תוקנו; ממתין לתיקון P4S ב-Stitch לפני סגירה חוזרת); ⛔ אין push/PR/merge |
| 13.09.2026 | 8–9 | **תיקון P4S ידני, שני סבבים, וסגירה סופית.** פרומפט ראשון: הסרת `(P4S)` מה-`<h1>` והסרת שני תגי `SUPER-CLIENT`. בוצע נכון, אך עם רגרסיה לא מבוקשת — מחיקת `<span>גרסת מודל: {model_version}</span>` יחד עם התג השני של `SUPER-CLIENT` (זהה לתקלה שכבר תועדה בסבב P4S קודם באותו checkpoint 8). נתפס ב-`diff` לפני עדכון סטטוס או החלפת נכס. פרומפט תיקון שני החזיר רק את שורת `{model_version}` | אימות סופי, `diff` מלא מול הבסיס לפני שני הסבבים (לא רק מול הסבב הקודם): שני שינויים בדיוק — הסרת `(P4S)` מה-`<h1>`, הסרת שני תגי `SUPER-CLIENT`; ספירות: `SUPER-CLIENT`=0, `(P4S)` ב-`<h1>`=0 (`<title>` הבלתי-נוגע כמבוקש עדיין מציגו), `{model_version}`=1, `פרמטרים`=0, `שדות`=4. `p4s.jpg` הוחלף (1451×1775, רזולוציה מקורית) | **checkpoint 8: `done`. checkpoint 9: `done`.** ⛔ אין push/PR/merge. הבא בתור: checkpoint 10 (טרם התחיל, לא יתחיל בלי הוראת משתמש נפרדת) |
| 13.09.2026 | 10 | **checkpoint 10 הותחל באישור מפורש.** בדיקה חזותית ידנית (D3ב + קריטריונים 16–18/31–37) על ששת נכסי `docs/design/*.jpg` ברזולוציה מקורית, מול `FunnelIQ_Assignment.html`/`REQUIREMENTS.md`/`IA.md`/`DESIGN.md`. דגימת פיקסלים בפועל (לא הערכה ויזואלית בלבד) על תגי רמת-ראיות בסימולטור התקציב | פירוט מלא בטבלת §ח שורה 10. תמצית חמשת הממצאים המהותיים: (1) תגי `evidence-high`/`medium` בסימולטור התקציב צבועים ירוק/ענבר בפועל (נמדד RGB), סותר במישרין את תיקון checkpoint 3 (המשפחה הנעולה היא כחול/צפחה). (2) כותרות גרפים באנגלית מלאה במסך Follow-up ("Record Frequency by Calls to Closed", "Drop-off Rate Across 5 Follow-up Stages"). (3) שלוש הצגות לוגו שונות לאותו מותג: Login ("IQ Funnel" הפוך + אייקון משפך), Overview/טופס משותף/P4S ("FunnelIQ" אחיד + אייקון סתום), Budget Simulator/Follow-up ("Funnel"+"IQ" דו-גוני ואייקון עם "IQ" כפול). (4) breadcrumb קיים רק ב-Budget Simulator/Follow-up. (5) פקד toggle ללא תווית בכל חמשת המסכים המחוברים. שלושה ממצאים משניים לא-חוסמים תועדו גם הם | checkpoint 10 `in_progress` — ממצאים תועדו, ⛔ אין תיקון בסבב הזה ואין מעבר ל-checkpoint 11. אין מודלים/Holdout, אין push/PR/merge |
| 13.09.2026 | 10 | **ביקורת עצמאית נוספת (ברזולוציה מלאה) מצאה שלושה ממצאים חוסמים/מהותיים שהביקורת הראשונה פספסה**, ותיקנה שני ניסוחים: (1) בטופס המשותף 9 מתוך 12 שדות עריכה אינם מציגים תיבת קלט/ערך כלל (רק תווית+יחידה), למרות `input-summary` שמצהיר `12/12` — אומת בזום ישיר על הרשת. (2) ב-Follow-up תוויות הגרף קוטעות ספרה: `9`/`35` במקום `91`/`356` שבטבלת ה-fallback הצמודה — אומת בזום. (3) ב-Overview `8.2%` נשבר ל-`8.2`/`%` בשתי שורות בעוד `5.4%` הסמוך נשאר שורה אחת — אומת בזום; **תוקן הניסוח**: "RTL/bidi עבר נקי" → "נכשל נקודתית". נוסף גם: מילות רמת-הראיות עצמן ("high"/"medium") הן אנגלית לא-מתורגמת, לא רק כותרות הגרפים — מוזג לתוך ממצא השפה הקיים. **הכרעות מינימליות אושרו לממצאים הקודמים** (לא בוצעו עדיין ב-Stitch): הסרת שתי כותרות הגרפים האנגליות ב-Follow-up (כותרות עבריות כבר קיימות שם); הסרת ה-breadcrumb משני המסכים; הסרת פקד ה-toggle מכל חמשת המסכים; איחוד הלוגו לתצוגת `FunnelIQ` הקיימת ברוב המסכים (Overview/טופס משותף/P4S), לא לוגו רביעי. **הוכרע שאינם ממצא**: `8.2%` הירוק (D10 — נתון נצפה, לא תחזית, עם תג טקסט נלווה) והכפילות גרף/טבלת-fallback (D4.1 — נדרשת במכוון) | דגימת פיקסל/זום ישירים על שלושת הממצאים החדשים, לא הערכה חזותית כללית; `pytest tests/test_roadmap_render.py tests/test_requirements_traceability.py` — 14/14 | checkpoint 10 נשאר `in_progress` — שמונה ממצאים מתועדים במלואם, כולל הכרעות מינימליות לחמישה מהם; שלושת החדשים (שדות חסרים, ספרות קטועות, שבירת %) עדיין דורשים החלטת תיקון בפני עצמם. ⛔ אין Stitch, אין מעבר ל-checkpoint 11, אין push/PR/merge |
| 13.09.2026 | 10 | **שלושה פרומפטים אטומיים בוצעו (שדות/מספרים → צבעים/שפה → מעטפת), אומתו מול HTML+צילום טרי מכל חמשת המסכים הנוגעים.** שישה מתוך שמונת הממצאים נסגרו: 9 שדות הטופס המשותף מציגים תיבה+ערך; תוויות Follow-up מציגות 91/356; `8.2%` ב-Overview בשורה אחת; תגי רמת-ראיות בסימולטור תורגמו לעברית **וגם** נצבעו מחדש לכחול/צפחה (RGB 57,64,74 בדגימה טרייה, לא ירוק/ענבר); breadcrumb הוסר משני המסכים; toggle הוסר מכל חמשת המסכים (0 מופעים בכל HTML). שני ממצאים נותרו פתוחים **חלקית**: כותרת אנגלית אחת הוסרה מ-Follow-up ("Record Frequency by Calls to Closed") אך השנייה נשארה ("Drop-off Rate Across 5 Follow-up Stages") — נבדק ב-`grep` על ה-HTML החי, לא הונח; לוגו — כפילות ה-"IQ" הוסרה ו-Login תוקן לסדר "FunnelIQ", אך הצבע לא אוחד: כעת שלושה מסכים (Login/Budget Simulator/Follow-up) מציגים גרסה דו-גונית ושלושה (Overview/טופס משותף/P4S) מציגים כחול אחיד — פיצול 3-3 חדש, לא איחוד. ⛔ תיקנתי עצמאית גם באג פורמט Markdown שנוצר בטעות בסבב הקודם: תוכן הממצאים בשורת checkpoint 10 (§ח) נכתב עם ירידות שורה פנימיות שפיצלו את שורת הטבלה לשורות נפרדות — אוחד בחזרה לשורה פיזית אחת | בדיקת HTML/`grep`/דגימת פיקסל על כל חמשת המסכים שנערכו; לא נסמכתי על צילום בלבד. `pytest tests/test_roadmap_render.py tests/test_requirements_traceability.py` — 14/14; `git diff --check` נקי אחרי תיקון הטבלה | checkpoint 10 נשאר `in_progress` — 6/8 ממצאים סגורים ומאומתים, 2 פתוחים חלקית (כותרת אנגלית שנייה ב-Follow-up, איחוד צבע לוגו). לא הוחלפו נכסי `docs/design/` הקנוניים — ממתין לסבב תיקון נוסף לפני זה. ⛔ אין Stitch נוסף כרגע, אין מעבר ל-checkpoint 11, אין push/PR/merge |
| 13.09.2026 | 10 | **פרומפט תיקון אחרון בוצע (Follow-up + Login/Budget Simulator/Follow-up), אומת מול HTML+צילום טרי.** שני הממצאים האחרונים נסגרו: "Drop-off Rate Across 5 Follow-up Stages" הוסרה מ-Follow-up (0 מופעים ב-`grep`, אין אנגלית נותרת בשני הגרפים); לוגו אוחד לכחול אחיד (`<span class="...text-blue-600...">FunnelIQ</span>` יחיד) בשלושת המסכים, כעת זהה לחלוטין ל-Overview/טופס משותף/P4S בכל ששת המסכים. **⚠️ אך נמצא side-effect לא מבוקש בסימולטור התקציב**, לא חלק משמונת הממצאים המקוריים: תווית משנית "מערכת FunnelIQ" (טקסט אפור קטן, נפרד מהלוגו הראשי) הפכה ללוגו מלא שני באותו סגנון — "מערכת" נעלם, הטקסט גדל והתעבה לזהה ללוגו הראשי — כך שכעת שני מופעי "FunnelIQ" מלאים על אותו מסך. נתפס לפני עדכון סטטוס לסגור | בדיקת HTML (`re.finditer` על כל מופעי "Funnel" + הקשר סביבם, לא רק הראשון), לא רק צילום; `Overview`/`טופס משותף`/`P4S` אומתו כלא-נגועים (file id זהה ב-`list_screens`, לא נערכו כלל בסבב הזה) | checkpoint 10 עדיין `in_progress` — 6/8 ממצאים מקוריים סגורים לגמרי, השניים האחרונים (אנגלית, לוגו) נסגרו גם הם, אך side-effect חדש בסימולטור התקציב דורש תיקון נפרד לפני החלפת נכסים קנוניים וסגירת checkpoint 10. ⛔ אין Stitch נוסף, אין מעבר ל-checkpoint 11, אין push/PR/merge |
| 13.09.2026 | 10 | **תיקון ה-side-effect בוצע וסבב checkpoint 10 נסגר.** פרומפט ממוקד לסימולטור התקציב בלבד: השיב את התווית המשנית לגודל/משקל/צבע המקוריים (`text-xs font-normal text-slate-500`) עם המילה "מערכת" בחזרה, בלי לגעת בלוגו הראשי. ששת נכסי `docs/design/*.jpg` הוחלפו לגרסתם הסופית (5 מהם עדיין נשאו את הגרסה המקורית מ-checkpoint 9, לפני כל תיקוני checkpoint 10 — רק `p4s.jpg` הוחלף קודם) | אימות סופי: `<span class="text-xs font-normal text-slate-500">מערכת FunnelIQ</span>` שוחזר בדיוק; הלוגו הראשי (`font-bold text-2xl text-blue-600`) ללא שינוי; אומת ב-HTML (`re.finditer` על כל מופעי "Funnel") ובזום צילום. `Overview`/`טופס משותף`/`P4S`/`Login`/`Follow-up` אומתו שוב כלא-נגועים באותו סבב. `pytest tests/test_roadmap_render.py tests/test_requirements_traceability.py` — 14/14; `git diff --check` נקי | **checkpoint 10: `done`.** שמונה ממצאים מקוריים + side-effect אחד — כולם תוקנו ואומתו; ששת הצילומים הקנוניים עדכניים. ⛔ אין push/PR/merge. הבא בתור: checkpoint 11 (החלפת המוקאפ ב-ROADMAP בנכסים הקנוניים) — טרם התחיל, לא יתחיל בלי הוראת משתמש נפרדת |
| 13.09.2026 | 10 | **הצהרת "done" הייתה מוקדמת — ביקורת עצמאית שלישית, ישירות מול ה-HTML החי (לא מול צילום), מצאה שלושה ממצאים שלא תוקנו בפועל.** (1) toggle: ב-Overview אותר `<button class="...rounded-lg...">` ריק לחלוטין תחת ההערה `<!-- Filter Controls: Date Range & Status Filter -->` — לא נתפס בסבב הקודם כי חיפוש ה-`grep` חיפש `role="switch"`/`toggle`/`peer-checked`, לא רכיב גנרי ריק; ב-P4S אותר ויזואלית רכיב מקביל, טרם אותר קו-קוד מדויק. (2) מיתוג: `<header` בכל חמשת המסכים נבדק ישירות — Overview/טופס משותף/P4S מכילים `<div class="w-9 h-9 rounded-lg bg-primary">` עם גלף `filter_alt` צמוד ללוגו, Budget Simulator/Follow-up לא מכילים רכיב כזה כלל. (3) `grep` על סימולטור התקציב מצא `evidence: high (n=249)` ו-`evidence: medium (n=57)` — תוויות תחת עמודות התרשים השמאלי, נפרדות מתגי הטבלה הימנית שכן תורגמו בסבב קודם | בדיקת HTML ישירה על כל חמישה — לא הסתמכות על תוצאת `grep` קודמת או על צילום; שלוש הבדיקות אישרו את כל שלוש טענות המשתמש במדויק | **checkpoint 10 חוזר ל-`in_progress`.** ⛔ אין Stitch עד הוראה נפרדת לפרומפט תיקון, אין מעבר ל-checkpoint 11, אין push/PR/merge. הערת פאזה 10A ב-ROADMAP.html שנשארה עם "טרם תוקנו" תוקנה למצב המדויק (8 תוקנו, 3 בתיקון) |
| 13.09.2026 | 10 | **ארבעה פרומפטים אטומיים בוצעו (Overview: מחיקת כפתור ריק+אייקון; P4S: מחיקת רכיב ריק+אייקון; טופס משותף: מחיקת אייקון; סימולטור: תרגום תוויות), אומתו ב-HTML+צילום טרי מכל ארבעת המסכים.** Overview: `<button></button>` הריק תחת "Filter Controls" נעלם לגמרי (0 מופעים); אייקון הלוגו (`w-9 h-9 rounded-lg bg-primary`+`filter_alt`) הוסר, נשאר טקסט "FunnelIQ" בלבד. P4S: הרכיב הריק (`<!-- User profile preview -->` שהכיל `<div>` ריק) נעלם; אייקון הלוגו הוסר (`filter_alt` — 0 מופעים). טופס משותף: אייקון הלוגו הוסר (0 מופעים). סימולטור: `grep` מאשר "רמת ראיות: גבוהה"/"רמת ראיות: בינונית" תחת עמודות התרשים במקום "evidence: high/medium"; תגי הטבלה הימנית ("גבוה"/"בינוני", כבר בעברית ובצבע ניטרלי) לא נגעו | דגימת HTML ישירה על כל ארבעת המסכים (לא רק `grep` צר כמו בסבב הקודם — נבדק תוכן מלא של האזור הרלוונטי); `Login`/`Follow-up` אומתו כלא-נגועים (`file id` זהה ב-`list_screens`). ⛔ לא הורץ pytest נוסף (אין שינוי קוד/מבנה) | **checkpoint 10: `done`.** אחד-עשר ממצאים + side-effect אחד, כולם תוקנו ואומתו במלואם לאורך ארבעה סבבי ביקורת עצמאית; ששת נכסי `docs/design/*.jpg` הוחלפו לגרסתם הסופית. ⛔ אין push/PR/merge. הבא בתור: checkpoint 11 — טרם התחיל, לא יתחיל בלי הוראת משתמש נפרדת |
| 13.09.2026 | 11 | **checkpoint 11 בוצע באישור מפורש: לשונית "דוגמת ממשק" ב-`ROADMAP.html` הוחלפה.** המוקאפ הידני שקדם לפאזה 8A — `login-card` מזויף עם שדות מנוטרלים, שלושה `predict-card` ל-P2/P3/P4 בלבד עם נתונים בדויים (`24 חודשים`, `62%`, `71/100`), `SVG` יד-מצויר לגרף Follow-up, מסך OOD נפרד; ⛔ ללא Overview וללא P4S כלל, כי קדם ליצירתם — הוסר לגמרי משורות 590–790 בקובץ, לא רק סומן כהיסטורי כפי שהיה עד כה. הוחלף בשישה תגי `<img>` (login/overview/shared-form/p4s/budget-simulator/followup) שמצביעים על `docs/design/*.jpg`, עם כיתוב עובדתי לכל מסך (במקום המספרים הבדויים הישנים) | ✅ נתיבים: כל שישה קבצי `docs/design/*.jpg` אומתו קיימים בפועל בנתיב היחסי מקובץ ה-HTML. ✅ אפס markup חזותי כפול: `grep` מאשר ש-`login-card`/`predict-card`/`strategy-row`/`funnel-wrap` אינם מופיעים עוד בקובץ. ✅ תחביר: `node --check` על ה-`<script>` המחולץ — נכשל פעם אחת (`SyntaxError: Unexpected identifier` על מרכאות כפולות לא-בורחות שהכנסתי במחרוזת JS חדשה), תוקן מיידית לפני הדיווח, ואז עבר. ✅ מבנה: `HTMLParser` של Python אימת איזון תגיות מלא על הקובץ כולו — אפס תגית פתוחה/יתומה בסיום. ✅ `git diff --check` נקי. ✅ `pytest tests/test_roadmap_render.py tests/test_requirements_traceability.py` — 14/14 | **checkpoint 11: `done`.** ⛔ אין push/PR/merge, אין commit. הבא בתור: checkpoint 12 — טרם התחיל, לא יתחיל בלי הוראת משתמש נפרדת |
| 13.09.2026 | 11 | **ניקוי CSS יתום, לפני סגירה סופית: כל כללי ה-CSS ששייכים בלעדית למוקאפ שהוסר נמחקו.** `.tag`/`.tag.demo`/`.tag.spec`; `.login-card` וחמשת ילדיו (`h3`, `.brandline`, `label`, `input`, `button`); `.login-fine`; `.predict-grid`/`.predict-card`(+`h4`)/`.predict-main`/`.predict-sub`; `.badge-row`/`.badge` וארבעת הוריאנטים (`evidence-high`/`evidence-med`/`calibrated`/`disclaimer`); `.predict-disclaimer`; `.funnel-wrap`/`.funnel-caption`; `table.fallback` ושני כלליו הנוספים; `.strategy-row`/`.strategy-label`(+`b`,`span`)/`.strategy-bar-wrap`/`.strategy-bar`/`.strategy-whisker`(+`::before`/`::after`)/`.strategy-meta`; `.alert-box` ושלושת ילדיו | **נבדק במפורש שלא הוסר selector בשימוש**, לפני המחיקה: `grep` לכל שם class בתבנית `class="...clsname..."` על ה-HTML כולו — כל אחד מהשמות שלמעלה חזר 0. `.mock-note`/`.mock-screen`/`.mock-screen img` הוצאו מהבדיקה כי בשימוש פעיל (ששת כרטיסי הצילום). `.phase-note` **נבדק ונמצא בשימוש פעיל** ב-JS (`noteHtml`, מציג את שדה `note` של כל פאזה) — לא נמחק, למרות שם דומה חזותית ל-`mock-note`/`predict-note`. אחרי המחיקה: `git diff --check` נקי; `node --check` על ה-`<script>` תקין; `pytest tests/test_roadmap_render.py tests/test_requirements_traceability.py` — 14/14 | **checkpoint 11 נשאר `done`, סגור סופית אחרי הניקוי.** ⛔ אין commit, אין push/PR/merge. checkpoint 12 טרם התחיל — לא יתחיל בלי הוראת משתמש נפרדת |
| 13.09.2026 | 12 | **checkpoint 12 הותחל באישור מפורש — שלב 1/6 בלבד (ביקורת קוד + סריקת סודות + בדיקות רלוונטיות).** ביקורת קוד: `git diff --stat` מול `a3d5674` — 3 קבצים (`ROADMAP.html`/`DESIGN.md`/`PHASE10.md`, 297+/284-) + `docs/design/*.jpg` לא-עקוב (6 קבצים, 16–208KB כל אחד); `git status` אימת אפס שינוי ב-`app/`/`tests/`/`REQUIREMENTS.md`/`IA.md`. נסרק כל תוכן ה-diff: עקביות סטטוסים (CP8–11 `done` בכל שלושת המקומות, אין "not_started" שיורי — אומת ב-`grep`), איזון תגיות HTML מלא, איזון `**` Markdown זוגי בשני קבצי ה-.md, אפס `TODO`/`FIXME`, נתיבי `docs/design/*.jpg` תואמים לקבצים בפועל. **סריקת סודות**: `grep` על `api[_-]key`/`secret`/`password`/`token`/JWT/`-----BEGIN`/`.env`/`SERVICE_ROLE`/`ANON_KEY` על ה-diff המלא — **0 סודות שמישים** (דפוסי החיפוש עצמם, כמילות מפתח, מופיעים ביומן הביקורת הזה גופו ואינם סוד — קריטריון 29); `login.jpg` נבדק חזותית — שדות ריקים; `git check-ignore` אימת ש-`docs/design/*.jpg` אינו מוחרג. **בדיקות**: `tests/test_design_tokens.py`+`tests/test_roadmap_render.py`+`tests/test_requirements_traceability.py` — 23/23 (⛔ לא `pytest` מלא — אין שינוי קוד). ⛔ אין מודלים/Holdout הורצו | לא נמצא ממצא הדורש תיקון מקומי. `git diff --check` נקי, `node --check` על ה-`<script>` תקין | checkpoint 12 `in_progress` — שלב 1/6 בלבד. ⛔ אין commit, אין push/PR/merge — כל אחד מחמשת השלבים הנותרים (commit→push→PR→CI→merge) דורש אישור נפרד ומפורש |
| 13.09.2026 | 12 | **שלב 2/6 (commit) בוצע באישור נפרד** ("מאושר", בהקשר ישיר לאמירתי הקודמת שאני ממתין לאישור נפרד ל-commit). `git status --short`/`git diff --stat` אומתו שוב מול הסקירה של שלב 1/6 — זהים, אין שינוי נוסף. `git add ROADMAP.html docs/DESIGN.md docs/planning/PHASE10.md docs/design/` ואז `git commit -F -` עם הודעה מפורטת לכל אחד מ-checkpoints 8–12(שלב 1). הודעת ה-commit כוללת `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>` **בלבד**, ⛔ ללא שורת `Claude-Session` (ריפו ציבורי — הנחיה שמורה בזיכרון). ⚠ **תוקן לפני שהתועד כ"מאומת":** הדיווח הראשוני טען "עץ עבודה נקי" ו-hash סופי, אך תיעוד שלב 2/6 עצמו (השורה הזו + §יד + ROADMAP.html) נכתב **אחרי** ה-commit ונשאר לא-מקומט — האימות העצמאי של המשתמש תפס זאת. תוקן: הוסר כל SHA עצמי מהתיעוד (משתנה ב-`amend`), הודעת ה-commit עודכנה משלב 1/6 לשלב 2/6, ובוצע `git commit --amend` הכולל את כל שינויי התיעוד יחד עם ה-commit המקורי | `git status --short --branch` אחרי ה-`amend` — עץ עבודה נקי, `feat/design...origin/feat/design [ahead 1]`; `git log --oneline -1`/`git show --stat` אומתו מול ה-SHA הנוכחי בפועל (לא מוקלד כאן — משתנה בכל `amend` נוסף) | **checkpoint 12: `in_progress`, שלב 2/6.** ⛔ אין push, אין PR/CI/merge — כל אחד מארבעת השלבים הנותרים (push→PR→CI→merge) דורש אישור נפרד ומפורש |
| 13.09.2026 | 12 | **שלב 2/6 תוקן פעמיים אחרי אימות עצמאי נוסף של המשתמש:** (1) שורה יתומה ב-§יד עדיין טענה "אין commit" אחרי שה-commit כבר בוצע — הוחלפה ב"אין commit נוסף ואין push/PR/CI/merge; השלב הבא הוא push באישור נפרד". (2) ספירת ה-insertions התועדת (`318`) לא תאמה את ה-commit בפועל (`332` בסבב תיקון ראשון, התברר גם הוא שגוי ב-1 בגלל שהתיקון הראשון עצמו שינה את מספר השורות; `331` הסופי הצליח כי היה זהה-אורך להחלפה ולא הזיז מספרי שורות). שני התיקונים בוצעו ב-`git commit --amend` אחד (ותיקון המספר עצמו דרש `amend` שני קטן, שהוכיח שהמספר מתכנס לנקודת שבת). אומת: `node --check` נקי, `git diff --check` נקי, `git status --short` ריק, `git show --stat` — 331 insertions(+)/286 deletions(-) תואם בדיוק לתיעוד | `git status --short --branch` — עץ עבודה נקי, `ahead 1`; `git show --stat HEAD` — 331/286 מדויק | **checkpoint 12: `in_progress`, שלב 2/6, מאושר סופית ע"י המשתמש.** ⛔ אין push/PR/CI/merge |
| 13.09.2026 | 12 | **שלב 3/6 (push) בוצע באישור מפורש** ("מאושר", בהקשר ישיר לאמירתי הקודמת שהשלב הבא הוא push באישור נפרד). אומת קודם: `git log --oneline -1` מקומי מול `origin/feat/design` — `3cf7d0a` מול `a3d5674`, הבדל commit יחיד, fast-forward אפשרי (אין divergence). `git push origin feat/design` — `a3d5674..3cf7d0a feat/design -> feat/design`, ⛔ ללא force | `git status --short --branch` אחרי ה-push — `feat/design...origin/feat/design` ללא `ahead`/`behind`; `git rev-parse HEAD origin/feat/design` — שני ה-SHA זהים (`3cf7d0aafe7c58e0fd924ad43f2f240e998803c9`) | **checkpoint 12: `in_progress`, שלב 3/6.** ⛔ אין PR, אין CI, אין merge — כל אחד משלושת השלבים הנותרים דורש אישור נפרד ומפורש. תיעוד שלב 3/6 (השורה הזו, §יד, §ח, ROADMAP.html) נכתב **אחרי** ה-push ונשאר לא-מקומט במתכוון — commit/push נוספים לשם תיעוד בלבד ימתינו לאישור נפרד, לא יבוצעו אוטומטית |
| 14.09.2026 | 12 | **תיעוד שלב 3/6 סונכרן: `git add`+`git commit` נפרד לתיעוד המנהלי בלבד** (לא צורף בשקט לאישור PR, כהוראת המשתמש) — `70bfec3`, 2 קבצים (`ROADMAP.html`/`PHASE10.md`), 23 insertions(+)/8 deletions(-), אפס שינוי קוד. אושר push נפרד: `git push origin feat/design` — fast-forward `3cf7d0a..70bfec3`, ⛔ ללא force | `git status --short --branch` — עץ עבודה נקי, אין `ahead`/`behind`; `git rev-parse HEAD origin/feat/design` — זהים (`70bfec398cdddbfe8c1775a47f4fea21fd10672a`) | **checkpoint 12: `in_progress`, שלב 3/6 (הפעם מקומט ונדחף במלואו).** ⛔ אין PR/CI/merge |
| 13.09.2026 | 12 | **שלב 4/6 (PR) בוצע באישור נפרד:** `gh pr create --base main --head feat/design` — נחסם תחילה ע"י מסנן ההרשאות של הסביבה (auto mode classifier), דווח למשתמש ללא ניסיון עקיפה; אושר מפורשות בסבב שני ובוצע בהצלחה — PR #26 (`feat/design`→`main`), עם כותרת/Summary/test plan וטריילר PR מלא | `gh pr create` החזיר URL בפועל של PR #26; לא הוסתפקתי בהצלחת הפקודה בלבד | **checkpoint 12: `in_progress`, שלב 4/6.** ⛔ אין CI decision, אין merge |
| 14.09.2026 | 12 | **PR #26 תוקן פעמיים באישור נפרד** אחרי סקירת המשתמש בפועל ב-GitHub: (1) הוסר קישור/אזכור Claude Session מגוף ה-PR — אומת `grep -i "claude\|session"` על ה-body בפועל דרך `gh pr view --json body` = 0 מופעים. (2) כותרת/Summary נכתבו מחדש: הטענה המקורית "אין שינויי app/tests" הייתה שגויה עבור ה-PR המצטבר בשלמותו — נבדק ב-`git show --stat` על כל commit ב-`main..feat/design` (13 commits) וזוהו שלושה commits של Phase 10A (`1c70ca8`/`4191805`/`17f1873`) שכן נוגעים ב-`app/insights.py`/`app/supabase_client.py`/`app/features.py`/`app/static/business_facts.json`/`scripts/train.py`/`scripts/business_facts.py`/`tests/test_business_facts.py`; רק commit הסגירה האחרון של Phase 10 (`3cf7d0a`) הוא design/docs-only בפועל (מאומת מוקדם יותר ב-`git diff --stat`). הכותרת/Summary עודכנו לשקף את שני חלקי ה-PR (10A + 10) במפורש, לא רק Phase 10. (3) ניסוח ה-CI תוקן מ"No CI trigger" (משתמע כאילו לא רץ) לתיאור עובדתי: CI רץ אוטומטית עם ה-push/PR, שלב 5/6 הוא אישור התוצאה ואינו ניתן עדיין | `gh pr edit 26` בוצע; `gh pr view 26 --json title,body` אומת בפועל את הניסוח החדש, לא רק את הכוונה לכתוב אותו | **checkpoint 12: `in_progress`, שלב 4/6 (מתוקן ומאושר סופית ע"י המשתמש).** ⛔ אין CI/merge decision — כל אחד משני השלבים הנותרים דורש אישור נפרד ומפורש |
| 14.09.2026 | 12 | **שלב 5/6 (אימות תוצאות CI) בוצע באישור נפרד.** `gh pr view 26 --json headRefOid,mergeable,mergeStateStatus,state,statusCheckRollup` — `headRefOid`=`70bfec398cdddbfe8c1775a47f4fea21fd10672a`, זהה ל-`HEAD` המקומי (נבדק ב-`git rev-parse HEAD`); הרולאפ הראה שתי בדיקות `test`, שתיהן `SUCCESS`/`COMPLETED`. **לא הוסתפקתי ברולאפ המצטבר** — כל ריצה אומתה בנפרד: `gh run view 34782288802` (headSha=70bfec3, event=push, conclusion=success), `gh run view 34782430525` (headSha=70bfec3, event=pull_request, conclusion=success) — שולל תרחיש של בדיקה ישנה שנשארה ירוקה מקומיט קודם. `mergeable`=`MERGEABLE`, `mergeStateStatus`=`CLEAN`, `state`=`OPEN` — אין קונפליקט מיזוג | `git status --short --branch` — עץ עבודה נקי לפני העדכון; אחרי עדכון התיעוד (השורה הזו + §יד + §ח + ROADMAP.html) נשאר לא-מקומט במתכוון, כהוראת המשתמש — אין commit/push לתיעוד הזה בלי אישור נפרד | **checkpoint 12: `in_progress`, שלב 5/6.** ⛔ אין merge — השלב האחרון והבלתי-הפיך ביותר דורש אישור נפרד ומפורש, נפרד מאישור שלב 5/6 עצמו |
| 14.09.2026 | 12 | **תיעוד שלבים 4/6-5/6 סונכרן: commit+push נפרדים לתיעוד המנהלי בלבד** (לא צורף בשקט לאישור המיזוג, כהוראת המשתמש) — `3cf03fd`, 2 קבצים (`ROADMAP.html`/`PHASE10.md`), 37 insertions(+)/5 deletions(-), אפס שינוי קוד; `git diff --check`/`node --check` אומתו נקיים לפני ה-commit. push נפרד: `git push origin feat/design` — fast-forward `70bfec3..3cf03fd`, ⛔ ללא force. הפעולה הזו הפעילה CI חדש על ה-PR | `git status --short --branch` — עץ עבודה נקי, אין `ahead`/`behind`; `git rev-parse HEAD origin/feat/design` — זהים (`3cf03fd08a20a8d44d6d2a24529380079aa43160`) | **checkpoint 12: `in_progress`, שלב 3/6-4/6 מסונכרנים ל-remote (הפעם מקומטים ונדחפים במלואם).** ⛔ אין הכרעת CI/merge |
| 14.09.2026 | 12 | **שלב 5/6 (אימות CI) אומת פעם שנייה, על `3cf03fd`, באישור נפרד.** `gh pr view 26` — `headRefOid`=`3cf03fd08a20a8d44d6d2a24529380079aa43160`, זהה ל-`HEAD`. **בדיקה ראשונה נתפסה `IN_PROGRESS`** (`mergeStateStatus`=`UNSTABLE`) — לא דווחה כהצלחה על סמך זה; פולינג עם השהיות קצרות (עד 6 ניסיונות, 15 שניות בין ניסיון לניסיון) עד ששתי הריצות הגיעו ל-`completed`. `gh pr checks 26` ואז `gh run view` על כל ריצה בנפרד — run `34783062811` (`headSha`=`3cf03fd`, `event`=`push`, `conclusion`=`success`), run `34783064894` (`headSha`=`3cf03fd`, `event`=`pull_request`, `conclusion`=`success`). `mergeable`=`MERGEABLE`, `mergeStateStatus`=`CLEAN`, `state`=`OPEN` | שתי הריצות אומתו בנפרד מול `headSha` המדויק, לא רק דרך הרולאפ המצטבר — שולל תרחיש של SHA ישן שמתחזה לראיה עדכנית | **checkpoint 12: `in_progress`, שלב 5/6 (מאומת סופית).** ⛔ אין merge — השלב האחרון דורש אישור מפורש ונפרד |
| 14.09.2026 | 12 | **שלב 6/6 (merge) בוצע באישור מפורש ונפרד — checkpoint 12 סגור, פאזה 10 נסגרת.** לפני הביצוע אומת שוב מצב טרי מול GitHub (`headRefOid`/`mergeable`/`mergeStateStatus`/`state`/`statusCheckRollup`) — זהה בדיוק לאישור האחרון, ללא שינוי. `gh pr merge 26 --merge` (merge commit, עקבי עם היסטוריית המיזוגים הקודמת בריפו — `git log --merges --oneline main` הראה `Merge pull request #N` בכל מיזוג קודם, לא squash/rebase) | `gh pr view 26 --json state,mergedAt,mergeCommit` — `state`=`MERGED`, `mergeCommit`=`ef2eb83`, `mergedAt`=`2026-09-13T21:14:54Z`; `git checkout main`, `git pull origin main --ff-only` — `main` המקומי עודכן ב-fast-forward ל-`ef2eb83`, זהה ל-`origin/main`; עץ עבודה נקי | **checkpoint 12: `done`.** כל ששת השלבים (ביקורת+סריקה → commit → push → PR → CI → merge) הושלמו, כל אחד באישור נפרד משלו |
| 14.09.2026 | 12 | **פאזה 10 נסגרה, בהוראה מפורשת של המשתמש לעדכון תיעוד זה.** `execution_status: in_progress` → `done` (§יד, `ROADMAP.html` אובייקט `num:10`); `planning_status` ללא שינוי (`approved_for_execution`). עודכנו: top banner (§ הפתיח), §ח שורת checkpoint 12 (`in_progress`→`done`, פירוט שלבים 3/6-6/6), §יד (פסקת סגירה + תיקון הצהרת `execution_status` ופסקה מיושנת שטענה "checkpoint 12 טרם התחיל"), `ROADMAP.html` (אובייקט `num:10`: `execution_status`, `note`, פריט checkpoint 12; פסקת `policy-fine` חדשה בפאנל התכנון, לפי התקדים של סגירת פאזה 9). ⚠ **תוקן לפני commit, בשני סבבים.** סבב ראשון: הניסוח הראשוני טען "אין ראיה חיה/auto-deploy נדרשת... ללא שינוי לשירות החי" — לא מדויק, כי PR #26 המצטבר כלל גם שינויי app/models אמיתיים מ-Phase 10A. תוקן גם gateHtml() של 10A ב-ROADMAP.html — טען "הבא בתור: checkpoint 12 (טרם התחיל)" אחרי שכבר נסגר; עודכן לציין שפאזה 10 נסגרה (merge ef2eb83) והשער הבא הוא פאזה 11. סבב שני: הניסוח המתוקן עדיין טעה בכיוון ההפוך — כינה את פאזה 10 כולה "design/docs-only בלבד", בעוד שפאזה 10 בפועל הוסיפה גם `app/static/tokens.css` (לא חווט לממשק) ו-`tests/test_design_tokens.py`; רק commit הסגירה הבודד `3cf7d0a` הוא design/docs-only בפועל, לא הפאזה כולה. תוקן בשלושת המקומות (ROADMAP.html policy-fine, PHASE10.md §יד, שורה זו) לניסוח מדויק: פאזה 10 לא שינתה לוגיקת שירות/API או חיווט runtime (כללה tokens.css+test_design_tokens.py+נכסי עיצוב); PR #26 המצטבר כלל בנוסף שינויי Phase 10A ב-app/, scripts/, tests/ ו-models/ (models/ אומת עצמאית ב-`git log --name-only` — P4S.joblib/metrics.json/P6_simulation.json/run_metadata.json אכן שונו); auto-deploy לא נבדק ואינו ראיית סגירה | `node --check` על ה-JS המחולץ מ-`ROADMAP.html`, `git diff --check`, ובדיקת שלמות טבלת Markdown (סורק תלוי-כותרת שמוודא שכל שורה בטבלה מתחילה ב-`\|`) — כולם נקיים | **checkpoint 12: `done`. פאזה 10: `execution_status: done`.** תיעוד זה עצמו טרם מקומט/נדחף — ממתין לאישור נפרד לשלב הבא (commit) |
