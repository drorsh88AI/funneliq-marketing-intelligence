# פאזה 12A — תיקוני ביקורת הבוחן: שפה פשוטה ו-UX (`docs/phase12a-planning`)

נכתב 24.09.2026, בעקבות ממצאי הבוחן החיצוני מפאזה 12 (CP7,
`Funneliq_External_audit.docx`) וסבבי ביקורת Codex–Claude על התכנון.
מקור האמת לדרישה הוא `FunnelIQ_Assignment.html`; לתכן — `SPEC.md`,
`docs/DESIGN.md`, `docs/IA.md`; מרשם העקיבות היחיד הוא
`REQUIREMENTS.md`. ⛔ מסמך זה אינו יוצר טבלת עקיבות מקבילה.

**✅ התכנון אושר בביקורת (Codex, 24.09.2026) ואושר לביצוע על ידי
המשתמש (24.09.2026).** `planning_status: approved_for_execution`. זה
**אינו** הוראת ביצוע: `execution_status` נשאר `not_started` עד
שתוצרי שלב התכנון (מסמכי שער א) ייסגרו ב-commit ייעודי → PR → CI →
merge (שלב ג, §יא), ויינתן אישור נפרד ומפורש להתחיל ביצוע.

**סטיות מאושרות, מצוינות מחדש:** Render ולא Railway (היתר מפורש
בבריף §07) · ⛔ בלי notebook · ⛔ בלי סרטון דמו (B58).

---

## מצב הפתיחה — נמדד 24.09.2026, קריאה בלבד

| נבדק | תוצאה |
|---|---|
| `main` | `431195e`, עץ עבודה נקי |
| פאזה 12 | `execution_status: done`. PR #35 מוזג (`242cc1e`), CI ירוק, checkpoint 9 סגר את שער `§ט` (52/52 `live/` על `main@a349f7f`) |
| המרשם (`REQUIREMENTS.md`) | 74 שורות: 49 `done` · 8 `planned` · 15 `gap` · 1 `N/A` · 1 `parent` (`_EXPECTED_STATUS_COUNTS` תואם) |
| תוצרים חסרים | `README.md`, `REPORT.md`, `schema.sql` אינם קיימים — עדיין תכנון-על בפאזה 13 |
| מקור ההפעלה | `docs/acceptance/EVIDENCE.md` §ד–ה (ממצאי שימושיות + ציון P4S נמוך/חוזר), עדיין פתוחים ולא משויכים לפאזה, בהכרעת המשתמש ב-CP8 של פאזה 12 |

---

## א. הקשר

- **מקור:** `Funneliq_External_audit.docx` (בוחן חיצוני אמיתי, CP7 בפאזה 12) — ר' ניתוח מלא ב-§ב.
- **דרישה עיקרית: `B55b`** — "a real link you can share that **actually answers** the founder's questions". קריטריון ההצלחה של הפאזה כולה.
- ⛔ אין שינוי סטטוס במרשם בלי הכרעה מפורשת (ר' §י).
- **הכרעות קודמות שנפתחות במפורש, בהוראת המשתמש** (מפורט ב-§ה2):
  - S11 / P11A-D7 — כותרות/צירים/מקרא של גרפים באנגלית ⇒ עברית (גרפים חיים).
  - `DESIGN.md:270` / P11A-D8 — פריסת Overview מוגבלת ל-3 עמודות ⇒ 5 בשורה אחת.
  - `SPEC.md:365-367` / `IA.md:188` — סדר המסך וכמות הדוגמאות.

**מקרא לטבלאות §ב:**
- **מקור** — שורה בדוח (`audit:N`).
- **EVIDENCE** — האם כבר מתועד ב-`docs/acceptance/EVIDENCE.md`.
- **מצב** — ✔ נבדק בקוד/מסמך · ⧗ יאומת ב-CP0.
- **הבנה** — נבחן במסע האנושי החוזר (CP11).

---

## ב. ממצאים לפי סדר הדוח

### ב1. כללי

| # | מקור | ממצא | EVIDENCE | מצב | תיקון | קבלה |
|---|---|---|---|---|---|---|
| G1 | audit:2 | אין הסבר קצר בראש עמוד | §ד ✓ | ⧗ | שורת פתיחה בכל מסך: "מה המסך עונה" ו"מה עושים כאן", כולל Login (CP3) | קיימת בששת המסכים, כולל Login · הבנה |
| G2 | audit:4, 15, 41, 42 | שפה גבוהה/סטטיסטית | §ד ✓ | ⧗ | תבנית §ט-2 + עקרונות §ט-1 | בדיקה מכנית §ט-5 · הבנה |
| G3 | audit:3 | Overview ו-P4S לא נכללו בצ'קליסט | §ג.1 ✓ | ✔ | להוסיף אותם לצ'קליסט של המסע החוזר | CHECKLIST מעודכן |
| G4 | audit:5 | הניווט עקב אחרי סדר הצ'קליסט | §ג.1 ✓ | ✔ | אין תיקון מוצר | — |

### ב2. כניסה

| # | מקור | ממצא | EVIDENCE | מצב |
|---|---|---|---|---|
| L1 | audit:6–7, 52 | הושלם בלי עזרה | §ב ✓ | ✔ ⛔ אין ממצא |

### ב3. Overview (סקירה כללית)

| # | מקור | ממצא | EVIDENCE | מצב | תיקון | קבלה |
|---|---|---|---|---|---|---|
| O1 | audit:9 | כרטיסים בגבהים לא שווים. ⚠ אי-התאמה בדוח עצמו: "5 בלוניות" וגם "3 למעלה ושלושה למטה" | ✗ | ⧗ צילום המצב הנוכחי ב-CP0, ⛔ בלי ליישב בהנחה | **הוכרע:** 5 בשורה אחת, בגובה שווה (§ו.7) | §ו.7 |
| O2 | audit:10 | לא ברור שהטבלה מציגה נתוני עבר ("לא בוצע שום חישוב") | ✗ | ⧗ | כותרת/שורת פתיחה: "נתוני העבר של Northbound — לא תוצאה של קלט" | הבנה |
| O3 | audit:11 | כותרת גרף באנגלית, **וגם** "מה היא אמורה להציג ועל סמך מה" | ✗ | ✔ `overview.js:305` | (1) כותרת: §ז1 (הוכרע). (2) הסבר צמוד לגרף — ר' הנוסח המאושר ב-§יב-2 (R1): מה המדד (ממוצע, על פני מקרי העבר בכל רמת תקציב, של היחס "עסקאות שנסגרו ÷ לידים"; ⛔ לא סך÷סך — `views.sql:46`, ‏`analysis.py:82-96`), ומה המקור (נתוני העבר של Northbound, ⛔ לא קלט המשתמש) | אין כותרת ראשית באנגלית · ההסבר צמוד לגרף · הבנה |
| O4 | audit:12 | אותם נתונים פעמיים | ✗ | ✔ **fallback טבלאי קבוע** (`charts.js:160-191`, לנגישות, `IA.md:974`). ⧗ CP0 יאמת שזו הטבלה שהבוחן ראה | להוסיף כיתוב "אותם נתונים כטבלה, לנגישות". ⚠ אם רוצים לקפל, לבדוק מול דרישת הנגישות (`DESIGN.md` §4.1) | הכפילות מוסברת במסך |
| O5 | audit:13 | משפט תנאי מוצג כתשובה קבועה. הבוחן: "אין רצף של כלום" | ✗ | ✔ **פגם תוכן**: `overview.js:279-284` כבר קבע שהרצף אינו עולה, ובכל זאת מוצג משפט תנאי. מקור הנוסח: `DESIGN.md:452` | **קודם לתקן את מקור הנוסח ב-DESIGN §6.1**, ואחר כך את הקוד: נוסח קביעה לכל מצב, ⛔ לא משפט "אם" | בדיקות יחידה לשלושה מצבים: עולה · לא עולה · נתונים חסרים |
| O6 | audit:14 | "מה כדאי לעשות" לפני קלט נראה מיותר | ✗ | ⧗ | לציין שההמלצה נשענת על נתוני עבר | הבנה |

### ב4. תחזיות (הטופס המשותף P2/P3/P4)

| # | מקור | ממצא | EVIDENCE | מצב | תיקון | קבלה |
|---|---|---|---|---|---|---|
| F1 | audit:17 | תיבת האישור נמצאת מעל הטופס | §ג.2 ✓ | ✔ **כבר טופל** — הצ'קליסט תוקן; המוצר נכון (`predict.js`) | — | — |
| F2 | audit:18 | רשימת עד 1000 דוגמאות לפני הטופס; "מאוד לא נח" | ✓ חלקית | ✔ `supabase-prefill.js:69-71` (`limit=1000`) | **הוכרע:** §ו.2 — 10 דוגמאות קבועות, מקופלות, מתחת לטופס | הטופס ראשון; העזר מקופל מתחת לו |
| F3 | audit:19 | שדות ההזנה לא באותו גובה | ✗ | ✔ `style.css:492-502` (תווית בשלוש שורות, `flex-wrap`) | §ח.3 | צילום ברוחבי §ו.7 |
| F4 | audit:20 | טופס נקי → תחזית התקבלה | §ב ✓ | ✔ ⛔ אין ממצא | — | — |
| F5 | audit:21 | השדה "לידים שלא הומרו" לא מובן | ✗ | ⧗ | תווית/הסבר פשוט | הבנה |
| F6 | audit:22 | בלוקי תוצאה לא אחידים, ולא ברור מה התוכן המעשי | ✓ | ✔ `style.css:587-588` (`align-items: start`) | §ח.4 + §ט-2 | צילום · הבנה |

### ב5. ציון לקוח-על (P4S)

| # | מקור | ממצא | EVIDENCE | מצב | תיקון | קבלה |
|---|---|---|---|---|---|---|
| S1 | audit:24 | ‏50,000 לא קיבל ציון | §ד ✓ | ✔ תקין: `ood_bounds.ad_budget.max = 20000` ב-`P4S.meta.json`, תואם לטווח הבריף (₪500–₪20,000) | — | — |
| S2 | audit:25 | דוגמאות לפני השדות | ✓ חלקית | ✔ `supabase-prefill.js:90-92` | כמו F2 | כמו F2 |
| S3 | audit:26 | הודעת OOD הובנה כ"אפשר לחזות רק מקרים שכבר קרו באימון" | ✗ | ⧗ | נוסח מדויק: §ט-4 | הבנה |
| S4 | audit:27–28 | "בכל הדוגמאות, **או לפחות בהרבה מהן שבדקתי**, הציון היה 1" | §ה ✓ ⚠ טעון דיוק | ⧗ | **אבחון תחילה** (§ג) | §ג |
| S5 | audit:29 | עדיין גבוה ולא ברור מה לעשות | חלקית (ב-EVIDENCE §ד מתועד רק קושי שפה כללי; ⛔ הביקורת הספציפית על נוסח P4S אינה מצוטטת) | ⧗ | §ט-2, אחרי הכרעת המוצר | הבנה |

**דיוק ל-S4 (יבוצע ב-CP0):** ידוע שהבוחן בחר מתוך דוגמאות ה-prefill;
⛔ לא ידוע אילו שורות בחר ואם ערך אותן — סדר הרשימה אינו משחזר את
הקלטים המדויקים. `EVIDENCE.md` §ה יתוקן בהתאם.

### ב6. תקציב

| # | מקור | ממצא | EVIDENCE | מצב | תיקון | קבלה |
|---|---|---|---|---|---|---|
| B-1 | audit:31 | מספר בכותרת לא מוסבר | ✗ | ⧗ | תווית ("תקציב חודשי כולל: ₪50,000") | הבנה |
| B-2 | audit:32–33 | משפט D9 לא מובן; הבוחן חיפש "תקציב לכל לקוח" | ✓ חלקית | ✔ הנוסח הקנוני ב-`DESIGN.md:457` | נוסח §ט-4; להבהיר "קמפיינים", ⛔ לא לקוחות | הבנה |
| B-3 | audit:34–35 | מה ה-100, מה הטווח, מה n | ✗ | ⧗ | מילון §ט-3 (טווח, n) + תוויות ערכים ("100 קמפיינים של ₪500") + ערכי ציר ומקרא בעברית (§ז1, ✅ הוכרע) | הבנה |
| B-4 | audit:36 | סתירה נתפסת בין הטבלה לסיכום; הטבלה קטנה ובסוף | ✗ | ⧗ | §ט-4: למה דירוג ≠ המלצה; הגדלה והעלאה של הטבלה | הבנה |
| B-5 | audit:37 | "מה כדאי לעשות" בשורה אחת לא ברורה | ✓ | ⧗ | תיבת "מה לעשות" (§ט-2) | הבנה |

### ב7. מעקב שיחות (Follow-up)

| # | מקור | ממצא | EVIDENCE | מצב | תיקון | קבלה |
|---|---|---|---|---|---|---|
| U1 | audit:39 | תתי-כותרות באנגלית בטופס עברי | ✓ | ✔ `followup.js:133, 218` | **הוכרע:** §ז1 | כמו O3 |
| U2 | audit:40 | ה"תשובה" מתחת לטבלת הנשירה חוזרת על השיעורים | ✗ | ✔ (`followup.js:~150`) | תשובה שמסכמת, ⛔ לא חוזרת | אין חזרה מספרית מיותרת |
| U3 | audit:41 | תשובת "מספר שיחות" מבלבלת | חלקית (מתועד רק קושי שפה כללי; ⛔ הערה זו אינה מצוטטת ב-EVIDENCE §ד) | ⧗ | §ט | הבנה |
| U4 | audit:42 | ההמלצה "ללא רווחים או שורות חדשות", בשפה מקצועית | ✓ | ⧗ | עיצוב טקסט (פסקאות, נקודות) + §ט-2 | הבנה |

### ב8. ממצאי ביקורת קוד — ⛔ לא דווחו על ידי הבוחן

| # | מקור | ממצא | מצב | תיקון | קבלה |
|---|---|---|---|---|---|
| K1 | `followup.js:160`, ‏`DESIGN.md:458` | משפט התנאי "אם השיעורים אינם עולים ברצף…" מוצג **בלי שום בדיקה** של הרצף | ✔ | תיקון מקור הנוסח ב-DESIGN, ואחר כך ענף לפי הנתונים | בדיקות יחידה לשלושת המצבים |

---

## ג. אבחון P4S

**ידוע** (נקרא מ-`metrics.json`, ⛔ בלי חישוב חדש, ⛔ בלי לפתוח Holdout — S9):
- `P4S_holdout.budget_tier`: Low n=116 (0%) · High n=189 (0%) · Mid n=328 (32.3%, ROC-AUC בתוך Mid = 0.528).
- bin 0 בעקומת הכיול: n=305, ‏p≈0.0103.
- מתועד ב-`SPEC.md:512-517`.

**השערה, ⛔ לא מוכחת:** הציון הנמוך מרוכז ב-Low/High; שוויון הספירות
(305=305) **אינו מוכיח** שאלה אותן שורות; ה-AUC בתוך Mid **אינו
מוכיח** "אינדיקטור Mid בלבד"; "לא באג" עדיין לא הוכח (הבדיקה המקומית
מ-23.09.2026 לא בדקה את המסלול prefill→בקשה→מסך).

**קלטי האבחון — נקבעים ומקומטים לפני כל הרצה:**
- **(מ1)** עשר הדוגמאות הקבועות (§ו.2).
- **(מ2)** תרחיש ידני תקין שאינו שורת CSV קיימת: בתוך `ood_bounds`,
  ועומד ביחסי השדות. הערכים נכתבים בתוכנית ב-CP1.
- **(מ3)** אוכלוסיית ה-prefill הישנה (1000 שורות), **לתיאור בלבד**:
  מה הבוחן יכול היה לראות.

**בדיקות:**

| # | מה | על אילו קלטים | היכן | מה זה מוכיח |
|---|---|---|---|---|
| A1 | `app.inference` מקומי → `event_probability` וציון מוצג `Math.round(p*100)`, ⛔ בלי תוויות (אין הערכת Holdout) | מ1, מ2, מ3 | מקומי | ערכי ייחוס לשרשרת (A4). ל-מ3 גם טבלה **תיאורית** טייר × ציון |
| A2 | שיעור לקוח-על לפי טייר, **train בלבד** (⛔ לא Holdout) | train | מקומי | האם דפוס Mid קיים גם מחוץ ל-Holdout. תיאורי בלבד |
| A3 | מיפוי סטטי: שדות → גוף הבקשה → הצגת הציון | — | קוד | שהמיפוי תקין על הנייר |
| A4 | **מסלול חי על הגרסה הפרוסה הנוכחית, לפני הכרעת המוצר:** הקלדה ידנית של ערכי מ1 ומ2 לטופס P4S בדפדפן (⛔ לא דרך הבורר הישן) → לכידת גוף הבקשה → תגובה (`event_probability`, `model_version`) → הציון **הגלוי** | מ1, מ2 | חי, בהרצת המשתמש (credentials) | שהשרשרת החיה שלמה ועקבית |

**שני מסלולים, שלוש שאלות נפרדות (כל אחת עם ראיה משלה, ⛔ אין להסיק
אחת מהאחרת):**
- **ש1 — יכולת טכנית להפיק ציון:** A1+A3+A4. ⛔ בדיקה טכנית בלבד,
  ⛔ **אינה** מוכיחה איכות חיזוי.
- **ש2 — זמינות ארבעת הקלטים בזמן החיזוי:** מול `post_followup_1`
  (`PHASE8A.md` D1) ומול `docs/feature_matrix.md`. ⚠ זו **הנחת
  שימוש**, ⛔ לא עובדה נצפית משורת CSV.
- **ש3 — איכות זיהוי לקוח-על חדש:** רק ראיות קיימות (ROC-AUC 0.8014
  כולל; ‏0.528 בתוך Mid; Recall=0 בסף 0.5). ⛔ אין הערכה חדשה (S9).
  ⛔ מ1/מ2 אינם ראיה לאיכות.
  - **מסקנה מוגבלת מראש, לכתוב במפורש:** "קיימים מדדי עבר על Holdout
    היסטורי; **תוקף החיזוי האישי העתידי לא הוכח**. הוא תלוי בהנחת
    הזמינות (ש2), ויכולת הדירוג בתוך Mid חלשה." (`SPEC.md:226-231`,
    ‏`:513-526`)
  - ⛔ שום תוצאת אבחון (כולל תרחיש ידני תקין) אינה סוגרת אוטומטית את
    `B55b`.

**כלל הכרעה** (הנוסח הקודם "שורת Mid עם ציון 1 = באג" נבדק ונדחה: בתוך
Mid ה-AUC≈0.53, וציון נמוך שם אפשרי ולגיטימי):
- **באג** = כל אי-התאמה בשרשרת, לכל קלט ב-מ1/מ2:
  (i) גוף הבקשה ≠ הערכים שהוזנו · (ii) `model_version` בתגובה ≠
  `models/P4S.meta.json` · (iii) `event_probability` חי ≠ A1 מקומי
  לאותו קלט (סבולת ≤1e-6) · (iv) הציון הגלוי ≠ `Math.round(p*100)`.
  באג עוצר את הפאזה עד הכרעה.
- **אין באג** ⇒ ש1 עונה "כן" **רק עבור 11 הקלטים שנבדקו** (מ1+מ2).
  ⛔ אין הוכחה לכל קלט אפשרי. התפלגות הציונים (A1-מ3, ‏A2) היא
  **תיאור** של התנהגות המודל, ⛔ לא קריטריון באג.

**אחרי האבחון:** טבלת ש1/ש2/ש3 (ידוע / לא ידוע / מגבלה) מוצגת
למשתמש, ואחריה אפשרויות מוצר (א/ב/ג — הסבר במסך / טווחים משמעותיים
לצד 0–100 / ניסיון שיפור מודל, בכפוף למגבלות CatBoost/S9/PHASE8A D1).
⛔ לא מוכרע כאן. ⛔ ממצא ה-Mid אינו תשובה מוכחת ל-`B43` (ר' §יב-2,
R6, ונספח ב).

---

## ד. ראיות שדורשות אימות מחדש — לפי השינוי בפועל, ⛔ לא מראש

**עיקרון:** כל ראיה חיה שייכת לגרסה שנבדקה (`a349f7f` בפועל בשלב
נכתוב זה; ⚠ ה-SHA הפרוס בפועל ייבדק מחדש ב-CP0, כי `main` הנוכחי
(`431195e`) מכיל את `e0c7286`). אחרי redeploy, ראיה של מסך ששונה פגה;
המינימום הוא בדיקה ממוקדת על הגרסה החדשה: בקשה אמיתית → תוצאה
**גלויה**.

| מסך/רכיב ששונה | שורות מושפעות | אימות מינימלי |
|---|---|---|
| תחזיות | B28, ‏B36 (+P4) | בקשה → תוצאה גלויה; טעינת דוגמה מקופלת; OOD |
| P4S | B41 | A4 חוזר (רגרסיה) + דוגמה מקופלת + OOD |
| תקציב | B50, ‏B52 | שער P12-D9 + נראות "בקצרה"/"מה לעשות" |
| Follow-up | B45, ‏B44 (תצוגה) | תוצאה גלויה + בדיקת יחידה ל-K1 |
| Overview | B21/B24 (תצוגה) | יחידה ל-O5 + תוצאה גלויה + פריסה (§ו.7) |
| ארבעת הגרפים החיים (שפה) | B21, ‏B44, ‏B50, ‏B46b (תצוגה) | אימות חי בגרסה החדשה, ברוחבי §ו.7: כותרת, שמות צירים, ערכי X ומקרא בעברית; תוויות התקציב הארוכות ("100 קמפיינים × ₪500") קריאות, ⛔ בלי חיתוך וחפיפה. ⚠ ערכי Y מספריים אינם מוצגים היום, ⛔ ולא נבדקים |
| דוגמאות | B12 (ראיית RLS בממשק) | `live/test_02` מותאם + בדיקת דפדפן לשאילתה (§ו.2) |
| הכול | B55b | מסע אנושי חדש עם שאלות הבנה |
| הכול | B59 | **מחייב ב-CP11:** ה-CHECKLIST המעודכן כולל את **כל שבע רגלי B59** על הגרסה החדשה (URL · התחברות · טופס נקי ⛔ בלי דוגמה · הזנה ידנית וחיזוי · Follow-up ותקציב · קריאת ההמלצות · אפס התערבות), כי המסע הקודם נעשה על הממשק הישן. ⚠ נשארת `planned` עד README בפאזה 13 |

⛔ אין הרצה חוזרת קבועה מראש של כל 52 בדיקות ה-`live/`.

---

## ה. Checkpoints

| CP | תוכן |
|---|---|
| CP0 | פתיחת ענף מ-`main`; אימות SHA פרוס בפועל; צילום המצב הנוכחי (O1, ‏F3, ‏F6) ברוחבי §ו.7; אימות פריטי ⧗; תיקון `EVIDENCE.md` §ה (S4) |
| CP1 | (1) בחירת 10 הדוגמאות וקומוט המזהים (§ו.2) · (2) קביעת תרחיש מ2 · (3) אבחון P4S A1–A3 מקומי על שני המסלולים · (4) **A4 חי על הגרסה הפרוסה הנוכחית** (הרצת המשתמש) · (5) הצגת טבלת ש1/ש2/ש3 ואפשרויות מוצר. ⛔ עצירה עד הכרעת מוצר |
| CP2 | טבלת נוסחים מלאה לאישור המשתמש (§ט-5) → עדכון מסמכי המקור לפי המפה ב-§ה2. תיעוד לפני קוד |
| CP3 | Login (G1: שורת פתיחה) + Overview (O1–O6) |
| CP4 | תחזיות (F2, ‏F3, ‏F5, ‏F6) + דוגמאות (§ו.2) |
| CP5 | P4S (S2, ‏S3, ‏S5 + אפשרות המוצר שנבחרה) |
| CP6 | תקציב (B-1–B-5) |
| CP7 | Follow-up (U1–U4, ‏K1) |
| CP8 | בדיקות מקומיות (יחידה/e2e), התאמת `live/` לנראות, בדיקה מכנית §ט-5, ביקורת קוד. **שפת הגרפים:** `e2e/test_11_chart_english_chrome.py` דורש היום אנגלית בלבד ולכן **ייכשל** — לשכתב אותו (מקרה הפרכה 12 של P11A) כך שיבדוק, בארבעת הגרפים החיים: כותרת עברית + כותרת-משנה אנגלית · שמות צירים בעברית · ערכי X בעברית · מקרא התקציב בעברית. זו הבדיקה היחידה שנשענת על התוויות האנגליות |
| CP9 | commit → push → PR → CI → merge → redeploy (כל צעד באישור נפרד) |
| CP10 | אימות חי ממוקד לפי §ד (הרצת המשתמש), כולל A4 חוזר |
| CP11 | מסע אנושי חוזר (אותו בוחן), לפי CHECKLIST מעודכן: שבע רגלי B59 + שאלת הבנה נפרדת לכל אחת מחמש תשובות המייסדת (P2, P3, P4S, תקציב, Follow-up) + שאלת הבנה ל-Overview → מיפוי `REQUIREMENTS.md` (B55b לפי הראיה; B59 נשארת `planned` עד פאזה 13) |

### ה2. מפת מסמכי CP2 — תיקון מקור לפני קוד

| מסמך:שורה | מה משתנה | הכרעה |
|---|---|---|
| `DESIGN.md:452` | Overview: נוסח משמעות כקביעה לכל מצב (O5) + שכבות §ט-2 | O5 |
| `DESIGN.md:453-459` | כל שורות §6.1: הוספת "בקצרה" ו"מה לעשות" גלויים. מהנוסח הקנוני **רק ההסבר הטכני/המתודולוגי** עובר ל"פירוט טכני"; כל פעולה וכל הסתייגות שמשנה החלטה נשארות **גלויות**, בניסוח פשוט (`IA.md:980-981`) | §ט-2 |
| `DESIGN.md:458` | נשירה: ענף לפי הרצף (K1) | K1 |
| `DESIGN.md:270` | Overview: 5 בשורה אחת + התנהגות חלון צר (§ו.7) | §ו.7 |
| `DESIGN.md:271-272` | טופס משותף ו-P4S: `prefill-picker` **מתחת** לטופס, מקופל | §ו.2 |
| `DESIGN.md:244, 303` | ✅ כותרות: עברית ראשית + אנגלית ככותרת-משנה. ✅ צירים, ערכי ציר ומקרא: עברית (§ז1) | §ז1 |
| `DESIGN.md:667` | תבנית שדה: תווית עסקית בשורה אחת + שורה טכנית קטנה אחת | §ח |
| `SPEC.md:365-367` | סדר הזרימה: הדוגמה עוברת אחרי הטופס | §ו.2 |
| `SPEC.md:373-381` | עזר הדוגמה (טופס משותף ו-P4S): מקופל, 10 קבועות, מתחת לטופס; התנהגות ב"פחות מ-10" | §ו.2 |
| `SPEC.md:383-385` | שכבות התוצאה: "בקצרה"/"מה לעשות" גלויים; ההסתייגות המהותית גלויה; רק פירוט מתודולוגי מקופל | §ט-2 |
| `SPEC.md:784-801` | Prefill כקיצור דרך: מקור `in.(...)` במקום בורר רחב | §ו.2 |
| `IA.md:221-224` | "מבחר מוגבל ומסודר… תקרת 1,000" ⇒ 10 מזהים קבועים | §ו.2 |
| `IA.md:976` | שורת "שפת גרפים": גרפים חיים — כותרת עברית ראשית, אנגלית משנית, צירים ומקרא בעברית (§ז1). REPORT — עברית סביב ה-SVG בלבד (§ז2) | §ז |
| `IA.md:980-981` | "סיכום והמלצה"/"הסתייגות מהותית" — ✔ **נשמר ומחייב** את §ט-2 (ההסתייגות המהותית גלויה, ⛔ לא בכתב קטן) | — |
| `SPEC.md:1688`, ‏`1698-1727` (§ שפת הגרפים, S11) | **החלפה חלקית** של S11: (1) **גרפים חיים** — כותרות, צירים, ערכי X ומקרא בעברית; אנגלית רק ככותרת-משנה קטנה. (2) **REPORT** — כותרת וכיתוב בעברית **סביב** SVG שלא נוצר מחדש; הטקסט **בתוך** ה-SVG נשאר באנגלית. S11 נשמר כהחלטה היסטורית, עם הערה "הוחלפה בהיקף זה ב-12A" | §ז |
| `IA.md:188` ואילך (§3.3) | מיקום העזר ומספר הדוגמאות | §ו.2 |
| `IA.md:399-406` (§3א.3) | בורר P4S: "מבחר מוגבל לפי `source_row_id`" ⇒ 10 מזהים קבועים | §ו.2 |
| `IA.md:229-230` | ✔ **נשאר** ("אסור: בחירת שורה לפי כמה שהתחזית יצאה יפה"), תואם לכלל §ו.2 | — |
| `IA.md:58` ואילך (§2) | פריסה + שורת פתיחה (G1) | §ו.7, G1 |
| `PHASE11A.md:208` (P11A-D7), ‏`:219` (P11A-D8) | ⛔ **לא נמחקות** (היסטוריות). נוספת הערת "הוחלפה ב-12A" עם הפניה | §ז, §ו.7 |
| `style.css:230-236` | הערת P11A-D8 בקוד מתעדכנת עם הקוד (CP3) | §ו.7 |

---

## ו. הכרעות משתמש נעולות (2026-09-24)

1. **שם:** 12A — תיקוני ביקורת הבוחן. ✅

2. **דוגמאות — כ-10 קבועות, מקופלות, מתחת לטופס, להמחשה בלבד.** ✅
   **כלל הבחירה (אושר, כולל אחרי ביקורת נגדית):**
   - הרכב **2 Low / 5 Mid / 3 High**, קרוב להרכב הרוכשים ב-CSV (571 /
     1,640 / 952, מתוך 3,163; פער 1501–1999 = 0).
   - `budget_tier()` נגזר מקומית מ-`app/features.py:253`, כי אין
     עמודת רמת-תקציב בסכמת `funnel_records`.
   - **שלב 0, זכאות:** `purchased=1` **כתנאי אוכלוסייה** (לא תוצאת
     מודל — משמש כאן כתנאי זכאות, כמו ב-SPEC § אוכלוסיות אימון).
     בנוסף: עוברת את הוולידציה של שני הטפסים, ובתוך `ood_bounds` של
     P2/P3/P4/P4S. הבחירה בתוך האוכלוסייה ⛔ **אינה** משתמשת בתוצאת
     לקוח-על (`referred`/`upsell`/`ltv_months`/`cumulative_profit`)
     ולא בציון מודל.
   - **שלב 1, בחירה בתוך כל טייר:** מיקומים מפוזרים באופן שווה על
     `source_row_id` הממוין (⌊(i+0.5)·n/k⌋), ⛔ לא k המזהים הראשונים
     (הבורר הישן התרכז בתחילת הקובץ).
   - **נקבע ומקומט לפני A1.** ⛔ אין החלפת דוגמה בגלל ציון (תואם
     `IA.md:229-230`). אותם 10 מזהים לשני הבוררים (המשותף ו-P4S), כל
     אחד עם עמודות הקלט שלו.
   - **מה מוכיחה הבחירה המקומית, ומה CI בודק בלי CSV** (ה-CSV אינו
     ב-repo, החלטה נעולה; ר' ההכרעה הסופית ב-§יב-2, R5):
     - **מניפסט מצומצם:** קובץ אחד, מקומט, נושא לכל מזהה רק
       `source_row_id` וערכי הקלט של שני הטפסים — **בלי** טייר שמור
       ובלי SHA-256 בתוך הקובץ עצמו.
     - **מקומי** (`scripts/select_examples.py`, דורש CSV): מאמת מול
       ה-CSV שה-SHA-256 שלו זהה לערך ב-`PHASE0.md`. ה-hash נבדק
       **מקומית בלבד**.
     - **CI (בלי CSV), מבנה בלבד:** 10 מזהים ייחודיים, והרכב 2/5/3
       (נגזר מ-`budget_tier()` הטהורה על ערכי `ad_budget` השמורים —
       פונקציה בלבד, ⛔ לא קובץ נתונים). ⛔ **בלי** כפילות בדיקות
       מעבר לזה: ⛔ אין ב-CI בדיקת `ood_bounds` ו-⛔ אין בדיקת ולידציה
       חוזרת — אלה כבר הופעלו בזמן הבחירה המקומית (שלב 0).
     - **CI אינו יכול לבדוק** שהכלל יושם נכון (בחירת המיקומים) או
       שהערכים השמורים == ה-CSV == Supabase — זה נבדק **מקומית**
       בסקריפט, ו**חיה** ב-`live/test_02` המותאם, בשלושת מצבי
       ההרשאה. ⚠ ההשוואה החיה מוכיחה התאמה **לעשר הרשומות שנבחרו
       בלבד**, ⛔ לא לכל הטבלה.
     - ⚠ אלה דוגמאות **להמחשה**, ⛔ לא מדגם שמוכיח איכות מודל.
   - **מקור:** `select` מ-`funnel_records` עם JWT המשתמש,
     `source_row_id=in.(...)` במקום `limit=1000`. מסנן `in` **אינו
     עוקף RLS**.
   - **בדיקות:** `live/test_02` (מעבר למסנן המזהים + השלכת P4S,
     בשלושת מצבי ההרשאה) · בדיקת דפדפן שהשאילתה נשלחה והדוגמה נטענה
     · `e2e/test_08` (פתיחת האזור, מזהים, מוק) · `live/test_05` (פתיחת
     האזור אם הטעינה עצלה).
   - **תצוגה:** מקופל מתחת לטופס. כותרת: "דוגמאות מנתוני העבר —
     להמחשה בלבד". הסבר: "אפשר למלא תרחיש חדש בלי לבחור דוגמה; קלט
     שאינו תקין או מחוץ לתחום לא יקבל ציון." טעינת דוגמה מעדכנת
     **גלויות** את השדות ואת הסיכום.
   - **פחות מ-10 חזרו:** "נטענו N מתוך 10 דוגמאות"; ההזנה הידנית
     נשארת זמינה; ⛔ בלי השלמה שקטה. 0 = מצב כשל ה-prefill הקיים.
   - **P4S — תנאי הזמן** (רכישה ידועה, מעקב 1, חלון חודשי סגור) הם
     **הנחת שימוש** (`PHASE8A.md` D1), ⛔ לא מוכחים משורת CSV.
   - **סתירה בתכן, מתוקנת:** היות העזר **אופציונלי** נשמרת, אך
     **המיקום** מתחת לטופס משנה את סדר המסך המפורש ב-`SPEC.md:365-367`
     וב-`IA.md:188`. הכרעת המשתמש גוברת; שני המסמכים יעודכנו ב-CP2
     כתיקון מתועד.

3. **כותרות בעברית, גם ב-REPORT.** ✅ מנגנון ה-REPORT: אפשרות (א) —
   כותרת מעל הגרף, בלי יצירה מחדש של ה-SVG (§ז2).

4. **בלוקי המלצה:** תשובה פשוטה גלויה + פירוט טכני מקופל, בתבנית
   אחידה בכל המסכים (§ט-2). ✅

5. **המסע החוזר:** אותו בוחן. ✅ ⚠ מגבלה מתועדת: הבוחן כבר מכיר את
   המסכים, ולכן המסע מוכיח שיפור בהבנה, ⛔ לא "רושם ראשון". הצ'קליסט
   לפי CP11 ו-§ט-5: שבע רגלי B59 + שאלת הבנה נפרדת לכל אחת מחמש
   תשובות המייסדת + שאלה ל-Overview.

6. **P4S:** הכרעת המוצר רק אחרי האבחון (§ג). ✅

7. **Overview: 5 כרטיסים בשורה אחת, בגובה שווה.** ✅ (פותח במפורש את
   `DESIGN.md:270` ואת P11A-D8; יתועד ב-CP2 כהכרעה מתועדת.)
   - **התנהגות בחלון צר — הוכרעה:** רוחב נדרש: 5×220px + רווחים ≈
     1,164px לתוכן. ⧗ רוחב המיכל בפועל יימדד ב-CP0.

     | רוחב חלון | הצעה |
     |---|---|
     | ≥ 1,280px | 5 בשורה, גובה שווה |
     | 768–1,279px | **5 בשורה עם טקסט מוקטן**, גובה שווה. גודל הטקסט המינימלי נקבע ב-CP2, כדי שהטקסט יישאר קריא ולא ייחתך |
     | < 768px (טלפון) | עמודה אחת |

   - **רוחבי בדיקה (צילום בכל אחד):** 1920 · 1366 · 1280 · 1024 · 390.
   - **קבלה:** ב-1920/1366/1280/1024 — 5 בשורה, גובה שווה, טקסט קריא,
     ⛔ בלי חיתוך, ⛔ בלי גלילה אופקית של הגוף. ב-390 — עמודה אחת.

---

## ז. כותרות, צירים ומקרא — הכול בעברית בגרפים החיים (הוכרע 2026-09-24)

### ז1. גרפים חיים (`charts.js`)

- כותרת ראשית בעברית, וכותרת-משנה אנגלית קטנה (`titleEnglish` הקיים).
- צירים, ערכי ציר ומקרא — בעברית, לפי הנוסחים בטבלה (`xLabel`/`yLabel`
  העבריים כבר קיימים לטבלת ה-fallback). דיוקי ניסוח קטנים נשארים
  אפשריים ב-CP2.

| מסך | כותרת ראשית | כותרת-משנה (EN) | ציר X | ציר Y | ערכים / מקרא |
|---|---|---|---|---|---|
| Overview | שיעור ההמרה לפי רמת הוצאת הפרסום | Conversion Rate by Ad Budget Level | רמת הוצאה חודשית על פרסום | ⇐ נוסח מאושר §יב-2 (R1): "שיעור המרה — כמה מהלידים הפכו לעסקה (%)" | נמוכה (עד ₪1,500) · בינונית (₪2,000–5,000) · גבוהה (מעל ₪5,000) |
| נשירה | כמה לידים נושרים בכל שלב מעקב | Drop-off Rate by Follow-up Stage | שלב המעקב | שיעור נשירה | מעקב 1 … מעקב 5 |
| שיחות | ⇐ נוסח מאושר §יב-2 (R2): "בממוצע, כמה שיחות נדרשו עד סגירת עסקה" | Record Count by Number of Calls to Close | ממוצע שיחות עד סגירה | מספר מקרים | — |
| תקציב | רווח מצטבר צפוי לפי אופן חלוקת התקציב | Expected Profit by Allocation Strategy | אופן החלוקה (קמפיינים × תקציב לקמפיין) | רווח מצטבר צפוי (₪) | ערכים: "100 קמפיינים × ₪500". מקרא: "הקו האנכי בכל עמודה מראה את טווח האומדן (95%, דגימה חוזרת של נתוני האימון): עד כמה האומדן משתנה כשחוזרים על החישוב. הקו יכול לרדת גם מתחת לראש העמודה. זה אינו טווח לרווח שיתקבל בפועל." |

**הכיתובים הגלויים הסופיים** (אושרו על ידי המשתמש 24.09.2026, אחרי
ביקורת Codex נגדית — ר' §יב-2 להנמקה המלאה):

- **Overview**, מתחת לגרף: "**ליד** הוא פנייה של לקוח פוטנציאלי.
  **המרה** היא ליד שהפך לעסקה סגורה. לכל מקרה בנתוני העבר בדקנו איזה
  אחוז מהלידים הפכו לעסקה, ואז חישבנו את הממוצע בכל רמת תקציב."
- **שיחות**, מתחת לגרף: "כל עמודה מראה **בכמה מקרים** בנתוני העבר זה
  היה ממוצע השיחות עד סגירה. נכללו רק מקרים שבהם נסגרה לפחות עסקה
  אחת ({population_n} מקרים)."

⚠ **ערכי ציר Y:** היום **לא מוצגים ערכים מספריים על ציר Y** (רק קווי
רשת, `charts.js:98-110`); הטבלה מתרגמת את **שם** הציר בלבד. ⛔ לא
מוסיפים ערכי Y במסגרת זו בלי הכרעה נפרדת.

### ז2. גרפי REPORT (SVG offline, matplotlib; ביצוע בפאזה 13, ההכרעה נרשמת כאן)

- **הוכרע: אפשרות (א).** כותרת עברית כ-heading ב-Markdown מעל כל
  גרף; כיתוב עברי מתחת שמסביר את הצירים; ה-SVG נשאר כמות שהוא
  (הצירים והמקרא **בתוך** ה-SVG נשארים באנגלית); הכותרת האנגלית שבו
  משמשת ככותרת-משנה. ⛔ בלי תלות חדשה (`python-bidi`), ⛔ בלי יצירה
  מחדש, ⛔ בלי אימון, ⛔ בלי Holdout.
- **היקף "הכול בעברית":** גרפים חיים בלבד. ב-REPORT — עברית **סביב**
  ה-SVG בלבד.
- **קריטריון חזותי (ייכנס לתוכנית פאזה 13):** בתצוגת GitHub של
  `REPORT.md`, לכל גרף: (1) כותרת עברית מיושרת לימין, מיד מעליו ·
  (2) כיתוב עברי מתחתיו שמסביר את X ואת Y · (3) אין כותרת ראשית
  באנגלית שלא מלווה בכותרת עברית · (3א) הכותרת האנגלית שבתוך ה-SVG
  **נראית משנית בפועל** — ההיררכיה מושגת באמצעות רמת ה-heading
  והמרווחים **בלבד**, ⛔ לא הקטנת הגרף · (3ב) **קריאות הגרף נשמרת**:
  ⛔ אסור להקטין את הגרף כדי להבליט את הכותרת — ב-GitHub, ברוחב ברירת
  המחדל, התוויות, הצירים והמספרים בגרף קריאים בלי הגדלה · (4) בדיקה מכנית:
  מספר הגרפים ב-REPORT == מספר הכותרות העבריות שמעליהם.

---

## ח. עיצוב הבלוניות (כרטיסים ושדות) — אחידות

**ממצאי `style.css`** (✔ נקרא; ⧗ יאומת במסך ב-CP0):
- רשתות `auto-fit, minmax` ב-`.capability-index` (:230),
  `.field-grid` (:487), `.results-wrap` (:587),
  `.budget-results-columns` (:836), `.followup-layout` (:869).
- `align-items: start` ב-:588 וב-:837 ⇒ כל כרטיס מקבל את גובה
  התוכן שלו — ההסבר הסביר ל-F3/F6.
- תקרת 3 עמודות ב-`.capability-index` (:230-236, ‏P11A-D8) — ההסבר
  הסביר ל-O1.
- תוויות שדה בשלוש שורות עם `flex-wrap` (:492-502) — ההסבר הסביר
  לחוסר יישור השדות.

**תבנית אחת לכל הכרטיסים:**
1. **כרטיס:** `align-items: stretch` בכל שורה; מבנה פנימי קבוע:
   כותרת ← תוכן ← פעולה בתחתית; אותם ריווח, מסגרת ופינות בכל המסכים.
2. **Overview:** לפי §ו.7 (5 בשורה, גובה שווה, כולל חלון צר).
3. **שדות:** תווית עסקית בשורה אחת; שם טכני + יחידה בשורה קטנה אחת
   מתחתיה, בגובה שמור; תיבות ההזנה על קו אחד.
4. **כרטיסי תוצאה** (P2/P3/P4, ‏P4S): אותה תבנית ואותו סדר שכבות כמו
   בלוק ההמלצה (§ט-2), ובגובה שווה.

**קבלה:** צילומים ברוחבי §ו.7 — גובה שווה בכל שורה, שדות מיושרים.

---

## ט. שפה פשוטה

### ט-1. עקרונות (לכל טקסט **גלוי**)
- משפט = רעיון אחד, עד כ-20 מילים.
- קודם השורה התחתונה, אחר כך ההסבר.
- פעולה מתחילה בפועל: "להמשיך…", "לבדוק…", "לא להעביר…".
- ⛔ מונחים סטטיסטיים רק ב"פירוט טכני".
- ⛔ אין אנגלית בתוך משפט עברי; שם שדה מקורי רק בסוגריים, כשנחוץ.
- כל מספר עם יחידה ומשמעות.
- ⛔ פישוט אינו מחזק טענה; כל הסתייגות מהותית נשמרת.

### ט-2. תבנית אחידה לכל בלוק המלצה (הכרעה 4)

| שכבה | תוכן | תצוגה |
|---|---|---|
| השאלה | שאלת המייסדת, במילים פשוטות | כותרת |
| בקצרה | משפט אחד + המספר המרכזי | גדול, גלוי |
| מה זה אומר | 1–2 משפטים קצרים | גלוי |
| מה לעשות | 1–2 נקודות | תיבה מודגשת, גלוי |
| חשוב לדעת | **כל** הסתייגות מהותית (שמשנה החלטה) | גלוי, בגודל רגיל — ⛔ לא בכתב קטן, ⛔ לא מקופל (`IA.md:980-981`, ‏`SPEC.md:383-385`) |
| פירוט טכני | **רק** ההסבר הטכני/המתודולוגי מהנוסח הקנוני, מדדים, גרסה ומקור — ⛔ לא פעולה, ⛔ לא הסתייגות שמשנה החלטה | ⛔ מקופל |

### ט-3. מילון (טיוטה, לאישור ב-CP2)

| היום | פשוט |
|---|---|
| שיעור הבסיס | הממוצע בנתוני העבר |
| נטייה | הסיכוי |
| אוכלוסיית האימון | הלקוחות בנתוני העבר |
| טווח (P2, ‏conformal) | "טווח שבו אורך החיים **בפועל** נפל ב-{כ-X%} מהמקרים בבדיקה. ⛔ לא הבטחה ללקוח בודד." (X = `interval_details.measured_coverage`) |
| טווח (P6, ‏bootstrap) | "טווח האומדן: עד כמה האומדן משתנה בחישוב חוזר" — ⛔ לא "טווח לרווח" (`SPEC.md:545-548`) |
| OOD | "מחוץ לטווח שהמודל למד" + הטווח |
| תמיכה חלקית | היו מעט מקרים דומים בעבר |
| n (תקציב) | "מספר שורות האימון ברמת התקציב הזו" |
| מכויל / Bootstrap / ROC-AUC / Holdout | רק בפירוט הטכני |
| רשומה | "שורה בנתונים" (⛔ לא "לקוח") |
| `100x500` | 100 קמפיינים של ₪500 |

### ט-4. נוסחים מאושרים

⚠ המספרים `{}` נשלפים ממקור מזוהה בזמן ריצה, ⛔ אינם מוקלדים.

**תקציב** (מול `DESIGN.md:457` ו-`SPEC.md:539-578`):
- **בקצרה:** הדירוג לבדו אינו מכריע בין שתי דרכי החלוקה המובילות.
- **מה זה אומר:**
  - בתחזית המודל, 100 קמפיינים של ₪500 ({₪789,594}) מדורגים לפני
    25 קמפיינים של ₪2,000 ({₪530,953}).
  - אבל טווחי האומדן של שתיהן **חופפים** — אין כאן מבחן שמוכיח הבדל
    או שוויון.
- **מה לעשות:**
  - לא להעביר את מלוא ה-₪50,000 לפי הדירוג.
  - אם בוחנים שינוי, להתחיל בניסוי **בהיקף מוגבל**, בקמפיינים של
    ₪2,000, ולמדוד את הרווח המצטבר בפועל לפני שמרחיבים.
- **חשוב לדעת** (שלושה משפטים, כולם גלויים — הנוסח הסופי, מאושר
  24.09.2026):
  1. "**למה לא פשוט לבחור בדרך הראשונה?** החישוב שלה לא יציב. כשחוזרים
     עליו, התוצאה משתנה מאוד, ולפעמים יוצאת נמוכה כמו של הדרך
     השנייה. לכן אי אפשר לקבוע שהיא באמת טובה יותר."
  2. "**בדקנו את המודל על נתוני עבר:** בתקציב של ₪500 הוא חזה רווח
     ממוצע גבוה פי {8.6} ממה שהיה בפועל. בתקציב של ₪2,000 התחזית
     הייתה קרובה למציאות."
  3. "**המספרים הם הערכה** של הרווח הכולל שהלקוחות יביאו לאורך זמן.
     זה לא הרווח של החודש הבא, וזו לא הבטחה."

**Follow-up** (מול `DESIGN.md:459` ו-`findings.json.calls_to_closed`):
- **בקצרה:** אין בנתונים בסיס להפסיק לעקוב אחרי המעקב השלישי.
- **מה זה אומר:** בכמעט מחצית ({48%}) מהשורות שבהן נסגרה עסקה, ממוצע
  השיחות עד סגירה היה 4 או יותר.
- **מה לעשות:**
  - להמשיך במעקבים 4 ו-5 באופן מבוקר.
  - לבדוק בכל שלב כמה עסקאות נוספות נסגרות, כמה זמן זה לוקח ומה
    העלות, לפני שמשנים מדיניות.
- **חשוב לדעת:** זה ממוצע לשורה בנתונים, לא היסטוריה של עסקה בודדת.
  הנתונים מראים מה קרה, לא למה.

**OOD ב-P4S** (S3): "הערך {₪50,000} בשדה {תקציב פרסום} מחוץ לטווח
שהמודל למד ({₪500–₪20,000}), ולכן לא חושב ציון. אפשר להזין תרחיש חדש
שלא הופיע בעבר, כל עוד כל הערכים בטווח ותקינים." (נגזר מ-`warnings[]`,
לכל שדה שחרג.)

**P4S** (הציון עצמו): ⛔ הנוסח לא ייכתב לפני האבחון (§ג) והכרעת המוצר.

### ט-5. תהליך ואימות
1. **טבלת נוסחים מלאה** ("היום ← מוצע", כל טקסט גלוי בששת המסכים)
   לאישור המשתמש (CP2). אחר כך `DESIGN.md` §6.1, ורק אז קוד.
2. **בדיקה מכנית** (CP8): אורך משפט בשכבות הגלויות · רשימת מונחים
   אסורים · "בקצרה" ו"מה לעשות" **גלויים** (visibility, ⛔ לא
   `textContent` — ר' `live/test_03_capabilities.py:299-300`).
3. **בדיקה אנושית** (CP11): שאלת הבנה נפרדת לכל אחת מחמש תשובות
   המייסדת — (1) P2 · (2) P3 (⚠ חולקות מסך עם P2, שאלה נפרדת) ·
   (3) P4S · (4) תקציב · (5) Follow-up — ועוד שאלה ל-Overview. הבוחן
   עונה במילים שלו: "מה זה אומר, ומה כדאי לעשות?".

---

## י. מיפוי מול `REQUIREMENTS.md` (חובת שער לפי CLAUDE.md)

**עיקרון:** שינוי תצוגה ⛔ אינו מבטל ראיית **מקור** (מדד, חישוב,
תשובה כתובה, API). הוא מחייב לאסוף ראיית **ממשק** חדשה רק לשורות
שבהן הראיה הקיימת היא הצגה במסך. בשער: ⛔ אפס שינוי `status` ו-⛔ אפס
שינוי `owner`.

| שורה | סטטוס היום | מה 12A משנה | ראיית מקור (נשארת תקפה) | ראיית ממשק שתיאסף מחדש | `contributors += 12A` |
|---|---|---|---|---|---|
| **B55b** | planned | כל חמש התשובות: שפה, תבנית | — | מסע CP11 עם שאלות הבנה; ⚠ רק אחרי הכרעת P4S (§ג). ⛔ אין סגירה אוטומטית | ✅ |
| B28 (P2) | done | תוצאה, תבנית, דוגמאות | API/סכמה (CP4 בפאזה 12) | בקשה → תוצאה גלויה (CP10) | ✅ |
| B36 (P3) | done | כנ"ל | כנ"ל | כנ"ל | ✅ |
| B41 (P4S) | done | תבנית, דוגמאות, OOD, הצגת הציון | API + `model_version` (CP4 בפאזה 12) | A4 + תוצאה גלויה (CP10) | ✅ |
| B42 (פרופיל לקוח-על) | done | כרטיס ההקשר במסך P4S (שפה) | `metrics.json.super_customer_profile` | כרטיס גלוי ומדויק (CP10) | ✅ |
| B45 (המלצת Follow-up) | done | נוסח ותבנית (K1, ‏U4) | — (הדרישה היא הצגה בדשבורד) | המלצה גלויה בתבנית החדשה (CP10) | ✅ |
| B50 (המלצת תקציב) | done | נוסח (§ט-4), מקרא | — (הדרישה היא הצגה) | שער P12-D9 + נראות (CP10) | ✅ |
| B52 (סימולטור) | done | גרף, תוויות | API (`/api/simulate/budget`) | גרף גלוי וקריא (CP10) | ✅ |
| B12 (RLS) | done | שאילתת הדוגמאות (`in.(...)`) | RLS ב-DB + insights (ק' 72–73) — ⛔ לא נפגעת | `live/test_02` מותאם + בדיקת דפדפן (§ו.2) | ✅ |
| B9 (מסך התחברות) | done | שורת פתיחה ב-Login (G1) | מסך ההתחברות (פאזה 4) | ⚠ נגיעה חלקית: אימות שה-Login עובד כקודם | ✅ |
| B56 (דשבורד מאחורי לוגין) | done | מעטפת/שורות פתיחה | שער ההתחברות (CP2 בפאזה 12) | ⚠ נגיעה חלקית: אותו אימות | ✅ |
| B21 (המרה לפי טייר) | done | תוויות הגרף, הסבר המדד | `findings.json.budget_tiers` + view — ⛔ לא נפגעת | גרף גלוי (CP10) | ✅ |
| B44 (נשירה, ויזואלית) | done | תוויות הגרף | `findings.json.funnel_dropoff` — ⛔ לא נפגעת | גרף גלוי (CP10) | ✅ |
| B46a (שלב נשירה חריג) | done | נוסח "תשובה" מתחת לגרף הנשירה (U2, ‏K1) | `findings.json.funnel_dropoff` + התשובה הכתובה — ⛔ לא נפגעות | הנוסח המוצג נאמן לתשובה הכתובה | ✅ |
| B46b (כמה מעקבים לעסקאות שנסגרו) | done | נוסח "מספר שיחות" (U3) + כותרת הגרף (§ז1) | `SPEC.md` §CP4-D + ‏`findings.json.calls_to_closed` — ⛔ לא נפגעות | כנ"ל | ✅ |
| B46c (לשנות מדיניות?) | done | נוסח ההמלצה (U4, §ט-4) | `SPEC.md` §CP4-D + ‏FINDINGS §מסקנת P5 — ⛔ לא נפגעות | כנ"ל | ✅ |
| B24 (האם ההמרה מפתיעה) | **gap**, owner 5 | נוסח O5 המוצג | — | ⚠ **נגיעה חלקית בלבד**: תיקון המשפט המוצג. ⛔ התשובה הכתובה נשארת להשלמה בפאזה 13 (תורמת), ⛔ לא נסגרת כאן, ⛔ owner נשאר 5 | ✅ |
| B53a (לרכז או לפזר) | **gap**, owner 13 | נוסח התקציב המוצג (§ט-4) | — | ⚠ **נגיעה חלקית בלבד**: הנוסח המוצג. ⛔ התשובה הכתובה ב-REPORT נשארת בפאזה 13 | ✅ |
| B53b (מה לומר למייסדת) | **gap**, owner 13 | נוסח "מה לעשות" בתקציב (§ט-4) | — | כנ"ל | ✅ |
| B59 | planned | מסע חוזר (7 רגליים) | מסע CP7 בפאזה 12 (ממשק ישן) | מסע CP11 על הממשק החדש. נשארת `planned` עד README בפאזה 13 | ✅ |
| B43 | **gap**, owner 13 | ⛔ ממצא ה-Mid אינו תשובה מוכחת | — | — (⛔ 12A לא משנה אותה) | ⛔ |

**⛔ לא בנגיעה של 12A, ללא שינוי:** B1, ‏B6, ‏B18, ‏B22, ‏B23,
‏B29b/c, ‏B32, ‏B33, ‏B37a–d, ‏B57, ‏B60, ‏B65, ‏B66 — פאזה 13. B54
(parent), ‏B58 (N/A). `contributors += 12A` **לא** יתווסף להן.

**20 מזהי דרישה** יקבלו `contributors += 12A`: B55b, ‏B28, ‏B36,
‏B41, ‏B42, ‏B45, ‏B50, ‏B52, ‏B12, ‏B9, ‏B56, ‏B21, ‏B44, ‏B46a,
‏B46b, ‏B46c, ‏B24, ‏B53a, ‏B53b, ‏B59.

**דרישת בריף שאינה מכוסה:** אין חדשה. ממצאי הרישום של נספח ב נשארים
לשער 12A→13.

**סטיות מאושרות (⛔ לא לדיון):** Render במקום Railway (B64), בלי
notebook, בלי סרטון דמו (B58).

---

## יא. תוצרי השער 12→12A

**שלב א — לפני ביקורת ואישור סופי** (עדכון מסמכי התכנון הוא חלק
מהשער, ⛔ לא ביצוע הפאזה):
1. בדיקת מצב בפועל — ר' "מצב הפתיחה" למעלה.
2. `docs/planning/PHASE12A.md` — מסמך זה.
3. `docs/planning/REQUIREMENTS.md` — `contributors += 12A` **רק** ב-20
   המזהים לעיל. ⛔ אפס שינוי `status`/`owner`/`evidence`. הספירות
   49/8/15/1/1 נשמרות.
4. `docs/planning/codex-review.md` — רשומת יומן: ביקורת **התכנון**
   אושרה (24.09.2026), ולאחריה ביקורת **המסמכים שנערכו** אושרה גם
   היא (24.09.2026, אחרי שלושה סבבי תיקון ממוקדים — ר' יומן ההכרעות
   לפירוט). ⚠ שני האישורים הם אישורי ביקורת בלבד — אינם אישור משתמש
   סופי ואינם הוראת ביצוע.
5. `ROADMAP.html`: לשונית "תמונת מצב לתכנון" (מצב פעיל מעודכן, היסטוריה
   נשמרת) · כרטיס פאזה 12A חדש · תלות פאזה 13 ב-12A · תיקון התצוגה
   הדינמית (`planning_in_progress`) · `node --check` + אימות בדפדפן +
   `git diff`.
6. אחרי עריכת ROADMAP: הסרת הקביעה "לשונית התכנון מיושנת" מנספח ב.
7. החזרת ה-diff לביקורת Codex על המסמכים. ⛔ בלי git, ⛔ בלי ביצוע.

✅ **שלב ב — אישור סופי של המשתמש, 24.09.2026.** `planning_status:
planning_in_progress ⇒ approved_for_execution`.

**שלב ג — בביצוע, כל צעד באישור נפרד:** commit ייעודי → push → PR →
CI → merge, ואז CP0 וביצוע הפאזה.

---

## יב. הערות תכנון שנשמרות (רקע להכרעות §ו/§ז/§ט)

במהלך התכנון זוהו כמה מקומות שבהם דיוק עובדתי שהוצע לשכבה **הגלויה**
(מה שהמשתמש רואה במסך) התנגש עם מטרת הפאזה — שפה פשוטה, "actually
answers" (B55b). העיקרון שיושם: הדיוק נשמר, אך עובר לכיתוב קצר או
לפירוט הטכני; בשכבה הגלויה נכתבת אמת פשוטה, ⛔ בלי לחזק טענה.

לאחר ביקורת Codex נגדית על ההצעות, ההכרעות הבאות ננעלו (24.09.2026):

- **ציר Y ב-Overview ומדד ההמרה:** "שיעור המרה — כמה מהלידים הפכו
  לעסקה (%)", עם כיתוב שמגדיר "ליד" ו"המרה" (⛔ לא "ממוצע לשורה"
  בציר עצמו). המדד עצמו (ממוצע יחסים לשורה, ⛔ לא סך÷סך) מאומת מול
  `views.sql:46` — מקור הגרף החי בפועל — ותואם ל-`analysis.py:82-96`.
- **כותרת גרף השיחות:** "בממוצע, כמה שיחות נדרשו עד סגירת עסקה" —
  המילה "בממוצע" נשמרת כדי לא ליצור רושם של מניין שיחות לעסקה בודדת.
  אוכלוסיית `closed>0` מוסברת בכיתוב, ⛔ לא בכותרת.
- **גרעיניות בתקציב:** "קמפיין" הוא המונח הנכון לחלופות הסימולציה
  (SPEC:549-556, אדיטיביות); בדיקת העבר משווה תחזית לתוצאה **ברמת
  רשומה בודדת**, ולכן הנוסח נמנע מלכנות כל רשומת עבר "קמפיין"
  ("בתקציב של ₪500 המודל חזה רווח **ממוצע**…"). הגרעיניות עצמה נשארת
  הסקה בלבד (`SPEC.md:180-191`), לא הכרעה.
- **הסתייגויות מהותיות בתקציב:** שלוש, לא אחת — `IA.md:980-981` אינו
  קובע מכסה של הסתייגות יחידה. גם חפיפת הטווחים וגם פער בדיקת העבר
  משנים את ההחלטה, ולכן שתיהן גלויות, יחד עם ההבהרה שהסכום אינו
  הבטחה.
- **מנגנון בחירת הדוגמאות:** נשמר מלא (קובץ מקומט + hash מקומי +
  `live/test_02` בשלושת מצבי ההרשאה), ⛔ לא צומצם לרשימת מזהים בלבד —
  כי A1 (האבחון המקומי) מנקד את ערכי ה-CSV, ו-A4 (האבחון החי) מקליד
  את אותם ערכים בדפדפן; בלי אימות שה-CSV==Supabase, האבחון לא היה
  משקף בהכרח את מה שהמשתמש רואה.
- **B43 ("How could Northbound spot them earlier?"):** ⚠ חבילה 4
  בבריף מבקשת **גם** פרופיל תיאורי (B42) **וגם** צינור ציון לזיהוי
  מוקדם על נתוני לקוח חדש (B41, `FunnelIQ_Assignment.html:316-317`) —
  B43 עצמה היא שאלת ההמשך "איך לזהות מוקדם", ואינה מסתפקת בפרופיל
  לבדו. ממצא ריכוז לקוחות-העל ב-Mid, **אם A2 יאשר על train**, עשוי
  **לתרום** לתשובה תיאורית חלקית, אך ⛔ **אינו מחליף** יכולת זיהוי
  מוקדם ואינו סוגר את B43 — ר' נספח ב.

---

## נספח ב — פאזה 13: תכנון-על בלבד, מוקפא עד שער 12A→13

**תוצרים:** README (EN) · REPORT (HE) · `schema.sql` · גילויי
P12-D8/D13 · קריטריון חזותי לכותרות REPORT (§ז2).

**מועמדים לבדיקה מחדש בשער 12A→13:**
- B43: ריכוז לקוחות-העל ב-Mid (אם A2 יאשר על train) — מועמד לתשובה
  תיאורית. לבחון שיעור זיהוי, שיעור סימוני שווא ומועד זמינות הנתונים.
  ⛔ B43 אינה נסגרת מכוח A2 בלבד.
- מנגנון מספרים מתבנית (README/REPORT).
- `schema.sql` משרשור המיגרציות.
- תשובות חבילה 1 ב-`FINDINGS.md`.
- עקומת כיול P4S מנתונים שמורים.

**ממצאי רישום שנשמרים:**
- B18 חסר תורם 13.
- הסיכום הכמותי בתחתית `REQUIREMENTS.md` מיושן (יעודכן בנפרד משער
  זה, כי לא נגענו בו כאן — מחייב עדכון עתידי כי 12A מוסיפה 20 שורות
  תורמים אך לא משנה ספירות).
- `verifier=12` לשורות owner=13 אינו בר-ביצוע (פאזה 12 כבר סגורה).
