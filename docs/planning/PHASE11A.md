# פאזה 11A — יישור יישום מול חוזה התכן (`fix/phase11a-remediation`)

נכתב 19.09.2026 בעקבות ביקורת רוחב של קודקס על `6e36c17..af07d6f`
ותשעה סבבי ביקורת על התכנון עצמו. מקור האמת לדרישה הוא
`FunnelIQ_Assignment.html`; לתכן — `SPEC.md`, `docs/DESIGN.md`,
`docs/IA.md`; מרשם העקיבות היחיד הוא `REQUIREMENTS.md`.
⛔ מסמך זה אינו יוצר טבלת עקיבות מקבילה.

**סטיות מאושרות, מצוינות מחדש:** Render ולא Railway (היתר מפורש בבריף
§07) · ⛔ בלי notebook.

⚠ **פאזה 11 נשארת `execution_status: done`** — כפי שפאזה 10 נשארה סגורה
כשנפתחה 10A. ⛔ אין להחזירה לאחור. ⛔ אין להציג את שער 11→12 כפתוח כל
עוד 11A אינה סגורה; `ROADMAP.html` נושא כבר לוגיקה כזו מ-10A
(שורות 955/960/965/1135) ו-11A מקבלת רשומה ולוגיקה אנלוגית.

---

## א. מצב הפתיחה — נמדד, לא הונח

נמדד 19.09.2026 על `main` ב-`af07d6f`, קריאה בלבד, ללא credentials.

| נבדק | תוצאה |
|---|---|
| `main` | פאזה 11 מוזגה (PR #29, `c182b4b`); תכנון פאזה 12 מוזג (PR #30) |
| `tests/` | **794 בדיקות נאספו** — ⚠ מלאי שנמדד, ⛔ לא טענת מעבר ולא תוצאת הרצה |
| `e2e/` | **30 מקרי הפרכה**, ממומשים ב-32 פונקציות, ועוד 3 בדיקות תשתית = **35 פונקציות** בתשעה קבצים |
| `docs/design/states/` | 21 צילומים |
| המרשם | **74 שורות (B1–B66)**: 41 `done` · 16 `planned` · 15 `gap` · 1 `N/A` · 1 `parent`. ⚠ 74 הוא מספר שורות, ⛔ לא "74 דרישות פעילות" — `B58` היא `N/A` ו-`B54` היא `parent` |

### א1 — שלושת פערי היישום שהתגלו

**1. אמינות ה-degradation.** `facts.js:39` מאמת `key in data` בלבד ⇒
`{}` ובלוק חלקי עוברים; ⛔ אפס בדיקת שדה מקונן ⛔ ואפס numeric guard.
תוצאה: `NaN%` ב-`super-customer.js:472-477`, `"פי NaN"` ב-`budget.js`.
בנוסף — ו⚠ זה הלב — שלושה מסכים מדפיסים במצב מושפל טענות שהנכס אמור
לבסס: `predict.js:568-569` · `super-customer.js:467-471` ·
`budget.js:277/282/285`.

⚠ **האבחנה המדויקת:** `budget.js:285` ⛔ **אינו ממציא משפט** — הוא
מדפיס את תא התשובה הקנוני של שורת `Budget Simulator` במטריצת D9
(`DESIGN.md:457`) ללא תלות בזמינות הנכס. אותו דפוס ב-`P4S` (456)
וב-`P2` (453). ⇒ הפער אינו בנוסח ההצלחה אלא ב**היעדר חוזה degradation**.

**2. חוזה הגרפים.** כל ארבעת קוראי `renderBarChart` מעבירים שמות צירים
**בעברית**, והם מגיעים רק לטבלת ה-fallback. ⛔ אין כותרת גרף בשום שפה;
מקרא קיים ב-1 מ-4 ובעברית. `DESIGN.md:300-304` ו-`IA.md:957` מחייבים
אנגלית לכותרות/צירים/מקרא/שמות feature.

**3. פריסה.** `total_budget` אינו מוצג אף ש-`DESIGN.md:403` ו-`IA.md:792`
ממפים אותו לכותרת `₪50,000` · `.results-wrap` (`style.css:556-561`) הוא
`flex-direction: column` מול "שלוש קבוצות זו לצד זו" (`DESIGN.md:271`) ·
Overview מרנדר 5 כרטיסים בשורה מעל ~1212px מול "⛔ לא שורה קשיחה אחת של
חמישה" (`DESIGN.md:270`) · למסך Budget אין grid פריסה מול "שתי עמודות
בלבד" (`DESIGN.md:273`). ⚠ מחמיר: `style.css:827-833` **כן** מימש את
אותו ניסוח עבור Follow-up וציטט את המסמך בהערה — הדפוס הובן ולא הוחל.

### א2 — אבחנת השורש

כל פער יושב על **מסלול ההצלחה**, ופאזה 11 אימתה כמעט רק כשלים: 21
הצילומים הם bootstrap 401/403/500/503, loading, error, OOD ו-prefill —
⛔ **אפס מסך מוצלח**; ו-⛔ אין בכל הריפו assertion אחד על
`.summary-recommendation` או `.business-context-card`. ⛔ זה אינו כשל
ביצוע של CP12 אלא פער בהגדרת המטריצה עצמה.

### א3 — רשומות ראיה שמקדימות את המציאות

`ROADMAP.html:871` (CP9) טוען אימות שפת גרפים אף שאימת פריט אחד מארבעה
ב-`IA.md` §10 · `ROADMAP.html:873` (CP11) טוען "כיסוי מלא" בעוד
`e2e/test_07_followup_and_facts_failure.py:5-6` כותב שהצרכן השלישי
*"left for a follow-up batch"*.

---

## ב. גבולות הפאזה

### בתחום
תיקון שלושת הפערים ב-frontend · בדיקות שתופסות אותם, כולל **fixture של
בלוק חלקי** ו-**fixture תקין** · צילומי מצב מוצלח · תיקון רשומות הראיה ·
שמונה תיקוני התיעוד שהוסכמו בסבב הראשון על תכנון פאזה 12.

### מחוץ להיקף
⛔ שינוי API, סכמות, מודלים או ארטיפקטים · ⛔ אימון · ⛔ פתיחת Holdout
(S9) · ⛔ עיצוב מחדש · ⛔ `README.md`/`REPORT.md` (פאזה 13) · ⛔ ראיית
קבלה חיה (פאזה 12, מוקפאת) · ⛔ פתיחת `P11-D1`–`P11-D15` מחדש ·
⛔ חיבור `getFollowupContext` לצרכן (החלטת המשתמש מ-CP11 עומדת).

### ב1 — חריג מצומצם ויחיד לכלל "אין לשנות את מסמכי התכן"
מותר **להוסיף** ל-`DESIGN.md` §6.1 נוסחי degradation חסרים, ול-`IA.md`
תיעוד התנהגות + הפניה. ⛔ אסור לשנות נוסח מצב-תקין, כלל תכן קיים או
מבנה מטריצת D9.

⚠ **הנימוק:** `DESIGN.md:428-432` כבר קובע שני מקורות מותרים בלבד
ו*"כשהראיות חלשות — אומרים במפורש שאין המלצה חד-משמעית ומציעים בדיקה
ידנית, איסוף נתונים או ניסוי מבוקר"*. ⇒ **ההתנהגות כתובה; רק הנוסח
הפרטני חסר.** זהו מילוי פער מוצהר, ⛔ לא עיצוב מחדש.

---

## ג. הכרעות — בסדר תלות

### `P11A-D1` — תקן ההשפלה (רוחבי; כל היתר נגזרות שלו)

`DESIGN.md:102` ו-`:419` קובעים ש-`summary-recommendation` הוא רכיב בן
**ארבע שכבות** — תשובה · מה זה אומר · מה כדאי לעשות · חשוב לדעת —
וש**ארבעתן גלויות**; שורה 434 מוסיפה שהשורה חובה.

⇒ ⛔ **"להסתיר" אסור.** במצב מושפל ארבע השכבות נשארות קיימות ולא-ריקות,
ומנוסחות מחדש כך ש**לא תיטען בהן שום טענה שהנכס אמור לשמש לה ראיה**.

**שלושת המקורות המותרים בנוסח מושפל — רשימה סגורה:**
1. תגובת ה-API החיה.
2. הגדרות היעד/האוכלוסייה הנעולות.
3. ⚠ **מצב הזמינות של הנכס עצמו** — מותר לומר "הראיה אינה זמינה".
   ⛔ אך לא תוכן, ערך או מסקנה מתוכו. ההבחנה: *שהראיה חסרה* ⛔ אינו
   *מה שהראיה הייתה אומרת*.

⚠ **המלכודת שהקוד הקיים נפל בה:** `super-customer.js` **כן** החליף
נוסח — וההחלפה המשיכה לטעון את מסקנת הנכס. "החלפנו ניסוח" ⛔ אינו
הסטנדרט; **"אינו טוען דבר שהנכס היה צריך לבסס"** הוא.

### `P11A-D2` — ולידציה טיפוסית פר-בלוק ב-`facts.js`

`block()` יחזיר `null` על בלוק **חלקי** בדיוק כמו על חסר. לכל בלוק חוזה
מפורש: טיפוס ומפתחות נדרשים · בדיקה מספרית **רק** לשדות מספריים ·
⛔ דחיית מערכים (`typeof [] === "object"` ⇒ נדרש `Array.isArray`) ·
**מכנה חיובי**: `actual_mean_per_customer > 0` ⛔ ולא רק סופי, שכן
`Number.isFinite(0)` הוא `true` ו-`x/0` הוא `Infinity` ⛔ ולא `NaN`.

⛔ **`Number.isFinite` גורף בלתי-ישים:** `ltv.dominant_feature` מחרוזת ·
`ltv.rank_1_by_algorithm` אובייקט מקונן של `{feature, importance}` ·
`followup_context.population_definition` ו-`super_customer_profile.population_definition`
מחרוזות. ולידציה מספרית גורפת הייתה **פוסלת נכס תקין**.

⚠ `getSuperCustomerProfile`/`getFollowupContext` נשארים ללא בדיקת
`model_versions` — נכון לפי `PHASE11.md` ד3 (אין תלות מודל). ⛔ אין
לשנות; הוולידציה המבנית נוספת מתחת.

⚠ **`D2` הכרחית אך ⛔ אינה ממצה:** היא פותרת את מחלקת ה-`NaN`. דלף
במחרוזת fallback כתובה ביד ⛔ אינו מסלול `NaN` ⇒ `D4`–`D6` נדרשות
בנפרד.

### `P11A-D3` — מקור קנוני יחיד לנוסחי ההשפלה

הנוסח חי ב-**`DESIGN.md` §6.1 בלבד**. ב-`IA.md` מתועדת ההתנהגות עם
הפניה. ⛔ אין לשכפל נוסח מילולי בשני מסמכים.

**חוזה הטבלה החדשה:**
- תת-סעיף נפרד **אחרי** סיום טבלת §6.1, בכותרת Markdown רגילה שמפסיקה
  בוודאות את החילוץ הקיים.
- כותרת ייחודית, למשל `יעד נעול | שכבת D9 | נוסח מושפל קנוני | מקור מותר`
  — ⛔ **אינה** תואמת ל-`_D9_MATRIX_HEADER_ROW` (`test_design_tokens.py:666`,
  הדורש שש כותרות כולל "שאלת").
- **בדיוק 12 שורות** = שלושת המזהים הנעולים הקיימים `P2` · `P4S` ·
  `Budget Simulator` × ארבע שכבות. ⛔ לא מושג חדש בשם "מסך".
- מפתח ייחודי `יעד+שכבה`; כפילות, חוסר או תא ריק ⇒ **כשל**.
- **placeholders מקבוצה סגורה ומוטיפסת**, בשמות חד-משמעיים של שדות API
  והגדרות נעולות. ⛔ **אף placeholder לא ימופה לשדה ב-`business_facts.json`**
  — זו האכיפה המבנית של `D1`: שורה מושפלת ⛔ *אינה יכולה* לבטא טענה
  תלוית-נכס.
- ⛔ **אין לרשת את מוסכמת ה-placeholders של מטריצת ההצלחה**: שם `{X}`
  הוא חודשים ב-P2, אחוז ב-P3 וציון 0–100 ב-P4S, ויש free-form כמו
  `{הרמה בעלת שיעור ההמרה הגבוה בתגובה, או שוויון}` — ⛔ אינו בר-הצבה מכנית.
- עמודת **מקור מותר** מדגימה שהנוסח נשען **רק על שלושת המקורות הסגורים
  של `D1`** — תגובת API · הגדרות נעולות · מצב הזמינות של הנכס.
  ⚠ תיעוד לביקורת אנושית, ⛔ אינה נאכפת מכנית.
- ⛔ אין לכתוב פרוזה: החילוץ חייב להיות דטרמיניסטי.
- שימוש חוזר ב-`_extract_markdown_table` (`test_design_tokens.py:201`)
  עם regex ייעודי. ⛔ אין פרסר חדש.

**שרשרת הקבלה:** ביקורת אנושית מאשרת את הנוסח **במסמך** ⇒ המימוש
מקודד אותו ⇒ הבדיקה מוכיחה שהמימוש **מסונכרן למסמך** ⇒ fixture תקין
מוכיח שההשפלה ⛔ אינה מופעלת תמיד.

### `P11A-D4` — P2 (`predict.js`), נגזרת `D1`

`:568` (`meaning` — *"מספר השיחות הממוצע עד סגירה היה האות החזק ביותר"*)
ו-`:569` (`action` — *"לבחון שינוי במדיניות השיחות"*) **שניהם** תלויים
ב-`ltv.dominant_feature`. במצב מושפל **שניהם** מוחלפים בנוסח מ-§6.1.
⛔ אין להשאיר את `:566-571` פתוח לצד ה-tip הממוגן ב-`:553-564` — אם
הטענה בטוחה ללא הנכס, הגנת ה-tip היא תיאטרון; אם אינה, העותק הפתוח
הוא דלף.
✅ נשארים: התחזית, הטווח, והפעולה הגנרית לתכנון/פילוח.

### `P11A-D5` — P4S (`super-customer.js`), נגזרת `D1`

`buildBusinessContextCard()` (`:447-449`) תקין — מחזיר `null`.
⛔ `buildD9Meaning()` (`:467-471`) מחזיר במצב מושפל *"לקוחות-על הניבו
רווח גבוה יחסית מתוך עלות רכישה נמוכה יחסית"* — זו בדיוק מסקנת
`super_customer_profile`. מוחלף בנוסח מ-§6.1.
✅ נשארים: הציון החי, הגדרות היעד והאוכלוסייה.

### `P11A-D6` — Budget (`budget.js`), נגזרת `D1`

`answer:` (`:285`) עובר **לתוך** הענף הממוגן. `:277`/`:282` ⛔ אינם
ממליצים על פיילוט — `DESIGN.md:547-548` אוסר מילולית *"אין להחליפם
בהמלצה לפי rank בלבד"*. שכבת הפעולה המושפלת מורה ⛔ לא לקבל החלטת
הקצאה בלי הראיה החסרה.

✅ **נשארת טבלת ארבע האסטרטגיות, כולל עמודת ה-`rank`** —
`budget.js:186` מרנדר `s.rank` מתוך `BudgetSimulation.strategies`
(`schemas.py:477`, `ge=1 le=4`), וזה נתון API לגיטימי.
⚠ **דיוק:** האיסור הוא על **טענת דירוג בתוך D9 המושפל** ועל המלצה
הנגזרת ממנה, ⛔ **לא** על הצגת `rank` בטבלה. ⛔ אין יחס הערכת-יתר
⛔ ואין מסקנת backtest.

### `P11A-D7` — חוזה הגרפים הוא כלל **שפה**

לתוך ה-SVG: שמות צירי X ו-Y **באנגלית** וכותרת גרף **באנגלית**; מקרא
אנגלי לכל גרף עם whisker (`DESIGN.md:300-304` מחייב "מקרא טקסטואלי").
⚠ **ערכי tick של ציר Y ⛔ אינם חלק מהחוזה** — `IA.md:957` אומר "שמות
הצירים" ⛔ ולא ערכים, ⛔ ואין לרשום את היעדרם כהפרה.
⛔ **11A אף אינה מתחייבת להוסיף אותם** — פער איכות ידוע ומודע, שנשאר
פתוח. ⚠ הנימוק: הבטחה ללא קריטריון קבלה ומקרה הפרכה היא בדיוק הדפוס
שהפאזה הזו מתקנת. ⇒ ⛔ אין לכך שורה ב-`CP6` ⛔ ואין מקרה הפרכה.
טבלת ה-fallback נשארת בעברית (`charts.js:15-20`).

### `P11A-D8` — פריסה

`total_budget` ⇒ כותרת `₪50,000` (`DESIGN.md:403`) · `.results-wrap`
⇒ שלוש קבוצות זו לצד זו (`:271`) · Budget ⇒ שתי עמודות (`:273`) ·
Overview ⇒ תקרת 2–3 טורים, ⛔ לעולם לא 5 בשורה ברוחב Desktop (`:270`).

### `P11A-D9` — היררכיית כותרות

`h1` יחיד לכל מסך גלוי, ו-`h2` עקבי מתחתיו. היום כל הכותרות הן `h3`,
ה-`h1` היחיד יושב ב-`#login-section`, ול-`budget.js` אין כותרת כלל.

⚠ **שתי שכבות שיש להפריד:**

| | מעמד | היכן |
|---|---|---|
| **קיום** כותרת | **הפרת חוזה** | Budget (`DESIGN.md:403` ⇒ כותרת המסך היא `₪50,000`) · Overview (`IA.md:50`: *"היררכיה: כותרת → אינדקס חמש היכולות → …"*) |
| **רמה** סמנטית (`h1`/`h2`) ושאר המסכים | **שיפור נגישות** | `DESIGN.md`/`IA.md` שותקים על רמות כותרת |

⛔ **נעילה למניעת כותרות מתחרות:** ב-Budget יהיה **`h1` יחיד** הנושא
את הערך מ-`DESIGN.md:403`, כך ש-`D8` ו-`D9` ⛔ לא ייצרו שתי כותרות.
נוסחו המדויק נקבע באותו אופן קנוני כשאר נוסחי המסך.

⚠ **ההתחייבות ל-`h2` נשמרת ⛔ ואינה נמחקת — אך חייבת קריטריון.**
נשקלה הסרתה (כפי שנעשה לערכי ציר Y ב-`D7`) ⛔ ונדחתה: `h1` בלבד לצד
`h3` קיימים יוצר **דילוג רמה**, שהוא פגם נגישות בפני עצמו — כלומר
ההסרה הייתה משאירה את המסמך מתחייב לשיפור ומייצרת ליקוי חדש.
⇒ הקריטריון המכני: בכל מסך גלוי **`h1` אחד בדיוק**, ⛔ ואפס דילוגי
רמה — ⛔ אין `h3` בלי `h2` שקודם לו באותו מסך.

### `P11A-D10` — ההפניה ל-`REPORT.md`

`predict.js:653` ו-`:680`, אותה מחרוזת, בתוך `buildModelDetails`.

⛔ **ההסרה נפסלה.** `IA.md:300` מחייב *"הפניה טקסטואלית לעקומת הכיול
ב-`REPORT.md`"*, ו-`:335` חוזר על כך (*"ה-`<details>` מפנה אליהם בטקסט
בלבד"*). מחיקת ההפניה הייתה יוצרת **סטייה חדשה** מהתכן שהפאזה מיישרת.

⛔ **גם החלופה של קודקס — הרחבת חריג §ב1 כדי לתקן את IA — נפסלת.**
⚠ אין כאן פגם ב-IA: ההחלטה שעקומת הכיול חיה בדוח ושהממשק מפנה אליה
בטקסט היא החלטה מכוונת, חוזרת בשלושה מקומות (`:300`, `:335`, `:987`).

✅ **ההכרעה — ניסוח מחדש, ⛔ ללא שינוי תכן כלל:** ההפניה הטקסטואלית
נשארת (מקיים את IA), אך מנוסחת בשפת מוצר ובלי לטעון קיום:
**"עקומת הכיול המלאה אינה מוצגת כאן; מקומה בדוח הפרויקט."**
⚠ ⛔ **לא** *"מתועדת בדוח הפרויקט"* — זו טענת הווה על מסמך שטרם קיים.
⚠ שני רווחים נסגרים בבת אחת: ⛔ אין טענה שהמסמך כבר קיים, ⛔ ואין
שם-קובץ של ריפו בממשק — תואם להחלטה הנעולה ששפת המוצר גנרית ואינה
חושפת מזהים פנימיים. ⇒ ⛔ `§ב1` **אינו** מורחב.

### `P11A-D11` — מצב מוצלח נכנס למטריצת המצבים

צילום מסך מוצלח ומאוכלס לכל אחד מחמשת המסכים, ברינדור אמיתי.
⛔ בלעדיהם `D7`–`D9` נכתבות אך ⛔ אינן מאומתות.

### `P11A-D12` — fail-closed; חריג סביבתי יחיד ומוצהר

- ⛔ **להסיר** את `pytest.skip` בבדיקה 9 (`test_design_tokens.py:697-704`).
  המטריצה כבר חוזה קיים (`DESIGN.md:450` + שמונה שורות) ⇒ מחיקת הכותרת
  תגרום ל-`skip` במקום לכשל. זהו false-green סמוי. לתקן גם את ההודעה
  וההערות הטוענות שהמטריצה "טרם נכתבה".
- בדיקת טבלת ההשפלה **fail-closed מלכתחילה**: כותרת חסרה · יעד/שכבה
  חסרים · תא ריק · placeholder לא מוכר ⇒ **כשל**, ⛔ לעולם לא `skip`.
- ⛔ **להשאיר** את `test_metrics.py:1373` — `skip` **סביבתי** מוצהר:
  `funnel_marketing_data.csv` ב-`.gitignore` ואינו אמור להיות ב-CI.
  הפיכתו ל-fail-closed תשבור את ה-CI. ⚠ לסמן כדי שלא "ינוקה" בעתיד.
- ⚠ **סריקה מלאה:** אלה שני ה-`skip` **היחידים** ב-`tests/` וב-`e2e/`;
  ⛔ אין `skipif` ואין decorators.
- ⚠ הכלל כבר כתוב: `PHASE12.md` CP1 — *"⛔ לא `skip` שקט"*. ⇒ זו
  **החלת כלל קיים**, ⛔ לא אימוץ חדש.

### `P11A-D13` — פורמט דיווח ראיות

כל ראיה מבחינה בארבעה: **נאספו · עברו · דולגו (שם + סיבה) · הסביבה**.
לדוגמה: *"`X passed, 1 skipped` — בדיקת מספרי פרופיל לקוח-על דולגה
משום שה-CSV מוחרג מ-git ואינו ב-CI"*. ⛔ אין לטעון "כל 794 עברו".
⛔ **אין לשכתב רשומות היסטוריות** המתארות נאמנה הרצה מקומית שבה הבדיקה
עברה; האיסור חל על טענות **עתידיות** עמומות.

### `P11A-D14` — ראיה שנפגעה מול ראיה חסרה

| שורה | סיווג | מתי |
|---|---|---|
| `B50` | **ראיה שנפגעה** — `budget.js` הוא בדיוק מסך ההמלצה שהדלף פוגע בו | פסילה מפורשת ב-`CP0`; **ראיית יישום 11A מעודכנת** ב-`CP10` |
| `B45` | ⛔ **לא נפסלה** — תיקוני 11A ב-`budget.js`, ⛔ לא ב-`followup.js`; קוד ההמלצה קיים ומקרה 23 מוכיח כשל חלקי. הסיווג: **ראיית success חסרה** | השלמה ב-`CP10` |

⛔ **"ראיה סופית" הוא ניסוח אסור לשתיהן.** שתיהן נשארות `planned` עד
ראיית הקבלה החיה בפאזה 12 ⇒ מה שנכתב ב-`CP10` הוא **ראיית ביניים
מתוקנת / ראיית יישום 11A**, ⛔ לעולם לא "סופית". חל על כל מופע
ב-`D14`, ב-`CP10` ובטבלת המיפוי.

⛔ **אין להמציא סטטוס `in remediation`** — ערך שישי שאינו בסכימה, שובר
את `_EXPECTED_STATUS_COUNTS`, ומיותר: `planned` כבר אומר "טרם סופקה".
⛔ אפס שינוי `status` ⇒ ⛔ אפס שינוי בספירות ובבדיקות.

---

## ד. תוצרים

| תוצר | תיאור |
|---|---|
| `app/static/js/facts.js` | `D2` — ולידציה טיפוסית פר-בלוק |
| `app/static/js/screens/predict.js` | `D4` · `D10` |
| `app/static/js/screens/super-customer.js` | `D5` — `buildD9Meaning()` |
| `app/static/js/screens/budget.js` | `D6` · `total_budget` · מבנה דו-עמודתי |
| `app/static/js/charts.js` + ארבעת הקוראים | `D7` |
| `app/static/style.css` | `D8` |
| `app/static/js/screens/*` | `D9` — `h1`/`h2` |
| `docs/DESIGN.md` §6.1 | `D3` — טבלת ההשפלה (⚠ החריג היחיד ב-§ב1) |
| `docs/IA.md` | תיעוד התנהגות + הפניה. ⛔ ללא נוסח מילולי |
| `tests/test_design_tokens.py` | `D12` — הסרת ה-`skip`; בדיקת הטבלה החדשה |
| `e2e/` | fixture חלקי · fixture תקין · assertions על `.summary-recommendation` ו-`.business-context-card` · חוזה הגרפים |
| `docs/design/states/` | `D11` — חמישה צילומי הצלחה |
| `ROADMAP.html` | רשומת 11A + חסימת שער 11→12 + תיקון טענות CP9/CP11 |
| `REQUIREMENTS.md` | `CP0`: פסילת ראיית `B50` (`D14`) · `CP10`: ראיית יישום 11A מעודכנת ל-`B50`/`B45`. ⛔ `status` ללא שינוי |
| תיעוד | שמונה תיקוני סבב 1 ב-`PHASE12.md` · סנכרון 74 · `PHASE11.md` ד3 · `codex-review.md` |

---

## ה. Checkpoints

`CP0` הוא שער ואינו נספר. **עשרה checkpoints ביצוע** (`CP1`–`CP10`).

⛔ **סדר הביצוע ⛔ אינו סדר ההכרעות, וזו כוונה.** ההכרעות ב-§ג מסודרות
לפי **תלות לוגית**; ה-checkpoints לפי הכלל ש**אסור להשאיר checkpoint
מאומת שמחמיר ביודעין את המוצר**.

⚠ לכן `D2` (`facts.js`) מבוצעת **אחרונה מבין הארבע**, ב-`CP5`: הרחבת
הוולידציה גורמת לבלוקים חלקיים להגיע למסלול המושפל: אילו בוצעה לפני
`D4`–`D6`, היא הייתה מגדילה את **תדירות הדלפים** בין ה-checkpoints.
הצרכנים כבר מטפלים ב-`null` היום, ולכן `CP2`–`CP4` עומדים בפני עצמם.
⛔ נשקלה חלופה של checkpoint אטומי אחד לוולידציה + שלושת הצרכנים
ונדחתה: היא סותרת את כלל "לכל היותר checkpoint אחד בסבב" ומקשה על
ביקורת.

| # | תוצר | קריטריון קבלה עיקרי |
|---|---|---|
| **CP0** | שער: קומיט התכנון מוזג; `fix/phase11a-remediation` נפתח מ-`main`; רשומת 11A חוסמת את שער 11→12; `D14` נרשם | `HEAD == origin/main`, עץ נקי, CI ירוק, ⛔ אפס שינוי `status` |
| **CP1** | `D3` — טבלת §6.1 + `IA.md` | 12 שורות, שלושה יעדים × ארבע שכבות, ⛔ אפס תא ריק, placeholders מהקבוצה הסגורה, ⛔ אף אחד ממופה ל-`business_facts.json`; ⚠ בדיקה 9 ממשיכה לעבור ו**הטבלה החדשה ⛔ אינה נבלעת בטווח החילוץ שלה** — ⛔ זו אינה התחייבות שהתנהגות בדיקה 9 לא תשתנה לעולם: `D12` משנה אותה בכוונה ב-`CP8` |
| **CP2** | `D4` — P2 | ארבע שכבות קיימות ולא-ריקות; ⛔ אין טענת `calls_to_closed` ⛔ ואין פעולת מדיניות שיחות; ✅ הנוסח תואם מילולית ל-§6.1 |
| **CP3** | `D5` — P4S | כנ"ל; ⛔ אין טענת רווח/CAC |
| **CP4** | `D6` — Budget | כנ"ל; ⛔ אין **טענת** דירוג ב-D9 המושפל, ⛔ אין יחס/backtest/פיילוט; ✅ טבלת האסטרטגיות נשארת **כולל עמודת `rank`** |
| **CP5** | `D2` — `facts.js` | בלוק חלקי ⇒ `null` כמו חסר; ⛔ אפס `NaN`/`undefined`/`Infinity` תחת כל fixture מושפל; **fixture תקין ⇒ התוכן חוזר במלואו** |
| **CP6** | `D7` — גרפים | ארבעת הגרפים: שם ציר X, שם ציר Y וכותרת **באנגלית בתוך ה-SVG**; מקרא אנגלי היכן שיש whisker; fallback נשאר בעברית |
| **CP7** | `D8` + `D9` + `D10` | `₪50,000` מוצג; שלוש קבוצות זו לצד זו; Budget דו-עמודתי; Overview ⛔ לא 5 בשורה; **`h1` אחד בדיוק לכל מסך גלוי ו-⛔ אפס דילוגי רמה**; ⛔ אין מחרוזת `REPORT.md` בטקסט מוגש — ✅ **ההפניה הגנרית לדוח נשארת** (`IA.md:300`) |
| **CP8** | `D11` + `D12` | חמישה צילומי הצלחה ברינדור אמיתי; ה-`skip` ההיסטורי הוסר; החריג הסביבתי מתועד |
| **CP9** | תיעוד | שמונה תיקוני `PHASE12.md` · 74 שורות · `PHASE11.md` ד3 · תיקון CP9/CP11 ב-`ROADMAP.html` · `codex-review.md` |
| **CP10** | סגירה — תשעה שלבים | זהה ל-`PHASE11.md` §ח1; בסופו **ראיית יישום 11A מעודכנת** ל-`B50` ול-`B45` לפי `D14`. ⛔ לא "ראיה סופית" — שתיהן נשארות `planned` עד פאזה 12 |

---

## ו. מיפוי מול `REQUIREMENTS.md`

⛔ אינו טבלת עקיבות מקבילה.

| שורה | סטטוס | מה 11A משנה |
|---|---|---|
| `B50` | `planned` | ראיה **נפגעה** — פסילה ב-`CP0`, **ראיית יישום 11A מעודכנת** ב-`CP10` (`D6`, `D14`). ⛔ לא "סופית" |
| `B45` | `planned` | ⛔ לא נפגעה — **ראיית success חסרה**, מושלמת ב-`CP10` (`D11`, `D14`) |
| `B29c` | `gap`, בעלים 13 | ⚠ *"what's the strongest lever… and what should Northbound do about it?"* — **בדיוק** צמד מנוף+פעולה ש-`D4` נוגעת בו. ⛔ 11A אינה סוגרת אותה |
| `B52` | `planned` | ניסוח הראיה ("גרף + fallback טבלאי") מתעדכן אחרי `CP6` |
| `B28`/`B36`/`B41` | `planned` | ⛔ ללא שינוי |
| `B44` | `done` | ⛔ **אינה נפתחת מחדש** — ראייתה `funnel_dropoff.svg` ה-offline, ⛔ לא הגרף החי |
| `B55b`/`B59` | `planned` | ⛔ ללא שינוי ב-11A (פאזה 12/13) |

⚠ **מתח מוצהר ב-`B29c`:** אם `D4` ממגנת את משפט המנוף, אז במצב מושפל
האפליקציה ⛔ **אינה** עונה על `B29c`. זה מקובל למצב מושפל — ⛔ אך במצב
**תקין** שני החלקים חייבים להימסר. ⚠ לפי סדר ה-checkpoints המעודכן
זה נאכף ב-**`CP5`** (*"fixture תקין ⇒ התוכן חוזר במלואו"*) וב-**`CP2`**
(שני חלקי `B29c` — המנוף והפעולה — מוצגים במסך P2).

**דרישות יתומות:** ⛔ לא נמצאה אף אחת. 11A ⛔ אינה מוסיפה ואינה מסירה
שורות; הספירה נשארת **74 שורות (B1–B66)**.

---

## ז. מקרי הפרכה (יבוצעו ויתועדו, ⛔ לא יוסקו)

פר-מסך, בתוך `.summary-recommendation`:

1. `"ltv": {}` ⇒ ⛔ אין `NaN` בשום מקום ב-DOM.
2. `super_customer_profile` עם שדה מספרי אחד חסר ⇒ הכרטיס מוסתר,
   ⛔ ולא `NaN%` בתוך משפט `IA.md` §3א.6.
3. `budget_backtest: {"500": {}}` ⇒ ⛔ אין `"פי NaN"` ⛔ ואין מסקנת backtest.
4. `actual_mean_per_customer: 0` ⇒ ⛔ אין `Infinity` ⛔ ואין יחס מוצג.
5. הנכס נכשל לגמרי ⇒ D9 ⛔ אינו ממליץ על פיילוט (`DESIGN.md:548`).
6. `schema_version` לא מוכר ⇒ כמו כשל טעינה.
7. תחת 1–6: **בדיוק ארבע שכבות, ארבע התוויות, ⛔ אף ערך אינו ריק**.
8. תחת 1–6: הנוסח **תואם מילולית** לשורת §6.1 המתאימה — ⛔ לא רק
   היעדר מחרוזות אסורות.
9. תחת 1–6: ⛔ נעדרים העוגנים **מתוך ארבע שכבות D9 בלבד** — שם הפיצ'ר
   והתווית העברית שלו, טענות רווח/CAC, טענת דירוג, backtest, יחס,
   פיילוט. ⚠ ⛔ **אין לבדוק זאת על כל ה-DOM**: עמודת `rank` בטבלת
   האסטרטגיות היא נתון API לגיטימי שנשאר מוצג (`budget.js:186`).
10. תחת 1–6: תוצאות ה-API עצמן ⛔ אינן מושבתות.
11. **fixture תקין** ⇒ התוכן הקנוני התלוי בנכס חוזר, כולל **שני** חלקי
    `B29c`. ⚠ זהו המשלים ההכרחי: בלעדיו הדרך הקלה לעבור את 1–10 היא
    להשפיל תמיד — בדיקות ירוקות ומוצר שבור.
12. ארבעת הגרפים ⇒ ה-SVG מכיל שם ציר X, שם ציר Y וכותרת, כולם אנגלית.
13. מסך Budget ⇒ `₪50,000` מוצג.
14. ברוחב Desktop: שלושת פאנלי החיזוי באותה שורה; Overview ⛔ לא 5
    בשורה; Budget בשתי עמודות.
15. כל מסך גלוי ⇒ **`h1` אחד בדיוק**, ⛔ ואפס דילוגי רמה
    (⛔ אין `h3` בלי `h2` קודם באותו מסך).
16. ⛔ אין מחרוזת `REPORT.md` בשום טקסט מוגש — ✅ **אך ההפניה הגנרית
    לדוח קיימת** ב-`<details>` של P3/P4, כנדרש ב-`IA.md:300`/`:335`.
    ⚠ ⛔ אין לבדוק היעדר בלבד: היעדר ההפניה כולה הוא כשל.

---

## ח. סיכונים

| סיכון | מענה |
|---|---|
| רגרסיה בקוד מוזג ו-CI-ירוק | בדיקות ממוקדות לכל CP; `e2e/` מלא פעם אחת בסגירה |
| שינויי CSS פוסלים צילומים קיימים | `CP7` בודק אילו השתנו ומצלם מחדש **רק אותם** |
| "יישור" מחליק לעיצוב מחדש | §ב1 — החריג מתיר **רק** השלמת נוסחי degradation |
| טבלת §6.1 שוברת את בדיקה 9 | `D3` — כותרת ייחודית + תת-סעיף נפרד; ⚠ חובה לקרוא את הפרסר לפני הכתיבה |
| ולידציה מחמירה מדי מסתירה תוכן תקין | מקרה הפרכה 11 (fixture תקין) + מקרה 10 |
| נוסח מושפל שממשיך לטעון | `D1` — הסטנדרט; מקרה הפרכה 8 (התאמה מילולית ל-§6.1) |
| ראיה סופית נכתבת מוקדם | `D14` — פסילה ב-`CP0`, ראיה ב-`CP10` |
| חוזה הגרפים נקרא כדרישת ספרייה | `D7` — כלל שפה בלבד; ⛔ אין תלות חדשה |

---

## ט. אימות

⚠ ללא כפילויות: `test_design_tokens.py` יושב **בתוך** `tests/`.

1. **CI (כל push):** `python -m pytest -q` — `tests/` בלבד. ⚠ **CI הוא
   ראיית ה-pytest המלאה**; ⛔ אין לשכפלה מקומית בלי סיבה קונקרטית
   (`CLAUDE.md`).
2. **מקומי, לכל CP:** `node --check` לכל קובץ JS שנגעו בו + pytest
   **ממוקד** בלבד — frontend, design ו-traceability לפי הרלוונטיות.
3. **מקומי, בסגירה:** `e2e/` **פעם אחת**.
4. כל דיווח לפי `D13`: נאספו / עברו / דולגו / סביבה.
5. ⛔ אין אימון · ⛔ אין Holdout · ⛔ אין הרצה חיה מול הפריסה.

---

## י. הצהרת סטטוס

`planning_status`: `planning_in_progress` ⇒ **`approved_for_execution`**
— ✅ **המשתמש אישר את התכנון 19.09.2026.**
`execution_status`: **`not_started`**

⚠ **אישור התכנון ⛔ אינו הוראת ביצוע.** `execution_status` יזוז רק
בהוראה נפרדת ומפורשת. ⛔ אין `git checkout -b`, commit, push, PR או
merge לפני אותה הוראה, לפי פרוטוקול האישור הכפול.
⛔ פאזה 12 נשארת מוקפאת (`planning_in_progress`) עד סגירת 11A.

**מצב הביקורת:** אושר בביקורת Codex, 19.09.2026, לאחר שלושה סבבים על
המסמך עצמו — ביקורת רוחב על `6e36c17..af07d6f` (שלושה פערי יישום) ⇐
שבעה תיקוני מבנה ⇐ ששה תיקוני דיוק ⇐ חמש שאריות קונסולידציה. הסבב
האחרון נסגר ללא סתירה פנימית נוספת. **המשתמש אישר את התכנון לביצוע
19.09.2026** — זהו אישור סופי, ⛔ לא אישור מבקר בלבד.
