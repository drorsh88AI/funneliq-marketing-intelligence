# חפיפת שיחה — FunnelIQ

עודכן: 13.09.2026. מסמך זה מרכז את מצב העבודה המחייב לפתיחת שיחת Codex
חדשה, בלי להעביר אליה את היסטוריית השיחה הארוכה.

## נקודת פתיחה

- שפה: עברית, עניינית וקצרה. אם המשתמש טועה — להסביר לפני ביצוע.
- תיקייה: `C:\Users\dror_\AI\AI Course\9.Finel Project\Project 1`.
- ענף: `fix/phase10a-alignment`.
- HEAD בבסיס החפיפה: `4191805` — ראיות ותיעוד CP9.
- commit מקור CP9: `1c70ca8` — קוד התיקון שממנו נבנה P4S.
- פאזה 10A: `approved_for_execution` / `in_progress`.
- CP0–CP9: `done`. השלב הבא: **CP10 בלבד**.
- אין push, PR או merge. אין לבצע אותם בלי הוראה מפורשת.
- מסמך החפיפה עצמו נשאר בכוונה untracked; מותר לקרוא אותו, ואין לביים או
  לקבע אותו בלי בקשת המשתמש.

לפני עבודה יש לקרוא את `AGENTS.md`, מסמך זה, `PHASE10A.md` §CP10 ואת יומן
CP9 שם. לאחר מכן לאמת `git branch --show-current`, ‏`git log -2 --oneline`
ו־`git status --short`.

## מצב CP9 שננעל

- P4S הפעיל הוא CatBoost, גרסה `P4S-catboost-20260912-1c70ca8`, על היעד
  המשולב וארבעת הקלטים הקיימים. Holdout: ROC-AUC ‏0.8014, ‏PR-AUC ‏0.3420,
  Brier ‏0.1145 ו־Recall ‏0 בסף 0.5. Logistic ההיסטורי היה מעט טוב יותר
  ונשמר כ־audit; CatBoost נבחר מכוח דרישת הבריף והכרעת המשתמש. הציון הוא
  אות מסייע לבדיקה ידנית בלבד.
- P6 נבנה מפרופילי train רב־משתניים שנצפו, ללא שימוש ביעד ובלי לפתוח שוב
  את Holdout. `100x500` מדורגת ראשונה אך אינה המלצה: שתי המובילות חופפות
  ובדיקת העבר ברמת 500 הראתה הערכת־יתר פי 8.59. אם בוחנים אחת מארבע
  החלופות, ההמלצה היא פיילוט מוגבל של `25x2000` ומדידת רווח מצטבר באותו
  אופק.
- `calls_to_closed` משתמש ב־`closed>0` ‏(3,318 רשומות), עם עימוד מלא
  וספירה עצמאית באותו מסנן. חציון 3, שכיח 2, ממוצע 3.706; זהו ממוצע ברמת
  רשומה ולא מניין שיחות לעסקה בודדת.
- נוצר `app/static/business_facts.json`, נכס מצומצם וגרסאי ללא שורות לקוח.
- חוזה OpenAPI וסכמות התגובה לא השתנו. גיבובי P2/P3/P4/P6 joblib/meta
  נשארו זהים.

## ראיות בדיקה תקפות

- לאחר יצירת הארטיפקטים: `pytest -q` — ‏789 passed.
- לאחר עדכון המרשם וה־ROADMAP:
  `tests/test_requirements_traceability.py tests/test_roadmap_render.py` —
  ‏14 passed.
- JavaScript המוטמע ב־`ROADMAP.html` עבר בדיקת תחביר.
- `git diff --check` עבר.
- ספירת המרשם: 41 done / 15 planned / 15 gap / 1 N/A / 1 parent.

אין להריץ מחדש את המודלים, Holdout או `pytest` מלא רק לצורך אישור חוזר.
מריצים בדיקה נוספת רק אם שינוי חדש יוצר אי־ודאות רלוונטית.

## CP10 — המשימה הבאה

יש לבצע שתי ביקורות של התוצר המתוקן לפי `PHASE10A.md`:

1. מלמעלה למטה: מהבריף אל 73 הדרישות, הראיות, הממשק העתידי והדוח; לוודא
   שכל שאלה מקורית נענית ולא הוחלפה בשאלה קלה יותר.
2. מלמטה למעלה: מכל פלט והמלצה אל מקורו; לבדוק אוכלוסייה, יחידה, זמינות,
   משמעות והסתייגות.
3. לבצע ולתעד כל אחד מ־15 מקרי ההפרכה ב־PHASE10A.md §ח.
4. לבדוק שכל תוצר וקריטריון קבלה של CP0–CP9 קיבל ראיה, וכל דרישה פתוחה
   משויכת לפאזה מסוימת.
5. להכין לקלוד חבילת ביקורת קצרה עם ה־diff, ההכרעות, הספקות ומפת הראיות;
   קלוד זמין שוב, ולכן יש לבקש ממנו ביקורת עצמאית ולתקן כל ממצא מאומת.
6. לתקן ממצאים בתוך CP10, לעדכן מיד את `ROADMAP.html`, ‏`PHASE10A.md`
   ו־`REQUIREMENTS.md` אם סטטוס או ראיה השתנו. אין להתקדם ל־CP11 באותו סבב.

יש להבחין ביומן בין ביקורת Codex לביקורת Claude, ולא לטעון לאישורו לפני
שהתקבלה ממנו תשובה בפועל.

## מקורות אמת והחלטות נעולות

- דרישה: `FunnelIQ_Assignment.html`.
- מרשם יחיד: `docs/planning/REQUIREMENTS.md` — 73 דרישות.
- תכן: `docs/planning/SPEC.md`.
- מסע ותוכן: `docs/IA.md`; עיצוב: `docs/DESIGN.md`.
- רשימות פיצ'רים: `app/features.py` בלבד.
- Render ולא Railway; אין notebook; CSV הוא snapshot סופי של מטלת הקורס
  ולעולם לא יתעדכן.
- התחזיות אינטראקטיביות מול קלט המשתמש, אך נשענות על ה־CSV הקפוא.
- README באנגלית; REPORT בעברית; ממשק עברי RTL.
- anon/publishable key בדפדפן בלבד; secret/service key רק בסקריפטים מקומיים;
  קריאות משתמש נושאות JWT כדי ש־RLS תיאכף.
- סביבת Python היחידה:
  `C:\Users\dror_\programs\anaconda\envs\pro1_FunnelIQ\python.exe`.

## העדפות המשתמש וגבולות עבודה

- להתקדם בחלקים קטנים, לכל היותר checkpoint אחד בכל סבב.
- לעדכן ROADMAP אחרי כל CP.
- לחשוב פעמיים ולבצע פעם אחת; לחשוף תוצאה חלשה או סתירה ולא לייפות אותה.
- לפני כל פרומפט ל־Stitch להסביר בקצרה איזה מסך ושינוי מבוקשים, לקבל את
  דעת המשתמש, ואז למסור פרומפט קצר וממוקד. המשתמש מזין אותו ידנית.
- אחרי שהמשתמש כותב "בוצע", לבדוק את המסך המדויק ששונה ולא לעבור למסך אחר
  בלי לציין זאת.
- פאזה 10 checkpoint 8 ממתינה לסגירת 10A; אין עבודת Stitch במהלך CP10.
- אין לשנות או לביים את `.obsidian/`, ‏`Funneliq_prompts.docx` או
  `~$nneliq_prompts.docx`.

## פרומפט פתיחה לשיחה החדשה

> המשך את פרויקט FunnelIQ מהמצב שב־`docs/planning/SESSION_HANDOFF.md`.
> קרא תחילה את `AGENTS.md`, את מסמך החפיפה, את `PHASE10A.md` §CP10 ואת
> יומן CP9. אמת את הענף, שני ה־commits האחרונים ו־git status. המשך ל־CP10
> בלבד לפי שתי הביקורות ו־15 מקרי ההפרכה. קלוד זמין שוב: הכן לו חבילת
> ביקורת קצרה, בקש ביקורת עצמאית ותקן ממצאים מאומתים. עדכן ROADMAP; אל
> תריץ מחדש מודלים או Holdout ואל תבצע push/PR/merge.
