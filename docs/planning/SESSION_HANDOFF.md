# חפיפת שיחה — FunnelIQ

עודכן: 22.09.2026. מסמך זה מרכז את מצב העבודה המחייב לפתיחת שיחת Codex או
Claude חדשה, בלי להעביר אליה את היסטוריית השיחה הארוכה.

## נקודת פתיחה פעילה

- שפה: עברית, עניינית וקצרה. אם המשתמש טועה — להסביר לפני ביצוע.
- תיקייה: `C:\Users\dror_\AI\AI Course\9.Finel Project\Project 1`.
- ענף בסיס: `main`. **`HEAD = 00717f6` `== origin/main`, CI ירוק.** ⚠ עץ
  העבודה נושא כעת את חמשת מסמכי התכנון של הטיוטה שלהלן — ⛔ אינו נקי.
- פאזות 10, 10A, 11 ו-11A: **סגורות** (`approved_for_execution` / `done`).
- פאזה 11 מוזגה ב-PR #29 (`c182b4b`, 18.09.2026). פאזה 11A — תיקון היישור
  מול חוזה התכן — מוזגה ב-PR #31/#32/#33 ונסגרה 22.09.2026. פאזה 11 עצמה
  ⛔ אינה נפתחת מחדש.
- **פאזה 12 (קבלה): `planning_status: approved_for_execution` /
  `execution_status: not_started`.** התכנון ב-`docs/planning/PHASE12.md`
  עבר חמישה סבבי תיקון וביקורת Codex–Claude ב-22.09.2026 אחרי בדיקת שער
  מחודשת מול המצב בפועל וחזרה מפורשת לנוסח הבריף (שבע רגלי `B59`).
  ✅ **אושר לביצוע ע"י המשתמש, 22.09.2026** ("יש אישור ביצוע"). ⛔ עדיין
  לא נפתח `test/acceptance` — נדרשת קודם סגירת שער התכנון (§ה1/CP0).

## המשימה הבאה — סגירת שער התכנון (CP0), ואז פתיחת test/acceptance

התכנון אושר. השלב הבא, לפי `PHASE12.md` §ה1/CP0: תוצרי שלב התכנון (חמשת
המסמכים שהשתנו) נסגרים בקומיט ייעודי על ענף תכנון → PR → CI → merge
ל-`main`. **רק אחרי המיזוג** נפתח `test/acceptance` מ-`main` עם עץ נקי —
זהו `checkpoint 0` עצמו. ⛔ בניית `live/` (checkpoint 1 ואילך) היא עבודה
נפרדת שמתחילה רק אחרי ש-CP0 נסגר ותועד.

⛔ אין `git checkout -b`, commit, push, PR או merge בלי אישור נפרד ומפורש
לכל שלב — לפי פרוטוקול האישור הכפול ומדיניות השער האחיד ב-`CLAUDE.md`.
"יש אישור ביצוע" הוא האישור הראשוני לסגירת השער; המיזוג ל-`main` דורש
אישור סופי נפרד לפני ביצועו.

## מה נמדד בבדיקת השער, 22.09.2026 (ללא credentials)

- **8/8 הנכסים ש-11A שינתה** — `js/charts.js`, `js/facts.js`, חמשת
  `js/screens/*.js` ו-`style.css` — מוגשים **זהים בייט-לבייט ל-blob**
  (`git show HEAD:app/static/<path>`), `status=200`. ⚠ עוגן ההשוואה הוא
  ה-blob: מול **עץ העבודה ב-Windows** יש הפרש סיומות שורה בלבד, כי
  `.gitattributes` מגדיר `* text=auto` — מדידה שמדווחת `text_exact=False`
  השוותה מול עץ העבודה. ⚠ הסקה על התוכן המוגש; ⛔ לא קריאה מרשומת
  ה-deploy של Render. ⚠ `index.html` ו-`business_facts.json`
  נמדדו וזהים גם הם, אך ⛔ אינם ראיה ל-11A — היא לא נגעה בהם.
- `GET /health` ⇒ `200` · שני נתיבי insights ללא טוקן ⇒ `401` ·
  `/api/config` ⇒ שני מפתחות ציבוריים בלבד.
- חמש מחרוזות `model_version` ב-`models/*.meta.json` זהות ל-
  `business_facts.json.model_versions`.
- ⛔ אין עמודת `organization` ב-`funnel_records`; RLS היא שער בינארי על
  `app_metadata.organization = 'northbound'`.
- משתני `.env`: `SUPABASE_URL`/`SUPABASE_PUBLISHABLE_KEY`/
  `SUPABASE_SECRET_KEY`/`OBSIDIAN_API_KEY` — ⛔ **אין משתנה env של סיסמת
  דמו**. ⚠ ⛔ אין לנסח "אין סיסמאות דמו על הדיסק": סייג B7 ל-`D5א`
  (`PHASE4.md:88-91`) מתעד הערות `#` מקומיות ב-`.env`, שאינן נטענות
  כמשתני סביבה ו-⛔ אינן חלק מהרתמה. ⛔ קלוד אינו קורא את הקובץ.
- `e2e/` מכסה **46 מקרי הפרכה** (30 פאזה 11 + 16 פאזה 11A) ב-**67
  פונקציות בחמישה-עשר קבצים**; `docs/design/states/` מכיל 26 נכסים.

## תיקון תכנון פאזה 12 שנכתב (22.09.2026, טיוטה)

1. `PHASE12.md` §א נמדד מחדש; שלוש ספירות שגויות תוקנו (35/35 ⇒ 46
   מקרים/67 פונקציות, 21 ⇒ 26, 794 התיישן); §ב מחריגה את כל 46 המקרים.
2. `P12-D3` תוקנה **במקום**: סיסמאות ב-`getpass`, פעם אחת לכל session,
   אחרי **שער opt-in שנבדק לפני ה-`getpass`**, בהרצת המשתמש עם
   `pytest -s`. הרתמה ⛔ אינה קוראת ואינה כותבת סיסמאות לקובץ/env/`argv`
   (⚠ הערות `D5א` מקומיות ב-`.env` עשויות להתקיים ואינן חלק ממנה);
   הרצה בלי opt-in נכשלת מיד, ⛔ לא skip. ⚠ `PHASE4.md:85` — `getpass`
   תולה תחת כלי לא-אינטראקטיבי; ⛔ `stdin.isatty()` **אינו** הבטחת
   fail-fast (נצפה `isatty=True` בסביבת סוכן ללא משתמש), ולכן השער הוא
   ה-opt-in ⛔ ולא בדיקת ה-TTY. ⛔ אפס דליפה ל-log/traceback/צילום/HAR/
   video/trace/`storage_state`. הנימוק: הנוסח הקודם סתר את `D5א` של פאזה 4
   (`PHASE4.md:83-86`, ⛔ המזהה אינו `D5a`) ואת התקדים ב-`PHASE9.md:343`.
3. `P12-D4` חודדה: **שני** ערוצי אכיפה — guard על הקשר הדפדפן, ו-wrapper
   קריאה-בלבד לקריאות PostgREST הישירות של CP3 (⚠ ניתוב ההקשר אינו
   מיירט `APIRequestContext`) — שניהם דוחים כל מתודה שאינה `GET`/`HEAD`
   אל `/rest/v1` לפני שהבקשה יוצאת, עם ראיה שאף כתיבה לא יצאה; ⛔ בלי
   לחסום `POST` ל-Auth או לחיזוי. ⚠ הנוסח הקודם ערבב פעולות SQL עם
   מתודות HTTP.
4. נוספו `P12-D9`–`P12-D14`: שער נוסח בריא ל-P2/P4S/Budget · בוחן חיצוני
   כתנאי מחייב ל-CP7, שפרטי הדמו נמסרים לו בערוץ חיצוני על ידי המשתמש
   (כדפוס `PHASE4.md` checkpoint 16, ⛔ לא דרך קלוד) · טענת RLS מוגבלת
   לסכמה · הפרדת backend חי מהזרקת כשל · מדידת צריכת הזיכרון שמדווחת על
   ידי Render, עם שם מדד/יחידה/חלון/peak ⛔ בלי הנחת 512MB ·
   `model_version` בחמש תגובות מודל. ⛔ `P12-D8` שמור לקור start ב-CP5.
5. `P12-D10` מונה את **שבע רגלי `B59`** למסע הבוחן (URL · התחברות
   עצמאית · טופס נקי ⛔ בלי prefill/דוגמה · הזנה ידנית של לקוח חדש
   וחיזוי עבורו · Follow-up ו-Budget · קריאת בלוקי ההמלצה · אפס
   התערבות), ומגדיר "לקוח חדש" כהזנה ידנית מטופס נקי ⛔ ללא דרישת
   אי-התאמה ל-`funnel_records`. ⚠ הרגליים הן **תוצאות חובה ⛔ ולא מסלול
   כפוי** — אחרי ההתחברות הבוחן מנווט עצמאית, ו-`EVIDENCE.md` יתעד את
   המסלול שנבחר וכל בלבול/צורך בעזרה כממצא. ⚠ פער העברה מ-`SPEC.md:1764`,
   ⛔ לא פער תכן.
6. מיפוי הסגירה פוצל לפי ראיה: `B10`/`B56` ⇐ CP2 · `B45`/`B50`/`B28`/
   `B36`/`B41`/`B52` ⇐ CP4 · `B55b` ⇐ CP4 + CP7 · `B59` ⇐ פאזה 13.
   CP7 הוא תנאי לסגירת הפאזה, ⛔ לא לכל שורה.
7. עודכנו `ROADMAP.html` (תמונת מצב + כרטיס פאזה 12), `REQUIREMENTS.md`
   (`B10`/`B55b`/`B56`/`B59` — ⛔ אפס שינוי `status`), ו-`codex-review.md`.
   ⚠ הטיוטה עברה ביקורת Codex (תשעה ממצאים) ותוקנה לפיה; ⛔ אין אישור.

## מצב הדרישות והראיות

- ספירת המרשם הפעילה: 41 `done`, ‏16 `planned`, ‏15 `gap`, אחת `N/A` ואחת
  `parent` — **74** בסך הכול (B1–B66).
- `B10`/`B45`/`B50`/`B56` (בבעלות פאזה 11) נשארות `planned` — הסגירה
  דורשת ראיית קבלה חיה בפאזה 12, לא רק מימוש ובדיקות מקומיות. עמודת
  ה-`evidence` שלהן עודכנה בפאזה 11A (CP10, `3dbfedf`), וראיית `B50`
  שנפסלה בביקורת 19.09 הוחלפה בראיית היישום המתוקן — ⛔ לא ראיה סופית.
- `B59` אינה נסגרת בפאזה 11 או 12 לבדן: דורשת מסע של **בוחן חיצוני**
  בפאזה 12 (`P12-D10`) **וגם** רכיב שחזור ב-README מפאזה 13.
- עשר השורות שפאזה 12 מאמתת (`B10`/`B56`/`B45`/`B50`/`B28`/`B36`/`B41`/
  `B52`/`B55b`/`B59`) כולן `planned`; קריטריוני הסגירה ב-`PHASE12.md` §ו.
- ראיית הבדיקות התקפה: `tests/test_requirements_traceability.py` +
  `tests/test_roadmap_render.py` — עברו אחרי עדכוני התכנון של 22.09.2026.
  אין להריץ אותן שוב ללא אי־ודאות חדשה.
- אין לפתוח שוב Holdout או להריץ מודלים. חריגת S9 מפאזה 6 נשארת בתוקף.

## מקורות חובה לפני ביצוע

1. `AGENTS.md` ו־`CLAUDE.md` — אם יש סתירה ביניהם, להעלות אותה כבאג סנכרון.
2. `docs/planning/PHASE12.md` — התכנון הפעיל (אושר לביצוע), מקור האמת
   לפאזה 12.
3. `FunnelIQ_Assignment.html` · `docs/planning/REQUIREMENTS.md` ·
   `docs/planning/SPEC.md` · `docs/IA.md` · `docs/DESIGN.md` (§6.1 ו-§6.1ג —
   הנוסח הקנוני הבריא והמושפל, שער `P12-D9`).
4. `docs/planning/PHASE11.md`, `PHASE11A.md`, `PHASE10.md`, `PHASE10A.md` —
   תוצרי הפאזות הסגורות.
5. החוזים החיים: `app/schemas.py`, `app/auth.py`, `docs/api/openapi.json`,
   `app/features.py`, `app/static/business_facts.json`.

## החלטות נעולות

- Render ולא Railway; בלי notebook.
- `app/features.py` הוא מקור האמת היחיד לרשימות פיצ'רים.
- anon/publishable key בלבד בדפדפן; service/secret key רק בצד השרת המקומי.
- קריאות נתונים למשתמש נושאות את ה־JWT שלו כדי ש־RLS תיאכף בפועל.
- `README.md` באנגלית; `REPORT.md` בעברית.
- P4 הוא חיזוי referral ואינו ציון לקוח־על. P4S הוא ציון לקוח־על לרוכש ידוע
  אחרי מעקב 1 וסגירת חלון חודשי; ההסתברות מה־API מומרת למספר שלם 0–100
  בממשק (`Math.round(p*100)`, `P11-D12`).
- CatBoost הוא מודל P4S הפעיל מכוח דרישת הבריף, אף ש־Logistic ההיסטורי מעט
  חזק יותר; הציון הוא אות מסייע לבדיקה ידנית בלבד.
- `100x500` מדורגת ראשונה בסימולציה אך אינה המלצה. אם בוחנים חלופה, ההמלצה
  הזהירה היא פיילוט מוגבל של `25x2000`.
- צילומי Stitch הם ראיית עיצוב בלבד. `tokens.css`, ‏`DESIGN.md` ו־`IA.md` הם
  מקורות המימוש; אין לייבא markup מ־Stitch.
- **פאזה 11: SPA ב-vanilla JS ללא תלות frontend/framework/build step
  חדשה; Playwright כתלות-בדיקה בלבד, מבודדת מ-CI וב-`e2e/`** (`P11-D1`,
  `P11-D7`). כלל תיקון מקור: סתירה בין הבריף/SPEC/IA/DESIGN לחוזה החי עוצרת
  checkpoint ⛔ ואינה נעקפת בקוד ה-frontend.

## מדיניות בדיקות לתכנון/ביצוע

לפני הרצה יש לציין איזו אי־ודאות חדשה היא פותרת. שינויי תיעוד בלבד נבדקים
ב־diff; מותרת בדיקה ממוקדת של מבנה `ROADMAP.html` אם השינוי עלול לשבור HTML
או JavaScript. אין להריץ `pytest` מלא, מודלים, Holdout או סריקת סודות מחדש
רק כדי לאשר ראיה קיימת.
