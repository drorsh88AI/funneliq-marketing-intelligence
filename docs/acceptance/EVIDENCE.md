# Phase 12, checkpoint 7 — ראיית המסע האנושי (B59, P12-D10)

מקור: `Funneliq_External_audit.docx`, דוח שכתב הבוחן החיצוני בעצמו, ללא
תיווך של קלוד. הציטוטים להלן הם תרגום/ציטוט ישיר מהדוח; שום פרט מזהה
אישי, credentials, אימייל או סיסמה אינם מופיעים כאן ולא הופיעו בדוח
המקורי.

**✅ `CP7` אושר וסגור (2026-09-23, ר' `ROADMAP.html`).** `B55b` נשארת
דרישה נפרדת ופתוחה — אלו שתי דרישות שונות (ר' סעיף ד׳). **הכרעת
המשתמש (2026-09-23): ממשיכים כעת בתוכנית כמתוכנן; כל ממצאי הביקורת
החיצונית (סעיפים ג׳/ד׳/ה׳) נשארים פתוחים לטיפול בהמשך, בלי לשייך אותם
כעת באופן אוטומטי לפאזה 13 או לכל פאזה אחרת** — הבחירה בין תיקון
עכשיו / דחייה מתועדת / קבלה מתועדת של המגבלה (ר' סעיף ו׳) טרם נעשתה.

---

## א. זהות הבוחן ותנאי הבדיקה (נדרש לפי `PHASE12.md` §ט)

| פרט | ראיה מהדוח |
|---|---|
| תפקיד/קשר לפרויקט | "חבר -- הוצג בפניי במהלך החודשים האחרונים חלקים מהפרויקט אבל מעולם לא בצורה כזו" |
| מכשיר/דפדפן | "לפטופ -- Edge" |
| מעורבות בבניית המוצר | "נכון -- אני חבר של המתכנן ושמעתי אותו מדבר על כך בחודשים האחרונים" (לתיבת "לא הייתי מעורב/ת בבניית המוצר") |
| מחשב/פרופיל שונה מהמפתח | "נכון -- במחשב שלי" |
| ללא עזרת מפתח, ללא טרמינל | "נכון -- המערכת נפתחה ישר עם שם המשתמש והסיסמא שנתתם לי (הפרטים לא הופיעו מראש במסך אלא אני הזנתי אותם), והכל עבר טוב רק לא תמיד מובן מה התקבל" |

**⚠ דיוק ביושר, ⛔ לא הצגה מייפה:** הבוחן **אינו זר גמור** לפרויקט —
הוא חבר של מתכנן הפרויקט שנחשף בעבר לחלקים ממנו דרך שיחה ("שמעתי אותו
מדבר על כך"). לגבי שימוש קודם במוצר החי עצמו — הציטוט המדויק שלו הוא
"הוצג בפניי במהלך החודשים האחרונים חלקים מהפרויקט אבל מעולם לא בצורה
כזו". ⚠ **הבחנה בין ציטוט להסקה:** זה מה שהוא כתב — ⛔ **הוא אינו
אומר במפורש שמעולם לא השתמש במוצר החי בעצמו**, רק ש"לא בצורה כזו"
(כלומר, לא במסע המלא הזה). אין לקבוע כעובדה שהוא מעולם לא נחשף לגרסה
החיה בשום צורה אחרת — זה לא ידוע מהדוח. מה שכן ידוע בוודאות: הוא חבר
של מתכנן הפרויקט, ⛔ לא אדם שמעולם לא שמע על FunnelIQ. זה עומד בתנאי
`P12-D10` המדויק — "אדם שאינו בונה המוצר" — ⛔ אך אינו "stranger" מוחלט
במובן החווייתי.

---

## ב. מיפוי שבע רגלי `B59` (`P12-D10`) — השלמת פעולות בלבד

⚠ הטבלה הזו עונה על שאלה אחת בלבד: **האם הפעולות הנדרשות בוצעו בפועל?**
היא **אינה** שופטת אם הבוחן *הבין* את מה שראה — זה נדון בנפרד בסעיף ג׳.

| # | רגל | בוצעה? | ראיה |
|---|---|---|---|
| 1 | פתיחת ה-URL החי ממחשב/פרופיל נקי ואחר | ✅ | לפטופ, Edge, "במחשב שלי" (לא של המפתח) |
| 2 | התחברות עצמאית עם credentials שנמסרו מראש | ✅ | "אני הזנתי אותם" (הפרטים לא הופיעו מראש במסך) |
| 3 | התחלה מטופס נקי, ⛔ בלי prefill/דוגמה | ✅ | "התחיל מטופס נקי" (הבהרה נוספת מהבוחן, לפני כתיבת המסמך הזה); הבוחן ציין גם שראה את רשימת הדוגמאות אך **לא לחץ עליה** |
| 4 | הזנה ידנית של תרחיש חדש, תקין ובתוך התחום, וקבלת חיזוי | ✅ | "הוזנו נתוני הדוגמא שסיפקתם והתקבל חיזוי" — הוקלדו ידנית מהערכים שב-`CHECKLIST.md` (מאומתים בתוך `ood_bounds` האמיתי מ-checkpoints קודמים), ⛔ לא נלחץ כפתור דוגמה |
| 5 | הגעה מפורשת ל-Follow-up ול-Budget וצפייה בשתיהן | ✅ | פרשנות מפורטת נכתבה על שני המסכים ("תקציב", "מעקב שיחות") |
| 6 | קריאה בפועל של בלוקי ההמלצה | ✅ (חזק) | ⚠ **דיוק:** ציטוט מילולי **מלא** של בלוק המלצה קיים בבירור רק עבור מסך "תקציב" (הטקסט המלא של "100×500 מדורגת ראשונה... אינה המלצה... 8.60..."). ⛔ אין ציטוט מילולי מלא כזה מכל מסך. במסכים האחרים ("תחזיות", "ציון לקוח־על", "מעקב שיחות") הבוחן כותב ביקורת ספציפית ומפורטת דיה על תוכן ומבנה הבלוק (למשל "כתוב ללא רווחים או שורות חדשות", תת-כותרות באנגלית, "מה כדאי לעשות מוצג בשורה אחת") — ביקורת שלא ניתן לכתוב בלי לקרוא את הטקסט בפועל, גם בלי ציטוט מילולי מלא. הראיה **חזקה עבור תקציב, ומספקת (לא ציטוט מלא) עבור השאר** |
| 7 | אפס התערבות מפתח, אפס טרמינל | ✅ | ר' טבלה א׳ — "רק לא תמיד מובן מה התקבל" מתאר בלבול, ⛔ לא בקשת עזרה בפועל |

**מסקנת ב׳: שבע הרגלים בוצעו.** ⚠ ר׳ עם זאת ג׳ ו-ד׳ להלן לפני הכרעה
על המשמעות המלאה של "מסע מוצלח".

---

## ג. שני דיוקים ביחס לצ'קליסט עצמו

1. **סדר הניווט לא היה חופשי בפועל, למרות שהצ'קליסט התיר זאת.** הבוחן
   כתב במפורש: "עברתי בין המסכים בהתאם לסדר שכתבתם בקובץ." הוא גם בדק
   שני מסכים (סקירה כללית, ציון לקוח־על) שכלל לא נדרשו ב-`CHECKLIST.md`
   (הם אינם בין שבע הרגלים). ⛔ אין לתאר את המסלול כ"מסלול שהבוחן בחר
   באופן חופשי בפועל" — הוא עקב אחרי הסדר הכתוב במסמך, גם שהמסמך אמר לו
   שאין סדר מחייב. זה ממצא לגבי **איך המסמך נקרא בפועל**, לא הפרה של
   B59 (הרגליים הנדרשות עדיין בוצעו כולן, בכל סדר).

2. **טעות עובדתית ב-`CHECKLIST.md` עצמו, שהבוחן חשף (ממצא היסטורי,
   מתועד כאן ותוקן בעקבות ביקורת זו):** המסמך שנמסר לבוחן בזמנו כתב
   "תיבת האישור שמופיעה **מתחת** לטופס" — זו הייתה טעות. אומת ישירות
   מול `app/static/js/screens/predict.js` (`buildOnce`, סדר ה-DOM
   בפועל): תיבת האישור (`context-confirmation`) נבנית **לפני** בורר
   הדוגמאות (`prefill-picker`) ו**לפני** רשת השדות עצמה (`field-grid`,
   שנמצאת אפילו בתוך `<details>` מקופל כברירת מחדל) — כלומר התיבה
   נמצאת **מעל** הטופס, בדיוק כפי שהבוחן דיווח. זהו המיקום ה**נכון**
   (`IA.md` §3.3 צעד 1, אושר כנכון בביקורת קודמת של פאזה 11A) — ⛔
   **התיאור בצ'קליסט היה שגוי, לא המוצר**. **תוקן**: `CHECKLIST.md`
   עודכן ל"מעל הטופס" בעקבות ביקורת המשתמש על מסמך זה (2026-09-23),
   ⛔ לא נותר "לא בוצע".

---

## ד. ממצאי שימושיות — קריאה מול הבנה (⛔ אין הצלחה אוטומטית מעצם השלמת שבע הרגליים)

⚠ `P12-D12` **אינו רלוונטי כאן** — הוא עוסק בהפרדה בין ראיית backend
חי לבין הזרקת כשל מכוונת בדפדפן (נושא של CP6), ⛔ לא בממצאי שימושיות
ממסע אנושי. הוסר משורת הכותרת.

הבוחן **קרא** את בלוקי ההמלצה (רגל 6 הושלמה, ר׳ לעיל — ראיה חזקה
לתקציב, מספקת לשאר), אבל דיווח **קושי עקבי להבין אותם ואת הפעולה
הנדרשת**, כמעט בכל מסך. אלו שני דברים שונים — קריאה בפועל, מול הבנה —
ואין לערבב ביניהם.

**ציטוטים נבחרים, לפי מסך:**

- כללי, על כל עמוד: "בכל עמוד ולא משנה איזה, היה קצת לא ברור מה צריך
  לעשות בהיעדר הסבר קצר בתחילת העמוד."
- כללי, שפה: "היה מבלבל בעיקר השפה הגבוהה בה משתמשים באפליקציה...
  צריך לפשט את הרמה שתהיה יותר ברורה."
- תחזיות: "בלוניות התשובות לא מסודרות בצורה אחידה ולא ברור כל כך מה
  תוצאות הבדיקה אומרות... מה התוכן המעשי במילים פשוטות למשתמש הפשוט."
- תקציב: "זה משפט לא מובן בכלל למשתמש הפשוט ואינו מסביר מה התקציב לכל
  לקוח" (על בלוק ה-D9 של האסטרטגיות); "מה כדאי לעשות -- זו מהות הטופס
  והסיבה לקיומו, אם זאת הוא מוצג בשורה אחת, שאינה ברורה דיה."
- מעקב שיחות: "'המלצה' -- נראה מאוד לא ברור ולא מובן, כתוב ללא רווחים
  או שורות חדשות, שפה נורא מקצועית ואינה נהירה לאדם ההדיוט שהוא הוא
  המשתמש באפליקציה."; תת-כותרות באנגלית בתוך טופס עברי.

**נימוק מול רוח הבריף — ייחוס מדויק לשתי דרישות שונות, ⛔ לא ניחוש
ו-⛔ לא ערבוב:**

`REQUIREMENTS.md` (השורות `B55b`/`B59`) מפריד בין שני נוסחים שונים,
ויש לייחס את הממצא לכל אחד בנפרד:

- **`B59`** (§06 בבריף) — "A stranger can open your live URL, sign in,
  get a prediction for a new customer, see the follow-up and budget
  insights, and **read your recommendations** — without you touching
  anything." הפועל כאן הוא **"read"**, ⛔ לא "understand"/"actually
  answer". שבע הרגליים (כולל רגל 6, הקריאה) עונות בדיוק על הנוסח הזה
  כפי שהוא כתוב — ⛔ לא על רף הבנה.
- **`B55b`** (§06 בבריף) — "A live application URL... a real link you
  can share that **actually answers** the founder's questions." **כאן**
  נמצא הביטוי "actually answer" ש-`REQUIREMENTS.md` עצמו (שורה 221)
  מצטט לגבי `B55b`, ⛔ לא לגבי `B59`. וזה בדיוק מה שממצאי הבוחן מטילים
  בספק — לא רק שהמספרים קיימים, אלא שהם **מובנים** ועונים בפועל על
  שאלה למי שרואה אותם בפעם הראשונה. הביקורת חוזרת על עצמה במספר מסכים
  (ר' ציטוטים למעלה — לא רק במסך בודד אחד), אך ⛔ ראיית הציטוט המלא
  ביותר היא ממסך אחד ספציפי (תקציב, ר' דיוק בטבלת ב׳ למעלה).

**מסקנתי (לביקורת, ⛔ לא הכרעה סופית):**
- שבע רגלי `B59` (פעולות, כולל "read") **הושלמו** כלשונן — קריטריון
  CP7 המצומצם, כפי שכתוב במפורש בטבלת ה-checkpoints של `PHASE12.md`,
  מתקיים.
- ⚠ **`B55b`, ⛔ לא `B59`**, הוא הדרישה שממצאי השימושיות נוגעים אליה
  ישירות — "actually answers the founder's questions" הוא הנוסח שלה,
  לא של `B59`. אני ממליץ (⛔ לא קובע) שלא לסגור אותה בלי הכרעה מפורשת
  על הממצאים האלה: תיקון עכשיו, דחייה מתועדת, או קבלה מתועדת של
  המגבלה — ר' סעיף ו׳ להלן, שתי דרישות בנפרד.
- ⛔ אין כאן ראיה שהבוחן "נכשל" או ש"המוצר שבור": שום שגיאה לא נצפתה,
  כל מסך נטען. ⚠ **דיוק:** גם אין לומר "כל תחזית שהתבקשה התקבלה"
  כטענה גורפת — במסך "תחזיות" (הנדרש) אכן התקבלה תחזית אמיתית; בניסיון
  היזום, הלא-נדרש, במסך "ציון לקוח־על" עם קלט מחוץ לתחום (ר' סעיף ה׳)
  **לא** התקבלה תחזית, ובצדק — זו התנהגות OOD תקינה, ⛔ לא כשל. שני
  המקרים שונים ואין לאחד אותם למשפט אחד.

---

## ה. ממצא, חלקית מאומת: ציון לקוח־על נמוך וחוזר על עצמו בכמה קלטים

מסך "ציון לקוח־על" **לא נכלל** בשבע רגלי `B59` ולא נדרש ב-`CHECKLIST.md`
— הבוחן בדק אותו ביוזמתו. הממצא **כן רלוונטי ל-`B55b`:** `PHASE12.md`
(שורה 511) ממפה את `B41` ל-P4S, ו-`B41` הוא אחת מחמש השאלות ש-`B55b`
דורש מענה לכולן (`REQUIREMENTS.md` שורה 93). ⚠ **דיוק חשוב:** `B41`
(`REQUIREMENTS.md` שורה 189) דורש "output a 0–100 likelihood... served
by your app" — ציון 0–100 תקף שמוגש. ⛔ **הבריף אינו דורש שהציון ישתנה
בין דוגמאות** — "ציון משתנה" אינו קריטריון קבלה בפני עצמו; השאלה
הרלוונטית היא אם הציון **תקף ומחושב נכון** עבור הקלט הנתון, ⛔ לא אם
הוא שונה מדוגמה לדוגמה.

**בדיקה מקומית שבוצעה (2026-09-23), לא על הבוחן להסתמך עליה בלבד:**
הרצתי את המודל המאומן עצמו (`models/P4S.joblib`, `P4S-catboost-
20260912-1c70ca8`) **באופן מקומי ואופליין**, דרך `app.inference` הישיר
— ⛔ בלי לגעת בפריסה החיה, ב-Supabase, או בכל credential — על שבעה
תרחישי קלט **שונים זה מזה**: **חמישה** בתוך התחום (`ood_bounds`
האמיתי, קיבלו חיזוי) ו**שניים** נדחו כ-OOD (מחוץ לתחום, לא קיבלו
חיזוי — התנהגות תקינה, לא כשל):

| קלט (ad_budget / num_leads / leads_answered / followup_1) | תוצאה |
|---|---|
| 5000 / 100 / 60 / 55 | הסתברות גולמית 0.0533 → ציון מוצג **5** |
| 2000 / 30 / 20 / 18 | הסתברות גולמית 0.3355 → ציון מוצג **34** |
| 800 / 15 / 8 / 5 | הסתברות גולמית 0.0102 → ציון מוצג **1** |
| 10000 / 60 / 55 / 50 | הסתברות גולמית 0.0102 → ציון מוצג **1** |
| 1200 / 40 / 12 / 8 | הסתברות גולמית 0.0107 → ציון מוצג **1** |
| (שני תרחישים נוספים) | מחוץ לתחום (`leads_answered`/`followup_1`), נדחו נכון לפני חיזוי |

**מסקנה מהבדיקה המקומית, בזהירות:**
- **⛔ המודל אינו "קפוא" — הוא בהחלט משתנה** לקלטים שונים באופן מהותי
  (5, 34, 1) — זה **סותר** פרשנות של "באג שמחזיר תמיד את אותו הערך".
- **⚠ עם זאת, יש התכנסות אמיתית לאזור הסתברות נמוכה** אצל כמה קלטים
  שונים לגמרי (רמת הוצאות 800 מול 10,000, לידים 15 מול 60) — שתיים
  מהן נתנו הסתברות גולמית **כמעט זהה עד הספרה החמישית** (0.010192
  מול 0.010193). ⚠ **הציונים הדומים עשויים לנבוע** מאופן פעולת המודל
  (מודלי עצי החלטה כמו CatBoost יכולים להחזיר הסתברות דומה לקלטים
  שונים שנופלים באותו עלה בעץ — התנהגות ידועה ולא-באגית, ⛔ אך הסיבה
  המדויקת כאן **לא אומתה**, זו רק אפשרות סבירה) ו/או מעיגול תצוגה
  (`Math.round(p*100)`): הסתברויות שונות כמו 0.0102 ו-0.0107 שתיהן
  מוצגות כ-"1". ⛔ **אין כאן קביעה** מה בדיוק קרה בקלטי הבוחן עצמו —
  רק שתי סיבות אפשריות ולא-באגיות להתכנסות שנצפתה מקומית.
- ⛔ **לא הוכח ולא נשלל** שהתרחישים המדויקים שהבוחן הזין בעצמו (שלא
  נמסרו ערכים מלאים עבורם, מלבד הניסיון הראשון שהיה OOD) נופלים באותו
  אזור הסתברות נמוכה מסיבה תקינה, או שיש שם התנהגות שונה שלא נבדקה
  כאן. **הבדיקה המקומית מחזקת מאוד את ההשערה שמדובר בהתנהגות תקינה +
  עיגול תצוגה, ⛔ אך אינה קובעת מסקנה סופית** בלי הערכים המדויקים
  שהבוחן עצמו הזין.
- ⛔ **הבדיקה המקומית אינה בודקת את המסלול החי או הממשק בכלל** — היא
  רצה ישירות מול `models/P4S.joblib` דרך `app.inference`, ⛔ בלי לעבור
  דרך ה-API החי, Supabase, או קוד ה-JS בדפדפן (`super-customer.js`). היא
  **שוללת** אך ורק את ההשערה ש"המודל המקומי עצמו קפוא על 1 תמיד" —
  היא **אינה שוללת** תקלה נפרדת במסלול החי (למשל בהעברת הפרמטרים
  מהטופס לבקשת ה-API, או בעיבוד התשובה בצד הלקוח) שאינה קיימת כשקוראים
  למודל ישירות כפי שנעשה כאן.

---

## ו. הכרעת `CP7`, והמלצה נפרדת עבור `B55b`

`CP7` ו-`B55b` הן שתי דרישות שונות עם נוסח שונה (ר' סעיף ד׳). ⛔ אין
לגזור החלטה אחת משתיהן יחד.

### CP7 (קריטריון שבע הרגליים, `PHASE12.md`) — ✅ אושר וסגור

| שאלה | תשובה |
|---|---|
| האם בוחן חיצוני כשיר (`P12-D10`) ביצע את שבע הרגלים **כלשונן** (כולל "read", ⛔ לא "understand")? | ✅ כן — ר׳ סעיף ב׳ |
| האם נדרשה התערבות מפתח, או שהמסע נעצר באמצע? | ⛔ לא — הושלם עד הסוף, ללא התערבות. (`P12-D6` חל כאשר **מסע הקבלה נכשל**; הוא אינו מופעל אוטומטית כאן — ⛔ לא מפני ש-`P12-D6` מוגבל מטבעו לכשל שעוצר מסע, אלא מפני שקריטריון `CP7` (שבע הרגליים) הושלם במלואו. ⚠ זה אינו אומר שכל ממצא אחר שעלה מהמסע — כמו סעיף ד׳/ה׳ — אינו יכול בעצמו להיחשב "כשל קבלה" לפי `P12-D6`; זו בדיוק ההכרעה הנפרדת שמוצעת להלן לגבי `B55b`) |

**✅ אושר על ידי המשתמש (2026-09-23) וסגור `done` ב-`ROADMAP.html`.**
זו הכרעה על **`CP7` בלבד** — ⛔ אינה אומרת דבר על `B55b`, ואינה קובעת
שממצאי סעיף ד׳/ה׳/ג׳.2 כבר טופלו: הם נשארים פתוחים לטיפול בהמשך, בלי
שיוך אוטומטי לפאזה כלשהי (ר' הכרעת המשתמש בראש המסמך).

### B55b ("actually answers the founder's questions")

זו הדרישה שממצאי השימושיות (סעיף ד׳) נוגעים אליה ישירות. ⚠ **תנאי
מקדים נפרד, לא תלוי באפשרות שנבחרת למטה:** `B55b` דורשת מענה לכל
**חמש** השאלות, ו-P4S (ציון לקוח־על) היא אחת מהן (`B41`, ר' סעיף ה׳
לעיל). ⛔ **אף אחת משלוש האפשרויות למטה אינה יכולה להעביר את `B55b`
ל-`done` כל עוד ממצא הציון הקבוע לא נבדק** — קבלה מתועדת של מגבלת
השימושיות אינה, כשלעצמה, תשובה לשאלה אם P4S מחזיר ציון תקף ומחושב
כראוי עבור הקלט.

שלוש אפשרויות לממצאי השימושיות (סעיף ד׳) — לכל אחת סטטוס מפורש
ל-`B55b`, **בכפוף לתנאי המקדים למעלה**, ⛔ בלי להניח שאחת מהן כבר
אושרה:

| אפשרות (לממצאי השימושיות) | סטטוס `B55b` | משמעות ל-`CP7` |
|---|---|---|
| **תיקון עכשיו** (בפאזה 12, לפני שממשיכים ל-CP8/CP9) | `planned` עד שהתיקון ייבדק (כולל אולי מסע נוסף של בוחן) **וגם** עד שממצא P4S נבדק | לא משתנה — `CP7` כבר עומד בקריטריון שלו כשלעצמו |
| **דחייה מתועדת לפאזה 13** | `planned` עד פאזה 13, עם ממצא מתועד ב-`REQUIREMENTS.md`/`README.md` שמסביר למה; ⚠ **זו הצעה בלבד — טרם אושרה**. עדיין `planned`, ⛔ לא `done`, עד שממצא P4S נבדק בנפרד | לא משתנה |
| **קבלה מתועדת של מגבלת השימושיות** (בלי תיקון שפה) | ⛔ **לא `done` אוטומטית** — קבלת מגבלת שימושיות מתועדת **אינה** מוכיחה ש-P4S עונה תשובה תקפה; `B55b` נשארת `planned` עד שממצא הציון הקבוע נבדק בנפרד ונסגר (כבאג שתוקן, או כהתנהגות תקינה שהוסברה) | לא משתנה |

**המלצתי (⛔ לא הכרעה):** לא לסגור `B55b` `done` בלי (א) שהמשתמש בוחר
במפורש אחת משלוש האפשרויות לממצאי השימושיות, **וגם** (ב) שממצא הציון
הקבוע של P4S נבדק בנפרד — שני תנאים, ⛔ לא אחד מחליף את השני.

### ממצא נלווה נוסף, לא תלוי בהכרעה למעלה

- **טעות המיקום ב-`CHECKLIST.md` (סעיף ג׳.2):** **תוקנה** — "מתחת
  לטופס" הוחלף ל"מעל הטופס" בעקבות ביקורת המשתמש (2026-09-23). זו
  הייתה טעות בניסוח המסמך, לא במוצר או בראיית המסע עצמה, ואינה משפיעה
  על ההכרעות למעלה.

`B59` עצמה, לפי `PHASE12.md` (§ו, שורה קבועה): נשארת `planned` בכל
מקרה — גם אחרי מסע מוצלח — עד השלמת רכיב התיעוד (`README.md`) בפאזה
13. זו הכרעה **נעולה כבר בתכנון**, ⛔ לא המלצה חדשה כאן.

**עדכון סטטוס (2026-09-23):** `CP7` אושר וסגור `done` על ידי המשתמש
(ר' `ROADMAP.html`, `checkpoint 7`). `B55b` **נשארת פתוחה** — ההכרעה
בינה, ובפרט האם לפתוח סבב תיקון שימושיות לפני שסוגרים אותה, נשארת
אצל המשתמש; הוא הכריע להמשיך כעת בתוכנית (`CP8`/`CP9`) ולחזור לממצאי
סעיפים ג׳/ד׳/ה׳ בהמשך, בלי לשייך אותם כעת לפאזה מסוימת.
