# AGENTS.md — Project 1 (FunnelIQ)

הנחיות תפעול לכל סוכן AI שעובד בפרויקט הזה (Codex, ואחרים). `CLAUDE.md`
הוא הגרסה המקבילה עבור Claude Code — שני הקבצים נשמרים מסונכרנים ידנית;
אם משהו כאן סותר את `CLAUDE.md`, זה באג בסנכרון ויש להעלות אותו, לא
לבחור אחד מהם בשתיקה.

## הבריף הוא מקור אמת לדרישות — נבדק בכל שער פאזה

`FunnelIQ_Assignment.html` הוא קובץ הדרישות המקורי של הלקוחה.
`docs/planning/SPEC.md` הוא מקור האמת ל**תכן**; הבריף הוא מקור האמת
ל**דרישה**. `SPEC.md` הוא נגזרת של הבריף — מרגע שנכתב, קל להסתמך עליו
בלבד ולשכוח לחזור למקור. **זה בדיוק מה שקרה:** שישה סבבי ביקורת על
תכנון פאזה 9 בדקו אותו מול `SPEC.md` בלבד, ואף אחד לא מול הבריף, עד
שחזרה יזומה למקור חשפה סתירה פנימית בבריף עצמו (חבילה 4: `Target: referred`
לצד בקשת ציון 0–100 לסופר-לקוח) שנפתרה בפאזה 8A.

**בכל שער מעבר, לפני שתכנון מוצג לאישור — חובה:**

1. מיפוי מול `docs/planning/REQUIREMENTS.md` (מרשם 73 הדרישות האטומיות
   B1–B65 מהבריף, כל אחת עם ציטוט מילולי, בעלים יחיד, תורמים, מאמת
   וראיה): אילו דרישות הפאזה סוגרת, נוגעת בהן חלקית, ודוחה — ולעדכן
   שם `status`/`evidence`.
2. דרישה בבריף שאף פאזה אינה מכסה היא **ממצא שמועלה במפורש**, גם אם
   נראה מחוץ להיקף. שתיקה אינה כיסוי.
3. `tests/test_requirements_traceability.py` אוכף שהמרשם עצמו שלם: כל
   ציטוט נמצא מילולית ב-`FunnelIQ_Assignment.html`, לכל דרישה בעלים
   יחיד, ולכל דרישה `done` יש ראיה. אין ליצור טבלת עקיבות מקבילה.
4. סטיות מאושרות מהבריף (Render במקום Railway, בלי notebook) מצוינות
   מחדש בכל שער, ואינן נפתחות לדיון מחדש.

**בדיקת שלמות נפרדת, שאינה תלויה בבריף:** לפני אישור כל תכנון, לעבור
על מה שהתכנון עצמו הבטיח (טבלת תוצרים, קריטריוני קבלה, סיכונים)
ולוודא שהוא אכן כתוב, לא רק מוזכר בכותרת.

## שער מעבר אחיד בין כל שתי פאזות

אישור תוצרי הפאזה → בדיקת המצב בפועל → מיפוי מול `REQUIREMENTS.md` →
תכנון מפורט מחדש של הפאזה הבאה → עדכון `ROADMAP.html` ומסמכי התכנון →
ביקורת Codex–Claude → אישור סופי → הוראת ביצוע. שני סטטוסים נפרדים
לכל פאזה ב-`ROADMAP.html` (`planning_status`/`execution_status`)
שאסור למזג. תכנון פאזות עתידיות הוא תכנון-על בלבד — אין להרחיבו
לתוכנית ביצוע על בסיס ניחושים.

## מדיניות בדיקות — אין להריץ בדיקות על בדיקות

לפני כל פקודת בדיקה יש לשאול: **איזו אי־ודאות חדשה ההרצה פותרת?** אם
אין תשובה קונקרטית, לא מריצים אותה. ראיה תקפה שכבר נאספה נשארת תקפה כל
עוד הקוד, התלויות, התצורה או סביבת הריצה הרלוונטיים לא השתנו.

- אין להריץ שוב `pytest` מלא רק כדי לאשר מחדש תוצאה ירוקה, מפני שסוכן
  אחר כבר הריץ אותו, לפני `commit`/`push`/סגירת פאזה, או לאחר שינויי
  תיעוד, ניסוח וסטטוס בלבד.
- בביקורת על שינוי קוד מריצים פעם אחת את הבדיקות הרלוונטיות לשינוי.
  `pytest` מלא נדרש רק כשהיקף שינוי הקוד מצדיק זאת, כשיש כשל או ספק
  ממשי שלא כוסה, כשהראיה הקיימת אינה שייכת לגרסה הנבדקת, או כשהמשתמש
  ביקש זאת במפורש.
- בשינויי תיעוד או סטטוס בלבד בודקים את ה־diff. מותר להפעיל בדיקה
  ממוקדת וקלה רק אם השינוי עצמו עלול לשבור מבנה בר־הרצה, למשל תחביר
  JavaScript ב־`ROADMAP.html`; אין להריץ בגללם את כל `pytest` או סריקת
  סודות מחדש.
- אחרי בדיקה מוצלחת, תיקון ניסוח בלבד אינו מאפס את הראיה ואינו מצדיק
  הרצה חוזרת. מריצים שוב רק בעקבות שינוי רלוונטי, כשל חדש, שינוי
  תלויות/סביבה, קונפליקט מיזוג, או הוראה מפורשת.
- CI ירוק על ה־commit שנדחף הוא ראיית האימות המרוחקת. אין לשכפל אותה
  מיד בהרצה מקומית נוספת ללא סיבה קונקרטית.

המטרה היא לקבל ביטחון מספק פעם אחת ולהתקדם; ריבוי הרצות זהות ללא מידע
חדש הוא עיכוב, לא הקפדה.

## סביבת Python

סביבת Conda ייעודית `pro1_FunnelIQ`. לכל פקודת Python/pip — הנתיב המלא:

```
C:\Users\dror_\programs\anaconda\envs\pro1_FunnelIQ\python.exe
```

לא `venv`/`.venv`, ואין להסתמך על Python גלובלי.

## החלטות נעולות — אין לפתוח מחדש

Render ולא Railway (היתר מפורש בבריף עצמו, §07: "Render or Fly.io are
drop-in substitutes for this pillar") · `app/features.py` הוא מקור
האמת היחיד לרשימות פיצ'רים לכל משימה — סקריפט האימון וה-API מייבאים
משם, לא משכפלים · anon/publishable key בדפדפן בלבד, service/secret key
רק ב-`scripts/*.py` מקומי · קריאות נתונים למשתמש נושאות את ה-JWT שלו
כדי ש-RLS תיאכף בפועל · `README.md` באנגלית, `REPORT.md` בעברית.

לפרטים המלאים על כל אחת מהן — `CLAUDE.md` ו-`docs/planning/codex-review.md`
(יומן ההכרעות).
