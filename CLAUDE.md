# CLAUDE.md — Project 1 (FunnelIQ)

## ⚠ קרא קודם: האפיון קיים ומלא

הפרויקט נראה ריק (ארבעה קבצים, אפס קוד) אבל **הוא לא**. קיים אפיון מלא שעבר
סבבי ביקורת, עם 14 פאזות. אין החלטה פתוחה שחוסמת את פאזה 2.
**אל תתחיל לתכנן מחדש.**

**שער מעבר אחיד בין כל שתי פאזות** (`0→1` … `13→14`), לא חריג נקודתי לפני
פאזה 4: אישור תוצרי הפאזה → בדיקת המצב בפועל → תכנון מפורט מחדש של הפאזה
הבאה → עדכון `ROADMAP.html` ומסמכי התכנון → ביקורת Codex–Claude → אישור סופי
→ הוראת ביצוע. הטבלאות של פאזות 3–14 הן **תכנון-על בלבד** — אין להרחיבן
לתוכניות ביצוע על בסיס ניחושים. ב-`ROADMAP.html` שני סטטוסים נפרדים לכל פאזה
(`planning_status` / `execution_status`) שאסור למזג. ההכרעה הייעודית הידועה
היום — אופן מסירת גישת הדמו — תלויה **בשער `3→4`**, ואינה תוצר של פאזה 4.

| קובץ | מה יש בו |
|---|---|
| `docs/planning/SPEC.md` | **האפיון המלא — מקור האמת.** ארכיטקטורה, מדיניות דליפה, snapshots, פרוטוקולי conformal/bootstrap/כיול, Auth ו-RLS, SQL, עיצוב, 14 פאזות |
| `docs/planning/codex-review.md` | מצב מסוכם + **יומן הכרעות**: מה הוכרע, מאיזה סבב ולמה. שם בודקים אם סוגיה כבר נסגרה |
| `docs/planning/codex-history.md` | ארכיון תשעת סבבי הביקורת. ⚠ **רובו הופרך** בסבבים מאוחרים — לא לפעול לפיו |

**סוגיות שנסגרו ואסור לפתוח מחדש** (הרשימה המלאה ביומן ההכרעות): Render ולא
Railway · בלי notebook · הכפילויות נשמרות · P2/P3/P4 על `purchased = 1` ·
snapshot = תום מחזור הקמפיין · הרשמה ציבורית כבויה · `sigmoid` נעול מראש ·
בלי אינדקסים ובלי `CHECK` מעבר ל-`NOT NULL`.

**שני מספרים שמתבלבלים בהם:** שיעור בסיס `upsell` = **46.35%** ·
majority baseline = **53.65%**.

**סטטוס ביצוע:** לא בוצע דבר. אין git repo, אין קוד, שום שירות לא חובר.
**כל פאזה דורשת אישור מפורש של המשתמש לפני ביצוע.**

## סביבת Python

לפרויקט זה יש סביבת Conda ייעודית בשם `pro1_FunnelIQ` (לא `venv`/`.venv` בתוך
התיקייה, ולכן החוק הגלובלי שמחפש תיקיית venv לא מוצא אותה — הנתיב המדויק
מתועד כאן וגובר עליו).

**אין צורך ב-`conda activate`.** לכל פקודת Python/pip בפרויקט הזה יש לקרוא
ישירות לפייתון של הסביבה, בנתיב המלא:

```
<CONDA_ENV>\python.exe
```

לדוגמה:
- Bash: `"<CONDA_ENV>/python.exe" script.py`
- PowerShell: `& "<CONDA_ENV>\python.exe" script.py`
- pip: `... python.exe -m pip install <pkg>`

זה מבטיח שכל הרצה משתמשת בסביבה הנכונה בלי הפעלה ידנית, גם כשכל קריאת Bash
פותחת שֶׁל חדש (state לא נשמר בין קריאות).

VSCode כבר מוגדר להשתמש באותה סביבה (`.vscode/settings.json` →
`python.defaultInterpreterPath`), אז טרמינל אינטגרטיבי ב-VSCode מפעיל אותה
אוטומטית גם הוא.

### `pytest` עדיין לא מותקן

הסביבה כוללת כבר את כל תלויות הריצה — fastapi, uvicorn, xgboost, lightgbm,
catboost, supabase, pyjwt, scikit-learn, pandas, matplotlib, seaborn, joblib —
אבל **לא** את `pytest`, ו-CI תלוי בו. להתקין לפני הרצת בדיקות ראשונה:

```
... python.exe -m pip install pytest httpx
```

## פריסה: Render, לא Railway

`FunnelIQ_Assignment.html` נוקב ב-Railway כעמוד התשתית. **ההחלטה בפרויקט הזה
היא Render**, מכוח ההיתר המפורש בבריף עצמו (סעיף 07): *"Render or Fly.io are
drop-in substitutes for this pillar"*. הסיבה: Render פורס ישירות מ-GitHub בלי
כרטיס אשראי ובלי Dockerfile.

אל תשנה את זה חזרה ל-Railway בעקבות קריאת הבריף.

## Supabase: שני מפתחות, שני שימושים — לא לערבב

| מפתח | איפה מותר | איפה אסור |
|---|---|---|
| anon key | בדפדפן (login דרך supabase-js) וכל קריאת נתונים בשם המשתמש המחובר | — |
| service key | רק ב-`scripts/load_data.py`, ריצה מקומית, מתוך `.env` | לעולם לא בדפדפן, לא בגיט, ולא בנתיב שמשרת משתמש |

קריאות נתונים למשתמש חייבות להגיע ל-Supabase עם ה-access token שלו, כדי ש-RLS
תיאכף בפועל. backend שקורא הכל עם service key **עוקף את ה-RLS** ומרוקן את כל
תרגיל האבטחה מתוכן. זו הטעות הקלה ביותר לעשות כאן.

## `app/features.py` הוא מקור אמת יחיד לפיצ'רים

רשימות העמודות לכל משימת מידול — כולל ההחרגות שמונעות דליפה (`cumulative_profit`,
`ltv_months`, `upsell`, `referred`, לפי המשימה) — מוגדרות ב-`app/features.py`
בלבד. גם סקריפט האימון וגם ה-API מייבאים משם.

שינוי פיצ'ר בצד אחד בלבד יוצר מודל שאומן על סט אחד ומשרת סט אחר — באג שקט שלא
מתפוצץ, רק מחזיר תשובות שגויות.

## שפת מסמכים

- `README.md` — **אנגלית** (repo ציבורי, משמש גם כפורטפוליו).
- `REPORT.md` — **עברית** (המנחה קורא אותו).
