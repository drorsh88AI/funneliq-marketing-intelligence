# Pinned to the versions verified in the pro1_FunnelIQ conda env (Python 3.12.14).
# ponytail: one file for API + training + tests. Split into requirements-dev.txt
# only if the Render image size or the 512MB RSS budget (phase 6) actually bites.

# API
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.4

# Data / ML
pandas==3.0.5
numpy==2.5.2
scikit-learn==1.9.0
xgboost==3.4.1
lightgbm==4.7.0
catboost==1.2.10
joblib==1.5.3

# Plots (analysis scripts, phase 5/6)
matplotlib==3.11.1
seaborn==0.13.2

# Supabase / auth
supabase==2.31.0
PyJWT==2.13.0
python-dotenv==1.2.3

# Tests
pytest==9.1.1
httpx==0.28.1
